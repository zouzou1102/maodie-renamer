# P3 · 第 2–8 批 —— 范围澄清与交付总规划

> 文档版本：**v0.2**
> 日期：2026-09-18
> 上游：`需求清单.md` §三·P3 表 · `P3-1编号系统升级轻量设计确认.md` §0 进度表
> 本文档的作用：把 2–8 批从「一行标题」变成**可以直接开工设计的范围**。
> **在你勾完 §8 之前，我不写任何一份设计文档** —— 理由见 §0。

## ★ 已拍板（2026-09-18，产品负责人确认）

| # | 问题 | 结论 |
| --- | --- | --- |
| **A** | 第 2 批是哪个 | **① 导出文件名清单（Excel / TXT）**。「从文件名里抠一段当新名」挪到第 5 批的模式层 —— ✅ **已落地（2026-09-21 / DEC-17）** |
| **B** | 2/3/6 批放哪一层 | **② 落在规则化模式内（新变量）** —— `RuleConfig.mode` 一字不动，2/3/6 批不依赖第 5 批 |
| **C** | Excel / Word | **② 自己写最小 xlsx 读写器**，保持运行时零第三方依赖；Word 那档降级为 TXT |
| **D** | 顺序与节奏 | **① 按风险序 2→3→4→5→6→7→8，一批一交**（每批：确认文档 + 画布画板，过一遍再上下一个） |

剩余待确认（**E / F / G / I / J**，都已归口到具体批次，不阻塞第 2 批）：

- **E** 第 3 批属性范围 → 开工前定
- **F** 第 5 批拆不拆 → 做到第 4 批时定
- **G** 第 7/8 批要不要做「移动/复制的撤销」 → 做到第 6 批时定
- **I** 第 7 批那 4 种合并类型 → 做到第 6 批时请你给
- **J** 第 5 批五模式是哪五个 → ✅ **已定（2026-09-22）：自定义 / 插入 / 替换 / 删除 / 导入**；「导入」的交互模型见 §2.3 待定项

---

## 0. 为什么先出这一份，而不是直接出 7 份设计

2–8 批目前在全仓库里只有**一行标题**，而且**两份文档对同一批的写法不一致**（§2）。按一行标题直接出设计，等于替产品负责人猜需求 —— 猜错就是 7 份文档 + 7 块画板一起返工。

这份文档需要你做的事只有一件：**逐批回「要 / 不要 / 改哪里」**。勾完之后，7 份设计文档我按 §6 的节奏一份一份交。

---

## 1. 编号现状（全仓库实扫，2026-09-18 17:07）

扫描口径：`*.md / *.html / *.ts / *.vue / *.js / *.json / *.css`，排除 `node_modules / .git / out / dist / artifacts / .workbuddy`，按**前缀全量**正则 `\b(EL|IX|TC|DEC|EX|ST|SCR|ADR|MOD|R|F)-(\d{2,3})\b` 取 max。

| 前缀 | max | 说明 |
| --- | --- | --- |
| EL | **125** | 另有 `EL-900` / `EL-901`（`原型图.html` 里的模态外壳 id），**不是设计编号，不占号** |
| IX | **109** | |
| TC | **49** | |
| DEC | **16** | |
| EX | **17** | |
| SCR | **8** | |
| ADR | **6** | |
| F | **16** | 功能编号（F1–F16） |
| R | **7** | 区域编号 |
| ST | **5** | 状态（形象）编号 |
| MOD | **0** | 该前缀全仓库**未使用**（`MOD-x` 目前只出现在立项书的设想里） |

→ **2–8 批统一从以下号段起编**（每批按实际用量往下吃）：

```
EL  →  EL-126 起
IX  →  IX-110 起
TC  →  TC-50  起
DEC →  DEC-17 起
EX  →  EX-18  起
SCR →  SCR-09 起（只有真的新增独立界面才用）
ADR →  ADR-07 起（只有动架构不变量才用）
```

> 本批不再新增 `F` 号：2–8 批都是既有 F 能力的扩展（第 2 批属 F5 相邻、第 5 批属 F5/DEC-16、第 7/8 批是新能力）。**若你认为第 7、8 批该单独立 F 号（F17/F18），说一声。**

> ⚠️⚠️ **扫编号时必须分辨「已占用」与「已预留」**（2026-09-21 实测踩到）。
> 上面 §5 逐批写下的「建议编号」一旦落进文档，**下次全量扫编号时会被当成已占用**：本次扫描里 `EL` 直接跳到 **161**、`TC` 跳到 **83**、`IX` 跳到 **127**、`DEC` 跳到 **25** —— 这些**全是本文件里的预留号**，不是真被用了。
> → **判据**：真正的「已落地占用」只认**代码与已落地批次文档**里的号。扫描时先把**本文件排除**，或人工把预留号减掉。
> → **另一个后果（更严重）**：预留号写得早，就会被后面的批次吃掉 —— **已经发生三次**（`TC-53~55` 被第 2 批吃掉、`TC-58~60` 被第 3 批吃掉）。
> → **结论（2026-09-22）：取消「预留号」这个做法。** 本文件 §5 里的「建议编号」**全部作废**，改成只写「**编号用量**」；具体号一律**开工前重扫、接着上一批实际用到的号连续取**。

## 2. ★ 三处必须先澄清的冲突（文档之间自己就打架）

### 2.1 第 2 批「提取文件名」是两个完全不同的功能

| 出处 | 原文 | 这个写法读出来是 |
| --- | --- | --- |
| `P3-1编号系统升级轻量设计确认.md` §0 | 提取文件名（**导出 Excel / TXT / Word**） | **导出**：把列表里的文件名导成一份清单表（Excel / TXT / Word），给交接、归档、对账用 |
| `需求清单.md` §三·P3 | 提取文件名（**从文件名里取一段当新名**） | **提取**：从原文件名里抠出某一段（如 `【某某公众号】x.mp4` 里的 `某某公众号`）当新名 |

这两个是**两件毫不相干的事**，工作量差一个量级。

**我的判断（供你否）**：`P3-1` 那份写法**内部是自洽的** —— 它和第 8 批「**文件**提取（按类型 / 按名称 / 按 Excel）」凑成一对：**批 2 只提「名」→ 出清单；批 8 提「文件」本体 → 复制到别处。** 所以我倾向：

- **第 2 批 = 导出文件名清单**（Excel / TXT / Word）。
- 「从文件名里抠一段当新名」**不是丢掉，而是挪到第 5 批的模式层** —— 它天然属于「自定义 / 插入」这类模式（§3 会讲为什么）。这样两件事都不丢，且各自归位。

### 2.2 第 3 批的「属性」到底包括哪些

| 出处 | 原文 |
| --- | --- |
| `P3-1...md` §0 | 按文件属性命名（**创建时间 / 修改时间**） |
| `需求清单.md` | 按属性命名（按**日期 / 尺寸**等文件属性） |

「尺寸」这一项差别很大：`FileItem` 现在**没有** size / mtime / ctime 任何一个字段（见 §5·第 3 批），要多取哪些系统属性、以及**要不要为它扩展 fs-scan 的返回**，是个真决策。

### 2.3 第 5 批「五模式」到底是哪五个

> ✅ **2026-09-23 已落地**：五模式 = **自定义（`rule`）/ 插入（`insert`）/ 替换（`replace`）/ 删除（`delete`）/ 导入（`import`）**，互斥；
> 默认页签改成「自定义」；扩展名处理**不是模式**（与大小写同级的结果处理，默认关）。见 `DEC-20` / `DEC-21`。

已写死的是：`DEC-16` 把原有的**三模式互斥**（`delete` / `replace` / `rule`）正式重开成**五模式互斥**，落地在第 5 批。但**五个具体是哪五个，全仓库只写了半句**：

> `P3-1...md` §12：「三模式 → 五模式（**自定义 / 插入 / 替换 / 删除 / …**）」

「…」里是什么没定。而这一条**直接决定 2 / 3 / 6 批的界面怎么排**（§3）。

> ✅ **2026-09-22 已拍板（产品负责人）**：五模式 = **自定义 / 插入 / 替换 / 删除 / 导入**。
> - 「…」是**预留位**（以后可能加模式），不是第六个。
> - 映射：`自定义` = 现有 `rule`（规则化）；`替换` = `replace`；`删除` = `delete`；`插入` = 新增模式；`导入` = 第 4 批已落地的导入表格能力。
> - **顺带解决 §196 歧义②**：扩展名更改**不是**第五个模式 —— 它是第 5 批里一个**独立的开关/能力**（与 `EX-07` 扩展名保护正面冲突，待第 5 批单独设计），不占模式名额。
> - **顺带确认路线 B（§3 已定）**：提取（第 6 批）/ 按属性（第 3 批）**不是**模式，仍是规则化模式内的新变量。五模式里没有它们，是对的。
> - ⚠️ `导入` 作为「第五个互斥模式」与第 4 批已落地的「导入 = 叠加覆盖层（导一些、锁死、其余按规则）」模型有冲突，**已拍板选互斥模式**，回退方案见下方。

> ✅ **2026-09-22 已拍板（产品负责人）**：`导入` 当**第 5 个互斥模式**（选甲）。理由：导入的名字规则改不动，说明它是个独立大功能，该占一个模式位。
> - 含义：选「导入」模式 = 这批文件新名**全部**来自表，**不再走规则引擎**。
> - ⚠️ **与第 4 批已落地设计的冲突（必须第 5 批处理）**：第 4 批当初做的是「导入 + 规则共存」（导一些、锁死、其余按规则算），且你在 P3-4 评审里同意了「表外的项保持按规则算」。现选「互斥模式」意味着那一条要**收回 / 改掉** —— 第 5 批设计时要正式处理这个回退，并同步更新 P3-4 设计文档与已上线行为。
> - `插入` 与 `自定义(rule)` 的前缀/插入能力有重叠，第 5 批设计时要说清两者边界。

**⚠️→✅ 2026-09-23 已拍板：「导入」模式下，表里没写到的文件 = 保持原名不动。**

> 这一条是**收回**他原先在 P3-4 评审里同意的第 4 条（那次同意的是「表外的项保持按规则算」）—— 因为「导入」是**五选一**的模式，选了它**规则引擎整个不参与**，不存在「按规则算」这回事。
> **→ P3-4 设计文档与已上线行为都要同步回改**（§16 之外再加一处）。

下面是当时的三个选项（保留作**决策留痕**）：

- 他在 P3-4 评审里同意过：**「表外的项保持按规则算」**（拖 100 个文件、表里 8 行 → 8 个用表里的名字，**其余 92 个按规则改名**）。
- 但「导入」是**五选一**的模式 → 选了导入，**规则引擎整个不参与** → 那 92 个就没有「按规则算」这回事了。
- ~~三个选项（**第 5 批设计时必须先定**）~~ —— ✅ **已定（2026-09-23）：「导入」是五选一里的一个互斥模式**（不是叠加层），第 4 批的「表外按规则算」已收回（`DEC-22`）：

| 选项 | 表里没写到的文件 | 评价 |
| --- | --- | --- |
| **甲（推荐）** | **保持原名不动**（列表里标「表里没有，不参与」） | 最符合「导入 = 名字由表决定」的直觉；不会静默出错 |
| 乙 | **要求表覆盖全部文件**（数量不等就拒绝导入） | 最严格，但与「表里少几行也能用」的常见习惯冲突 |
| 丙 | **仍按规则算** | 等于保留第 4 批行为 → 那「导入」就不是真正的互斥模式 |

- 无论选哪个，**P3-4 设计文档与已上线行为都要同步回改**（§16 之外再加一处）。

**✅ 2026-09-23 已确认：「插入」= 在名字的第 N 个字符后插入「任意一段文字」**（不是只能插序号 / 日期，那是现有 `rule` 里的位置档）。

他的确认同时定下了与「自定义(rule)」的边界：

- **插入模式** = 在名字的**第 N 个字符后插入任意一段文字**（例：`报告.docx` → 在第 2 个字后插「2026」→ `报告2026.docx`）。
- **自定义（原规则化）**里的「插在第 n 个字符后」**只对序号 / 日期生效**（它插的是**变量**，不是任意文字）。
- → 两者的区别是「**插什么**」：插入 = 任意文字；自定义 = 序号 / 日期。**若他的理解不是这样，得先纠正再设计。**

> ★ **顺带一条命名副作用**：`自定义` = 现有 `rule`（规则化）**改名** → 而**历史记录里已经写死了老叫法**（`ruleSummary` 的返回值进了 `history.json`，是撤销的依据，**不能改写历史**）→ 所以界面上会**新老两种叫法并存**（新记录叫「自定义」，老记录仍显示「规则化」）。要不要在历史页加一句说明，第 5 批设计时提。

---

## 3. ★ 一个会决定整个顺序的架构判断：第 5 批是结构性前提

**现状（读代码实得）**：

```ts
// src/shared/types.ts
export type RuleMode = 'delete' | 'replace' | 'rule'      // 三模式，互斥的一层
export interface RuleConfig {
  mode: RuleMode
  caseSensitive, autoResolveConflict, regexEnabled, caseTransform
  delete:  { text: string }
  replace: { find: string; to: string }
  rule:    { /* 前言/后缀/日期/序号 4 种类型/位置 3 档 … */ }
}
```

**问题**：第 2 / 3 / 6 批都是「**新名从哪儿来**」的新来源，它们有两条路可走 ——

| 路线 | 形态 | 影响 |
| --- | --- | --- |
| **A · 落在模式层** | 新页签（如「提取」「属性」） | **依赖第 5 批**：模式集合要被 `DEC-16` 重定义，现在按三模式设计的页签，第 5 批后要重排两遍 |
| **B · 落在规则化模式内** | `rule` 组里加**新变量**（如 `{extract:1-5}`、`{ctime}`、`{folder}`），或加一个「新名来源」下拉 | **不依赖第 5 批**，`RuleConfig.mode` 一字不动，回归面小 |

**我的推荐是 B**，理由三条：

1. `DEC-16` 的结论是「模式互斥解决『算新名走哪条路』」—— 而「抠文件名的一段」「用创建时间」**不是另一条路，是规则化模式里的一个新变量**，跟 `{n}` `{d}` 同级。放在模式层是**分层放错**（这正是 P3-1 §1.1 已经立过的规矩：类型是规则化内部的参数，不是第四种模式）。
2. 走 B 则 2 / 3 / 6 批**谁都不等谁**，7 批可以按你原来的风险序老老实实做下去。
3. 走 A 的话，第 5 批必须先做完，而第 5 批本身是**风险最高的一批**（动模式层 = 动 `RuleConfig` 结构 = 动 `sanitizeRule` 白名单 = 动 CLI 语法）——把 2/3/4/6 全压在它后面，交付节奏会被它卡死。

> **请你拍这一条。** 它决定 §6 的交付顺序是「2→3→4→5→6→7→8」还是「5→2→3→4→6→7→8」。

## 4. ★ 贯穿第 2 / 4 / 8 批的技术前提：Excel / Word 要不要引第三方库

**实测**：`maodie-renamer/package.json` 的 `dependencies` 是**空的 `{}`** —— 目前这套工程**运行时零第三方依赖**（electron / vue / vite 全在 devDependencies，打包后不带）。

而第 2 / 4 / 8 批都要碰「Excel」，第 2 批还要碰「Word」：

> `.xlsx` 和 `.docx` **都是 ZIP + XML 容器**。不引库，就要自己写「ZIP 打包 / 解包 + XML 生成 / 解析」。

| 方案 | 覆盖 | 代价 |
| --- | --- | --- |
| **A · 降级为 CSV / TXT** | 用户拿到后在 Excel 里直接打开（CSV）；Word 那档砍掉 | 零依赖、最省事；但用户点「另存为 xlsx」的心理预期落空 |
| **B · 自己写最小读写器** | 单表、纯文本单元格、无样式、无公式 —— 覆盖 99% 的真实用法 | 约 200–400 行 + 一整套单测（ZIP 的 CRC32 / 本地头 / 中央目录 / XML 转义 / 共享字符串表）。**仍然零依赖** |
| **C · 引 SheetJS / exceljs** | 全格式 | 破「零依赖」；`exceljs` 体积不小，`SheetJS` 社区版对样式支持有限；且要评估许可证 |

**我的推荐是 B**，并把 Word 那档**降级为「导出 .docx 最小实现 或 只给 TXT」** —— 因为「文件名清单」这种纯文本表格，Word 相比 TXT 没有增量价值。

> **请你拍这一条。** 它决定第 2 / 4 / 8 批的地基，也是这三批开工前唯一绕不过的技术前置。

---

## 5. 逐批范围提案（每批：我的理解 / 关键歧义 / 推荐 / 依赖 / **编号用量**）

> ★★ **2026-09-22 更正：本节原来的「建议编号」全部作废，改成「编号用量」。**
> 原因：**「预留号」这个做法已经被证伪三次** —— 后面的批次实际用超了，前面的预留就得让位（`TC-53~55` 被第 2 批吃掉、`TC-58~60` 被第 3 批吃掉）。**根因不是算错，是「预留」这个做法本身不成立**（用量只能估）。
> → **新规矩**：每批开工前**按前缀全量重扫**（**必须先排除本文件**，否则上面那些数字会被当成已占用），**接着上一批实际用到的号连续取**。下面每批只给「大概要用几个号」，**不给具体号**。
> → 另外：下面每批后面会补一行占用情况，**分两种写**：**「已落地占用」**（代码里真的有）与**「已分配」**（只有设计文档占着、还没落地）。**扫编号时两种都算「不能再给别人」，但只有前者算「真正在用」。**

### 第 2 批 · 导出文件名清单

- **我的理解**：把当前列表（或改名后的结果）导成一份**清单文件**：序号 / 原文件名 / 新名 / 路径 / 状态。用途是交接与对账，不是改名。
- **关键歧义**：① 导「原名」还是「新名」还是两者对照（我推荐**对照**，这才是清单的价值）；② 导全部还是只导选中项；③ 格式（§4 定了之后可回答）；④ 是**只导名字**还是连**路径/大小/时间**一起导。
- **推荐**：导出**对照清单**（序号 / 原名 / 新名 / 状态），列范围可选「全部 / 仅选中 / 仅冲突项」。
- **依赖**：§4 的格式决策。**不依赖第 5 批。**
- **编号用量**：约 2 个 EL（入口 / 选项气泡）· 2 个 IX · 约 6 个 TC · 1 个 DEC。
  > **已落地占用**：`EL-126~127` · `IX-110~111` · `TC-50~55`（Word 那档额外扩了 `TC-55`）· `DEC-17`。
- **文件风险**：**极低 —— 只读列表、只写一个新文件，不碰任何既有文件。**

### 第 3 批 · 按文件属性命名

- **我的理解**：把文件自己的属性当成新名的**变量来源**（如 `{ctime}` 创建日期、`{mtime}` 修改日期，可能还有尺寸）。
- **关键歧义**：① 属性清单到底哪几项（§2.2）；② 尺寸要先定**单位与精度**（B/KB/MB？保留几位？）；③ 日期用**哪种格式**（复用 P3-1 已有的 5 档 `DateFormat` 样式表 —— 我推荐复用，零新增）。
- **★ 一条硬约束（读代码实得）**：`FileItem` 目前只有 `id / fullPath / dirPath / name / stem / ext / isDir / isSymlink`，**没有时间与尺寸字段**；而 `fs-scan` 是主进程扫盘产出列表的地方。要加属性就得**扩展 fs-scan 的返回**，这会同时触及 `shared/types.ts` → `sanitizeRule` 之外的第二处白名单（**列表出参**），属于本批「漏一处就出静默 bug」的重点。
- **推荐**：只加 `mtime` + `ctime` + `size` 三项，全部**只活在内存**（`DEC-07` 不持久化文件列表），日期复用既有 `DateFormat`，尺寸给「单位 + 小数位」两个小参数。
- **依赖**：跟着 §3 的路线走（推荐 B → 做成 `rule` 组内的新变量，不依赖第 5 批）。
- **编号用量**：约 2 个 EL · 2 个 IX · 约 5 个 TC · 1 个 DEC。
  > **已分配**（设计文档已占用，别再给别人）：`EL-130~131` · `IX-112~113` · `TC-56~60` · `DEC-18`。（原预留 `EL-132~134` 本批没用上。）

### 第 4 批 · 导入 Excel（按表格里的对照关系批量改名）

- **我的理解**：用户给一份表（原名 → 新名），程序按表改名，**表就是规则**。
- **关键歧义**：① 表格**怎么对上**文件 —— 按完整路径？按文件名？按表格里的序号顺序？**这是本批最大的设计点**，也是唯一会「改错文件」的地方；② 表里**查不到的行**怎么处理（跳过 / 报错 / 汇总提示）；③ 表里**文件不存在的行**怎么处理；④ 改写的是**主体**还是**全名**（扩展名保护 `EX-07` 还起不起作用）。
- **推荐**：**默认按「文件名（含扩展名）精确匹配」**，匹配不上的**一律不猜、进「未匹配」清单**（与 `F16` 冲突项同一套「跳过 + 标红 + 让人自己改」的安全底线）。扩展名保护**继续生效**（表里只给主体时，扩展名不动）。
- **依赖**：§4 的格式决策（这是**导入**，比导出难，因为要解析别人的表）。**不依赖第 5 批。**
- **编号用量**：约 3 个 EL · 3 个 IX · 约 7 个 TC · 1 个 DEC · 1 个 EX。
  > **已分配**（设计文档已占用，别再给别人）：`EL-132~134` · `IX-114~116` · `TC-61~67` · `DEC-19` · `EX-18`。（`TC` 又整体后移了一次 —— 这就是取消预留的原因。）

### 第 5 批 · 三模式 → 五模式 + 扩展名更改 + 「仅使用编号」入口

- **我的理解**：三块相对独立的事捆在一批里 ——
  1. **模式层从 3 扩到 5**（`DEC-16`，正式重开 `DEC-02`）；
  2. **扩展名更改**（位置 / 替换 / 删除 / 新扩展名）—— 这是**第一次主动碰扩展名**，与 `EX-07`「扩展名保护」正面冲突，要单独讲清两者关系；
  3. **「仅使用编号作为文件名」的显眼入口** —— 这个能力**现在就能等价做到**（取消「保留原文件名」+ 前后缀留空 + 不启用日期），所以本批只需要给它一个入口，**不需要重新实现能力**。
- **关键歧义**：① 五模式到底是哪五个（§2.3）；② 扩展名更改是**规则化模式里的一个开关**还是**第五个模式**；③ 改了 `RuleMode` 联合类型之后，**老的 `RuleConfig`（含 `mode: 'delete'`）怎么读**（规则不落盘，所以只是内存里 —— 但要确认 `DEFAULT_RULE` / CLI 的 `--rule` 兼容）。
- **★ 建议拆成两半**：`5a 模式层与扩展名`（结构性，高）＋ `5b 仅编号入口`（低，可以提前搭车到第 2 批一起做）。**请你定要不要拆。**
- **编号用量**：约 8 个 EL · 4 个 IX · 约 7 个 TC · 2 个 DEC。

### 第 6 批 · 提取文件夹名

- **我的理解**：把文件**所在文件夹的名字**当成新名的变量来源（如把 `D:\项目A\素材\1.jpg` 变成 `项目A_1.jpg`）—— 常见于「把上级目录名补进文件名」。
- **关键歧义**：① 取**直接父目录**还是**往上第 N 级**；② 取**整段**还是**抠第 n 段**（用 `\` 拆开后取第几级）；③ 文件夹名里有非法字符（`<>:"/\|?*`）时**替换成什么**。
- **推荐**：给「往上第 N 级」+「整段 / 第 n 段」两档，非法字符替换成 `_`（而不是报错）。
- **依赖**：**完全同 §3** —— 与第 3 批是同一个「新名来源」机制，**建议第 3、6 批共用一套「变量」框架**，一起设计（省一半工作，且两个来源的交互方式一致）。
- **编号用量**：约 4 个 EL · 2 个 IX · 约 4 个 TC · 1 个 DEC。
  > 建议与第 3 批**共用同一套「属性快照」机制**（文件夹名也是一种属性）。

### 第 7 批 · 文件夹合并（4 种合并类型 × 复制 / 剪切）· 2026-09-23 已落地

- **★ 本批真正移动 / 复制文件，`需求清单.md` 已标「必须单独评审」，现已落地。**
- **结论（产品负责人拍板，2026-09-23）**：① 4 种合并类型 = **只合并到同一目录 / 按扩展名各建文件夹 / 按创建日期（到日）/ 按修改日期（到日）**；② **复制 + 剪切都做，剪切默认关**；③ **不做撤销，先干跑出报告**再确认执行；④ 源文件夹**递归摊平**（本项目第一次主动递归，新能力，配 DEC-25 护栏）；⑤ **独立对话框**，不碰改名主流程；⑥ 目标**选已有 / 新建都支持**；⑦ 完成后**默认打开目标目录（可取消）**。
- **关键护栏**：不跨 symlink/junction 递归（DEC-01）；深度≤20、文件≤10000（EX-19 超上限整体拒绝）；剪切两阶段先全复制再删源（EX-20 半移动态标红、已复制不回滚）；冲突跳过+标红（守「绝不覆盖」铁律，不开覆盖档，DEC-26）。
- **编号用量（已落地占用）**：EL-141~148（8）· IX-122~125（4）· TC-80~87（8）· DEC-24~26（3）· EX-19~20（2）· SCR-10~12（3）；IPC **新增** `merge:plan` / `merge:run` 两通道。详见 `P3-7文件夹合并轻量设计确认.md` v1.0。
### 第 8 批 · 文件提取（按类型 / 按名称 / 按 Excel）· 2026-09-24 已落地

- **★ 同样会复制 / 删除文件，也要单独评审。**
- **★ 本批真正复制 / 移动文件，`需求清单.md` 已标「必须单独评审」，现已落地。**

- **我的理解**：按条件**挑出**列表里的文件，**复制（或移动）到指定目录**。
- **关键歧义**：① 复制还是移动（同第 7 批的安全问题）；② 目标目录怎么给（选一个 / 按类型分子目录 / 保持原目录结构）；③ 「按名称」是**关键字包含**还是**通配符 / 正则**（项目已有 `F10` 正则，可复用 —— 我推荐「包含 + 可用既有正则开关」）；④ 「按 Excel」与第 4 批共用解析器。
- **结论（产品负责人拍板，2026-09-24）**：① 三种筛选条件 = **按类型（图片/文档/视频/音频/压缩包/其他）/ 按名称（包含 + 正则开关，复用 F10）/ 按 Excel（复用 P3-4 解析器）**；② **复制 + 移动都做，移动默认关**；③ **不做撤销，先干跑出报告**再确认执行；④ **源 = 当前已加载列表**（不递归摊平）；⑤ **单一目标目录**（选已有/新建都支持）；⑥ 完成后**默认打开目标目录（可取消）**；⑦ 三种筛选**可叠加（AND）**。
- **关键护栏**：绝不覆盖（冲突跳过+标红，DEC-28，不开覆盖档）；移动两阶段先全复制再删源（与 P3-7 同机制，DEC-26/DEC-28 待拍板去留）；Excel 对不上的名字进 `unmatched`（EX-21，不阻塞）；按名称正则非法在计划阶段抛 EX-22。
- **编号用量（已落地占用）**：EL-149~155（7）· IX-126~129（4）· TC-88~95（8）· DEC-27~29（3）· EX-21~22（2）· SCR-13~15（3）；IPC **新增** `extract:plan` / `extract:run` 两通道。详见 `P3-8文件提取轻量设计确认.md` v1.0。

---

## 6. 交付节奏与产物清单

### 6.1 每批的产物（与第 1 批同规格）

| 产物 | 说明 |
| --- | --- |
| `<批次号>轻量设计确认.md` | 固定 13 节骨架（结论摘要 / 前提 / 界面形态 / 完整定义 / 算法边界 / 主动判断 / 与既有坑的关系 / 改动面 + **「漏一处就出静默 bug」表** / 编号清单 / 验收用例 / 测试要求 / 待同步文档 / 明确不做的事 / 待确认） |
| 画布画板 | 接在 P3-1 右边，**继续等距 1620 推位** |

### 6.2 画布落位（已实测这排块是等距的，直接推位，不必再找空位）

| 批次 | x 坐标 |
| --- | --- |
| P3-1（已完成） | 11680 |
| 第 2 批 | **13300** |
| 第 3 批 | **14920** |
| 第 4 批 | **16540** |
| 第 5 批 | **18160** |
| 第 6 批 | **19780** |
| 第 7 批 | **21400** |
| 第 8 批 | **23020** |

### 6.3 节奏建议

**一批一交，你过一遍再上下一个。** 理由：第 1 批的经验是「文档出来之后你都会改几处」，7 份一起跑等于把同一个偏差复制 7 遍。若你希望赶进度，我可以连做 2 / 3 / 6（这三批共用一个「新名来源」框架），一次交三份。

---

## 7. 我这一轮已经查实的事（都留了证据，不是凭记忆）

1. **编号上限**：见 §1，脚本全量扫，含 `EL-900/901` 的排除依据。
2. **第 1 批确实已落地**：`需求清单.md` §P3 表标 `✅ 已落地（2026-09-18 / DEC-15）`；设计文档已升到 v1.1（§13 六条已拍板）；工作区里存在 `P3-1-交付报告.html`；`package.json` 的 `test:core` 里挂着 `tests/p3-1-seq.test.ts`。
3. **运行时零第三方依赖**：`maodie-renamer/package.json` 的 `dependencies` 是 `{}`。
4. **`RuleMode` 目前只有三个值**，`RuleConfig` 是三组并列（`delete` / `replace` / `rule`）—— §3 的判断基于这个实读。
5. **`FileItem` 没有时间 / 尺寸字段** —— §5·第 3 批那条硬约束基于这个实读。
6. **这排画板块等距 1620**（P1 3580 → P2-A 5200 → P2-A深色 6820 → P2-C 8440 → P2-B 10060 → P3-1 11680）。

---

## 8. 待你勾选（就这一张，勾完我立刻开工）

| # | 问题 | 选项 | 我的推荐 |
| --- | --- | --- | --- |
| **A** | 第 2 批是哪个？ | ① 导出文件名清单（Excel/TXT/Word）　② 从文件名里抠一段当新名　③ 两个都要 | **①**，「抠一段」挪到第 5 批的模式层（§2.1） |
| **B** | 2/3/6 批走哪条路线？ | ① 落在模式层（新页签，依赖第 5 批）　② 落在规则化模式内（新变量，不依赖第 5 批） | **②**（§3，三条理由） |
| **C** | Excel / Word 怎么办？ | ① 降级 CSV/TXT　② 自己写最小 xlsx/docx 读写（仍零依赖）　③ 引 SheetJS / exceljs | **②**，Word 那档降级为 TXT（§4） |
| **D** | 顺序与节奏 | ① 按风险序 2→3→4→5→6→7→8，一批一交　② 先做 2/3/6（共用框架）一次交三份　③ 其他 | **①**，但接受 ② 若你要赶 |
| **E** | 第 3 批属性范围 | ① 只要 创建 / 修改时间　② 时间 + 尺寸　③ 再加更多 | **②**（§2.2） |
| **F** | 第 5 批拆不拆 | ① 拆成 5a 模式层 + 5b 仅编号入口　② 不拆，一起做 | **①**，「仅编号入口」可搭车到第 2 批 |
| **G** | 第 7、8 批的撤销 | ① 做「移动 / 复制」的撤销（要扩 `history.json`）　② 不做，但动手前明确警告不可撤销 | **①** —— 没有撤销的批量移动文件，风险太高 |
| **H** | 画布画板要不要 | ① 每批都出画板　② 只出文档，画板后面统一补 | **①**（你上次就是要画板） |
| **I** | 第 7 批那「4 种合并类型」 | **请你直接写出是哪 4 种**（全仓库没记，我只能猜「跳过 / 重命名 / 覆盖 / 较新者胜」） | — |
| **J** | 第 5 批五模式 | **自定义 / 插入 / 替换 / 删除 / 导入**（「…」= 预留位，非第六个）。扩展名更改**不是**模式；`导入` 当第 5 个互斥模式（见 §2.3）。 | 2026-09-22 已定 |

---

## 9. 本批明确不做的事

| 不做 | 归到哪 |
| --- | --- |
| 替产品负责人猜 §8 里 A / C / E / F / G / I / J 的答案 | **不猜。等你回。** |
| 在范围定下来之前写任何一份设计文档 | 本文档签完即开工 |
| 动 `history.json` / `prefs.json` / `window.json` 的既有格式 | 只有 §8·G 选①时才需要扩，且只**加字段**、不写迁移 |

---

## ★ P3-5（第 5 批）交付小结（2026-09-23）

| 项 | 结果 |
| --- | --- |
| 范围 | **三模式 → 五模式** + **扩展名更改**（默认关）+ 「只用编号」入口（= 第 7 个内置模板） |
| 五模式 | 自定义（`rule`）/ 插入（`insert`）/ 替换（`replace`）/ 删除（`delete`）/ 导入（`import`），互斥 |
| 新编号 | `EL-135~139` · `IX-117~120` · `TC-68~74` · `DEC-20~22`；**零新增 `EX` / `SCR` / IPC 通道** |
| 编号用量（截至本批） | `EL` 139 · `IX` 120 · `TC` 74 · `DEC` 22 · `EX` 18 · `ST` 5 · `SCR` 8 · `ADR` 6 · `F` 16 · `R` 7；`MOD` 仍未使用 |
| 第 4 批回退 | **已收回**「导入 = 叠加覆盖层」→ 互斥模式（`DEC-22`）；已同步回改 P3-4 设计文档 |
| 验证 | `typecheck` 0 错 · `lint` 0 error · 单测 **241/241** · `build` 0 错 · 真实冒烟 **74/74 × 2 夹具** |

---

## ★ P3-6（第 6 批）交付小结（2026-09-23）

| 项 | 结果 |
| --- | --- |
| 范围 | 「自定义」模式里加**第 4 个属性变量** `{文件夹}`（取**直接上级**文件夹名） |
| 新编号 | `EL-140` · `IX-121` · `TC-75~79` · `DEC-23`；**零新增 `EX` / `SCR` / IPC 通道** |
| ★ 零变更 | `RuleConfig` **无新字段**；`FileItem` / `PreviewItemInput` / `ExecuteItem` / `ResolveInput` **四个跨层形状一个都没动**（`dirPath` 本来就在）；`sanitize-rule` / `cli-args` / `table-map` 也没动 |
| 顺手根治 | 属性变量清单收进 `shared/attr-vars.ts` → **chip / 前缀提示行 / 规则摘要三处同一份数据**（「加了变量忘了改提示行」这个坑已咬过两次，本批第三次 → 从根上消掉） |
| 四条取舍 | 盘符根取**空串** · **只做直接上级** · **不做完整路径变量** · 标签叫「**文件夹名**」 |
| 编号用量（截至本批） | `EL` 140 · `IX` 121 · `TC` 79 · `DEC` 23 · `EX` 18 · `ST` 5 · `SCR` 8 · `ADR` 6 · `F` 16 · `R` 7；`MOD` 仍未使用 |
| 验证 | `typecheck` 0 错 · `lint` 0 error · 单测 **257/257** · `build` 0 错 · 真实冒烟 **79/79 × 2 夹具** |
