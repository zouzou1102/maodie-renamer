# 技术方案文档（Technical Design Document）

> 产品名：**耄耋改名（MaoDie Renamer）**
> 文档版本：**v1.0.5**
> 日期：2026-09-15
> 上游文档：`PRD.md` v1.1 · `交互说明文档.md` v1.1 · `设计规范.md` v1.0 · `需求清单.md` v0.3 · 《原型图.html》v1.1 · **`P1轻量设计确认.md` v1.0（P1 范围唯一权威）** · **`P2-A轻量设计确认.md` v1.3（P2-A 范围唯一权威）**
> 配套文档：`接口文档.md` v1.0 · `数据库设计.md` v1.0 · `排期表.xlsx` v1.0
> 本文档作用：**把「怎么做」写成工程可直接照着搭的方案**——分层边界、目录结构、算法、时序、性能预算、安全底线、打包发布。读完本文档即可开工，不需要再猜任何一处。

---

## 0. 阅读指南

### 0.1 本文档在上游文档中的位置

| 阶段 | 文档 | 回答的问题 | 状态 |
| --- | --- | --- | --- |
| 立项 | 立项书 v0.3 | 为什么做、值不值得做 | ✅ |
| 需求 | 需求清单 v0.3 | 要做什么（粗颗粒） | ✅ |
| 产品 | PRD v1.1 | 怎么做才叫做完（功能细颗粒） | ✅ |
| 交互 | 交互说明文档 v1.1 | 点了会怎样（逐条规则） | ✅ |
| 视觉 | 设计规范 v1.0 + 切图资源 | 什么颜色 / 字号 / 尺寸 / 状态 | ✅ |
| **技术（本文档）** | **技术方案文档 v1.0** | **怎么搭、怎么写、怎么保证不出事** | **v1.0** |
| 技术（配套） | 接口文档 v1.0 | 主进程与界面之间怎么对话 | v1.0 |
| 技术（配套） | 数据库设计 v1.0 | 数据落在本机哪里、长什么样 | v1.0 |
| 计划 | 排期表 v1.0 | 先做什么、做多久、怎么算做完了 | v1.0 |

### 0.2 编号体系（沿用上游 + 本文档新增）

上游全部编号**原样沿用**，不重新编号：

| 前缀 | 含义 | 来源 |
| --- | --- | --- |
| `F-xx` / `MOD-x` / `DEC-xx` | 功能 / 模块 / 定稿决策 | PRD |
| `SCR-xx` / `R-xx` / `EL-xxx` / `IX-xxx` | 界面 / 区域 / 元素 / 交互 | PRD、原型图、交互说明 |
| `ST-xx` / `EX-xxx` / `TC-xxx` | 形象状态 / 异常 / 验收用例 | PRD |
| `MS-x` | 里程碑 | PRD |

本文档新增：

| 前缀 | 含义 | 示例 |
| --- | --- | --- |
| `ADR-00x` | 架构决策记录（Architecture Decision Record） | ADR-002 本地 JSON 存储 |
| `T-x.x` | 排期任务编号 | T-1.8 两阶段改名执行器 |
| `md:<域>:<动作>` | IPC 通道命名空间 | `md:rename:execute` |

### 0.3 一句话总结本方案

> **一个纯 Node 的改名内核（可脱离 Electron 单独运行），被主进程与渲染进程共同引用；界面永远算不过内核，也永远不直接碰文件。**

这句话是三件事：

1. **改名内核是纯函数**——给什么算什么，不碰文件、不碰系统、不需要 Electron。所以它可以在命令行里跑测试（满足 PRD MS-1「命令行可验证」）。
2. **主进程和渲染进程引用同一份内核代码**——预览用的和真正执行用的是同一段逻辑，物理上不可能算出不同结果。
3. **界面没有文件操作能力**——所有 `fs` 调用都关在主进程里，界面只能递「请求」，不能递「命令」。

---

## 1. 架构总览

### 1.1 分层图

```
┌────────────────────────────────────────────────────────────────────────┐
│  渲染进程 · Renderer（Vue3 + Pinia）            ★ 无 Node 权限         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐          │
│  │ 主窗口   │ │ 历史页   │ │ 弹窗组   │ │ 预览 Web Worker  │          │
│  │ SCR-01   │ │ SCR-02   │ │SCR-03~05 │ │ 200ms 防抖重算   │          │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘          │
└────────────────────────────────┬───────────────────────────────────────┘
                                 │  IPC invoke / 事件推送
┌────────────────────────────────▼───────────────────────────────────────┐
│  预加载 · Preload（contextBridge）        ★ 唯一的门，白名单逐个登记  │
│  暴露 window.maodie = { app, window, fs, rename, history }             │
└────────────────────────────────┬───────────────────────────────────────┘
                                 │
┌────────────────────────────────▼───────────────────────────────────────┐
│  主进程 · Main（Node.js）                ★ 唯一有文件操作权限的地方    │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────────┐      │
│  │ 改名执行器 │ │ 文件系统   │ │ 存储服务   │ │ 窗口管理·菜单  │      │
│  │ 两阶段     │ │ 服务       │ │ JSON 原子写│ │ 尺寸位置记忆   │      │
│  │ 绝不覆盖   │ │ 扫描·校验  │ │ FIFO 淘汰  │ │                │      │
│  └────────────┘ └────────────┘ └────────────┘ └────────────────┘      │
└────────────────────────────────┬───────────────────────────────────────┘
                                 │ import（同一份源码）
┌────────────────────────────────▼───────────────────────────────────────┐
│  共享层 · shared                                          ★ 纯函数      │
│  规则引擎 · 冲突检测 · 扩展名保护 · 校验器 · 类型 · 错误码 · 常量      │
│  ⚠ 不 import electron、不 import fs、不 import path（自身实现路径拼接）│
└────────────────────────────────────────────────────────────────────────┘
                                 │
┌────────────────────────────────▼───────────────────────────────────────┐
│  本地磁盘                                                              │
│  userData/history.json · window.json · prefs.json                     │
│  resources/ 切图（icons / cats / illustration）· 音效                  │
└────────────────────────────────────────────────────────────────────────┘
```

### 1.2 各层职责与「不做什么」

| 层 | 职责 | 明确**不**做 |
| --- | --- | --- |
| 渲染进程 | 渲染界面、维护界面状态、收集用户输入、算预览结果、播动画 | ❌ 不读写文件 ❌ 不 import `fs` / `path` ❌ 不做冲突的最终判定 |
| 预加载 | 把主进程能力按白名单包装成 `window.maodie`，做入参形状校验 | ❌ 不含业务逻辑 ❌ 不暴露 `ipcRenderer` 原始对象 ❌ 不暴露 `require` |
| 主进程 | 文件扫描与改名、冲突权威判定、数据持久化、窗口生命周期 | ❌ 不做界面渲染 ❌ 不承担动画与展示逻辑 |
| 共享层 | 提供纯函数：算新名、判冲突、算差异区间、校验合法性 | ❌ 不碰文件系统 ❌ 不依赖 Electron ❌ 不持有状态（无全局变量、不用 `Date.now()`，时间由调用方传入） |

> **「共享层不持有状态、不取当前时间」是刻意的**：日期由调用方以参数传入（见 §4.1.2），这样同一个函数在测试里能给出确定结果，也让 DEC-03「日期取执行当天」的语义落在调用方而不是算法里。

### 1.3 进程安全配置（BrowserWindow）

| 配置项 | 值 | 为什么 |
| --- | --- | --- |
| `contextIsolation` | `true` | 隔离预加载与页面的 JS 上下文，页面改不了桥本身 |
| `nodeIntegration` | `false` | 渲染层拿不到 `require` / `process`，从根上切断误删文件 |
| `sandbox` | `false` | **需要 `false`**：预加载要 `require` 共享层的错误码常量。代价是预加载有 Node 权限——但它只有几十行、不做业务逻辑，由**代码评审 + 单测**保证它只做转发 |
| `webSecurity` | `true` | 保持默认开启 |
| `nodeIntegrationInWorker` | `false` | 预览 Worker 只算数学，不需要 Node |
| `allowRunningInsecureContent` | `false` | — |
| 内容安全策略（CSP） | 生产环境 `default-src 'self'`；`connect-src 'none'` | 直接兑现 PRD「零网络请求」——**连不出去**，不是「不连」 |

> **CSP 里的 `connect-src 'none'` 是「完全本地离线」这个卖点的技术兑现方式**：它能从浏览器内核层面拦住任何意外的网络请求（包括某个依赖偷偷加的埋点）。TC-16 的「断网验证」因此从「人工观察」升级为「架构保证」。

### 1.4 依赖方向（严格单向）

```
renderer  ──→ shared
preload   ──→ shared
main      ──→ shared
main      ──→ (Node API: fs / path)
shared    ──→ 什么都不依赖
```

**禁止反向依赖**。用 ESLint 的 `no-restricted-imports` 规则强制：

| 目录 | 禁止 import |
| --- | --- |
| `src/shared/**` | `electron`、`fs`、`node:fs`、`path`、`node:path`、`os` |
| `src/renderer/**` | `electron`、`fs`、`path`、`node:*`（除 `node:worker_threads` 类型提示） |
| `src/preload/**` | `fs`、`path`、任何 `src/main/**` |

> 这几条 lint 规则不是形式主义——它们是**这份方案里最便宜的护栏**。违反了会直接报错，AI 辅助开发时也不会「顺手」写错。

---

## 2. 技术选型

### 2.1 选型总表

| 项 | 选择 | 版本基线 | 说明 |
| --- | --- | --- | --- |
| 运行时 | Electron | 3x（LTS 主版本） | 首版锁定一个大版本，不追新 |
| 界面框架 | Vue 3 | 3.4+ | 组合式 API + `<script setup>` |
| 语言 | TypeScript | 5.x，`strict: true` | 类型即契约，见 §2.2 |
| 构建 | electron-vite | 最新 | 一份配置同时构建 main / preload / renderer |
| 状态管理 | Pinia | 2.x | 替代 Vuex，TS 推导友好 |
| 单测 | Vitest | 1.x | 与 Vite 同源，配置零成本 |
| 图表/动画 | 原生 CSS + SVG + WAAPI | — | 见 §4.4.3，**不引动画库** |
| 虚拟滚动 | 自实现 | — | 见 §4.3.4，**不引库** |
| 打包 | electron-builder | 最新 | 输出 NSIS x64 安装包 |
| 代码规范 | ESLint + Prettier | — | 含 §1.4 的依赖方向规则 |
| 持久化 | 原生 `fs` + JSON | — | 见 ADR-002、`数据库设计.md` |

### 2.2 为什么选 TypeScript（而不是 JavaScript）

本项目**不是**「有类型更优雅」的问题，而是三件具体的事：

1. **跨进程字段改名会漏改。** PRD 定义了 `FileItem`、`RuleConfig`、`RenameTask` 等结构，它们在主进程、预加载、渲染层、Worker 四处出现。用 JS 时，把 `stem` 改成 `baseName` 必然漏掉一两处，且**只在运行时炸**；用 TS 时编译期就红。
2. **安全底线必须写在类型里。** 「绝不覆盖」的一部分保障就是让「直接覆盖写」这个操作**在类型上表达不出来**（见 §4.2.1）。
3. **AI 辅助开发时，类型是给 AI 的合同。** 有类型约束时，生成代码的返工率明显更低——这是本项目选 TS 的现实理由。

**代价（诚实说明）**：写代码时多了类型声明的负担，报错信息有时不好读。缓解：`tsconfig` 开启 `skipLibCheck`，只对自己代码严格；类型集中在 `src/shared/types.ts` 一处定义，避免到处写。

### 2.3 为什么不选另外两条路

| 备选 | 好处 | 为什么不选 |
| --- | --- | --- |
| **Tauri（Rust + WebView2）** | 安装包可小到 5–10MB，内存占用低 | 需要 Rust 工具链；**「零网络请求」要靠 Rust 配置去挡**，排查成本高；本项目 PRD 已定 Electron，且要复用「一键换形象」的将来迭代。→ **保留为将来迁移方向**（PRD 已预留），但首版不做 |
| **纯 Node CLI + 界面套壳（如 node-gui）** | 体积小 | 达不到 PRD 的视觉与动效要求（猫咪 5 状态动画、差异高亮、虚拟滚动） |
| **Web 应用 + 本地服务** | 开发最快 | 直接违背「完全本地离线、零网络请求」定位，且需要用户手动开服务 |

### 2.4 Electron 版本策略

- 首版**锁定**一个大版本（如 32.x），全程不升级，避免 MS-5 打包时踩 ABI 变动。
- 大版本升级**只在 MS-5 之后**作为独立任务做，且必须重跑全量回归。
- 理由：Electron 的大版本升级常伴随 `fs` 行为、`BrowserWindow` 默认值变化。在「改名工具」这类要动用户文件的软件里，**稳定性优先于新特性**。

---

## 3. 工程目录结构

### 3.1 完整目录树

```
maodie-renamer/
├─ package.json
├─ tsconfig.json                    # 根：paths 别名
├─ tsconfig.node.json               # main + preload
├─ tsconfig.web.json                # renderer
├─ electron.vite.config.ts          # main / preload / renderer 三段构建
├─ electron-builder.yml             # NSIS 打包配置
├─ vitest.config.ts
├─ eslint.config.js                 # 含 §1.4 依赖方向规则
│
├─ src/
│  ├─ shared/                       ★ 纯函数层（主 / 渲染 / 预加载共用）
│  │  ├─ types.ts                   # FileItem · RuleConfig · RenameTask · ResolvedBatch …
│  │  ├─ errors.ts                  # MD_ERROR 错误码 + 文案映射
│  │  ├─ constants.ts               # 上限值、临时名前缀、补零范围、保留字符表
│  │  ├─ path-utils.ts              # 自己实现的路径拼接 / 取目录 / 取文件名（不用 node:path）
│  │  ├─ name-split.ts              # 主体 / 扩展名拆分（EX-07 全部细则）
│  │  ├─ rule-engine.ts             # ★ 算新名：删除 / 替换 / 规则化
│  │  ├─ rule-summary.ts            # 生成历史页的「规则摘要」文案
│  │  ├─ templates.ts               # 常用规则模板（P2-B：6 个内置模板 = 预填好的完整 RuleConfig）
│  │  ├─ diff-range.ts              # 差异高亮区间（最长公共前缀 / 后缀）
│  │  ├─ conflicts.ts               # ★ 冲突检测（批次内 + 磁盘）
│  │  ├─ validator.ts               # 非法字符 / 路径过长 / 空名 / 扩展名保护
│  │  ├─ preview.ts                 # 组装：一批 items + rule → 完整预览结果
│  │  └─ rename-plan.ts             # 计划构建：校验 → 冲突 → 策略 → 待执行清单
│  │
│  ├─ main/
│  │  ├─ index.ts                   # app 生命周期、单实例锁、创建窗口
│  │  ├─ window.ts                  # BrowserWindow 创建 / 安全配置 / 尺寸位置记忆
│  │  ├─ menu.ts                    # 应用菜单（含键盘快捷键，对应交互文档 §9）
│  │  ├─ ipc/
│  │  │  ├─ index.ts                # 注册全部通道
│  │  │  ├─ app.ipc.ts              # md:app:*
│  │  │  ├─ window.ipc.ts           # md:window:*
│  │  │  ├─ fs.ipc.ts               # md:fs:*
│  │  │  ├─ rename.ipc.ts           # md:rename:*
│  │  │  └─ history.ipc.ts          # md:history:*
│  │  └─ services/
│  │     ├─ fs-scan.ts              # 路径解析 → FileItem[] + 目录清单快照
│  │     ├─ rename-executor.ts      # ★ 两阶段改名 + 回滚 + 取消
│  │     ├─ storage.ts              # JSON 原子读写 + 损坏恢复
│  │     ├─ history-store.ts        # 历史记录 FIFO / 淘汰 / 撤销标记
│  │     └─ prefs-store.ts          # 偏好设置
│  │
│  ├─ preload/
│  │  └─ index.ts                   # contextBridge 白名单（唯一出口）
│  │
│  ├─ renderer/
│  │  ├─ index.html
│  │  └─ src/
│  │     ├─ main.ts                 # createApp + Pinia
│  │     ├─ App.vue                 # 外壳 + 视图切换（main / history）
│  │     ├─ views/
│  │     │  ├─ MainView.vue         # SCR-01
│  │     │  └─ HistoryView.vue      # SCR-02
│  │     ├─ components/
│  │     │  ├─ TitleBar.vue         # R-01
│  │     │  ├─ CatStage.vue         # R-02 形象区（状态机载体）
│  │     │  ├─ CatBiteLane.vue      # EL-013~017 咬名字动画区
│  │     │  ├─ ActionPanel.vue      # R-03 操作区
│  │     │  ├─ FileList.vue         # R-04 列表（虚拟滚动容器）
│  │     │  ├─ FileRow.vue          # 单行（含差异高亮）
│  │     │  ├─ DiffText.vue         # 差异高亮文本组件
│  │     │  ├─ RulePanel.vue        # R-05 规则区
│  │     │  ├─ SegmentedTabs.vue    # EL-040 页签组
│  │     │  ├─ ActionBar.vue        # R-06 动作区
│  │     │  ├─ StatusBar.vue        # R-07
│  │     │  ├─ Modal.vue            # 弹窗外壳
│  │     │  ├─ ResultModal.vue      # SCR-03
│  │     │  ├─ ConflictModal.vue    # SCR-04
│  │     │  ├─ ConfirmModal.vue     # SCR-05
│  │     │  ├─ HistoryCard.vue      # EL-084
│  │     │  └─ base/                # MdButton / MdInput / MdCheckbox / MdSwitch / MdBadge …
│  │     ├─ stores/
│  │     │  ├─ files.ts             # 列表与预览
│  │     │  ├─ rule.ts              # 规则配置
│  │     │  ├─ task.ts              # 执行进度与结果
│  │     │  ├─ history.ts           # 历史记录
│  │     │  └─ cat.ts               # 形象状态机
│  │     ├─ workers/
│  │     │  └─ preview.worker.ts    # ★ 预览计算（复用 shared）
│  │     ├─ composables/
│  │     │  ├─ useDebounce.ts
│  │     │  ├─ useDragDrop.ts
│  │     │  ├─ useKeyboard.ts
│  │     │  └─ useVirtualList.ts
│  │     ├─ styles/
│  │     │  ├─ tokens.css           # ★ 设计规范 → CSS 变量
│  │     │  ├─ base.css
│  │     │  └─ animations.css
│  │     └─ assets/
│  │        └─ (从 resources/ 引入图标与猫咪)
│  │
├─ resources/                       # 不打进 asar 的静态资源
│  ├─ icons/                        # 11 个 SVG ← 切图资源/icons
│  ├─ cats/                         # 5 个状态 SVG ← 切图资源/cats
│  ├─ illustration/                 # 空态插画 @2x PNG
│  ├─ sounds/                       # 成功「叮」/ 失败「噗」
│  └─ app-icon.ico                  # 安装包与窗口图标
│
├─ tests/
│  ├─ unit/                         # shared 纯函数单测（重点）
│  │  ├─ name-split.spec.ts
│  │  ├─ rule-engine.spec.ts
│  │  ├─ diff-range.spec.ts
│  │  ├─ conflicts.spec.ts
│  │  └─ validator.spec.ts
│  ├─ integration/                  # 主进程服务（真实临时目录）
│  │  ├─ rename-executor.spec.ts
│  │  └─ history-store.spec.ts
│  └─ fixtures/                     # 造文件用的辅助函数
│
├─ scripts/
│  └─ cli-verify.ts                 # MS-1 命令行验证入口（不依赖 Electron）
│
└─ docs/
   └─ (本项目的技术文档可软链至此)
```

### 3.2 目录职责的三条规则

| 规则 | 说明 |
| --- | --- |
| **`shared` 是唯一能被三方 import 的目录** | 想在三处复用的逻辑，就必须放进 `shared`，而放进去就等于接受「不能碰 fs / electron」的约束 |
| **`resources/` 与 `src/renderer/assets/` 分工固定** | `resources/` 放**运行时按路径读取**的东西（猫咪 SVG、音效、插画）；`assets/` 放**被构建工具处理**的东西（会被 hash 重命名）。猫咪要支持「一键替换」，所以必须放 `resources/` |
| **`scripts/cli-verify.ts` 是 MS-1 的交付物** | 它直接 import `src/shared`，用 Node 跑，不需要 Electron。这是 PRD「MS-1 引擎先行、命令行可验证」的落地形式 |

### 3.3 路径别名

```ts
// tsconfig.json
{
  "compilerOptions": {
    "paths": {
      "@shared/*": ["src/shared/*"],
      "@renderer/*": ["src/renderer/src/*"]
    }
  }
}
```

> `main` / `preload` **不能**用 `@renderer/*`，反之亦然——跨进程引用只允许经过 `@shared/*`。

---

## 4. 核心模块设计

### 4.1 共享层 shared（改名内核）

#### 4.1.1 名称拆分与扩展名保护（EX-07）

**函数签名**

```ts
// src/shared/name-split.ts
export interface NameParts {
  stem: string;   // 规则作用的主体
  ext: string;    // 扩展名，含点；无扩展名时为空串
}

export function splitName(name: string, isDir: boolean): NameParts;

export function joinName(stem: string, ext: string): string;  // stem + ext
```

**判定规则（逐条覆盖 PRD §4.2.4，并补足 PRD 未列出的边界）**

| 输入 | isDir | stem | ext | 依据 |
| --- | --- | --- | --- | --- |
| `报告.docx` | false | `报告` | `.docx` | PRD 已列 |
| `IMG_0001.JPG` | false | `IMG_0001` | `.JPG` | 扩展名大小写原样保留 |
| `我的.备份.tar.gz` | false | `我的.备份.tar` | `.gz` | 只切**最后一个**点 |
| `.gitignore` | false | `.gitignore` | `` | 点在第 0 位 → 不视为扩展名 |
| `README` | false | `README` | `` | 无点 |
| `文件夹名.2026` | true | `文件夹名.2026` | `` | 文件夹不拆分 |
| **`.env.local`** | false | **`.env`** | **`.local`** | **补足**：点不在第 0 位，是常规扩展名 |
| **`报告.`（结尾点）** | false | **`报告.`** | `` | **补足**：`.后无内容`不算扩展名，整名为主体（Windows 本身会去掉结尾点，此处保守处理） |
| **`a.b.c.`** | false | **`a.b.c.`** | `` | **补足**：同上规则 |
| **`..`或`.`** | false | 原样 | `` | **补足**：这两个是目录引用，理论上入列时已被主进程过滤 |
| **全角点 `报告．docx`** | false | `报告．docx` | `` | **补足**：全角点不是路径分隔符语义，不作扩展名分隔 |

**实现要点**

```
1. 若 isDir 为真 → 直接返回 { stem: name, ext: '' }
2. idx = name.lastIndexOf('.')
3. 若 idx <= 0                → { stem: name, ext: '' }        （无点，或点在开头）
4. 若 idx === name.length - 1 → { stem: name, ext: '' }        （结尾点，补足规则）
5. 否则                       → { stem: name.slice(0, idx), ext: name.slice(idx) }
```

> **扩展名一律不参与规则**。`joinName` 永远把 `ext` 原样拼回，作为「扩展名保护」的最后一道实现。

#### 4.1.2 规则引擎

**函数签名**

```ts
// src/shared/rule-engine.ts
export interface RuleContext {
  index: number;      // 该项在列表中的序号（从 0 开始）
  total: number;      // 列表总数
  date: string;       // 目标日期，格式 YYYY-MM-DD，由调用方传入（DEC-03）
}

/** 只算「新主体」，不管扩展名、不判冲突、不碰文件 */
export function computeNewStem(parts: NameParts, rule: RuleConfig, ctx: RuleContext): string;
```

**三个模式的实现**

| 模式 | 算法 | 注意 |
| --- | --- | --- |
| `delete` | `caseSensitive` 为 false 时，把 `stem` 与 `deleteText` 都做大小写归一（`toLowerCase`）后定位全部出现位置，按原索引切片删除 | **不能**用 `stem.replaceAll(deleteText, '')`——那会在大小写不敏感时失效。正确做法：循环 `indexOf` 归一化后的串，按索引拼接 |
| `replace` | 同上定位，把命中片段替换为 `replaceText`；`replaceText` 为空 = 删除 | 替换结果**不再二次匹配**（避免 `a→aa` 无限膨胀） |
| `rule` | 见下方组合公式 | 前/后缀中的 `{n}` `{d}` 变量先展开 |

**规则化模式的组合公式（★ 本方案以此为唯一权威）**

```
序号补零后的文本：n = seqStart + index * seqStep，左侧补 0 到 seqPad 位
日期文本：        d = 按 dateFormat 格式化 ctx.date

[A] keepOriginal = true,  seqPosition = 'suffix'（默认）
      prefix + d + stem + suffix + n
[B] keepOriginal = true,  seqPosition = 'prefix'
      prefix + n + d + stem + suffix
[C] keepOriginal = false, seqPosition = 'suffix'
      prefix + d + suffix + n
[D] keepOriginal = false, seqPosition = 'prefix'
      prefix + n + d + suffix
```

> ### ⚠ 上游文档矛盾修正（必须记录）
>
> PRD §4.2.3 的正文写「新名 = `前缀 + 序号 + 日期 + 原主体 + 后缀`」，但 PRD §4.2.5 的表格在**默认**情况（`seqPosition = 'suffix'`）下写的是 `前缀 + 日期 + 原主体 + 后缀 + 序号`。两处不一致。
>
> **本方案采用 §4.2.5 的表格为准**，理由：§4.2.3 是说明性文字、未区分 `seqPosition`；§4.2.5 是按 `seqPosition` 分列的实施细表，粒度更细、更可能是实现依据。§4.2.3 那句实际对应的是**前缀位**（`seqPosition = 'prefix'`）的情形。
>
> 影响：默认配置下结果不变（默认 `seqPosition = 'suffix'`，两处描述都不适用于默认位的排序细节）；**只有用户在 `keepOriginal = true` 且 `seqPosition = 'suffix'` 时，序号落位有差异**。
> **处理（已定稿，2026-09-12）**：产品侧确认**以 §4.2.5 的表格为准**。PRD 已据此升版到 **v1.1.1**：§4.2.3 中那句矛盾说明被改写为「只定义要素含义与默认值，不定义次序」，次序统一收敛到 §4.2.5 的表格（该表已标注为唯一权威依据）——**矛盾已从上游根治，不再是遗留项**。本方案按 §4.2.5 实现，`rule-engine.spec.ts` 为 A/B/C/D 四种组合各写一条单测固化。（对应 TC-06）

**变量展开与去重**

| 规则 | 实现 |
| --- | --- |
| 前缀 / 后缀中的 `{n}` | 替换为序号文本；并置 `seqConsumed = true` |
| 前缀 / 后缀中的 `{d}` | 替换为日期文本；并置 `dateConsumed = true` |
| `seqEnabled && !seqConsumed` | 按 `seqPosition` 插入序号文本 |
| `dateEnabled && !dateConsumed` | 按 `seqPosition` 插入日期文本（与序号同侧、序号在前） |
| `seqEnabled === false` | `{n}` 展开为**空串**（并保持位置），不报错 |
| `dateEnabled === false` | `{d}` 展开为空串 |

> 变量去重对应 PRD §4.2.3 的「变量去重规则」。**实现要点**：先扫一遍前后缀收集 `seqConsumed / dateConsumed`，**再**做组合，不能边替换边判断（否则 `{n}` 出现在后缀里会被漏掉「前缀已用」的判断）。

**三个模式的互斥**

`rule.mode` 决定走哪条分支，**单文件只走一条**（DEC-02）。`computeNewStem` 内部用 `switch` 而非「顺序执行三条规则」，从实现上排除「删了又替换还加序号」。

#### 4.1.3 差异高亮（F-08）

```ts
// src/shared/diff-range.ts
export interface DiffRange {
  oldStart: number; oldEnd: number;   // 原名中「被移走」的区间（半开区间）
  newStart: number; newEnd: number;   // 新名中「新增/变化」的区间
}

/** 返回 null 表示两串完全相同（无变化） */
export function computeDiffRange(oldName: string, newName: string): DiffRange | null;
```

**算法**（PRD §4.3 给定思路，此处补足边界）

```
p = 最长公共前缀长度（逐字符比较 oldName / newName，直到不等）
s = 最长公共后缀长度，且约束 p + s <= min(len(oldName), len(newName))
若 p === len(oldName) && p === len(newName) → 返回 null（完全相同）

oldStart = p, oldEnd = len(oldName) - s
newStart = p, newEnd = len(newName) - s
```

| 边界情形 | 处理 |
| --- | --- |
| 原名 = `abc.txt`，新名 = `abc.txt` | 返回 `null` → 界面显示灰色 `—`，不计入「将变化」（EX-12） |
| 原名 = `a`，新名 = `b` | `p=0, s=0` → 两侧区间都是整个串 |
| 原名 = `aa`，新名 = `aaa` | `p=2`，`s` 受约束 ≤ `min(2,3)=2`，但 `p+s` 不得超过 → `s=0`；结果 `old` 区间 `[2,2)` 为空，`new` 区间 `[2,3)` 为最后那个 `a` |
| 原名 = `ab`，新名 = `ba` | `p=0, s=0` → 两侧整串高亮（不做更聪明的匹配，因为「移动」在这种算法下就是「全变」） |
| 原名 = `报告.docx`，新名 = `报告.docx` | 注意：**比较的是完整名字（含扩展名）**，因为用户看到的是完整名 |
| 含 emoji / 中文 | 按 **UTF-16 code unit** 逐个比较即可。不需要按码点切分——因为高亮只要求「区间正确」，不对字符串做切片运算之外的语义处理 |

> **性能**：`computeDiffRange` 是 O(n)，n 为文件名字符数（通常 < 100）。1000 项 ≈ 10^5 次比较，微秒级。不需要优化。

#### 4.1.4 冲突检测（F-16 / DEC-05）

**这是整个产品最重要的一段逻辑。** 它必须同时处理两种冲突，且**绝不产生覆盖语义**。

```ts
// src/shared/conflicts.ts
export interface DirSnapshot {
  dirPath: string;              // 目录绝对路径（已归一化）
  names: string[];              // 该目录下的现存名称（不含本批次的待改名项）
  namesLower: Set<string>;      // 上面 names 的小写集合，供 O(1) 查询
}

export interface ConflictInput {
  dirPath: string;
  fromName: string;             // 当前名
  toName: string;               // 目标名（已含扩展名）
}

export type ConflictKind =
  | 'none'
  | 'batch'         // EX-05 批次内两项撞名
  | 'disk'          // EX-06 与目录下已有文件撞名
  | 'self'          // 目标名与原名相同（不区分大小写）→ 不算冲突，算「无变化」
  | 'case-only';    // 仅大小写不同（如 a.txt → A.txt）→ 特殊处理，见下

export interface ConflictResult {
  kind: ConflictKind;
  conflictWith?: string;        // 撞上的那个名字，用于界面显示「已经有同名文件了」
}

export function detectConflicts(
  inputs: ConflictInput[],
  snapshots: Map<string, DirSnapshot>,
  autoResolve: boolean
): Array<{ input: ConflictInput; result: ConflictResult; resolvedName?: string }>;
```

**目录快照的构造（关键，容易写错）**

阶段 0 拿到的目录清单，**必须先把「本批次中位于该目录的源名称」剔除**。原因：

```
目录 D 下现有：  A.txt,  B.txt
本批次要改：     A.txt → C.txt,   B.txt → A.txt

若不剔除源名称：
  检查 B.txt → A.txt 时，发现 A.txt「已存在」→ 误判为磁盘冲突。
  但实际执行时 A.txt 会先被改成 C.txt，A.txt 这个位置是空的。

剔除后：
  可用集合 = {A.txt, B.txt} − {A.txt(源), B.txt(源)} = {}
  检查 B.txt → A.txt：不在可用集合 → 不是冲突 ✓
```

> 这也正是 §4.2.1「两阶段改名」要解决的问题的**检测侧对应物**。检测侧剔除源名称 + 执行侧两阶段，是同一件事的两半，缺一不可。

**判定顺序（严格按此顺序，短路返回）**

```
对每个 input：
  1. toName 与 fromName 完全相同（含大小写）      → kind = 'self'（无变化，界面显示 —）
  2. toName 与 fromName 仅大小写不同              → kind = 'case-only'
  3. toName 在「批次已分配目标名集合」中存在（不区分大小写）→ kind = 'batch'
  4. toName 在「目录可用名称集合」中存在（不区分大小写）  → kind = 'disk'
  5. 否则                                        → kind = 'none'，并把 toName 记入已分配集合
```

> **`case-only` 的处理（本方案新增，PRD 未覆盖）**：Windows 文件系统不区分大小写，`fs.rename('a.txt', 'A.txt')` 会**直接成功且只改大小写**，但存在两个真实风险：
> 1. 部分文件系统 / 网络盘上 `rename` 行为不一致；
> 2. 若实现走「先改临时名再改目标名」，`a.txt → __md_tmp → A.txt` 是安全的，但若实现走「直接 rename」，可能被系统认为「同名」而报错或静默失败。
>
> **决定**：`case-only` **不算冲突**，允许执行，但**强制走两阶段**（阶段 1 先改成临时名，就天然绕开了这个问题）。同时在预览里**不做标红**，仍按「将变化」正常高亮。
> **验收**：新增一条单测——`a.txt → A.txt` 必须成功，且改名后目录里只有大小写不同的那一个文件。

**两种策略的实现差异**

| 策略 | `autoResolve = false`（默认，DEC-05） | `autoResolve = true` |
| --- | --- | --- |
| `batch` / `disk` 冲突项 | 标记为 skip，`reason = E_CONFLICT_BATCH / E_CONFLICT_DISK`，**不执行改名** | 在扩展名**之前**追加 `_1`、`_2`… 生成 `resolvedName`，并**重新走一遍第 3、4 步**校验；最多尝试 99 次，仍冲突则退回 skip |
| 追加序号的格式 | — | `报告.docx` → `报告_1.docx` → `报告_2.docx`；无扩展名时 `说明` → `说明_1` |
| 与「规则化模式已启用序号」的关系 | — | 两者互不干扰：规则化序号是用户命名的一部分（`发票-001`），冲突序号是系统的**补救后缀**（`发票-001_1`），语义不同，允许叠加 |

#### 4.1.5 校验器

```ts
// src/shared/validator.ts
export interface ValidationResult {
  ok: boolean;
  code?: MDErrorCode;
  detail?: string;
}

export function validateNewName(newName: string, dirPath: string): ValidationResult;
```

| 检查 | 条件 | 错误码 | 界面文案（与 PRD §4.4.4 一致） |
| --- | --- | --- | --- |
| 空名 | 主体为空，或整体只剩扩展名 | `E_EMPTY_NAME` | 改完名字就空了，检查一下规则 |
| 非法字符 | 含 `\ / : * ? " < > \|` 或控制字符（U+0000–U+001F） | `E_INVALID_CHAR` | 名称里不能包含 `\ / : * ? " < > \|` |
| 保留名 | 主体（不含扩展名，不区分大小写）为 `CON` `PRN` `AUX` `NUL` `COM1`–`COM9` `LPT1`–`LPT9` | `E_INVALID_CHAR` | 这是系统保留名，换个名字吧 |
| 结尾点 / 结尾空格 | 完整名以 `.` 或空格结尾 | `E_INVALID_CHAR` | 名字不能以点或空格结尾 |
| 路径过长 | `dirPath.length + 1 + newName.length > 255`（保守阈值，见下） | `E_PATH_TOO_LONG` | 名字太长了，系统放不下 |

> **保留名检查是本方案新增的（PRD 未覆盖，但会真实发生）**：Windows 上 `CON`、`NUL` 等是设备保留名，创建会失败。用户在删除规则里很容易把 `contract` 删成 `con`。若不提前拦截，这条会在执行阶段报一个难懂的 `EPERM`。→ 提前到预览阶段拦截，错误码复用 `E_INVALID_CHAR`（界面文案统一归入「非法」徽标）。
>
> **路径长度阈值说明**：PRD EX-02 写「> 259 字符」。本方案取更保守的 **255**（单段路径 + 目录），理由：259 是「整条完整路径」的上限（`MAX_PATH` 减 1），而在 Windows 10 1607+ 开启长路径支持后，限制变为 255 个字符**每段**。取 255 更安全——**宁可少改一条，不让它在半途失败**。若实测发现误拦，再放宽到 259。

#### 4.1.6 错误码（`src/shared/errors.ts`）

错误码**直接对应 PRD 的 EX 编号**，不另造一套语义——这样界面文案、结果弹窗、验收用例三处天然对齐。

| 错误码 | PRD 编号 | 触发条件 | 界面文案 | 可重试 |
| --- | --- | --- | --- | --- |
| `E_INVALID_CHAR` | EX-01 | 含非法 / 保留字符、结尾点或空格 | 名称里不能包含 `\ / : * ? " < > \|` | 改规则后重试 |
| `E_PATH_TOO_LONG` | EX-02 | 路径超长 | 名字太长了，系统放不下 | 改规则后重试 |
| `E_BUSY` | EX-03 | `EBUSY` / `EPERM` 且文件被占用 | 文件被占用 | 关掉占用程序后重试 |
| `E_PERM` | EX-03 | `EACCES` / `EPERM` 且为权限问题 | 无写入权限 | 换目录或提权 |
| `E_EMPTY_NAME` | EX-04 | 改后为空 | 改完名字就空了，检查一下规则 | 改规则后重试 |
| `E_CONFLICT_BATCH` | EX-05 | 批次内撞名 | 有 N 项改完会撞名 | 开「自动加序号」后重试 |
| `E_CONFLICT_DISK` | EX-06 | 与磁盘已有文件撞名 | 已经有同名文件了 | 同上 |
| `E_SOURCE_MISSING` | EX-10 | 源文件不存在（改名期间被移走） | 文件找不到了 | 刷新列表后重试 |
| `E_UNDO_SOURCE_GONE` | EX-10 | 撤销时源文件已不存在 | 有 N 个文件找不到了，已跳过 | 不可重试 |
| `E_UNDO_ALREADY` | — | 该任务已撤销过 | 这条已经撤销过啦 | 不可重试 |
| `E_LIST_EMPTY` | EX-11 | 空列表执行 | —（按钮已置灰） | — |
| `E_NO_CHANGE` | EX-12 | 全部无变化 | 当前规则不会改变任何名字 | — |
| `E_OVERFLOW` | EX-13 | 单批超 10000 | 单批最多 1 万个，超出部分未加入 | 分批处理 |
| `E_UNKNOWN` | — | 未归类的系统错误 | 出了点小意外，稍后再试试 | 是 |

> **判断 `E_BUSY` 还是 `E_PERM` 的方法**：捕获错误后看 `err.code`——`EBUSY` → `E_BUSY`；`EPERM`（Windows 上被占用与无权限都可能报这个）→ 再做一次 `fs.access(dir, W_OK)` 探测：能写则是 `E_BUSY`，不能写则是 `E_PERM`。这个探测只在**出错路径**上跑，不影响正常性能。

### 4.2 主进程

#### 4.2.1 改名执行器（★ 安全底线的落地点）

**对外只有两个入口**

```ts
// src/main/services/rename-executor.ts
export async function execute(
  req: ExecuteRequest,
  onProgress: (p: ProgressPayload) => void,
  signal: AbortSignal
): Promise<ExecuteResult>;

export async function undoTask(task: RenameTask, onProgress): Promise<UndoResult>;
```

**入参是「规则 + 清单」，不是「已经算好的计划」**

```ts
export interface ExecuteRequest {
  taskId: string;
  items: Array<{        // 只传算新名所需的最小信息
    id: string;
    dirPath: string;
    fromName: string;
    isDir: boolean;
  }>;
  rule: RuleConfig;
  date: string;         // 执行当天的日期串（渲染层传入，主进程校验其等于今天）
  autoResolveConflict: boolean;
}
```

> **为什么不让渲染层把「算好的新名」传进来**：那样主进程就是在盲改别人给的名字，一旦渲染层有 bug 或被篡改，主进程会照着错名字改文件。**让主进程用同一份 `shared` 引擎自己重算一遍**，主进程验证的、主进程执行的，是同一个东西。这条是 ADR-004。

**执行流程（两阶段，对应 PRD §4.4.3）**

```
┌─ 阶段 0：构建计划（所有判定在此完成，执行器本身不做任何决策）───────┐
│ 0.1 校验日期参数 == 本机当前日期（不一致则用本机当天并记日志）        │
│ 0.2 按 dirPath 分组 → 并发 readdir 取目录清单 → 构造 DirSnapshot     │
│     （剔除本批次在该目录的源名称，见 §4.1.4）                        │
│ 0.3 逐项 computeNewStem → joinName → validateNewName                 │
│     失败 → 记 status = 'invalid' + 错误码，不进待执行清单            │
│ 0.4 detectConflicts(snapshots, autoResolve)                          │
│     → 每个条目得：'ready' | 'skip'(冲突且不加序号) | 'ready'+resolvedName │
│ 0.5 输出 RenamePlan { ready: Entry[], skipped: Entry[], invalid: Entry[] }
└──────────────────────────────────────────────────────────────────────┘
                              ↓
┌─ 阶段 1：原名 → 临时名 ─────────────────────────────────────────────┐
│ tmpName = `__md_tmp_${taskId}_${String(i).padStart(6,'0')}` + ext     │
│ 对每个 ready 条目：                                                   │
│   1) 存在性检查：dirPath/tmpName 是否已存在？（不区分大小写）         │
│      存在 → 换下一个序号重试（最多 5 次）→ 仍失败则该项 failed        │
│   2) rename(from → tmp)                                              │
│      失败 → 该项 failed（原名未动，无需回滚），继续下一项             │
│   3) 该项状态 → 'tmp'                                                │
│ 进度：每 50 项 或 每 100ms 推送一次 onProgress                        │
│ 取消：signal.aborted → 中断，进入「回滚」                             │
└──────────────────────────────────────────────────────────────────────┘
                              ↓
┌─ 阶段 2：临时名 → 目标名 ───────────────────────────────────────────┐
│ 对每个 'tmp' 条目：                                                   │
│   1) 防御性存在性检查：dirPath/toName 是否已存在？（不区分大小写）     │
│      ★ 这是最后一道闸门——存在就回滚该项（tmp → from）并记 failed     │
│   2) rename(tmp → toName)                                            │
│      失败 → 回滚（tmp → from）+ 记 failed                            │
│   3) 该项状态 → 'done'                                               │
│ 进度同上；取消 → 中断，进入「回滚」                                   │
└──────────────────────────────────────────────────────────────────────┘
                              ↓
┌─ 收尾 ──────────────────────────────────────────────────────────────┐
│ 1. 汇总 entries（含 result / reason / 是否跳过）                      │
│ 2. 写历史记录（数据库设计.md §4.3）——只记录 result='success' 的明细    │
│ 3. 返回 ExecuteResult                                                │
└──────────────────────────────────────────────────────────────────────┘
```

**回滚（取消时）**

| 条目当前状态 | 回滚动作 |
| --- | --- |
| `ready`（还没动） | 无需处理 |
| `tmp`（已改成临时名） | `rename(tmp → fromName)` |
| `done`（已改成目标名） | `rename(toName → fromName)` |
| `failed` | 无需处理（失败时已回滚或原名未动） |

**回滚顺序：按 `tmp` / `done` 的逆序执行**——与撤销同理，避免中间态撞名（例如批次里有 `A→B`、`B→C`，回滚时先还 `B→C` 这一项，再还 `A→B`）。

**「绝不覆盖」的三重保障（对应 DEC-05、TC-07）**

| # | 保障 | 位置 |
| --- | --- | --- |
| 1 | **执行器不做决策**——冲突判定全部在阶段 0 完成，执行器只执行已判定为「安全」的清单 | `rename-plan.ts` → `rename-executor.ts` |
| 2 | **每次 `rename` 前都有存在性检查**（阶段 1 查临时名、阶段 2 查目标名），存在即拒绝 | `rename-executor.ts` |
| 3 | **禁写 API 分层**——执行器内部**不使用** `fs.rename` 的裸调用，只用下面这个封装： |

```ts
// ★ 唯一允许的改名原语。它不可能覆盖任何东西。
async function safeRename(from: string, to: string): Promise<void> {
  if (await existsInsensitive(to)) {
    throw new MdError(MD_ERROR.E_CONFLICT_DISK, to);
  }
  await fs.promises.rename(from, to);
}
```

> **ESLint 规则**：`src/main/services/rename-executor.ts` 内**禁止**直接出现 `fs.promises.rename` / `fs.rename` / `fs.renameSync`，只允许调用 `safeRename`。这条规则把「绝不覆盖」从口头约定变成了**写不出来的代码**。

**`existsInsensitive` 的实现**

```ts
async function existsInsensitive(target: string): Promise<boolean> {
  // 直接 existsSync 在 Windows 上已是不区分大小写的，但为了行为一致与可测试，
  // 统一走「读目录 + 小写集合比对」，同时能规避网络盘 / 开发机上 Linux 的差异。
  const dir = dirname(target);
  const base = basename(target);
  let entries: string[];
  try { entries = await fs.promises.readdir(dir); }
  catch { return false; }                 // 目录不存在 → 目标必然不存在
  const lower = base.toLowerCase();
  return entries.some((n) => n.toLowerCase() === lower);
}
```

**性能设计**

| 关注点 | 做法 |
| --- | --- |
| 目录清单查询次数 | 阶段 0 按 `dirPath` **去重**后并发 `readdir`，1000 个文件若在 1 个目录 → 只查 1 次 |
| `rename` 的并发度 | **串行**执行（Windows 上 `rename` 是本地元数据操作，1000 项实测通常 < 2 秒）。串行换来的是**确定的顺序、确定的进度、确定的回滚次序**——这三个性质比多省 1 秒更重要 |
| 进度推送频率 | `done % 50 === 0` 或距上次推送 > 100ms 时推一次，避免 1000 次 IPC 把界面打崩 |
| 内存占用 | 不缓存文件内容，只持有路径与名称字符串。1000 项约 100–200 KB |
| 超大批次 | 超过 2000 项时，阶段 1 / 阶段 2 之间 `await setImmediate()` 让出事件循环，保证 IPC 心跳不被饿死（对应 PRD §4.4.3「按每 50 项一次批量提交」） |

#### 4.2.2 文件系统服务（`fs-scan.ts`）

**职责：把用户给的一堆「路径」变成可用的 `FileItem[]` + 目录快照。**

```ts
export interface ResolvedBatch {
  items: FileItem[];                 // 新入列的项（不含重复、不含超限）
  snapshot: Record<string, string[]>; // dirPath → 该目录现存名称清单
  stats: {
    accepted: number;
    ignoredDuplicates: number;       // EX-09
    overflow: number;                // EX-13
    rejected: number;                // EX-08 非文件对象
    vanished: number;                // 扫描时已不存在
  };
}
```

| 步骤 | 处理 | 对应 |
| --- | --- | --- |
| 1. 过滤非文件系统对象 | 拖入的 URL / 纯文本 → `rejected++` | EX-08 |
| 2. 去重 | 与「已在列表的路径」+「本批内重复」比对，**不区分大小写**，比较前做路径归一化（统一 `\`、去掉末尾 `\`、`path.resolve` 规范化）；重复 → `ignoredDuplicates++` | EX-09 |
| 3. 取 `lstat` | 判断文件 / 文件夹，同时过滤已消失的路径 → `vanished++` | — |
| 4. 文件夹不递归 | 直接入列，**不展开内部** | DEC-01 |
| 5. 上限 | 列表总数 > 10000 → 超出部分丢弃，`overflow` 记数 | EX-13 |
| 6. 目录快照 | 收集本批涉及的所有 `dirPath`（去重）→ 并发 `readdir`（并发度 8）→ 得到 `snapshot` | 供预览与冲突检测 |
| 7. 拆分名称 | 调 `splitName` 填 `stem` / `ext` | EX-07 |

> **符号链接（symlink）处理（PRD 未覆盖，本方案定义）**：
> - `lstat` 判定为 symlink 的**文件**：按普通文件处理（改名只改链接自身的名字，不影响目标）。
> - 判定为 symlink 的**文件夹**：**不递归**（本来就不递归），但**允许改名**（与普通文件夹一致）。
> - 判定为 **junction / 挂载点**：同上，只改自身名字。
> - 一律**不做** `realpath` 解引用——理由：本项目只改名字，不需要跟随链接；解引用反而会把「改链接名」变成「改目标名」，是危险行为。
>
> **这条必须写进代码注释**：这是「不递归」之外，第二个防止「C 盘分析脚本 junction 死循环」类事故的护栏。

#### 4.2.3 存储服务（`storage.ts`）

详细字段设计见 `数据库设计.md`。此处只定接口与**原子写**实现：

```ts
export async function readJson<T>(file: string, fallback: T): Promise<T>;
export async function writeJsonAtomic(file: string, data: unknown): Promise<void>;
```

**原子写三步（保证「写到一半断电」不会损坏原文件）**

```
1. 序列化 → 写入同目录临时文件 `${file}.tmp-${process.pid}-${Date.now()}`
   （同目录很关键：跨盘 rename 不是原子操作）
2. fsync（用 filehandle.sync()）→ 确保数据真的落盘，再关闭
3. rename(tmp → file)  ← 同一文件系统上的 rename 是原子操作
```

**损坏恢复**：`readJson` 解析失败时，把坏文件重命名为 `${file}.corrupt-<时间戳>`，返回 `fallback`，并**弹一条状态栏提示**「历史记录文件损坏，已重置」。绝不静默丢数据。

**写入串行化**：所有写操作进一个 Promise 队列（`writeQueue = writeQueue.then(job)`）。理由：单进程内虽然不会真并发，但「改名完成后写历史」与「窗口关闭时写 window.json」可能几乎同时触发，串行化能避免两个 `writeJsonAtomic` 交叉写同一个文件。

#### 4.2.4 窗口管理

| 项 | 实现 |
| --- | --- |
| 默认尺寸 | 1080 × 680（PRD §5.2） |
| 最小尺寸 | 900 × 560 |
| 自绘标题栏 | `frame: false` + 自绘三按钮（对应设计规范 §4.4 标题栏按钮 38×30） |
| 尺寸记忆 | `close` 事件里读 `getBounds()` → 写 `window.json`；启动时先校验「保存的位置是否还在某个显示器的可见区域内」，不在则回到主显示器居中（**防止用户拔掉外接屏后窗口跑到屏幕外**） |
| 最大化状态 | 单独记 `maximized: boolean`，启动时先 `setBounds` 再 `maximize` |
| 单实例锁 | `app.requestSingleInstanceLock()`；第二次启动时 `focus()` 已有窗口。理由是「两个实例同时改同一批文件」会绕过文件内的写入队列 |
| 关闭行为 | 直接关闭，**不弹确认**（交互文档 §2.3 EL-004 已定） |

### 4.3 渲染进程

#### 4.3.1 组件树

```
App.vue
├─ TitleBar.vue                        R-01（EL-001~004）
├─ <MainView v-if="view === 'main'">
│  ├─ CatStage.vue                     R-02（EL-010~012, EL-013~017）
│  │  └─ CatBiteLane.vue               仅 ST-03 挂载
│  ├─ ActionPanel.vue                  R-03（EL-020~022）
│  ├─ FileList.vue                     R-04（EL-030~037）
│  │  ├─ (表头) EL-030 计数 · EL-031 全选
│  │  ├─ FileRow.vue × 可视区行         EL-033~036
│  │  │  └─ DiffText.vue               差异高亮
│  │  └─ EmptyState.vue                EL-037 空态插画
│  ├─ RulePanel.vue                    R-05（EL-040~055）
│  │  ├─ SegmentedTabs.vue             EL-040
│  │  └─ (三模式表单) DeleteForm / ReplaceForm / RuleForm
│  ├─ ActionBar.vue                    R-06（EL-060 / EL-061）
│  └─ StatusBar.vue                    R-07（EL-070~072）
└─ <HistoryView v-else>                SCR-02（EL-080~084）
   └─ HistoryCard.vue × 20
（三个弹窗用 Teleport 挂到 body，与 view 无关）
├─ ResultModal.vue                     SCR-03
├─ ConflictModal.vue                   SCR-04
└─ ConfirmModal.vue                    SCR-05
```

> **弹窗为什么用 Teleport**：设计规范 §5.4 要求「遮罩覆盖整个窗口（含标题栏）」。若弹窗嵌在视图容器里，会被父级 `overflow` 裁掉标题栏区域。Teleport 到 `body` 是唯一正确的做法。

#### 4.3.2 Pinia Store 划分

| Store | 状态 | 关键 action |
| --- | --- | --- |
`files.ts` | `items: FileItem[]`、`previewPending: boolean`、`snapshot` | `addPaths(paths)`、`removeItem(id)`、`clear()`、`requestPreview()`
`rule.ts` | `rule: RuleConfig` | `setMode(m)`、`patchRule(p)`（内部触发 `requestPreview`）
`task.ts` | `running`、`progress`、`lastResult`、`modal: 'none'\|'result'\|'conflict'\|'confirm'`、`confirmContext` | `start()`、`cancel()`、`confirmYes()`、`resolveConflicts()`
`history.ts` | `tasks: RenameTask[]`、`loading` | `load()`、`undoTask(id)`、`undoAll()`
`cat.ts` | `state: ST-xx`、`message: string` | `trigger(s)`、`tick()`（回落计时）

**存疑点：`snapshot` 放 files store 还是单独？** → 放 `files.ts`，因为它与列表同生命周期（每次入列更新）。不单独开 store，避免「列表清了快照还在」的不一致。

**跨 store 依赖方向**（防止循环）：

```
rule ──(变更)→ files.requestPreview()      ← rule 不 import files 的 state，只调 action
files ──(算完)→ 无
task  → files（读 items）、rule（读 rule）
history → 无
```

#### 4.3.3 预览 Worker（`preview.worker.ts`）

**为什么放在渲染进程的 Worker 里，而不是每次都问主进程：**

| 方案 | 每次输入的开销 | 200ms 目标 |
| --- | --- | --- |
| 每次变更 → IPC → 主进程算 → IPC 回 | 2 次跨进程序列化 + 主进程被占用（会阻塞改名任务） | 1000 项要序列化两次，约 10–30ms，**且与改名任务抢主进程** |
| **渲染进程 Worker 内算** | 1 次 `postMessage`（结构化克隆） | 1000 项约 5–15ms，**主进程完全不受影响** ← 采用 |

**协议**

```ts
// 主线程 → Worker
interface PreviewRequest {
  reqId: number;                       // 递增，用于丢弃过期结果
  items: Array<Pick<FileItem,'id'|'dirPath'|'stem'|'ext'|'isDir'>>;
  rule: RuleConfig;
  date: string;
  autoResolveConflict: boolean;
  snapshot: Record<string, string[]>;  // 目录快照（入列时由主进程带回）
}

// Worker → 主线程
interface PreviewResponse {
  reqId: number;
  items: Array<Pick<FileItem,'id'|'newStem'|'status'|'reason'|'diffRange'>>;
  stats: { changed: number; unchanged: number; conflict: number; invalid: number };
  elapsedMs: number;
}
```

**过期结果丢弃**：主线程只接受 `reqId` 等于「最新发出的 reqId」的响应，其余丢弃。这是防「用户快速连着改了 5 次，第 2 次的结果后到覆盖了第 5 次」的经典竞态。

**两层校验的分工（重要概念）**

| 层 | 时机 | 权威性 | 用途 |
| --- | --- | --- | --- |
| **预览层**（Worker） | 每次规则变更后 200ms | **尽力而为** | 让用户看清会发生什么。用的是**入列时**的目录快照，可能已过期 |
| **执行层**（主进程阶段 0） | 点「开始改名」时 | **权威** | 重新 `readdir`、重新算、重新判冲突。这才是真正决定改不改的地方 |

> 这个「两层」不是冗余，而是**必须的**：用户可能在入列后、执行前，在资源管理器里新建了一个同名文件。预览不可能知道（它没有文件系统权限），但执行层必须知道。**预览是提示，执行是判定。** 因此：`md:rename:execute` 返回的结果**可能比预览多出「跳过」项**——界面要能接受这个差异，并在结果弹窗中如实呈现。

**空列表兜底**：Worker 收到空 `items` 时立即回 `{ items: [], stats: 全 0 }`，不做任何计算。

#### 4.3.4 虚拟滚动（`useVirtualList.ts`）

**为什么不引库**：行高是**固定 36px**（设计规范 §5.1 明确写死），所以窗口化的实现只有约 40 行代码；引一个通用虚拟滚动库会带来「行高测量」「动态高度」等用不上的复杂度，还会多一个依赖。

**实现要点**

| 项 | 做法 |
| --- | --- |
| 触发阈值 | 行数 > 500 启用；≤ 500 全量渲染（对应 PRD「列表 > 500 行启用虚拟滚动」） |
| 结构 | 外层固定高度 `overflow-y: auto`；内层一个 `height = total * 36` 的占位 div；可视行用 `transform: translateY(startIndex * 36)` 定位 |
| 可视范围 | `startIndex = floor(scrollTop / 36)`，`endIndex = startIndex + ceil(viewportH / 36)`，上下各多渲染 5 行作为缓冲 |
| 滚动条加粗缓动 | 对应交互文档 IX-032 的「滚动条加粗缓动」——纯 CSS，不在 JS 里做 |
| 与差异高亮的关系 | 每行独立渲染 `DiffText`，行之间无共享状态，虚拟化不影响高亮正确性 |
| 全选 | 全选状态存在 store 的 `Set<id>` 里，**不依赖 DOM**（虚拟化下 DOM 里只有可视行） |

> **注意**：全选、行内删除按钮的悬停态、`Delete` 键删除，都必须基于 `id` 而非 DOM 索引——虚拟滚动会让「第 3 行」在滚动后变成别的文件。这条是虚拟滚动的常见陷阱，明确写在这里。

#### 4.3.5 防抖

```ts
// src/renderer/src/composables/useDebounce.ts
export function useDebounce<T extends (...a: any[]) => void>(fn: T, ms: number) {
  let timer: number | undefined;
  return (...args: Parameters<T>) => {
    if (timer) clearTimeout(timer);
    timer = window.setTimeout(() => fn(...args), ms);
  };
}
```

- 规则参数变更 → `useDebounce(postPreview, 200)`（PRD 要求 200ms；原型演示用 120ms，**实现必须是 200ms**，交互文档 IX-050 已注明）。
- 超过 2000 项时，先显示「计算中…」占位再发 Worker 消息。

### 4.4 耄耋状态机

#### 4.4.1 状态定义与优先级（沿用 PRD §4.6.1 / 设计规范 §6.3）

```
ST-03（执行中） > ST-05（失败） > ST-04（成功） > ST-02（接收） > ST-01（待机）
```

```ts
// src/renderer/src/stores/cat.ts
const PRIORITY = { 'ST-01': 0, 'ST-02': 1, 'ST-04': 2, 'ST-05': 3, 'ST-03': 4 } as const;

function trigger(next: CatState, message: string, holdMs?: number) {
  // 低优先级不得抢占高优先级（交互文档 §7.1）
  if (PRIORITY[next] < PRIORITY[current]) return;
  current = next; message = message;
  if (holdMs) fallbackTimer = setTimeout(() => backToIdle(), holdMs);
}
```

| 状态 | 进入条件 | 持续 | 回落 |
| --- | --- | --- | --- |
| ST-01 | 启动 / 无操作 | 循环 | — |
| ST-02 | IX-020 / 021 / 022 入列后 | 1500ms | → ST-01 |
| ST-03 | IX-060 点击开始改名 | 至执行结束 | — |
| ST-04 | 全部成功 / 撤销成功 | 3000ms | → ST-01 |
| ST-05 | 有跳过或失败 | 3000ms | → ST-01 |

> **两条例外**：
> - **执行中（ST-03）不接受 ST-02 抢占**：拖拽入列仍可用，只更新列表、不切形象（交互文档 §7.1）。
> - **用户取消 → 回 ST-01，不判为失败**（交互文档 §7.2）。实现上：`cancel()` 走 `backToIdle()` 而不是 `trigger('ST-05')`。
> - **回落定时器必须可取消**：切状态前 `clearTimeout(fallbackTimer)`，否则会出现「ST-04 的 3 秒计时到点，把 ST-03 拽回待机」这类幽灵 bug。

#### 4.4.2 状态机与外界的两个连接点

| 连接 | 输入 | 输出 |
| --- | --- | --- |
| ← 列表/入列事件 | `files.addPaths()` 成功后 | `trigger('ST-02', \`收到啦，${n} 个！\`, 1500)` |
| ← 执行事件 | 进度 `md:rename:progress`；结束 `md:rename:done` | `trigger('ST-03', ...)`；成功后按 result 决定 ST-04 / ST-05 |

> **状态机不自己监听 IPC**：进度事件由 `task.ts` 统一接收，再由它调 `cat.trigger()`。理由：让「谁能改形象」只有一处，避免多个监听器争抢状态。

#### 4.4.3 ST-03「一口一口咬掉旧名字」实现方案

这是全项目**最复杂的一段动画**，也是产品灵魂。技术选择：

| 项 | 方案 | 理由 |
| --- | --- | --- |
| 猫咪本体 | SVG + CSS 关键帧（头部前倾、嘴巴开合） | 只需「姿态切换」，不需要逐帧控制 |
| 卡片队列 | Vue `<TransitionGroup>` + 每张卡片一个独立定时器序列 | 卡片内容要随数据变化（旧名→新名），用 Vue 管理 DOM 最省事 |
| 卡片的飞行 | CSS `transform: translateX()` + `opacity`，WAAPI 可选 | GPU 合成，不触发重排 |
| 时序 | 单张 1600ms，按交互文档 §7.3 的 8 个关键进度点（0/14/38/42/56/62/86/100%）用 `@keyframes` 的百分比描述 | 直接照搬交互文档，不做二次创作 |
| 音效 | 「咔嚓」在 42% 处触发一次 | 需要 JS 时间点，用 `animationstart` + `setTimeout(0.42 * 1600)` |

**队列规则（照搬交互文档 §7.3）**

| 规则 | 实现 |
| --- | --- |
| 取谁 | 从 store 里筛 `status ∈ {changed, conflict}` 的项，按列表顺序，**最多取 12 个**循环 |
| 文件还是文件夹 | 不写死——卡片图标读该条目的 `isDir`。文件批次咬文件图标、文件夹批次咬文件夹图标 |
| 空列表兜底 | ST-03 在正式产品中必然有文件；为防边界崩溃，空时用 3 条演示数据 |
| 名字来源 | 读 `preview` 结果里的 `fromName` / `toName`，与列表显示**同源** |
| 与真实进度无关 | 卡片节奏 1.6s/张独立于真实进度。卡片只是「在干活」的视觉表达（PRD 明确） |
| 离开即停 | `onUnmounted` / 状态切走时 `clearTimeout` 全部定时器 + 清空卡片数组，**不留残影** |

**两条硬约束（PRD §4.6.3 / 设计规范 §6.4，代码注释里必须写一遍）**

1. **动作不得有歧义**——严禁「双肢快速上下抽动 + 躯干抖动 + 张嘴」的组合。双爪在 ST-03 中**保持静态**，只有头、嘴、尾参与动作。
2. **撕咬要卡通化**——只做干脆的「咔嚓一口」，不做尖牙特写、不做破碎 / 流血效果。卡片是「乖乖变成新名字飞走」。

**「减少动画」开关（无障碍，设计规范 §7 / §8）**

```css
:root[data-reduce-motion='true'] * {
  animation-duration: 0ms !important;
  transition-duration: 0ms !important;
}
```

开启后状态切换只换表情、不做位移，所有动效时长归零。ST-03 在减少动画模式下**不播卡片队列**，只保留「正在改名…（N/M）」文字与进度条。

> **开关入口（P2-A / DEC-11）**：这个开关此前**写好了、也能生效，但界面上根本没有设置入口**，用户打不开。
> P2-A 的建设置面（SCR-07）把它补上了（元素 EL-110 / 交互 IX-098）。

**主题（深色模式）的落地方式（P2-A / F-14）**

P2-A 把「主题」做成**一层新的令牌覆盖**，而不是给组件加 `dark` 分支 —— 组件里一行判断都不写：

1. `tokens.css` 的 `:root` 写浅色值，并新增 **15 个 P2-A 令牌**（`--md-on-brand` / `--md-scrim` / `--md-switch-track-line` 等）。
2. 深色值写在**同一个文件的 `@media (prefers-color-scheme: dark)` 块里**，逐令牌对应。
3. 主进程在 `new BrowserWindow` **之前**执行 `nativeTheme.themeSource = theme`，并用 `windowBackgroundFor(isDarkNow(theme))` 定窗口底色 → **首帧就是对的颜色**（验收 TC-30）。
4. 渲染层**不需要任何样式逻辑**：Chromium 的 `prefers-color-scheme` 会自动跟随 `themeSource`。

**零新增 IPC 通道**：接口文档把事件通道固定成 3 个，所以**不做**「主题变了」的广播 —— 第 3 步一改 `themeSource`，渲染层与 CSS 就都知道了。

`theme` 三态（`system` / `light` / `dark`）的类型、校验与兜底常量放在**共享层** `src/shared/theme.ts`（不 import electron，可被单测直跑）；主进程侧只有 `src/main/services/theme.ts` 的两行接线。

`<html data-theme>`（解析后的 `light` / `dark`）仍然会挂上，但**只用于冒烟断言与排查 —— 它不是样式的真源**。真源是系统 / 主进程给出的深浅色信号。

> ⚠️ **两处踩过的静默失败（都已修）**：
> ① `app.ipc.ts` 的 `sanitizePatch` 必须放行 `theme` —— 原实现只放行 `soundEnabled` / `reduceMotion` / `confirmThreshold`，渲染层改主题会被**静默丢掉**；
> ② `prefs-store.ts` 的 `sanitize` 里原先把 `theme` **硬钉成 `'light'`**，无论存了什么读出来都是浅色。
> 两处的共同特征是**界面看起来完全正常、值却永远不变** —— 光靠点界面发现不了，所以由单测 + 冒烟断言盯死。

### 4.5 设计规范落地

设计规范是一份**给人看的数值表**，代码需要的是**可引用的令牌**。转换关系：

```
设计规范.md §1 色彩令牌  ──┐
设计规范.md §2 字阶      ──┼─→  src/renderer/src/styles/tokens.css   （CSS 变量）
设计规范.md §3 间距圆角阴影─┤     + tokens.ts                        （TS 常量，给 SVG / Canvas 用）
设计规范.md §7 动效      ──┘
```

**`tokens.css` 示例（节选）**

```css
:root {
  --md-orange-primary: #F5A623;
  --md-orange-accent:  #FF8A3D;
  --md-orange-dark:    #E08B33;
  --md-bg-cream:       #FFFBF5;
  --md-bg-card:        #FFFFFF;
  --md-ink-1:          #3A2A20;
  --md-ink-3:          #8A8178;
  --md-ok:             #4CAF7D;
  --md-bad:            #E5544B;
  --md-space-4:        16px;
  --md-radius-card:    16px;
  --md-radius-btn:     12px;
  --md-shadow-card:    0 4px 16px rgba(180,120,60,.12);
  --md-ease-pop:       cubic-bezier(.34,1.5,.64,1);
}

/* 深色：P2-A / F-14。整块只换值，组件一行都不动 */
@media (prefers-color-scheme: dark) {
  :root {
    color-scheme: dark;
    --md-bg-cream:    #251A14;
    --md-bg-card:     #2E2119;
    --md-bg-sunken:   #1A1210;  /* 仍然必须比卡片暗，方向不能反 */
    --md-ink-1:       #F2E9E0;
    --md-shadow-card: 0 4px 16px rgba(0,0,0,.40); /* 暖棕影在深底上看不见，换纯黑 */
  }
}
```

**两条落地纪律**

| 纪律 | 检查方式 |
| --- | --- |
| 组件里**禁止**出现硬编码色值 / 圆角 / 阴影 | ESLint 自定义规则：`src/renderer/**` 的 `.vue` / `.css` 中，`#` 开头的色值只允许出现在 `tokens.css`。<br>**P2-A 起再加一道机器检查（验收 TC-32）**：单测 `tests/p2a-theme.test.ts` 扫 `src/` 下全部 `.css` / `.vue` / `.ts`，命中 `#RRGGBB` / `rgba(` 即**失败**；唯一白名单是 `src/shared/theme.ts` 里的 `WINDOW_BG` 常量（主进程建窗时还没加载 CSS，必须有字面量），并由另一条测试把它**逐字节钉在 `tokens.css` 的 `--md-bg-cream` 上** —— 两个值一旦分叉就红。**深色模式有没有漏改，不靠人眼** |
| 间距只允许落在 4px 台阶（`space-1/2/3/4/5/6/8/12`） | 代码评审检查。设计规范 §3.1 已定「不出现 5/7/9/13/18px 这类随手值」（`6px` 可接受） |

**猫咪资源的「一键替换」机制（PRD 要求的能力）**

```
resources/cats/
├─ manifest.json          ← 新增：描述每个状态对应哪个文件
├─ cat-ST-01-待机.svg
├─ cat-ST-02-接收.svg
├─ cat-ST-03-改名中.svg
├─ cat-ST-04-成功.svg
└─ cat-ST-05-失败.svg
```

```json
// resources/cats/manifest.json
{
  "version": 1,
  "states": {
    "ST-01": { "file": "cat-ST-01-待机.svg", "holdMs": 0 },
    "ST-02": { "file": "cat-ST-02-接收.svg", "holdMs": 1500 },
    "ST-03": { "file": "cat-ST-03-改名中.svg", "holdMs": 0 },
    "ST-04": { "file": "cat-ST-04-成功.svg", "holdMs": 3000 },
    "ST-05": { "file": "cat-ST-05-失败.svg", "holdMs": 3000 }
  },
  "text": {
    "ST-01": "待机中…",
    "ST-02": "收到啦，{n} 个！",
    "ST-03": "正在改名…（{n}/{m}）",
    "ST-04": "改好啦，得意！",
    "ST-05": "炸毛了！有 {n} 项没改成"
  }
}
```

> **`manifest.json` 是本方案新增的一层间接**（PRD 只要求「架构上保留一键替换能力」）。有了它，替换形象 = 换掉 `resources/cats/` 目录下的 SVG + 改 manifest 里的文件名，**代码一行不用动**，文案与停留时长也能一起换。
>
> 设计规范 §6.1 已把形象分为 6 组（`head / ear / eye / mouth / fur / paw`）。**首版把 5 个状态 SVG 当整图用**（设计规范 §9.2 说明「这五个是整图占位」）；等要做真正的骨骼动画时，再把这 6 组拆成独立图层。**本方案不为尚不存在的需求预留分层代码**——但 manifest 的 `states` 结构留了扩展位。

---

## 5. 关键流程时序

### 5.1 入列（IX-020 / 021 / 022）

```
用户拖入 / 点按钮
   ↓
renderer: useDragDrop 收集路径数组
   ↓  md:fs:resolvePaths(paths, 现有路径列表)
main: fs-scan
   ├─ 过滤非文件对象 → rejected
   ├─ 去重（不区分大小写 + 路径归一化）→ ignoredDuplicates
   ├─ lstat 判类型 / 过滤已消失 → vanished
   ├─ 上限裁剪 → overflow
   ├─ 涉及目录去重 → 并发 readdir → snapshot
   └─ splitName 拆主体 / 扩展名
   ↓  返回 ResolvedBatch
renderer: files.addPaths()
   ├─ items 追加到列表（默认名称升序）
   ├─ snapshot 存入 store
   ├─ 状态栏提示「已忽略 N 个重复项」（3s 后恢复）
   ├─ cat.trigger('ST-02', `收到啦，${n} 个！`, 1500)
   └─ files.requestPreview()
```

**拖拽蒙层的计数细节**（交互文档 IX-022 实现要点）：`dragenter` 计数递增、`dragleave` 递减，计数归零才隐藏蒙层；`drop` 后**必须重置计数为 0**。

### 5.2 预览刷新（IX-050）

```
rule 参数变更 / 列表变更
   ↓
useDebounce(postPreview, 200)
   ↓  postMessage（结构化克隆 items 的最小字段 + rule + date + snapshot）
preview.worker
   ├─ 逐项 computeNewStem → joinName
   ├─ validateNewName → status = 'invalid' 或继续
   ├─ detectConflicts → status = 'conflict' 或 'changed' / 'unchanged'
   └─ computeDiffRange
   ↓  postMessage（reqId 校验，过期即丢弃）
renderer: 更新 items 的 newStem / status / reason / diffRange
   ├─ 列表重绘（虚拟滚动，无过渡动画）
   ├─ 状态栏统计文案（交互文档 §11.2）
   └─ EL-060 按钮状态矩阵（置灰 / 可用）
```

### 5.3 改名执行（IX-060 ~ IX-063）

```
点击 EL-060
   ↓
task.start()
   ├─ 列表空？ → 无动作（EX-11，按钮已置灰）
   ├─ 无变化？ → 无动作（EX-12，按钮已置灰）
   ├─ 有冲突项？ → 弹 SCR-04
   │     ├─ EL-101「先不改（推荐）」→ 关闭，提示「已取消，冲突项未改动」
   │     └─ EL-102「自动加序号后继续」→ 勾 EL-055 → 重算预览 → 继续 ↓
   ├─ 待改 ≥ 10？ → 弹 SCR-05 二次确认 → 确认后继续 ↓
   ↓
md:rename:execute({ taskId, items, rule, date, autoResolveConflict })
main: rename-executor
   ├─ 阶段 0 构建计划（readdir + 重算 + 冲突判定）
   ├─ 阶段 1 原名 → 临时名
   ├─ 阶段 2 临时名 → 目标名
   │     每个阶段内：每 50 项 / 每 100ms → webContents.send('md:rename:progress')
   └─ 收尾：写 history.json（只记 success 明细）→ 返回 ExecuteResult
   ↓
renderer
   ├─ 进度事件 → 状态栏「已处理 N / M」+ 进度条 + ST-03
   ├─ 完成后 → ST-04（无失败）/ ST-05（有跳过或失败）
   └─ 弹 SCR-03 结果弹窗（摘要 + 前 3 条成功示例 + 失败清单）
```

### 5.4 取消与回滚（IX-061）

```
执行中点 EL-060（文案已是「取消」）
   ↓
task.cancel() → md:rename:cancel(taskId)
main: abortController.abort()
   ├─ 当前正在进行的 rename 让它跑完（不中断单个原子操作）
   ├─ 按 tmp / done 的逆序回滚
   └─ 返回 { canceled: true, rolledBack: n }
   ↓
renderer: cat.backToIdle()  ← 注意：不回 ST-05（交互文档 §7.2）
           状态栏「已取消，改了 N 项已全部改回去」
```

### 5.5 撤销（IX-071 / IX-072）

```
历史页点「撤销这一条」
   ↓
弹 SCR-05 二次确认（撤销单条文案）
   ↓ 确认
md:history:undoTask(taskId)
main: history-store
   ├─ 校验：该任务 status 必须为 'active'（否则 E_UNDO_ALREADY）
   ├─ entries **倒序**遍历（交互文档 §3.3「倒序执行」）
   │    每项：existsInsensitive(fromName) ？
   │           不存在 → 记 E_UNDO_SOURCE_GONE，跳过并继续
   │           存在   → safeRename(toName → fromName)
   ├─ 该任务 status → 'undone'（不写新记录）
   └─ 返回 UndoResult { undone, skipped, failures }
   ↓
renderer: 历史页卡片灰显 + 「已撤销」徽标；cat.trigger('ST-04', ...)
         若被撤销的文件仍在当前列表中 → 同步改回名称并重算预览
```

**「全部撤销」的实现顺序**：把 `tasks` 里所有 `status === 'active'` 的任务**按时间倒序**逐条调用 `undoTask`（最新任务先撤）。任一条失败**不中断**，最后汇总（交互文档 IX-072）。

---

## 6. 性能方案

### 6.1 性能预算与对应手段

| 指标 | PRD 要求 | 手段 | 验证方式 |
| --- | --- | --- | --- |
| 1000 项改名 | < 10 秒 | 目录清单按目录去重后并发查；`rename` 串行；进度节流 | 集成测试：造 1000 个文件实测（TC-15） |
| 1000 项预览 | ≤ 200ms | Worker 内纯计算；只传最小字段（结构化克隆开销最小）；`reqId` 丢弃过期结果 | 单测：`preview.ts` 跑 1000 项，断言 `elapsedMs < 200` |
| 万级列表滚动 | 60fps | 固定行高虚拟滚动；行内无阴影 / 滤镜；`will-change` 只加在滚动容器 | 手工：拖入 10000 项，滚动观察帧率 |
| 连续输入不卡 | — | 200ms 防抖 + Worker + 过期结果丢弃 | 手工：快速连打 20 个字符，观察列表无闪动 |
| 安装包 | 力争 < 100 MB（**首版目标，非发布阻断项**，DEC-08） | 见 §8.3 | 打包后实测并记录数值 |

### 6.2 内存

| 场景 | 占用估算 | 说明 |
| --- | --- | --- |
| 空载 | Electron 基线约 120–180 MB | 不可避免 |
| 10000 项列表 | 约 2–4 MB（只存字符串） | 不缓存文件内容 |
| Worker | 约 1000 项 / 一次计算约 1–2 MB 临时对象 | 计算完即释放，不累积 |

> **明确不做的优化**：不做文件内容哈希、不做缩略图、不做目录递归扫描。这些都会让「1000 项 < 10 秒」和「体积 < 100MB」变得困难，而它们都不是本产品的需求。

### 6.3 性能反模式（明确禁止）

| 反模式 | 为什么禁止 |
| --- | --- |
| 在渲染层用 `v-for` 渲染 10000 行 | 直接卡死。必须走虚拟滚动 |
| 每次规则变更都发 IPC 问主进程 | 主进程同时可能在跑改名任务，会互相阻塞 |
| 用 `fs.existsSync` 逐个文件检查目标是否存在 | 10000 次同步 IO，阻塞事件循环。必须用「目录清单 + 内存集合比对」 |
| 在滚动回调里做 `getBoundingClientRect` | 强制同步重排（layout thrashing）。行高固定，不需要测量 |
| 给每一行都加 `box-shadow` / `filter` | 每帧重绘 10000 个阴影，GPU 扛不住 |

---

## 7. 安全与隐私基线

### 7.1 「绝不覆盖」的四层防护（对应 DEC-05 / TC-07）

| 层 | 防护 | 失效后会怎样 |
| --- | --- | --- |
| 1. 产品层 | 默认跳过冲突，不覆盖（EL-055 默认未勾选） | 用户主动开「自动加序号」→ 仍不覆盖，只是改了个不冲突的名字 |
| 2. 计划层 | 阶段 0 完成全部冲突判定，执行器不做决策 | 执行器拿到的一定是已判定安全的清单 |
| 3. 原语层 | 唯一改名原语 `safeRename` 内含存在性检查 | 任何调用路径都覆盖不了 |
| 4. 规则层 | ESLint 禁止在 `rename-executor.ts` 直接调用 `fs.rename` | 想绕都绕不过去（编译期报错） |

**验收方式**：`rename-executor.spec.ts` 里必须有一条测试——**故意构造一个「目标已存在」的调用**，断言抛 `E_CONFLICT_DISK`，且**目标文件内容未被改变**（改名前后读一次字节比对）。

### 7.2 其他安全项

| 项 | 措施 |
| --- | --- |
| 路径穿越 | 所有来自渲染层的路径都经 `path.resolve` 规范化；重命名只允许**同目录内改名**（`fromName` 与 `toName` 的 `dirPath` 必须相同），从接口形状上就无法「把文件移到别处」 |
| 不删除任何文件 | 全部代码中**不出现** `fs.unlink` / `fs.rm` / `fs.rmdir`。仅在 `storage.ts` 里出现 `fs.unlink` 用于删除自己的 `.tmp` 临时文件，且该文件路径由程序自身生成（不接受外部输入） |
| 不跟随符号链接 | 见 §4.2.2，一律 `lstat`，不做 `realpath` 解引用 |
| 单实例 | 见 §4.2.4，防止两个实例并发改同一批文件 |
| 命名冲突（自身临时文件） | `__md_tmp_` 前缀 + 存在性检查 + 最多 5 次换号重试 |

### 7.3 隐私（兑现 PRD「完全本地离线」）

| 承诺 | 技术兑现 |
| --- | --- |
| 零网络请求 | 生产环境 CSP `connect-src 'none'`；`session.defaultSession.setPermissionRequestHandler` 一律拒绝；代码中不引入任何分析 / 上报 SDK |
| 零数据收集 | 无账号、无日志上报。日志只写本地 `userData/logs/`（可选，且默认关闭） |
| 文件不外传 | 没有任何上传代码路径 —— 这是结构保证，不是纪律保证 |
| 可离线验证 | TC-16：断网跑全流程。另有更硬的检查——构建产物中搜索 `fetch(` / `XMLHttpRequest` / `net.` 应为零命中 |

> **验收建议**：MS-5 阶段用「断网 + 抓包工具空跑一遍全流程」，作为「零网络请求」的实证。这既是产品卖点，也是 PRD 的非功能需求。

---

## 8. 打包与发布

### 8.1 electron-builder 配置要点

```yaml
# electron-builder.yml
appId: com.maodie.renamer
productName: 耄耋改名
directories:
  output: release
  buildResources: build
files:
  - out/**              # electron-vite 的构建产物
  - resources/**        # 切图与音效
  - package.json
asar: true
asarUnpack: []           # resources 走 asar 内部读取即可，无需解包
win:
  target:
    - target: nsis
      arch: [x64]
  icon: resources/app-icon.ico
  electronLanguages:     # ★ 体积优化：只留中文
    - zh-CN
nsis:
  oneClick: false                 # 让用户选安装目录
  perMachine: false               # 装到当前用户，不需要管理员
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: 耄耋改名
  artifactName: MaoDieRenamer-Setup-${version}.${ext}
  deleteAppDataOnUninstall: false # 卸载不删用户历史（用户可能还想看）
```

### 8.2 版本号与发布

| 项 | 规则 |
| --- | --- |
| 版本号 | 语义化 `主.次.修`；首版 `1.0.0` |
| 发布物 | 1 个 NSIS exe + 1 个 `.sha256` 校验文件 |
| 发布前检查单 | 全量回归（TC-01~TC-18）通过 → **体积实测并记录数值**（力争 < 100MB；超标**不阻断发布**，DEC-08）→ 断网验证 → Windows 10 / 11 各装一遍 → 卸载后不残留进程 |

### 8.3 体积控制（诚实评估）

Electron 的安装包体积是硬成本。**PRD 的「< 100 MB」已经 DEC-08 定为「首版目标、非发布阻断项」**，所以下面的措施是「尽量做好」，而不是「必须达标」：

| 措施 | 预估收益 |
| --- | --- |
| `electronLanguages: ['zh-CN']`（砍掉其他 50+ 个 locale） | −8 ~ 15 MB |
| NSIS 压缩（默认 LZMA） | 相对解压后 −60% |
| `files` 白名单（不打源码、不打测试、不打 `docs/`） | −1 ~ 3 MB |
| 不装任何体积大的依赖（本方案零运行时依赖） | 这是本方案「自实现虚拟滚动 / 不引动画库」的额外收益 |
| **预期结果** | **NSIS 安装包约 75–95 MB** |

> **已定口径（DEC-08）**：这条指标是**首版目标，不是发布阻断项**。实测超过 100 MB 时**记录数值即可**，不为压体积牺牲 §7 的任何一条安全防护，也不因此返工架构（唯一有效手段是换 Tauri，属架构级返工，不做）。
> 理由：**体积是体验指标，改名的安全与正确是正确性指标**；两者冲突时，让体积让步。

### 8.4 首次运行体验

| 项 | 处理 |
| --- | --- |
| 无需安装配置 | 双击 exe → 安装向导（可改目录）→ 桌面快捷方式 → 直接可用 |
| 首次启动 | 列表为空 + 空态插画 + ST-01 待机（DEC-07：不恢复上次列表） |
| 无登录 / 无引导弹窗 | 直接进主界面 |

---

## 9. 测试策略

### 9.1 测试分层

| 层 | 工具 | 覆盖对象 | 覆盖率目标 |
| --- | --- | --- | --- |
| 单元测试 | Vitest | `src/shared/**` 全部纯函数 | **≥ 90%**（这是安全底线所在，必须最高） |
| 集成测试 | Vitest + 临时目录 | `rename-executor`、`history-store`、`fs-scan` | 关键路径全覆盖 |
| 组件测试 | （可选）Vitest + @vue/test-utils | 差异高亮、按钮状态矩阵 | 只测有逻辑的组件 |
| 手工验收 | 人工 | TC-01 ~ TC-18、视觉与动效 | 全量 |

### 9.2 必须有单测的「安全底线」用例（缺一不可）

| # | 用例 | 断言 |
| --- | --- | --- |
| U-01 | 目标名已存在（磁盘冲突） | 抛 `E_CONFLICT_DISK`，目标文件**字节未变** |
| U-02 | 批次内两项撞名 | 两项都判 `batch` 冲突，都不执行 |
| U-03 | 循环改名 `A→B`、`B→C` | 两阶段执行成功，结果正确，无中间态失败 |
| U-04 | 大小写不同 `a.txt → A.txt` | 成功；目录中只剩 `A.txt` |
| U-05 | 扩展名保护：`报告.docx` 删除 `.` | 得 `报告docx.docx`（**扩展名点位保留**） |
| U-06 | `.gitignore` 不拆分 | `stem = '.gitignore'` |
| U-07 | 组合公式 A/B/C/D 四种 | 与 §4.1.2 的公式逐一对应 |
| U-08 | 变量去重：前缀含 `{n}` + 序号启用 | 序号只出现一次 |
| U-09 | 保留文件名 → `con` | 判 `E_INVALID_CHAR`（保留名拦截） |
| U-10 | 改后为空 | 判 `E_EMPTY_NAME` |
| U-11 | 冲突 + 自动加序号 | 依次得 `_1`、`_2`，且都与现存名不冲突 |
| U-12 | 撤销倒序 | 撤销 `A→B`、`B→C` 后两个名字都还原 |
| U-13 | 撤销时源文件已消失 | 跳过该项，其余继续，返回 `E_UNDO_SOURCE_GONE` |
| U-14 | 差异区间 | 覆盖 §4.1.3 表格里的全部边界情形 |
| U-15 | 目录快照剔除源名称 | §4.1.4 里 `A→C`、`B→A` 的例子**不**误判为冲突 |
| U-16 | FIFO 淘汰 | 21 次改名后只剩 20 条，最旧的被淘汰 |

### 9.3 手工验证辅助

`scripts/cli-verify.ts` 提供不依赖 Electron 的验证入口：

```
node --loader ts-node/esm scripts/cli-verify.ts \
  --dir ./tmp-test \
  --rule '{"mode":"rule","rule":{"prefix":"{d}-发票-","seqEnabled":true,"seqPad":3,"keepOriginal":false}}' \
  --dry-run
```

输出预览表（原名 → 新名 → 状态），用于 MS-1 阶段在**没有界面的情况下验证引擎**。这就是 PRD「MS-1 引擎先行、命令行可验证」的落地形式，也是排期表 T-1.10 的交付物。

### 9.4 无法自动化的部分（诚实列出）

| 项 | 为什么 | 替代方案 |
| --- | --- | --- |
| 视觉还原度 | 需人眼比对设计稿 | 按设计规范 §5/§6 的数值逐项核对，不靠感觉 |
| 猫咪动效「有没有歧义」 | 是**主观判断**，正是 PRD §4.6.3 强调的风险点 | 必须人工看一遍，并请至少 1 位非项目成员看（自查容易失灵） |
| 真实磁盘异常（被占用、无权限） | 环境相关 | 手工构造：用记事本占用文件；在只读目录试改 |
| 10000 项滚动流畅度 | 需真实设备体感 | 手工拖入 10000 项观察 |

---

## 10. 架构决策记录（ADR）

> 以下 6 条是本方案里**不可轻易推翻**的决策。每条都记录了「放弃了什么」——请连同代价一起评审。

### ADR-001 改名内核做成纯函数，且主进程与渲染进程共用同一份源码

**Status**：Accepted

**Context**
预览结果与执行结果必须一致（PRD TC-03 是验收项之一：预览与实际结果一致）。若预览与执行各写一套算法，两套实现的分歧是时间问题，不是概率问题——尤其本项目是 AI 辅助开发，两处代码很容易逐渐漂移。

**Decision**
把规则引擎、冲突检测、校验器、差异高亮全部做成纯函数放进 `src/shared`，不依赖 Electron、不碰 `fs`。渲染进程的 Worker 与主进程的改名执行器**import 同一份源码**。

**Consequences**
- ✅ 「预览 ≠ 执行」在结构上不可能发生。
- ✅ 内核可在命令行 / 单测里独立跑 → 满足 MS-1「命令行可验证」，也让 90% 覆盖率目标可实现。
- ✅ 将来迁 Tauri 时，这层内核可以直接搬走（PRD 已预留迁移可能）。
- ⚠️ 代价 1：`shared` 里不能用 `path` 模块（渲染层没有），要自己实现路径拼接。约 30 行代码。
- ⚠️ 代价 2：时间 / 日期必须由调用方传入，函数不能自己取「现在」。这让调用处多一个参数，但换来了可测试性。
- ⚠️ 代价 3：主进程在阶段 0 要**重算**一遍预览已经算过的东西。1000 项约 5–15ms，可以忽略；换来的是「执行的是主进程自己验证过的结果」。

### ADR-002 本地数据用 3 个 JSON 文件，不使用 SQLite

**Status**：Accepted

**Context**
PRD DEC-06 要求保留最近 20 次改名任务、DEC-07 要求不持久化文件列表。数据量小（正常情况下几十 KB），无并发、无查询、无关联。

**Decision**
用原生 `fs` 写 3 个 JSON 文件（`history.json` / `window.json` / `prefs.json`），配 `schemaVersion` 与原子写。

**Consequences**
- ✅ 零原生依赖 → electron-builder 打包不需要 `electron-rebuild`，不需要处理 ABI 对齐。**对非技术背景的独立开发者，这是最大的一笔收益**：SQLite 在 Electron 里的原生模块编译是最常见的卡点之一。
- ✅ 安装包更小（省去 `better-sqlite3` 的约 3–5 MB）。
- ✅ 出问题时用户能用记事本打开看（调试成本极低）。
- ⚠️ 代价 1：无 SQL 查询能力。但本项目没有任何「按条件查历史」的需求，也没有需要联表的数据。
- ⚠️ 代价 2：并发写需自己保证。已用「写入队列串行化」解决（§4.2.3）。
- ⚠️ 代价 3：整体重写文件而非增量更新。20 条任务最多几十 KB，全量重写耗时可忽略。
- **迁移路径**：若将来 F-13（完整历史）需要搜索 / 分页，`数据库设计.md` 附录 A 给了 SQLite 的表结构与迁移步骤。**这个决策是可逆的**——这正是选它的原因之一。

### ADR-003 预览计算放在渲染进程的 Web Worker，不走主进程

**Status**：Accepted

**Context**
PRD 要求 200ms 防抖后重算全量预览，1000 项 ≤ 200ms。同时主进程可能正在执行一场改名任务（1000 项可能跑数秒）。

**Decision**
预览在渲染进程的 Web Worker 内计算，复用 `shared` 引擎。目录快照在**入列时**由主进程一次性带回。

**Consequences**
- ✅ 预览计算完全不占用主进程，改名执行中改规则也不会互相阻塞。
- ✅ 只发生一次结构化克隆（主线程 → Worker），没有跨进程 IPC 开销。
- ✅ 预览在离线状态下完全闭环（不依赖主进程可用）。
- ⚠️ 代价 1：Worker 拿不到 `fs`，所以用途快照。**快照会过期**——用户在外部新建了同名文件，预览不会知道。
- ⚠️ 代价 2：因此**必须有两层校验**：预览是「尽力而为的提示」，执行前的阶段 0 是「权威判定」。用户在结果弹窗里可能看到比预览更多的「跳过」项。这个行为差异**必须让用户能理解**（结果弹窗里如实列出原因即可）。
- 缓解：入列时取快照 + 执行前重取快照，两层各自成立，风险有限。

### ADR-004 IPC 传「规则 + 清单」，不传「已经算好的计划」

**Status**：Accepted

**Context**
渲染层已经算好了预览（含新名），很自然可以让它把整份计划传给主进程执行，省一次重算。

**Decision**
`md:rename:execute` 只接收 `items`（id / dirPath / fromName / isDir）、`rule`、`date`、`autoResolveConflict`，主进程用共享引擎**自己重算**新名与冲突判定。

**Consequences**
- ✅ 主进程验证的、主进程执行的，是同一个东西。不存在「渲染层算错了，主进程照着改」的窗口。
- ✅ 主进程不需要信任渲染层的数据（虽然本项目是本地应用，但这个性质让「界面有 bug」永远不可能变成「文件被改错」）。
- ⚠️ 代价：多一次重算，1000 项约 5–15ms。**明确接受这个开销。**
- ⚠️ 代价：`md:rename:execute` 的返回结果可能与界面上的预览不完全一致（见 ADR-003 的代价 2）。这是**特性**：说明有一层真的在重新校验。

### ADR-005 虚拟滚动与动画自实现，不引第三方库

**Status**：Accepted

**Context**
需要支撑万级列表滚动（PRD 非功能需求）与猫咪状态动画。

**Decision**
虚拟滚动自己实现（行高固定 36px，约 40 行代码）；动画用原生 CSS + WAAPI。不引入 `vue-virtual-scroller`、`GSAP` 之类的库。

**Consequences**
- ✅ 少 2 个运行时依赖 → 对「安装包 < 100MB」和「供应链安全」都有直接收益。
- ✅ 固定行高让我们不需要「动态高度测量」这套通用库里最复杂、最容易出 bug 的部分。
- ✅ 自实现的代码 AI 全程可读可改；引库的话出问题要读别人的源码。
- ⚠️ 代价：将来若要做**可变行高**（例如让用户调节行高、或长文件名换行），自实现方案要重写。**这是可接受的风险**——设计规范 §5.1 已把行高写死为 36px，短期没有变更计划。
- ⚠️ 代价：ST-03 的动画编排要自己写定时器管理。已用「离开状态时清理全部定时器」来防内存泄漏与幽灵回调（§4.4.3）。

### ADR-006 锁定 Electron 大版本，全程不升级

**Status**：Accepted

**Context**
Electron 大版本升级常带有行为变更（`fs` 行为、窗口默认值、CSP 默认值）。本项目要动用户的真实文件。

**Decision**
首版锁定一个大版本，MS-1 ~ MS-5 期间不升级。升级作为独立任务，且必须重跑全量回归后单独发布。

**Consequences**
- ✅ 开发过程中环境稳定，不会出现「昨天还好好的，今天改名失败」这类难以归因的问题。
- ✅ 打包配置一次调通，不用反复适配。
- ⚠️ 代价：拿不到新版 Electron 的性能与体积改进。**接受**——本项目的瓶颈不是 Electron 版本。
- ⚠️ 代价：安全补丁需要手动跟进。**缓解**：锁定的是已进入稳定期的版本；发布前检查一次是否有该版本的安全公告。

---

## 11. 已知取舍与遗留项

| # | 项 | 取舍 / 现状 | 建议处理时机 |
| --- | --- | --- | --- |
| 1 | **规则组合公式**（原上游文档矛盾） | ✅ **已关闭**：产品侧确认以 PRD §4.2.5 表格为准；PRD 已升 **v1.1.1**，次序统一收敛到该表（标注为唯一权威依据），§4.2.3 改为只定义含义与默认值。本方案四种组合各有单测固化 | 无需动作 |
| 2 | **安装包体积** | ✅ **已关闭（DEC-08）**：指标改为「首版目标、**非发布阻断项**」；实测超标仅记录数值，不牺牲 §7 的安全防护、不返工架构 | MS-5 实测并记录数值 |
| 3 | **预览与实际执行的差异** | 两层校验的必然结果（ADR-003）。执行结果可能比预览多几个「跳过」 | 结果弹窗如实呈现即可；不需要额外机制 |
| 4 | **猫咪形象仍是占位 + 版权 Accepted Risk** | 本方案用 `manifest.json` 把「一键换形象」做成改目录就能生效 | 发布前复核版权；若换形象，代码零改动 |
| 5 | **深色模式（F-14）** | ✅ **已关闭（DEC-11，2026-09-15 / P2-A）**：**暖褐色深底**方案（非冷灰黑）；**品牌橘三色两主题完全相同**；深色令牌挂在 `@media (prefers-color-scheme: dark)` 上（**不挂 `[data-theme]`** —— 挂属性就得等 JS 跑起来，首帧必然先浅后深）；主进程建窗前设 `themeSource` + 窗口底色 → 首帧不闪（TC-30）。**零新增 IPC 通道**。顺手补上 P0 遗留的「设置入口缺失」（SCR-07，见第 11 条） | 已落地（`src/shared/theme.ts` / `src/main/services/theme.ts` / `tokens.css` / `stores/prefs.ts` / `SettingsModal.vue`）；令牌覆盖率由 **TC-32** 自动盯 |
| 11 | **「设置入口缺失」P0 缺口** | ✅ **已关闭（DEC-11 / P2-A）**：`Prefs` 的音效 / 减少动画 / 二次确认阈值 / 主题此前**界面上一个都改不了**（`setPrefs` 只在 `stores/prefs.ts` 内部被调用），交互说明 §9 与设计规范 §8 要求的「设置中提供『减少动画』开关」**实际上达不到**。P2-A 的 SCR-07 把入口补上（二次确认阈值按 YAGNI 仍不暴露，它是内部常量） | 已落地（`SettingsModal.vue` / `TitleBar.vue` 的 EL-106） |
| 12 | **主题切换后 `BrowserWindow.backgroundColor` 不跟着改** | 它只在建窗时设一次。页面自身的底色（`--md-bg-cream`）覆盖整窗，所以正常使用看不出来；理论风险是**深色下拖拽改变窗口尺寸时，可能有一瞬间露出建窗时的浅色底**。本轮未处理 | 若真被观察到，在 `applyThemeSource` 里对已有窗口补一次 `setBackgroundColor` 即可（约 3 行）。**未观察到，先不做** |
| 13 | **P2-C：历史完整版 + 命令行入口**（F-13 / F-15） | ✅ **已落地（2026-09-15）**：① 历史明细**只渲染前 100 条**（超出提示「还有 N 项未显示」）—— 刻意取舍，避免几千条明细卡顿；② **清空历史**新增**第 16 个请求响应通道** `md:history:clear`（事件通道仍是 3 个，不变）；③ 清空的两条红线 —— **只删记录、绝不触碰任何文件**，以及**界面必须重置淘汰检测基准**（否则用户主动清空会被 `stores/history.ts` 假报成「较旧的记录已被清理」）；④ `AppInfo` 加 `execPath` 字段，**加字段不加通道**；⑤ **F-15 CLI 的 `--dry-run` 是默认行为**，必须显式 `--yes` 才真改（命令行没有预览界面，与「绝不毁数据」一致）。详见 `P2-C轻量设计确认.md` v1.2 | P2-C 排期时落地。⚠️ **CLI 完整语法属「接口草案」**，需与 `接口文档.md` 一起定稿后再实现 |
| 14 | **多规则链式组合** | **已推迟**（PRD §11.3 的触发条件原文是「若用户反馈强烈需重新评估」，**目前零反馈**）。它才需要把 `computeNewStem` 重构为「可增删排序的规则数组」，届时 **DEC-02 需重新评审**。挂载点已预留：P1 的「⚙ 进阶设置」折叠区（**只有「多规则链」进这里**；P2-B 的模板库是给不会配规则的人的一键入口，**刻意常显在外**，见遗留项 15） | 待用户反馈；建议排在 P2-B（模板库）之后 |
| 15 | **P2-B：常用规则模板库**（F-12） | ✅ **已落地（2026-09-18 / DEC-14）**：① **模板 = 一份预填好的完整 `RuleConfig`**，套用 = **整份替换**；② 新增 `src/shared/templates.ts`（纯数据：`RULE_TEMPLATES` + `cloneTemplateRule()` + `templateAppliedMessage()`），**必须深拷贝** —— 直接赋值等于把模板常量的引用交出去，用户随后每次改参数都会改到常量本身，症状是「模板越用越怪」而**界面永不报错**；③ `RulePanel.vue` 顶部加常显的模板条（**不进折叠区**），`stores/rule.ts` 加 `applyTemplate()`；④ 历史卡片摘要**只在渲染层截断**（`truncateSummaryForDisplay()`），`buildRuleSummary` **一个字不动** —— 那一层的数据要落盘，截断等于改持久化格式；⑤ **零新增 IPC / 持久化 / 令牌**（规则本来就不落盘，`StoreName` 只有 history / window / prefs） | 已落地；`tests/p2b-templates.test.ts` 18/18 + 冒烟 7 条 |
| 6 | **P1 正则（F-10）/ 大小写（F-11）** | ✅ **已关闭（DEC-10，2026-09-15）**：两者都是**正交选项**，**不需要**重构 `computeNewStem` —— F-10 是删除 / 替换模式内的一个**匹配开关**；F-11 是模式结果算完后的一次**收尾**（`新名主体 = 大小写转换(模式结果)`）。**DEC-02「模式互斥」不变**。小白隔离靠默认折起的「⚙ 进阶设置」折叠条（**不新增页签**）。**「多规则链」明确归 P2** —— 本文档原写「P1 需重构为规则数组顺序执行」已作废 | 已落地（`src/shared/rule-engine.ts` 的 `compileRegex` / `spliceAllRegex` / `applyCaseTransform`）；**EX-16 见第 10 条** |
| 7 | **`sandbox: false`** | 因为预加载要 `require` 共享层的常量。这是一个明确的安全折衷 | 若想收紧，可把错误码 / 常量在构建时内联进 preload 产物，即可开 `sandbox: true`。留给后续优化 |
| 8 | **未做自动更新** | 本方案不含 electron-updater。PRD 也未要求 | 用户量起来后再考虑；会引入网络请求，与「零网络」定位需要重新权衡 |
| 9 | **`scripts/cli-verify.ts` 是临时工程** | 它依赖 ts-node 之类的开发依赖 | 可在 MS-2 之后保留（它有回归价值），也可删除 |
| 10 | **EX-16 ReDoS 主动中止未落地** | 设计稿《P1轻量设计确认》§7 要求「预览 Worker 每处理若干项检查一次耗时，累计超 3 秒即中止」。本轮**只落地了 `pattern 长度 ≤ 200`** 这一纯兜底（`REGEX_MAX_LENGTH`），Worker 侧的按项计时中止未做 | 建议 P1 收尾或 P2 做；需要动预览 Worker 的循环（`src/renderer/src/workers/preview.worker.ts`），成本约 0.5 人日 |

---

## 12. 变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| **v1.0** | 2026-09-12 | 首次成文。确立「纯函数内核 + 三层进程边界」架构；产出 6 条 ADR；修正上游 PRD §4.2.3 与 §4.2.5 的公式矛盾；补足 PRD 未覆盖的边界（symlink 处理、Windows 保留名、结尾点、`case-only` 改名、目录快照剔除源名称、预览与执行两层校验）；明确「绝不覆盖」的四层防护与代码级禁写规则 |
| **v1.0.1** | 2026-09-12 | **技术方案评审后的三处落定（与 PRD v1.1.1 同步）**：① 规则组合公式**已定稿**采用 §4.2.5 表格，矛盾已从上游 PRD 根治（§4.1.2 的修正块已转为定稿记录）；② 体积指标依 **DEC-08** 放宽为「首版目标、非发布阻断项」，§6.1 / §8.2 / §8.3 相应改写；③ 历史条数上限依 **DEC-09** 正式定为「常态 20 条、极端场景可少于 20 条 + 界面提示」，§11 遗留项 1 与 2 关闭 |
| **v1.0.2** | 2026-09-15 | **P1 落地后的回填（与 `P1轻量设计确认.md` v1.0 同步）**：① §11 遗留项 6 **改判** —— P1 的 F-10 正则 / F-11 大小写均为**正交选项**，**不需要**重构 `computeNewStem`，**DEC-02 不变**，「多规则链」明确归 **P2**（原写「P1 需重构为规则数组顺序执行」作废）；② 新增遗留项 10：**EX-16 的 ReDoS 主动中止未落地**，本轮只做了 `pattern 长度 ≤ 200` 兜底；③ 落地位置：`src/shared/rule-engine.ts` 的 `compileRegex` / `spliceAllRegex` / `applyCaseTransform`，`RuleConfig` 新增 `regexEnabled` / `caseTransform` 两个字段 |
| **v1.0.3** | 2026-09-15 | **P2-A 落地后的回填（与 `P2-A轻量设计确认.md` v1.2 / DEC-11 同步）**：① **§11 遗留项 5 关闭** —— 深色模式（F-14）已落地，含方案要点与落地位置；② 新增遗留项 **11**（「设置入口缺失」P0 缺口已关闭）与 **12**（主题切换后窗口底色不跟着改，如实记录未处理）；③ **§4.4 新增「主题（深色模式）的落地方式」** —— 令牌覆盖 + `@media (prefers-color-scheme: dark)` + 主进程建窗前定 `themeSource`/底色 + **零新增 IPC 通道**，并记录两处「界面正常但值不变」的静默失败；④ **§4.5** `tokens.css` 示例补深色块，落地纪律表新增**可自动检查的令牌覆盖率（TC-32）**及其白名单；⑤ 顺手修好 §12 表格里被打断的一行 |


| **v1.0.4** | 2026-09-15 | **P2-C（历史完整版 + 命令行入口）设计定稿（与 `P2-C轻量设计确认.md` v1.2 / DEC-12 同步）**：① **§11 新增遗留项 13**（P2-C 内容与两条实现红线）与 **14**（多规则链已推迟，待用户反馈）；② **契约变更：请求响应通道 15 → 16**（新增 `md:history:clear`），事件通道仍 3 个不变；`AppInfo` 加 `execPath` 字段（**加字段不加通道**）。**两条红线必须写进实现**：**只删记录、绝不触碰任何文件**；**清空后必须重置淘汰检测基准**（否则用户主动清空会被假报成「较旧的记录已被清理」）。详见 `接口文档.md` v1.0.3 |
| **v1.0.5** | 2026-09-18 | **P2-B（常用规则模板库）落地后的回填（与 `P2-B轻量设计确认.md` v1.0 / DEC-14 同步）**：① §3 目录树登记 **`src/shared/templates.ts`**；② **§11 新增遗留项 15**（P2-B 落地内容 + 两条实现红线：模板必须深拷贝、摘要截断只在渲染层）；③ 澄清遗留项 14 的挂载点 —— 「⚙ 进阶设置」折叠区只留给**多规则链**，**模板库不进折叠区**。**契约未变**（仍为 16 个请求响应通道）。 |

**技术方案文档 v1.0.4 · 2026-09-15**
