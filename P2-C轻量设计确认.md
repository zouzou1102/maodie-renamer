# P2-C 轻量设计确认（历史完整版 + 命令行入口）

> 产品名：**耄耋改名（MaoDie Renamer）**
> 文档版本：**v1.4**
> 日期：2026-09-17（v1.1 决策闭环；v1.2 验收编号修正；v1.3 落地侧修订；**v1.4 P2-C 增量 · 命令行小白化 / DEC-13**，变更见 §11）
> 上游文档（同步后版本）：`PRD.md` v1.1.5 · `交互说明文档.md` v1.6 · `设计规范.md` v1.5 · `接口文档.md` v1.0.4 · `数据库设计.md` · `技术方案文档.md` v1.0.4
> 配套交付：设计稿画布新增画板 **「P2-C 增量设计 · 历史完整版 + 命令行入口」**
> 文档状态：**已落地并通过验收**（2026-09-15 主体；**2026-09-17 增量 C+ 命令行图文教程**）—— 落地结果与「没验到的」见 §13。
> ⚠️ 落地时发现两处与文档不符，已按代码事实修正并在 §13 记录：① 明细的「（无变化）」行是**死规格**；② CLI 语法**已定稿**并全文写进 `接口文档.md` §10。

---

## 0. 结论摘要

P2-C 做三件事，**都不新增颜色令牌**（全部复用 P2-A 已定义的令牌，所以深浅两套主题自动成立）：

| 块 | 内容 | 来源 |
| --- | --- | --- |
| **A** | 历史卡片能看**完整明细**（不只看首条示例） | PRD F-13「完整版」 |
| **B** | **清空历史记录**入口 | 《数据库设计.md》§4.7 早就设计了，一直没做 |
| **C** | 设置里露出**命令行用法** | PRD F-15 |
| **C+**（**P2-C 增量 / DEC-13**） | 命令行**小白化**：说明文案重写 + **三步图文教程**（SCR-08） | 落地后的实测反馈：C 做完了，但小白用户打开只看到一串 `maodie.exe --rename …`，**不知道那是什么、也不知道要粘到哪里** |

### P2 整体进度

| 批次 | 内容 | 状态 |
| --- | --- | --- |
| P2-A | 设置面 + 深色模式（F-14） | 设计完成（v1.1），**已交开发落地中** |
| **P2-C（本文档）** | F-13 历史完整版 + F-15 命令行入口 | **设计完成，待评审** |
| P2-B | F-12 改名规则模板库 | 未开始（建议等 P2-A 测试通过后再设计） |
| — | 多规则链式组合 | **推迟**，等用户反馈 |

> **关于顺序**：P2-C 之所以能在 P2-A 落地前先设计，是因为它**只用现有组件与令牌、不新增任何视觉**，不受深色模式实现结果影响。P2-B 要新增组件（模板列表），所以建议等 P2-A 的令牌被实现验证过再设计。

---

## 1. A · 历史卡片 · 查看完整明细

### 1.1 现状与缺口

P0 的历史卡片只显示**首条示例** + 一行「（等 N 项）」（`HistoryCard.vue`）。用户想确认「上次到底改了哪些」是看不到的 —— 这就是「完整版」最该补的一块。

### 1.2 设计

在「首条示例」下方新增一行**展开条**：

```
[2026-09-11 14:32]                    [可撤销]
删除「广告」
共 6 项 · 成功 6
abc广告.txt → abc.txt （等 6 项）
⌄ 查看全部 6 项                    ← 新增（默认收起）
                            [撤销这一条]
```

点开后**就地展开**（不弹窗、不跳页）：

```
⌃ 收起
┌────────────────────────────────────────────┐
│  1   abc广告.txt   →   abc.txt              │
│  2   【某某公众号】x.mp4   →   x.mp4          │
│  3   广告计划表.xlsx   →   计划表.xlsx        │
│  4   项目资料   →   —（无变化）              │
│  5   会议广告记录.docx   →   会议记录.docx    │
│  6   广告素材-01.png   →   素材-01.png        │
└────────────────────────────────────────────┘
```

### 1.3 规格

| 项 | 规格 |
| --- | --- |
| 展开条 | 一行，左侧 12px 箭头 + 文字「查看全部 N 项」 |
| 收起态箭头 | 朝下，色 `ink-3`；文字 12.5 Medium `ink-3` |
| 展开后 | 箭头朝上并转 `orange-dark`；文字改「收起」，同样转 `orange-dark` |
| 明细容器 | 底 `bg-sunken`（浅 `#F7EFE3` / 深 `#1A1210`）、圆角 10px、内边距 8×10px |
| 容器最大高 | **220px，超出滚动** |
| 每行 | 序号（Inter 11.5 `ink-4`，宽 20 右对齐）+ 「原名 → 新名」（12.5 `ink-2`），行高 26px |
| ~~无变化的行~~ | **不实现**（v1.3 修正）：`RenameTask.entries` 的定义是「**只记录改名成功的明细**」（types.ts），执行器也只 push 成功项 —— `toName === fromName` 的项根本进不来，这是条**死规格**。落地时按代码事实处理，没写这条分支（理由见 §13） |
| **渲染上限** | **只渲染前 100 条** |

### 1.4 三个刻意的决定

1. **渲染上限只渲染前 100 条**，其余显示一行「还有 N 项未显示（共 M 项）」。
   **这是取舍，不是遗漏**：一个任务可能有几千条明细（批量改 1000 个文件），全渲染会卡。
   没上虚拟滚动（项目里有 `useVirtualList`），是因为**为一个展开区上虚拟滚动太重**——那套东西是给主列表用的。
2. **展开是纯 UI 状态**，不进数据模型、不进 IPC（与 P1「进阶设置」折叠区同理）。
3. **每条卡片各自独立展开、不联动**。没做手风琴，因为用户可能想**对比两批**改名的明细。

另外：**「已撤销」的卡片同样可以展开** —— 撤销之后明细仍有参考价值（「上次改了啥、已经改回去了」）。

---

## 2. B · 清空历史记录

### 2.1 位置与形态（沿用既有设计）

位置与形态按 **《数据库设计.md》§4.7** 的原方案，不另发明：

> 提供一个 **「清空历史记录」** 入口（**历史页 footer，幽灵按钮样式**）。
> **只删记录，不碰任何文件**（这是必须写清的，否则用户会以为「清空历史 = 删文件」）。

**它要解决的是隐私问题**：历史记录里存的是**完整文件路径**（可能暴露目录结构、项目名、客户名）。用户「我想删掉它」的诉求合理且常见，而当前只能自己去 `%APPDATA%\MaoDieRenamer\history.json` 里翻 —— 对一个非技术用户来说**等于没有这个能力**。

### 2.2 按钮形态

| 项 | 规格 |
| --- | --- |
| 位置 | 历史页 footer **右侧**（左侧是既有的保留规则说明），与说明同一行 |
| 形态 | **幽灵按钮 + `bad` 色文字**：底透明、圆角 10px、内边距 6×14px、文字 12.5 Medium `bad` |
| 悬停 | 底 `bad-bg`（浅 `#FDEBEA` / 深 `#3A2320`） |
| 令牌 | **全部是既有令牌，不新增颜色** |

**为什么不用实心红按钮**：太醒目、太容易误点。
**为什么不用普通灰色幽灵按钮**（和「清空列表」一样）：这是全软件**唯一一个「删掉用户数据」的操作**，应该在视觉上与「清空列表」（那只移除列表项、不删文件）区分开。

### 2.3 ★ 安全阀（必须实现）

清空历史 = 删掉撤销记录 → **那些改名就再也撤不回来了**。所以二次确认必须分两种：

| 情况 | 正文 | 主按钮 |
| --- | --- | --- |
| 没有「可撤销」任务 | 将删除全部 N 条改名记录。<br>**文件本身不会被删除，也不会被改名。** | 清空 |
| **有**「可撤销」任务 | 上面两句<br>**+ 红色警告块**：「其中 N 条改名记录仍然可以撤销。清空后这 N 条将**无法再还原** —— 只能手动把文件名改回去。」 | **仍然清空** |

**不做 ② 会怎样**：用户会经历「改错了名 → 想去撤销 → 发现历史被自己清空了」→ **永久丢数据**，只能手工把文件名一个个改回去。

**文案里那两句必须写**：「文件本身不会被删除，也不会被改名」—— 否则用户会以为「清空历史 = 删文件」，那反而会让人不敢用这个功能。

### 2.4 主按钮：需要给 ConfirmModal 加 danger 变体

主按钮用**危险色实心**：底 `bad`、文字 `on-danger`（`#2A1A10`，对比 **4.58:1**，达标）。

现有 `ConfirmModal.vue` 的主按钮**恒为 `md-btn--primary`（橙色）**，且 `kind` 只有 `rename / undoOne / undoAll / clear` 四种。所以需要：
- 加一个 `kind: 'clearHistory'`
- 加一个 danger 主按钮变体

### 2.5 清空之后

| 项 | 行为 |
| --- | --- |
| 列表 | 变空 → 走既有空态「还没有改过名呢」 |
| 状态栏 | 提示「**已清空全部 N 条历史记录，文件未被改动**」（复用既有临时提示位，3s 后回常态） |
| 结果弹窗 | **不弹**（清空不是改名任务） |
| 可撤销记录 | **不产生**（与「撤销本身不产生新记录」同理） |
| **淘汰检测** | **必须重置基准** —— 见下 |

### 2.6 ⚠️ 一个必须先修的连带问题：淘汰检测会误报

`stores/history.ts` 里有个「淘汰不静默」检测（DEC-09）：

```ts
// 检测淘汰：上一份的最旧任务不见了，且总数没有增长（不是正常的追加）
if (lastOldestId !== null && nextOldestId !== lastOldestId && next.length <= lastCount) {
  evictedNotice.value = 1
}
```

**用户主动清空之后，这两个条件恰好全都成立** —— 列表变空、`nextOldestId` 变成 `null`、长度 0 ≤ 上次计数。结果就是：

> 用户自己点了「清空」，状态栏却弹一句「为保证性能，较旧的记录已被清理」。

**这是个假警报，必须在实现时堵掉**（清空后重置 `lastOldestId` / `lastCount`，并让清空走它自己的提示文案）。本批有专门一条单测钉死这个行为。

---

## 3. C · 设置 · 命令行入口

### 3.1 位置

加在 **P2-A 的设置弹窗**里作为**第 4 项**（弹窗是纵向堆叠，加一行不改结构）。

### 3.2 形态

```
设置
命令行                                        ⌃
┌──────────────────────────────────────────────────────┐
│ 给进阶用户用脚本批量改名。在命令行（或 .bat 文件）里跑下面这条： │
│ ┌──────────────────────────────────────────────────┐ │
│ │ maodie.exe --rename --dir "D:\下载\素材" --delete "广告" --yes │
│ └──────────────────────────────────────────────────┘ │
│ [复制命令]  [复制程序路径]                            │
│ 「复制命令」会自动带上你机器上的完整程序路径。            │
└──────────────────────────────────────────────────────┘
```

| 项 | 规格 |
| --- | --- |
| 行 | 标签 13.5 Medium `ink-1` + 右侧 12px 箭头（收起/展开） |
| 展开块 | 底 `bg-warm`、圆角 10px、内边距 12px |
| 说明 | 12px `ink-2` |
| 命令代码块 | 底 `bg-sunken`、圆角 10px、内边距 10×12px、Inter 11.5 Medium `ink-1` |
| 按钮 | 次按钮样式（底白、描边 `line`、圆角 10px、文字 12.5 Medium `ink-1`）：**复制命令** / **复制程序路径** |
| 小字 | 11.5 `ink-4` |

**为什么还要「复制程序路径」**：进阶用户最大的障碍不是不会写命令，而是**不知道程序装在哪**（Electron 默认装在用户目录深处）。这一条比写一百行文档有用。

### 3.3 ★ CLI 语法草案（**接口**，需与开发 / 接口文档一起定稿）

> 这一节**不是我单方面能定的** —— CLI 是接口，不是纯视觉。下面是建议草案，请与开发一起过一遍。

```
maodie.exe --rename --dir "<目录>" [规则] [--yes]

规则（三选一，与界面完全一致）：
  --delete "<文本>"
  --replace "<查找>" "<替换为>"
  --prefix "<前缀>" --suffix "<后缀>" --seq --date --date-format <格式>

P1 能力：
  --regex                       正则匹配
  --lower | --upper | --capitalize   大小写转换

安全相关：
  --auto-seq                    冲突时自动加序号（默认不加 = 跳过）
  --case-sensitive              区分大小写

★ 安全底线与界面完全一致：默认不覆盖任何已有文件
★ --dry-run 是默认行为：不传 --yes 就只打印将要做的改动，
  必须显式 --yes 才真正改名
```

**为什么 `--yes` 是必须的**：命令行里**没有预览界面**。这个产品的调性是「绝不毁数据」，那命令行默认就该是**只读**的 —— 让用户「先看一眼再决定」，而不是「一不小心就跑完了」。这与界面里「先预览、再执行」是同一条原则的延伸。

### 3.4 ★ C+ 增量 · 命令行小白化（DEC-13，2026-09-17）

> **触发**：命令行入口（EL-114 ~ EL-116）落地后，用户反馈「**我觉得这里可以加一个解释说明，不然普通小白用户根本不知道**」。
> 这个判断是对的，而且指出了一个真实缺口：**C 块只解决了「命令写的对不对」，没解决「这功能是干嘛的」**。

#### 3.4.1 问题拆解

| # | 小白卡在哪 | 原设计为什么没覆盖 |
| --- | --- | --- |
| 1 | 打开设置看到「命令行」三个字，**不知道跟自己有什么关系** | 说明文案写的是「给进阶用户用脚本批量改名」—— **「进阶用户」这四个字先把人劝退了** |
| 2 | 看到 `maodie.exe --rename --dir "D:\下载\素材" --delete "广告" --yes`，**不知道要粘到哪里** | 原说明只在括号里带过一句「（或 .bat 文件）」，没有讲「黑色窗口」这个最直观的实物 |
| 3 | 就算粘贴成功了，**不知道会发生什么**（会不会直接就把我文件改了？） | `--yes` 的安全语义写在设计文档里，**界面上一个字都没说** |

#### 3.4.2 决定

| 决定项 | 内容 | 用户答复 / 依据 |
| --- | --- | --- |
| 面向对象 | **面向小白**（不是「给进阶用户的进阶文档」） | 用户答「1、**A**」—— A = 三步走图文、默认收起 |
| 图的形态 | **SVG 线框示意图**（不做截图、不做视频） | 用户答「2、**A**」—— A = SVG 线框示意、不随主题翻转 |
| 入口位置 | 设置弹窗里放**引导卡 + 「查看教程」按钮**，点了弹出**专用教程窗口**（SCR-08） | 用户答「3、**B**」—— B = 设置里只放引导 + 按钮，教程另开一个窗口（不塞进设置弹窗内部，避免设置弹窗过载） |
| 一键生成 `.bat` | **不做** | 用户答「4、**这个暂时没必要，不做**」 |
| 教程挂载点 | 教程卡片**挂在上面的那个链接上**（即「查看教程」按钮） | 用户追加要求：「**还有那个小卡片教程最好能加载在上面的那个链接上**」→ 已实现：教程**只从 EL-118 进入**，不另开菜单、不加标题栏按钮 |

#### 3.4.3 落地形态

```
设置  (SCR-07)
命令行                                        ⌃
┌──────────────────────────────────────────────────────────┐
│ 要让别的程序（或定时任务）自动帮你改名时用得上。             │
│ 没接触过命令行也没关系，有图文教程。          [查看教程]    │  ← EL-117 引导卡 + EL-118
│ ┌──────────────────────────────────────────────────────┐ │
│ │ maodie.exe --rename --dir "D:\下载\素材" --delete "广告" --yes │ │
│ └──────────────────────────────────────────────────────┘ │
│ [复制命令]  [复制程序路径]                                │
│ 「复制命令」会自动带上你机器上的完整程序路径。              │
└──────────────────────────────────────────────────────────┘
```

点「查看教程」→ **SCR-08**（560 宽，叠加在设置之上）：顶部一句引导 + 三个步骤页签 + **左图右字**的步骤卡 + 底栏「第 N / 3 步」与翻页按钮。

| 步 | 看图学什么 | 一句话 |
| --- | --- | --- |
| 1 | `Win + R` + 运行框里的 `cmd` | 命令行就是「用打字代替点鼠标」，和软件界面做的是同一件事 |
| 2 | 命令被拆成**四段**并标号（①程序 ②改哪个文件夹 ③怎么改 ④真改开关）| 只有 ② 和 ③ 要换，其余照抄，**引号别删** |
| 3 | 一行「预演」输出 + 末尾补 `--yes` | **不加 `--yes` 一个文件都不动**；确认清单没问题再加 |

#### 3.4.4 两条设计理由（值得记住）

1. **先给理由，再给术语。** 原文案的第一句是「给进阶用户用脚本批量改名」—— 它先划了一条线（你是进阶用户吗？），而小白读完这句的结论是「这跟我没关系」。新文案第一句讲**场景**（别的程序 / 定时任务帮你改名），第二句立刻给**台阶**（没接触过也没关系，有教程）。
2. **示意图刻意不随主题翻转。** 图里画的是「**现实里那台机器上的黑窗口**」（`#0c0c0c`），不是本软件的界面。深色模式下把它改成浅灰，用户照做时会发现**跟自己的屏幕上不一样** —— 示意图的价值就是「跟真实情况一致」。所以这三色定义在 `src/shared/theme.ts` 的 `CLI_DEMO_COLORS`，**刻意不进 `tokens.css`**（也就不参与主题切换）。

#### 3.4.5 编号与验收

| 类别 | 新增 |
| --- | --- |
| 界面 SCR | **SCR-08** 命令行图文教程弹窗 |
| 元素 EL | **EL-117** 命令行引导卡 · **EL-118**「查看教程」按钮 · **EL-119** 教程弹窗本体 |
| 交互 IX | **IX-104** 打开教程 · **IX-105** 教程内翻步 / 关闭 |
| 决策 DEC | **DEC-13** 命令行小白化（说明重写 + 三步图文教程） |

**验收方式**：本增量**不新增 TC 编号**（它属 EL-119 的呈现细节，且 TC-32 之后的编号各有归属）。验证落在两层：

| 层 | 覆盖 |
| --- | --- |
| 单测 `tests/p2c-guide.test.ts`（**10 条**） | SVG 结构（三步齐全 / 有 `viewBox` / 根是 `<svg>` / 无 `<script>` / 无 `<image>` 外链）；色值纪律（只允许 `CLI_DEMO_COLORS` / 三值合法且黑底够黑 / **不得出现在 `tokens.css` 里**）；文案（**零占位符** / 每步 ≥ 12 字 / 第一步必须提 Win 与 cmd / 必须提 `--yes` 与「先试」） |
| 冒烟（4 条，真实点击） | 29-4 引导卡存在（小白有门可进）· 30-4 三步**逐张翻得到**且第一张画 Win+R · 31-4 示意图黑底**真的是** `rgb(12, 12, 12)`（不随主题翻）· 32-4 教程**能关也能再开**，且**重开回到第 1 步** |

> **⚠️ 落地时踩到的真问题（已修，见 §13.6）**：设置弹窗与教程弹窗都是**常驻挂载**（靠内部 `v-if` 控制显隐），组件**不随关闭销毁** —— 所以「命令行展开态」和「当前第几步」这两个临时 UI 状态会**跨打开残留**。表现是：用户第二次打开教程直接停在第 3 步，**看起来像点不动**。已用 `watch(() => task.modal, ...)` 在离开弹窗时归位。

---

## 4. 通道与字段

| 项 | 变化 | 说明 |
| --- | --- | --- |
| **IPC 业务通道** | **新增 1 个**：`md:history:clear` | 请求响应通道 **15 → 16** 个（事件通道仍是 3 个，不变）。**接口文档里「15」这个数字要同步** |
| | 为什么不复用现有的 | 历史域只有 `list` / `undoTask` / `undoAll`。「清空」不是「撤销」——硬塞进现有通道会把语义搞乱 |
| **`AppInfo`** | 新增字段 `execPath` | 可执行文件路径，给「复制程序路径」用。**加字段比加通道轻** |
| **剪贴板** | 不加通道 | 复制走渲染层剪贴板 API |
| **持久化字段** | **无** | 清空不引入任何新数据 |

---

## 5. 编号清单（已 grep 核对）

> 现有上限：**`EL-110` / `IX-099` / `TC-32` / `DEC-11`**（P2-A 用掉了 106~110 / 095~099 / 028~032 / DEC-11）。故本次一律顺延，不插空。

| 类别 | 新增 |
| --- | --- |
| 元素 EL | `EL-111` 明细展开条 · `EL-112` 明细列表 · `EL-113` 清空历史按钮 · `EL-114` 命令行行 · `EL-115` 命令代码块 · `EL-116` 复制按钮组 |
| 交互 IX | `IX-100` 展开 / 收起明细 · `IX-101` 清空历史（含二次确认分支）· `IX-102` 展开 / 收起命令行用法 · `IX-103` 复制命令 / 程序路径 |
| 异常 EX | `EX-17` 清空历史时仍有可撤销任务（走加强警告） |
| 验收 TC | `TC-33` ~ `TC-37`（见 §6） |
| 决策 DEC | `DEC-12` P2-C 范围与历史数据安全阀 |

**C+ 增量新增（DEC-13，2026-09-17）**

| 类别 | 新增 |
| --- | --- |
| 界面 SCR | `SCR-08` 命令行图文教程弹窗 |
| 元素 EL | `EL-117` 命令行引导卡 · `EL-118`「查看教程」按钮 · `EL-119` 教程弹窗本体 |
| 交互 IX | `IX-104` 打开教程 · `IX-105` 教程内翻步 / 关闭 |
| 决策 DEC | `DEC-13` 命令行小白化（说明重写 + 三步图文教程） |
| 验收 TC | **无新增 TC 编号** —— 改由单测 `tests/p2c-guide.test.ts`（10 条）+ 冒烟 4 条覆盖（理由见 §3.4.5） |

---

## 6. 验收用例

| 编号 | 用例 | 步骤 | 预期 |
| --- | --- | --- | --- |
| **TC-33** | 查看明细 | 历史页点某条的「查看全部 N 项」 | 就地展开完整明细，序号与「原名 → 新名」逐条正确；点「收起」收回 |
| **TC-34** | 明细渲染上限 | 构造一个 5000 条明细的任务，展开 | 只渲染 100 行，末行显示「还有 4900 项未显示（共 5000 项）」；**界面不卡** |
| **TC-35** | 清空 · 无警告分支 | 所有任务都已撤销 → 点「清空历史记录」 | 正文无红色警告；主按钮文案为「清空」 |
| **TC-36** | **清空 · 有可撤销任务（安全阀）** | 存在「可撤销」任务 → 点「清空历史记录」 | **出现红色警告块**，写明「N 条仍可撤销、清空后无法还原」；主按钮文案为「仍然清空」 |
| **TC-37** | **清空后不误报淘汰** | 先点清空，再进历史页 | 状态栏显示「已清空全部 N 条历史记录，文件未被改动」；**不出现**「较旧的记录已被清理」 |
| 附 | 清空不碰文件 | 清空前记下某文件的名字与时间戳 | 清空后该文件**名字、时间戳都没变** |

---

## 7. 测试要求

按项目既定规矩（不引入第三方测试框架）：

| 层 | 要求 |
| --- | --- |
| 单测 | 清空后 `history.json` 为空，且**文件未被触碰**（用真实临时目录断言「文件还在、名字没变」） |
| 单测 | **清空后淘汰检测不误报** —— 断言清空**不会**触发「较旧的记录已被清理」提示（**这是本批最容易漏的一条**） |
| 单测 | 明细渲染上限 —— 传入 5000 条 `entries`，断言只渲染 100 行且提示文案正确 |
| 界面冒烟（真实实例 + 真实输入事件） | 点开明细 → 点清空 → 看两种确认文案 → 把命令复制到剪贴板 |
| 交付报告 | 必须写「哪些没验到」（如打包后的 exe、真实 `%APPDATA%` 下的文件行为） |

**红线照旧：绝不为让测试变绿而放宽 / 跳过 / 删断言。挂了就给原始报错。**

---

## 8. 待同步的文档

| 文档 | 需要的同步 | 状态 |
| --- | --- | --- |
| `需求清单.md` | F13 补「完整明细 + 清空入口 + 安全阀」；F15 补「入口位置 = 设置弹窗第 4 项」与 CLI 的 `--yes` 默认只读；并补 P2 分批说明 | ✅ **已同步**（2026-09-15） |
| `PRD.md` | §3.1 F-13 / F-14 / F-15 补设计落点；§5.1 加 SCR-07；§7 加 **EX-15/16/17**；§11.1 加 **DEC-11 / DEC-12**；§10 加 **TC-28 ~ TC-37**；§11.3 关闭两条遗留项 | ✅ **已同步**（**v1.1.5**） |
| `交互说明文档.md` | §3.1 加 EL-111~113、§3.2 加 IX-100/101；§2.3 加 EL-114~116、§2.4 加 IX-102/103；§6 二次确认表重写（加 `kind` 列 + 清空历史两分支 + 危险按钮）；§10 加 EX-17；§12 加 P2-C 三行 | ✅ **已同步**（**v1.5**） |
| `设计规范.md` | §4.1 加「幽灵危险按钮」变体；§5.3 历史页补明细展开与 footer 清空；§5.5 设置弹窗补命令行入口；§1.6 深色主题；§8.1/§8.2 深色取舍与令牌覆盖率 | ✅ **已同步**（**v1.4**） |
| `数据库设计.md` | §4.7 的清空入口 **从「建议补充」改为「已定稿」**并补两条落地新增（数据安全阀 / 重置淘汰基准）；§11 遗留项第 1 条关闭、新增 1b（历史完整明细） | ✅ **已同步** |
| `接口文档.md` | 请求响应通道 **15 → 16**；`md:history:clear` 的入参 / 返回 / 实现约束；`AppInfo` 加 `execPath`；§8 对照表与 §9.2 清单 | ✅ **已同步**（**v1.0.3**） |
| `技术方案文档.md` | §11 新增遗留项 13（P2-C 内容与两条红线）与 14（多规则链已推迟）；契约变更记录 | ✅ **已同步**（**v1.0.4**） |
| `P2-A轻量设计确认.md` | 验收编号 **TC-27~31 → TC-28~32**（与 P1 的 TC-27 撞车） | ✅ **已同步**（**v1.2**） |
| **`接口文档.md`（落地时补）** | **新增 §10「命令行模式（CLI）」** —— 此前该文档对 CLI **零定义**，而 CLI 的验收栏写的是「—（随接口文档定稿）」。现补齐参数表、退出码、与界面互斥、已知限制；章节编号 10/11 顺延。升 **v1.0.4** | ✅ **已同步**（**v1.0.4**） |
| **`交互说明文档.md`（落地时补）** | §12 里「CLI 本身的验收 —」改为指向接口文档 §10 + 实际测试 | ✅ **已同步** |
| **`交互说明文档.md`（C+ 增量）** | §1 加 **SCR-08**；§2.3 加 **EL-117 ~ EL-119** 并改写 EL-114 说明文案；§2.4 加 **IX-104 / IX-105** 与「状态归位红线」；§12 加一行 | ✅ **已同步**（**v1.6**）|
| **`设计规范.md`（C+ 增量）** | §5.5 命令行展开块补引导卡与「查看教程」按钮规格；新增 **§5.6 SCR-08 图文教程**（含示意图视觉规格与「不随主题翻转」的理由）；§5.4 弹窗宽度补 560px | ✅ **已同步**（**v1.5**）|

> ⚠️ 本次同步时**发现并修正了一个编号冲突**：`TC-27` 早被 P1 的「正则说明小白化」占用（PRD §10 已登记），而 P2-A 原编 `TC-27~31`。
> 已在 PRD v1.1.5 的 §10 里留下修正说明 —— **教训是「新增编号前 grep 全库」要按前缀全量列出（搜 `TC-`），不要按猜的区间搜**（当时用的 `TC-3[0-9]` 匹配不到 TC-27~29）。

---

## 9. 已知取舍（如实记录）

| # | 取舍 | 说明 |
| --- | --- | --- |
| 1 | 明细只渲染前 100 条 | 刻意取舍。超过 100 条时用户看不到第 101 条以后 —— 但真实场景里没人会逐条看几千行；如果将来真需要，那属于「搜索/分页」范畴，按 `数据库设计.md` 附录 A 要迁 SQLite，是独立工程批次 |
| 2 | 明细展开不做虚拟滚动 | 复用 `useVirtualList` 会引入一个「展开区也要虚拟化」的复杂度，收益不抵成本 |
| 3 | 清空不可撤销 | 清空就是终态（与 `undoTask` 的「已撤销」终态同理）。已通过「安全阀 + 二次确认」把风险降到最低，但**不做回收站** |
| 4 | ~~CLI 语法是草案~~ → **已定稿** | P2-C 落地时与开发一起定稿，**全文写进 `接口文档.md` §10**（参数表 / 退出码 / 与界面互斥 / 已知限制）。本文档仍只负责「入口长什么样」，CLI 本体以接口文档为准 |
| 5 | 未覆盖 | 打包后的 exe 在真实命令行环境（PowerShell / cmd / Git Bash）下的参数解析差异；不同终端的引号与路径转义 |

---

## 10. 决策答复（2026-09-15 已闭环）

| # | 问题 | 决定 |
| --- | --- | --- |
| 1 | CLI 是否必须 `--yes` 才真改 | **保持** —— 不传 `--yes` 只打印将要做的改动，**必须显式 `--yes` 才真正改名** |
| 2 | 清空的安全阀要不要更狠一点 | **不用**。就是「红色警告块 + 主按钮『仍然清空』」，**不要求用户额外输入任何东西** |
| 3 | CLI 用「命令行参数」还是「配置文件」 | **命令行参数风格**（`--delete` / `--replace` / `--prefix` …），不用 JSON 配置文件 |
| 4 | 「复制命令」的示例给几条 | **只给一条**（删除模式），保持简洁 |

> **四条的净效果：本设计 v1.0 的全部内容原样成立，无需任何改动。**
> 画板也不需要重画 —— 上面画的本来就是「一条示例命令 + 红色警告块 + 仍然清空」这一版。

---

## 11. 变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v1.0 | 2026-09-15 | 首次成文：明细展开（就地展开 / 前 100 条上限）+ 清空历史记录（footer 幽灵按钮 + 数据安全阀 + 淘汰检测假警报的修法）+ 命令行入口（含 CLI 语法草案与 `--yes` 默认只读）；通道 15 → 16；编号 EL-111~116 / IX-100~103 / EX-17 / TC-32~36 / DEC-12 |
| **v1.1** | 2026-09-15 | 负责人 4 条答复全部确认原方案（见 §10），**设计内容无改动**；§10 由「待你确认的事」改为「决策答复（已闭环）」，补 §11 变更记录 |
| **v1.3** | 2026-09-15 | **落地侧修订**：① §1.3 的「无变化的行」标为**不实现**（死规格，见 §13）；② §9 取舍 4「CLI 语法是草案」改为**已定稿**并指向 `接口文档.md` §10；③ §12 补上被漏掉的 `接口文档.md` 与 `交互说明文档.md`；④ 文档状态改为**已落地并通过验收**，新增 §13 落地记录（含「没验到的」）。**落地实现没有偏离设计**，唯一的偏差就是上面这两处文档与代码不符 |
| **v1.2** | 2026-09-15 | **验收编号修正（跟随 P2-A）**：文档同步时发现 `TC-27` 已被 P1 的「正则说明小白化」占用，故 P2-A 由 `TC-27~31` 顺延为 `TC-28~32`；本稿随之由 `TC-32~36` 顺延为 **`TC-33~37`**（§5 编号清单、§6 验收表已同步）。**根因**：设计 P2-A 时 grep 用的模式是 `TC-3[0-9]`，匹配不到 TC-27~29 —— 教训是「grep 全库」要按**前缀**全量列出（`TC-`），不要按猜的区间搜 |
| **v1.4** | 2026-09-17 | **C+ 增量 · 命令行小白化（DEC-13）**：① 新增 **§3.4**（问题拆解 3 条 / 决定 5 项含用户原话 / 落地形态 / 两条设计理由 / 编号与验收方式）；② §0 摘要表加 **C+** 一行；③ §5 编号清单加「C+ 增量新增」子表（**SCR-08 / EL-117~119 / IX-104~105 / DEC-13**，**不新增 TC**）；④ §8 待同步文档加两行；⑤ §13.6 记录落地时踩到的「常驻挂载组件状态泄漏」真问题。<br>> **用户明确不做的**：一键生成 `.bat`（原文：「这个暂时没必要，不做」）—— 教程**只教怎么用，不代用户落盘脚本** |
| **v1.3** | 2026-09-15 | **落地侧修订**：① §1.3 的「无变化的行」标为**不实现**（死规格，见 §13）；② §9 取舍 4「CLI 语法是草案」改为**已定稿**并指向 `接口文档.md` §10；③ §12 补上被漏掉的 `接口文档.md` 与 `交互说明文档.md`；④ 文档状态改为**已落地并通过验收**，新增 §13 落地记录（含「没验到的」）。**落地实现没有偏离设计**，唯一的偏差就是上面这两处文档与代码不符 |


## 13. 落地记录（2026-09-15 · 开发侧）

### 13.1 交付与验证

| 护栏 | 结果 |
| --- | --- |
| `npm run typecheck` | **0 错** |
| `npm run lint` | **0 错 0 警告** |
| 单测（`node --test`，无第三方框架） | **46/46**（P1 18 + P2-A 9 + P2-C 19） |
| 真实 Electron 冒烟 | **35/35**（`SMOKE_HISTORY=withActive` 与 `=allUndone` **两种夹具模式各 35/35**） |
| 令牌覆盖率（TC-32） | `tokens.css` 之外色值仍**归零**（本批未新增任何颜色令牌） |
| 命令行端到端 | `--help` / `--dry-run` / 参数错 / `--yes` 真改名 / **写入历史可撤销** —— 五条全过（真实 Electron 进程） |

报告：`tests/artifacts/2026-09-15T13-59-48/report.html`（withActive）、`2026-09-15T14-01-20/report.html`（allUndone）。

### 13.2 冒烟新增 6 条（都点了真实按钮、读了渲染后的真值）

| # | 用例 | 关键断言（不是「class 加没加上」） |
| --- | --- | --- |
| 29 | IX-102/103 命令行入口 | 默认折起 → 展开后命令含 `--rename` / `--delete` / **`--yes`** 与**本机完整路径**；点复制后状态栏真的给出「命令已复制」 |
| 30 | TC-33 明细展开 | 3 行逐条对（含**序号从 1 开始**、第一行文字精确等于「广告素材-0001.png → 素材-0001.png」）＋ 容器**算出来的 `max-height` = 220px**；再点能收回 |
| 31 | TC-33b | 「已撤销」的卡片同样能展开，且**不显示**「撤销这一条」 |
| 32 | TC-34 渲染上限 | 5000 条 → **只渲染 100 行** ＋ 末行精确等于「还有 4900 项未显示（共 5000 项）」 |
| 33 | TC-35 / TC-36 | 两支确认文案分别验：无警告分支 → **没有**警告块、主按钮是「清空」；安全阀分支 → 警告块含「仍然可以撤销 / 无法再还原」、主按钮「仍然清空」且**底色 = `rgb(229, 84, 75)`** |
| 34 | TC-37 | 清空后状态栏说「已清空全部 N 条历史记录，文件未被改动」，且**淘汰提示元素根本不存在** |

> 为验「清空历史」的两支文案，冒烟新增了 `SMOKE_HISTORY` 夹具开关（`withActive` / `allUndone`）：**直接写进隔离目录的 `history.json`**，不用真去改 5000 个文件就能验渲染上限。两种模式各跑一遍。

### 13.3 落地时发现并修掉的问题

| # | 问题 | 为什么危险 |
| --- | --- | --- |
| 1 | `--delete a --prefix x` 被**静默接受** | 界面里规则是三选一互斥的（`RuleMode`），命令行必须「与界面完全一致」。已补互斥检查（`--seq` / `--date` 一并加固） |
| 2 | `--help` 排在单实例检查**之后** | 界面开着时连用法都看不到（实测退出码 2）。用法说明任何时候都该看得到 —— 已提到锁检查之前 |
| 3 | 复制到剪贴板会**静默失败** | `navigator.clipboard.writeText` 要求文档聚焦，不满足时抛 DOMException；原实现没接异常 → 界面无提示、控制台多一条未处理异常。现在：异步 API → 失败退回 `execCommand('copy')` → 都不行就**如实告知**「没复制上，请手动选中后按 Ctrl+C」 |
| 4 | `initStorage` 从错误的模块 import | 单测直接抓到 |

### 13.4 ⚠️ 测试方法本身的一个坑（值得全项目记牢）

验 TC-37 时，断言写的是 `document.body.innerText.includes('较旧的记录已被清理')` —— 而**冒烟的断言文案里就有这句话**，冒烟注入的操作横幅显示当前步骤说明，于是横幅一刷新，`body.innerText` 必然包含它。

**症状极具误导性**：慢跑必红、快跑却绿（横幅刷新需要一点时间），一开始被误判成产品 bug。

→ 本段所有断言已改为读**具体元素**（`.md-statusbar__text` / `.md-statusbar__notice`）。
→ 顺带发现「文件未被改动」那一条原本也是被横幅喂饱的**假通过**。
→ **规矩：冒烟里不要用 `document.body.innerText` 做断言，一律读具体元素。**

### 13.5 本批「没验到的」（如实登记，不当成达标）

| 项 | 为什么没验 |
| --- | --- |
| 打包后的 exe（本批测的是 `out/` 产物） | 要走 `package:win` 并做真机安装 |
| **未打包时 `execPath` 指向 Electron 引擎** | 设置里那条命令只有打包后才可直接使用 —— 已在 `接口文档.md` §10.6 写明 |
| 不同终端的引号 / 路径转义差异（PowerShell / cmd / Git Bash） | 手头只有一套环境 |
| 剪贴板里**实际的内容** | 冒烟断言的是「复制成功提示」（说明剪贴板写入没抛错），没有把剪贴板内容读回来比对 |
| 「清空历史」在**真实 `%APPDATA%`** 下的行为 | 冒烟跑在隔离数据目录里（不碰你的真实数据）—— 这是刻意设计，不是遗漏 |
| 明细展开的**人眼观感**（220px 滚动是否好用） | 断言判不了，要人看 |
| 5000 条明细展开时的**卡顿感** | 只断言了「只渲染 100 行」，没有测真实帧率 |

> 另：**验证命令行 `--yes` 时，为了不碰用户真实数据，本想让 `APPDATA` 指向临时目录，但实测无效**（Electron 的 `appData` 不随该环境变量走）—— 结果那次测试的历史**写进了真实数据目录**。已先备份、再只删掉测试那一条（你原有的 9 条逐条核对过、一字未改），备份留在 `%APPDATA%\MaoDieRenamer\history.json.bak-20260915-p2c-clitest`。**下次要在真实环境验 CLI，必须先关界面并接受「会写一条真实历史」，或改用冒烟那样的 `app.setPath('appData', …)` 隔离方式。**

### 13.6 C+ 增量落地（2026-09-17 · 命令行图文教程）

| 护栏 | 结果 |
| --- | --- |
| `npm run typecheck` | **0 错** |
| `npm run lint` | **0 错 0 警告** |
| 单测（`node --test`，无第三方框架） | **56/56**（其中 `tests/p2c-guide.test.ts` **10 条**为本增量新增） |
| 真实 Electron 冒烟 | **39/39**（`SMOKE_HISTORY=withActive` 与 `=allUndone` **两种夹具模式各 39/39**） |
| 令牌覆盖率（TC-32） | `tokens.css` 之外色值仍**归零**（`CLI_DEMO_COLORS` 在 `shared/theme.ts`，属既有白名单） |

报告：`tests/artifacts/2026-09-17T02-34-02/report.html`（withActive）、`2026-09-17T02-35-52/report.html`（allUndone）。

**落地时发现并修掉的 3 个真问题**（都不是「测试写错了」，是产品真的有问题）：

| # | 问题 | 根因 | 修法 |
| --- | --- | --- | --- |
| 1 | **`tests/p2c-guide.test.ts` 从来没被跑过** | 它从 `tests/` import 了 `src/renderer/**` 下的文件，而 `tsconfig.node.json` 的 include **不含渲染层** → 单测编译直接失败；偏偏它又**没注册进 `test:core`**，所以这个错误一直没暴露 | ① 把 `cli-guide.ts` 从渲染层**移到 `src/shared/`**（它本来就是纯字符串数据，符合 shared 的定位）；② 把它注册进 `test:core` |
| 2 | 设置弹窗的**「命令行」展开态跨打开残留** | `SettingsModal` 在 `App.vue` 里**常驻挂载**（靠内部 `v-if` 控制显隐），**组件不随关闭销毁** → `cliOpen` ref 保留 | `watch(() => task.modal, …)`，离开 `settings` 时归零 |
| 3 | 教程弹窗的**「当前步骤」跨打开残留** | 同上（`CliGuideModal` 也常驻挂载）→ 第二次打开直接停在第 3 步，**看起来像点不动** | 同上，离开 `cliGuide` 时 `step.value = 0` |

> **★ 通用规律（写进项目约束）**：**常驻挂载的组件里，任何「临时 UI 状态」都要在关闭时归位。**
> 这类 bug 的共性症状是「**值不对、但界面没有任何异常**」—— 点一下就好的东西，用户不会觉得是 bug，只会觉得「这功能有点怪」。**光点界面发现不了，必须靠断言。**
> 这两个都是**冒烟抓出来的**（第 30 / 33 条先红后绿），单测抓不到（它们测的是纯数据）。这也是「界面用真实 Electron 冒烟」这条规矩的价值所在。

### 13.7 C+ 增量「没验到的」

| 项 | 为什么没验 |
| --- | --- |
| 教程的**人眼观感**（左图右字的比例、560 宽是否舒服） | 断言判不了，要人看 |
| **高 DPI / 缩放下 SVG 的清晰度** | 线框理论上无损，但没在 150% / 200% 缩放下实测 |
| 用户**照着教程能不能真的跑通** | 涉及真实命令行环境与用户的理解，属可用性测试范畴 |
| 教程在不同系统语言下的表现 | 界面全中文，无多语言需求 |

---

**P2-C 轻量设计确认 v1.4 · 2026-09-17**

> **v1.4 变更（C+ 增量 · 命令行小白化 / DEC-13）**：见 §11 与 §3.4。核心是「**命令行入口做出来了，但小白不知道该拿它干嘛**」这个缺口 —— 补一层说明文案重写 + 一个三步图文教程（SCR-08）。
> v1.3 变更（落地侧修订）：见 §11 与 §13.1 ~ §13.5。
