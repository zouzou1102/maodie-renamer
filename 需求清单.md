# 需求清单

> 项目代号：**耄耋改名（MaoDie Renamer）**
> 一句话定位：一只圆头炸毛的耄耋（橘猫）帮你批量改好文件名 / 文件夹名。
> 文档版本：v0.3.5（功能大扩张实施中）
> 日期：2026-09-18
> 更新记录：
> - v0.2 依评审意见修订——预览 + 撤销提升为 **P0 安全项**；形象/名称/布局等决策已关闭
> - **v0.3** 依产品经理定稿意见修订——① **F7 拖拽提升为 P0**（修正与验收标准的矛盾）；② 补写 **重名冲突处理策略**（F16）；③ 补写 **撤销范围界定**（F9 细化）；④ 验收标准措辞同步修正
> - **v0.3.1**（2026-09-12）技术方案评审后同步——**体积指标放宽**：安装包 < 100 MB 改为「首版目标、非发布阻断项」（详见 PRD v1.1.1 的 DEC-08）
> - **v0.3.5**（2026-09-18）**P3 立项 + 第 1 批（编号系统升级）落地后同步** —— ① 新增 **P3 段**：功能大扩张按「文件风险低 → 高」拆 8 批、规矩是「只增不删」，第 7、8 批（文件夹合并 / 文件提取）会复制或删除文件本身，必须放最后并单独评审；② F5 的序号能力扩写：**编号类型 4 种**、**位置 3 档**、规则区常显**示例行**；③ 决策 **DEC-15 / DEC-16**（见 PRD §11.1）；④ 验收编号 **TC-42 ~ TC-49**
> - **v0.3.4**（2026-09-18）**P2-B（常用规则模板库）落地后同步** —— ① F12 标注**已落地**，并补「6 个内置模板 / 套用 = 整份替换 / 不做『当前模板』高亮 / 摘要只在显示层截断」四点；② 验收编号 **TC-38 ~ TC-41**（接 P2-C 的 TC-37 顺延）；③ **顺带更正 P1 文档里的一句话**：折叠区**不是**模板库的挂载点（见 F12 行）。至此 **P2 三批全部落地**
> - **v0.3.3**（2026-09-15）**P2-A（设置面 + 深色模式）落地后同步** —— ① F14 标注**已落地**，并补「建设置面的同时**关闭了一个 P0 遗留缺口**：音效 / 减少动画此前已生效但界面上没有入口」；② 明确 P2-A 的验收编号为 **TC-28 ~ TC-32**（`TC-27` 已被 P1 的小白化说明占用，故顺延）
> - **v0.3.2**（2026-09-15）P1 轻量设计定稿后同步——① F10 补「隔离方案 = 默认折起的『⚙ 进阶设置』折叠条、且只在删除/替换模式出现」；② F11 补「四种取值 + 只作用于主体、扩展名不动 + 执行顺序」；③ **「多规则链式组合」从 P1 明确移到 P2**（P1 只做 F10 + F11，见 `P1轻量设计确认.md` v1.0 / DEC-10）

---

## 一、项目背景

用户在日常整理文件时，经常需要**批量修改文件 / 文件夹名称**，例如统一删除文件名中的某个字符、替换某个字符串、按规则（序号 / 日期 / 前后缀）重命名。市面上的改名工具要么收费、要么需要开通 VIP，要么界面陈旧难用。

本项目参考爆火的 **FlyingMouse Format（飞鼠格式）** 的成功思路——用**可爱、受年轻人欢迎的网络表情包形象**作为软件灵魂，做一款**免费、可爱、好用**的 Windows 批量改名软件，形象采用网络热梗 **「耄耋」**（圆头炸毛橘猫）。

---

## 二、目标用户

| 类型 | 描述 | 典型场景 |
| --- | --- | --- |
| 学生党 | 整理课件、作业、网盘资源 | 批量删除下载文件名里的广告后缀、前缀 |
| 办公族 | 整理合同、报表、照片 | 统一给一批文件加日期前缀、序号 |
| 资源收集者 | 整理图片、音乐、视频素材库 | 批量替换乱码、去广告字样 |
| 追梗年轻用户 | 被可爱形象吸引「图一乐」 | 单纯觉得猫可爱，顺便改个名 |

---

## 三、功能需求（按优先级）

> 优先级说明：**P0** = 第一版必须有（MVP 核心）；**P1** = 重要但可第二版；**P2** = 增强项。

### P0（MVP 核心，第一版必须完成）

| 编号 | 功能 | 说明 | 验收标准 |
| --- | --- | --- | --- |
| F1 | **批量重命名文件** | 一次选中多个文件，统一应用改名规则 | 能一次处理 100+ 文件，逐个改名成功 |
| F2 | **批量重命名文件夹** | 一次选中多个文件夹，统一应用改名规则 | 文件夹改名成功，且文件夹内文件不受影响 |
| F3 | **删除指定字符** | 统一删除文件名中的某个字符 / 字符串 | 支持删除单字符、多字符、所有出现位置 |
| F4 | **替换指定字符** | 把文件名中的 A 字符串替换成 B 字符串 | 支持全部替换，区分大小写开关 |
| F5 | **按规则重命名** | 序号（从 N 开始、步长、补零）、日期、前后缀 | 支持 `前缀 + 序号 + 原文件名` 等组合。<br>**P3 第 1 批已扩展（2026-09-18）**：序号能力长成「**编号类型 4 种**（数字 / 字母 / 随机字符 / 时间）+ **位置 3 档**（排在最后 / 排在最前 / **插在第 n 个字符后**）+ 规则区常显**示例行**」—— 详见下文「### P3」段 |
| F6 | **可爱互动界面（灵魂）** | 耄耋形象跟随操作切换状态（待机 / 拖入 / 改名中 / 成功 / 失败），带可爱动画 | 状态切换流畅，形象生动，符合年轻人审美 |
| F7 | **拖拽文件进窗口**（v0.3 提为 P0） | 直接把文件 / 文件夹拖到窗口里，不用点按钮选；拖入时给出可落区高亮反馈 | 拖入 100 个文件可正常入列；非文件内容拖入有明确拒绝提示 |
| F8 | **实时预览改名结果**（v0.2 提为 P0） | 改名前先显示「原名 → 新名」预览列表，确认后再执行 | 预览与实际结果一致，变化字符高亮 |
| F9 | **一键撤销**（v0.2 提为 P0，v0.3 细化） | 改名后保留映射记录；**本地保留最近 20 次改名任务，每条可单独撤销，也可「全部撤销」**。撤销需二次确认 | 撤销后文件名全部还原，无遗漏；越界（被外部改名 / 文件已移动）的条目跳过并明确提示 |
| F16 | **重名冲突处理**（v0.3 新增） | 改名后若与同目录下已有文件重名，或批次内互相重名：**默认「跳过冲突项 + 清单标红 + 提示手动改」**；另提供「自动加序号后缀（_1/_2）」开关 | 默认策略下绝不覆盖任何已存在文件；冲突项在预览与结果中均可识别 |

> **【定稿决策】** F16 的默认值必须是「跳过」而非「覆盖」——批量改名工具最严重的事故是覆盖已有文件导致数据不可逆丢失。任何「自动重命名」都只是用户体验优化，不能作为默认行为。

### P1（重要，第二版）

> **P1 范围收敛（2026-09-15，见 `P1轻量设计确认.md` v1.0 / DEC-10）**：P1 **只做** F10 + F11 两件事，都不改规则引擎结构、不新增页签。**「多规则链式组合」与 F12 模板库明确归 P2。**

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| F10 | **正则表达式高级规则** | 支持用正则做复杂匹配替换（给进阶用户）。**隔离方案（2026-09-15 定稿）**：不做第四个页签，改为藏在规则区底部一个**默认折起的「⚙ 进阶设置」折叠条**里 —— 且**只在「删除字符 / 替换字符」模式出现**（与既有「区分大小写」同规则），小白默认看不到「正则」二字。非法正则给红描边 + 红字原因并置灰「开始改名」 |
| F11 | **大小写转换 / 扩展名保护** | 可选转换大小写：保持原样 / 全部小写 / 全部大写 / 首字母大写。**大小写只作用于「新名主体」，扩展名永远不动**（`IMG_0001.JPG` → `img_0001.JPG`）。**执行顺序**：先按模式算出主体，最后整体做大小写。（**v0.3：扩展名保护已作为 P0 的默认行为实现，F11 仅保留「大小写转换」**） |

### P2（增强，后续迭代）

> **P2 分批（2026-09-15 定稿，2026-09-18 全部落地）**：**P2-A 设置面 + 深色主题（F14）** · **P2-C 历史完整版（F13）+ 命令行入口（F15）** · **P2-B 模板库（F12）**。**三批均已落地并测试通过。**
> **「多规则链式组合」继续推迟** —— PRD §11.3 的原话是「若用户反馈强烈需重新评估」，目前零反馈。

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| — | **多规则链式组合**（2026-09-15 由 P1 移入，**已推迟**） | 把「删除 / 替换 / 规则化」等多条规则**按顺序叠加**执行。需要把 `computeNewStem` 重构为「可增删排序的规则数组」，**DEC-02 届时需重新评审**。挂载点已预留：P1 的「⚙ 进阶设置」折叠区。**触发条件：有用户反馈**（PRD §11.3） |
| F12 | 改名规则模板库 | 内置常用规则，**一键套用**。**P2-B 已落地（2026-09-18 / DEC-14）**：规则区**顶部**一行「常用规则」+ **6 个内置模板**（加日期前缀 / 补零编号 / 日期+编号 / 去掉括号 / 空格换下划线 / 全部小写），点一下**整份替换** `RuleConfig`（不是「只补不覆盖」—— 否则会出现「一半来自模板、一半是上次填的」，用户不知道点下去改了什么）。**零新增通道 / 持久化 / 令牌**：模板就是一份预填好的完整规则，套用后照样能接着手改。<br>① **不做「当前用的是哪个模板」高亮** —— 用户套用后一改参数，高亮就成了谎话（规则只有一个真源，不额外维护会过期的第二份状态）；② **不做用户自定义模板**（需要新增持久化 + 命名/删除/排序的完整管理界面，且与推迟中的「多规则链」耦合，属独立批次）；③ 历史页的规则摘要**在显示层截断**（长正则不糊在卡片上，完整原文留给悬停）—— **绝不改 `buildRuleSummary`**，它的返回值写进 `history.json`，在那层截断等于改变持久化数据 |
| **F13** | **历史记录（完整版）** | 记录每次改名任务，**可回溯查看**。P0 已有简版（列表 + 规则摘要 + 首条示例 + 单条 / 全部撤销）。**P2-C 补两块（2026-09-15 / DEC-12）**：① **完整明细** —— 卡片内就地展开全部 entries（只渲染前 100 条，超出提示「还有 N 项未显示」）；② **清空历史记录入口** —— 历史页 footer 幽灵按钮，理由是从**隐私**出发（历史里存的是完整文件路径，可能带客户名 / 项目名）。⚠️ **含数据安全阀**：若仍有「可撤销」任务，二次确认必须额外警告「这 N 条将无法再还原」并把主按钮改成「仍然清空」 |
| **F14** | **深色 / 浅色主题** | **跟随系统 / 始终浅色 / 始终深色**，**默认「始终浅色」**（让软件在任何机器上首屏一致）。**P2-A 已完成设计（DEC-11）**：**暖褐色深底**（不是冷灰黑 —— 本产品卖点是「奶油白 + 橘猫」，冷灰会把暖萌感洗掉）；**品牌橘三色两主题完全相同**；橘底上的文字两主题统一用深棕 `#2A1A10`（8.3:1）。设置入口在标题栏（滑杆按钮 → SCR-07 设置弹窗）。**2026-09-15 已落地**，验收 **TC-28 ~ TC-32**。**顺带关闭一个 P0 遗留缺口**：交互说明 §9 与设计规范 §8 那条「设置中提供『减少动画』开关」的无障碍要求，此前因为**软件根本没有设置入口**而达不到（开关写好了、也能生效，用户就是打不开）—— SCR-07 把入口补上了 |
| **F15** | **命令行模式** | 给进阶用户批量脚本调用。**P2-C 定了「入口」与「安全底线」（DEC-12）**：入口 = 设置弹窗里一行「命令行」，点开显示一条可复制的示例命令 + 「复制程序路径」；**安全底线与界面完全一致**（默认不覆盖、冲突跳过），且 **`--dry-run` 是默认行为 —— 必须显式 `--yes` 才真改**（命令行没有预览界面）。**完整 CLI 语法已定稿**，见 `接口文档.md` §10（落地前只是草案）；**P2-C 已落地（2026-09-15）**。<br>**P2-C 增量 · 小白化（2026-09-17 / DEC-13）**：入口落地后用户反馈「**普通小白用户根本不知道**」→ ① 展开块说明文案**重写**（原「给进阶用户用脚本批量改名」默认用户已懂「脚本」二字 → 改为先讲场景「要让别的程序（或定时任务）自动帮你改名时用得上」）；② 新增**引导卡 + 「查看教程」按钮**（EL-117 / EL-118），点开是 **SCR-08 三步图文教程**（`Win+R` 开命令行 → 命令拆四段照抄 → 先不加 `--yes` 试跑、确认后再加）。教程图是 **SVG 线框示意**，**刻意不随主题翻转**（画的是现实里的黑窗口，跟真实情况一致才有用）。<br>**明确不做**：一键生成 `.bat` 文件（用户原话：「这个暂时没必要，不做」）—— 教程**只教怎么用，不代用户往硬盘上写脚本** |

### P3（功能大扩张 · 8 批，2026-09-18 立项）

> **P3 = 一次功能大扩张。按「文件风险低 → 高」拆成 8 批，规矩是「只增不删」**（不清洗、不重构既有能力，只往上加）。
> ⚠️ **第 7、8 批会复制 / 删除文件本身**，所以必须排在最后，且各自单独走一次「不可逆操作」评审。

| 批次 | 内容 | 文件风险 | 状态 |
| --- | --- | --- | --- |
| 第 1 批 | **编号系统升级** —— 编号类型 4 种（数字 / 字母 / 随机字符 / 时间）+ 位置 3 档（多出「插在第 n 个字符后」）+ 规则区常显「示例行」 | 低（只改规则计算，不碰文件） | ✅ **已落地**（2026-09-18 / DEC-15） |
| 第 2 批 | **导出文件名清单**（原名 / 新名对照，xlsx / txt / csv / docx 四档）<br>⚠️ **纠正**：「从文件名里取一段当新名」**不是这一批** —— 它属第 5 批的模式层 | 低 | ✅ **已落地**（2026-09-21 / DEC-17） |
| 第 3 批 | 按属性命名（按日期 / 尺寸等文件属性） | 低 | 待做 |
| 第 4 批 | 导入 Excel（按表格里的对照关系批量改名） | 低 | 待做 |
| 第 5 批 | **三模式 → 五模式** + 扩展名更改 + 「仅使用编号作为文件名」入口 | 中 | 待做（`DEC-16`） |
| 第 6 批 | 提取文件夹名 | 中 | 待做 |
| 第 7 批 | **文件夹合并** | **高（会移动文件）** | 待做 · **必须单独评审** |
| 第 8 批 | **文件提取** | **高（会复制 / 删除文件）** | 待做 · **必须单独评审** |

#### 第 1 批 · 编号系统升级（F5 的扩展，2026-09-18 已落地）

**序号能力从 1 种扩到 4 种、位置从 2 档扩到 3 档**，并在规则区加了一行常显的示例：

| 能力 | 内容 | 验收 |
| --- | --- | --- |
| **编号类型 4 种**（`seqKind`） | **数字**（原有）/ **字母**（Excel 列标：`A`、`Z`、`AA`、`ZZ`、`AAA`）/ **随机字符**（只含 `a-z0-9`，长度 1–16，默认 6）/ **时间**（起点 + 每项往后 N 天，样式 5 档）。四个类型共用一个「类型」下拉，**不是第四种模式** | TC-42 / TC-43 / TC-46 / TC-49 |
| **位置 3 档**（`seqPosition`） | 排在最后 / 排在最前 / **插在第 n 个字符后**（`'at'`，本批新增）。越界（n 大于名字长度）自动落到末尾，**不报错**；按码点切，**不劈开 emoji**；名字为空时退化成「排在最前」 | TC-44 / TC-45 |
| **示例行**（EL-124 / IX-107~109） | 常显在序号组里，拿**固定的示例名**试前三个位置 —— **不跟随列表里的真实文件**，文案里明说「不是你列表里的文件」。它走的是与预览 / 执行**同一个** `applyRuleMode`，所以示例不可能与真正改下去的结果不一致 | TC-42 |
| **「换一批」**（EL-125） | 随机字符专用：种子**单调 +1**（不是重新随机），点一次必然换一批，也就不会出现「点了好像没变」 | TC-47 |
| 跨天提示 | 规则区常显一句「日期取的是你点『开始改名』那天；如果中途跨过了零点，需要重新预览一次」—— 把主进程**早就有的挡板**显式讲出来（此前只活在代码里，文档与界面都没有） | — |

- **零迁移**：规则**从来不落盘**（`StoreName` 只有 `history` / `window` / `prefs`），所以加 6 个字段**不需要任何数据迁移**。
- **老行为逐字节不变**：默认值组合（数字 / 起始 1 / 增量 1 / 位数 3 / 排在最后）完全等于 P0 / P1 的行为。
- **措辞对齐**：界面上「步长」改叫**增量**、「补零」改叫**位数**（与小白用户的说法一致）。
- 决策：**DEC-15**（第 1 批范围 + 三个默认值取舍）/ **DEC-16**（三模式互斥 → 五模式互斥，正式重开 `DEC-02`，落地在第 5 批）—— 见 `PRD.md` §11.1。
- 设计：`P3-1编号系统升级轻量设计确认.md` v1.1（§13 六条待确认项已拍板）。验收 **TC-42 ~ TC-49**。

---

## 四、非功能需求

| 类别 | 需求 | 说明 |
| --- | --- | --- |
| 平台 | Windows 10 / 11（x64） | 第一版仅支持 Windows 桌面 |
| 性能 | 1000 个文件改名 < 10 秒 | 本地操作，不联网 |
| 安全 | **改名必须可预览、可撤销** | 防止误改导致文件丢失（详见风险提示） |
| 隐私 | 完全本地离线 | 文件不联网、不上传，不收集任何数据 |
| 体积 | 安装包首版目标 < 100 MB（**非发布阻断项**，见 PRD DEC-08） | 优于 FlyingMouse 的 1.6 GB；实测超标仅记录数值，不为此返工架构 |
| 免费 | 完全免费、无广告 | 个人免费使用 |

---

## 五、验收标准（MVP 完成标志）

1. 用户能通过**拖拽**或**按钮**两种方式把多个文件 / 文件夹加入列表（两种方式均为 P0）。
2. 用户能设置「删除 / 替换 / 规则化」三种改名规则。
3. 界面有一只可爱的耄耋猫，会跟随操作切换状态。
4. 改名前后能看到预览，执行后结果正确。
5. **（v0.3 新增）** 出现重名冲突时，软件**不会覆盖任何已有文件**（默认跳过并标红）。
6. **（v0.3 新增）** 撤销页可查看最近 20 次改名任务，任意一条可单独撤销。
7. 软件离线可用、免费、无广告，Windows 双击即用。

---

## 六、边界与「不做的事」（MVP 范围控制）

- ❌ 第一版**不做**文件格式转换（那是 FlyingMouse 的事，我们只做改名）。
- ❌ 第一版**不做** Mac / 手机版。
- ❌ 第一版**不做**云端同步、账号系统。
- ❌ 第一版**不做**收费 / VIP / 广告变现。

---

## 七、风险提示（v0.2 评审后更新）

1. **表情包版权风险（已决策：接受风险）**：经评审确认，本项目**免费、非商用**，**直接使用网络「耄耋」表情包**。
   - ⚠️ 客观提示：著作权侵权的判定**不以「是否收费」为唯一标准**，非商用同样可能构成侵权，只是被追责概率与抗辩空间更大；参考案例「鼠鼠」未出问题 **≠ 法律上无风险**，属「接受风险」而非「消除风险」。
   - 缓解措施：软件内明确标注「仅供个人免费使用、禁止商用」；架构上保留「一键替换形象资源」的能力，出现问题时快速切换。
2. **舆论争议风险（持续关注）**：「耄耋」梗后被部分群体用作争议表达，品牌商用翻过车（品客删视频道歉）。缓解：形象设计「可爱为主、暴躁为辅」，不渲染攻击性；推广保持低调。
3. **改名安全风险（已解决）**：批量改名不可逆 → **预览 + 撤销已提升为 P0**（F8 / F9）。
