# 第 1 批 轻量设计确认（编号系统升级）

> 产品名：**耄耋改名（MaoDie Renamer）**
> 文档版本：**v1.0**
> 日期：2026-09-18
> 上游文档：`PRD.md` v1.1.6 · `交互说明文档.md` v1.6 · `设计规范.md` v1.5 · `数据库设计.md` · `技术方案文档.md` · `接口文档.md`
> 配套交付：`设计稿-P3-1编号系统升级.html`（规格稿 · 8 张对照）
> 文档状态：**待评审**（评审通过后再进开发）

---

## 0. 结论摘要

这一批把「序号」从**一种**做成**四种**，位置从**两档**做成**三档**，并加一行「当场算给你看」的示例。

| 项 | 是否需要新增 |
| --- | --- |
| IPC 通道 | **零**（规则随既有 `md:rename:execute` / Worker 协议走） |
| 持久化字段 | **零迁移**（规则**从来不落盘** —— 见 §1.2，所以加字段不用写迁移） |
| 颜色令牌 | **零**（全部复用 `orange-dark` / `highlight-bg` / `bg-sunken` / `ink-3` / `bad`） |
| 界面 SCR | **零**（都在规则区内部，不是独立界面） |
| 真正新增的 | `RuleConfig.rule` 的 **6 个字段** + 引擎里 **4 个纯函数** + 一处三分支 + 序号组内几行 UI |

### P3 整体进度（8 批 · 只增不删）

| 批次 | 内容 | 状态 |
| --- | --- | --- |
| **1（本文档）** | **编号系统升级**：4 种编号类型 + 位置第三档 + 示例行 | **设计完成，待评审** |
| 2 | 提取文件名（导出 Excel / TXT / Word） | ⬜ 未开始 |
| 3 | 按文件属性命名（创建时间 / 修改时间） | ⬜ 未开始 |
| 4 | 导入 Excel 表格（规则来自表格） | ⬜ 未开始 |
| 5 | **三模式 → 五模式** + 扩展名更改 + 「仅使用编号作为文件名」 | ⬜ 未开始 |
| 6 | 提取文件夹名 | ⬜ 未开始 |
| 7 | 文件夹合并（4 种合并类型 × 复制/剪切） | ⬜ 未开始 |
| 8 | 文件提取（按类型 / 按名称 / 按 Excel） | ⬜ 未开始 |

> **排序理由**：从「零文件风险」往「高文件风险」推。第 1~4 批只改名字、只读不写；第 5 批开始碰扩展名；第 7、8 批会**复制 / 删除文件本身**，必须放最后，且要单独走一轮「不可逆操作」的安全评审。

---

## 1. 先讲清三件事

### 1.1 「编号类型」不是第四种模式，是序号组里的一个下拉

`RuleConfig.mode` 仍然是**模式互斥**的那一层（删除 / 替换 / 规则化）。「类型（数字 / 字母 / 随机字符 / 时间）」是**规则化模式内部**的一个参数，跟「位置」「起始」「位数」同级。

这样分层的理由：**模式互斥解决的是「算新名走哪条路」，类型解决的是「序号长什么样」** —— 两件事不该混在同一层。等到第 5 批把模式拆成五个，这个下拉照样待在规则化（及后续的自定义 / 插入）里，位置不用重排。

> 换句话说：这一批做的是**规则化模式内的纵向加深**，不是横向多一个模式。这样第 5 批的改动不会把这一批的设计推翻。

### 1.2 规则**从来不落盘**，所以加 6 个字段零迁移

我核过：存储层只有三个 store —— `history` / `window` / `prefs`（`types.ts` 的 `StoreName`）。**规则只活在渲染层的 store 里**，`history.json` 存的是摘要字符串（`ruleSummary`），不是 `RuleConfig`。

所以：

- 新增 6 个字段**不需要写迁移**，也不需要版本号 +1。
- 老版本读新数据、新版本读老数据都**不会因为规则字段而报错**。
- 唯一会「跨版本不一致」的是 `ruleSummary` **文本**（见 §7.3），而它只用于显示、不参与任何解析。

这是这一批能「轻」下来的地基。

### 1.3 「序号」这个词不改叫「编号」

你在需求里说的是「编号设置」。产品里从 P0 起一路用的是**序号**（`启用序号`、`序号位置`、`{n} 序号`）。

**建议继续用「序号」**，理由是硬的：`序号(...)` 这个片段已经被写进 `history.json` 的 `ruleSummary`。把词换掉，就会出现「老记录写序号、新记录写编号」同库两种措辞。纯显示问题，但没必要制造。

（如果你更想要「编号」，那也行 —— 那就**连 `ruleSummary` 一起换**，并接受老记录保持旧词。你说一声我改。）

---

## 2. 序号组的新形态

### 2.1 组内顺序

```
┌─ 序号组（背景 bg-warm）────────────────────────────────┐
│  ☑ 启用序号                                             │
│  位置   [排在最前 ▾]                                     │  ← 现在是「序号位置」，改叫「位置」
│  类型   [数字 ▾]                                         │  ← 新增 EL-121
│  ── 参数行（随类型变化）──                               │  ← 新增 EL-122
│  起始 4   增量 4   位数 0                                │
│  ── 示例行 ──                                            │  ← 新增 EL-124
│  示例（拿「【素材】」这个名字试的，不是你列表里的文件）    │
│  4【素材】 · 8【素材】 · 12【素材】                       │
└──────────────────────────────────────────────────────────┘
```

**位置放类型上面**：一是沿用现在的排布（现在的「序号位置」就在这个位置），改动最小；二是你列需求时的语序也是「位置选择 → 类型」。

**组标题仍叫「启用序号」**：它是这个组的开关，不跟着类型改名。

### 2.2 「序号位置」→「位置」

因为已经在「序号」这个组里了，再叫「序号位置」是重复。摘要文案（`seqPositionLabel`）不受影响，仍是「排在最前 / 排在最后」，新增一档「第 n 个字符后」。

### 2.3 位置第三档的形态

选中「插在第 n 个字符后」时，**就地**在右边长出一个数字框，**不另开一行**：

```
位置   [插在第 n 个字符后 ▾]  [ 3 ]
```

选别的两档时这个框**不显示**（不是置灰）。理由：它只对第三档有意义，摆一个常驻的灰框会让人以为「填了就能用」。

下面补一行说明：`数字超出名字长度时，会自动放到末尾。` —— 一句话把越界规则说在前面，省掉一次「为什么没生效」。

---

## 3. 四种编号类型的完整定义

### 3.1 一览

| 类型 | 值 | 参数 | 位数 | 示例（示例名 `【素材】`，位置=排在最前） |
| --- | --- | --- | --- | --- |
| **数字** | `number` | 起始 / 增量 / 位数 | 有 | 起始 4 增量 4 位数 0 → `4【素材】`、`8【素材】`、`12【素材】` |
| **字母** | `letter` | 起始 / 增量 | **隐藏** | 起始 1 增量 1 → `A【素材】`、`B【素材】`、`C【素材】` |
| **随机字符** | `random` | 长度（1–16）/ 换一批 | **隐藏** | 长度 6 → `k7f2q9【素材】` |
| **时间** | `time` | 起点 / 样式 / 增量（天） | **隐藏** | 起点 2026-09-18、样式 `yyyy年mm月dd日` → `2026年09月18日【素材】`、`2026年09月19日【素材】`… |

> 「位数」在三种类型下**不显示**，而不是置灰 —— 字母没有补零、随机字符的长度是另一个字段、时间的补零由样式决定。摆一个填了也没用的框，只会让人怀疑自己填错了。

### 3.2 字母 = Excel 列标

`1 → A`、`26 → Z`、`27 → AA`、`702 → ZZ`、`703 → AAA`。就是 Excel 的列标序列 —— 不用解释，中国人一看就懂。

- **起始钳到 ≥ 1**：字母编号里 0 没有意义，填 0 自动变 1，不报错、不弹提示。
- **输出大写**。要小写不必在这里加开关：进阶设置的「大小写 → 全部小写」会把整个名字一起转过去，包括这几个字母。

### 3.3 随机字符

**字符集只有 `a–z` + `0–9`（36 个）**，两个理由：

1. 不含 `< > : " / \ | ? *` 和空格 → 生成的名字**天然合法**，不需要再靠校验器兜底。
2. **不掺大写** → Windows 文件名不区分大小写，一旦掺进大写，就可能出现「两个随机名只差大小写」而互相撞名。只用小写 + 数字，从来源上把这类冲突排除掉。

**同一个文件每次预览拿到的随机串必须是同一个。** 做法：用文件自己的 `id` 当种子做确定性伪随机（`FNV-1a` 哈希 + `xorshift32`），不调用 `Math.random()`。

> 这不是「顺手这么写」，是项目底线「**预览 ≡ 执行**」的硬要求。随机值如果每次预览都重掷，用户看到的和真正改下去的就是两个东西 —— 而且这种 bug 在界面上**完全看不出来**。

**「换一批」** 按钮把 `seqRandomSeed` 加 1，种子变了整批就变。不加这个按钮的话，用户第一次随机出来的串会跟他一辈子 —— 撞上重名也没法自救。

### 3.4 时间：起点 + 每往后一天

- **起点**（`seqTimeStart`，`YYYY-MM-DD`）：默认是「你点选『时间』那一刻」的本机日期，可以改成任意一天。
- **样式**（`seqTimeFormat`）：与「启用日期」共用同一张样式表（见 §3.5）。
- **增量（天）**：单位是**天**，最小 1。第 1 个文件用起点，第 2 个 +1 天，第 3 个 +2 天。填 7 = 一周一个。

> **为什么增量最小是 1，不给 0**：`seqStep` 在整个规则里是无符号步长（`clamp()` 里钉死 ≥ 1）。为了时间类型单独放宽到 0，会让 `clamp()` 和 `sanitizeRule` 都变成「看类型给范围」的条件分支 —— 那正是最容易在预览与执行之间产生静默差异的地方。而「所有文件同一天」这个需求，上面的「启用日期」本来就做得到。**不值得为它引入一个跨层条件。**

**起点是冻结进规则本身的字面值**，所以它**不会漂移**：23:58 预览、00:01 执行，算出来的时间串与预览逐字节一致。

### 3.5 日期样式表扩两档

`DateFormat` 从 3 档扩到 5 档（**对「启用日期」同时生效**，两处共用一份实现）：

| 值 | 显示 |
| --- | --- |
| `YYYY-MM-DD` | 2026-09-18 |
| `YYYYMMDD` | 20260918 |
| `YYYY年MM月DD日` | 2026年09月18日 |
| `MM月DD日` | **09月18日**（新增，适合按月建文件夹） |
| `YYMMDD` | **260918**（新增） |

实现上只是给既有的 `dateText()` 的 `switch` 加两支 —— 一个函数，时间类型直接复用它。

---

## 4. 位置第三档的算法

### 4.1 组合公式要加一支

现在 `applyRuleMode` 里是这么写的：

```ts
const seqBefore = rule.seqPosition === 'prefix'
return seqBefore
  ? prefix + autoN + autoD + body + suffix
  : prefix + autoD + body + suffix + autoN
```

是个**布尔二选一**。加第三档必须改成三分支：

| 位置 | 组合 |
| --- | --- |
| `prefix`（排在最前） | `prefix + n + d + body + suffix` |
| `suffix`（排在最后） | `prefix + d + body + suffix + n` |
| **`at`（第 n 个字符后）** | `prefix + d + [body 前 k 个字] + n + [body 后段] + suffix` |

> ⚠️ **这处不改就会出现「界面照常显示、结果静默错」**：`seqBefore` 是 `=== 'prefix'` 的布尔，第三档会被判成 `false` 而走「排在最后」。用户看到的是「插在第 3 个字符后」，拿到的是「排在最后」，**界面一点异常都没有**。

### 4.2 四条边界规则

| # | 规则 | 为什么 |
| --- | --- | --- |
| 1 | k 数在**主体**上，扩展名永不参与 | EX-07 扩展名保护。`【素材】.docx` 里的 `.docx` 既不算长度也不会被插进去 |
| 2 | 按**码点**切，不按字节切（`Array.from(...)`） | 直接 `slice(k)` 会把 emoji / 增补平面字符劈成半个代理对，生成出**非法文件名**。半个字符是必须挡掉的 |
| 3 | 越界**自动落到末尾**，不报错 | 名字只有 2 个字却填「第 3 个字符后」→ 等价于「排在最后」。按最接近的意思办，胜过弹一个用户看不懂的错 |
| 4 | 与「保留原文件名」的交互 | 「保留原文件名」取消勾选时 `body` 为空 → 没有可插的地方 → 退化成「排在最前」。这是自然结果，不额外处理，但要**在文档里写明**免得被当成 bug |

---

## 5. 示例行（EL-124）

### 5.1 形态

```
示例（拿「【素材】」这个名字试的，不是你列表里的文件）
4【素材】 · 8【素材】 · 12【素材】
```

样式与 P1 的「当场演示」（EL-105）**完全同构**：凹槽底 `bg-sunken`、圆角 `radius-input`、槽内 `code` 提亮回 `bg-card`、新名用 `orange-dark` + `highlight-bg`。**不新造样式。**

### 5.2 三个刻意的选择

**① 规则化模式下常显，不藏进进阶折叠。**

P1 的「当场演示」是**给想用正则的进阶用户**看的，所以藏在进阶折叠里是对的。而编号是**所有人都会配**的东西 —— 参数一改就必须立刻看到结果。藏起来等于让用户去文件列表里自己找。

**② 示例名固定用 `【素材】`，不用列表里真实的第一个文件。**

- 用真实文件的话，示例会跟着列表变，用户想对照「改了参数会怎样」时就失去了参照。
- 「拿固定例子试」这件事本身就必须**明说**（文案里那句「不是你列表里的文件」），否则用户会把它当成真实预览。

**③ 用真引擎函数算，不另写一套。**

示例走的是 `applyRuleMode` + `applyCaseTransform` —— **与预览 Worker、与主进程执行器同一个函数**（ADR-001）。所以示例不可能与实际结果不一致。

### 5.3 位数=0 也要示例得出来

位数填 0 时数字不补零，示例就显示 `4【素材】`。用户一填 3，示例立刻变 `004【素材】` —— **「位数」这个框是干嘛的，不用讲，看一眼就懂了。** 这是这一批用示例替代说明文字的主要收益。

---

## 6. 「跨天」这件事我要说清楚

你上一条认可了我的处理，这里把结论写死：

| 项 | 会不会漂移 | 说明 |
| --- | --- | --- |
| **时间类型**（新增） | **不会** | 起点是冻结进规则的字面值，跟当前时间无关 |
| **启用日期**（既有） | **会** | 它取「执行当天」。23:58 预览（日期=17 号）、00:01 执行 → 期望的是 18 号 |
| **既有挡板** | — | `ExecuteRequest.date` 有个校验：「必须等于本机今天」。跨过零点后 `date` 对不上，执行被挡下，界面要求**重新预览** |

**所以现在的行为是对的**：跨天不会改错名字，只会被要求重看一遍。这一批**不动这个机制**，只做两件事：

1. 在「启用日期」下面加一句常显提示：`日期取的是你点「开始改名」那天；如果中途跨过了零点，需要重新预览一次。`
2. 在本文档和接口文档里把这个行为**显式写下来** —— 它此前只活在主进程的一行校验里，文档里没有。

> ⚠️ 提醒一句：那句提示只在「启用日期」勾上时有意义。做成常显是因为它就在日期组里面，不加判断成本更低；如果你觉得啰嗦，改成只在勾选后显示也很容易。

---

## 7. 改动面

### 7.1 字段与类型

```ts
// src/shared/types.ts
export type SeqKind = 'number' | 'letter' | 'random' | 'time'
export type SeqPosition = 'prefix' | 'suffix' | 'at'          // ← 加 'at'
export type DateFormat =
  | 'YYYY-MM-DD' | 'YYYYMMDD' | 'YYYY年MM月DD日'
  | 'MM月DD日' | 'YYMMDD'                                      // ← 加两档

rule: {
  /* …既有字段一个不动… */
  seqKind: SeqKind          // 默认 'number' —— 所以老数据/老任务行为逐字节不变
  seqAt: number             // 默认 1，范围 1–200，仅 seqPosition === 'at' 时生效
  seqRandomLen: number      // 默认 6，范围 1–16，仅 'random'
  seqRandomSeed: number     // 默认 0，≥ 0，「换一批」+1
  seqTimeStart: string      // 默认 ''；切到时间类型且为空时填本机今天
  seqTimeFormat: DateFormat // 默认 'YYYY年MM月DD日'
}
```

> **为什么「长度」是新字段，而不是复用 `seqPad`**：`seqPad` 的钳制上限是 6，且**已有的单测正钉着这个 6**（`seqPad: 99` → `'000001'`）。要复用它就得把这个上限改成 16 —— 那等于**改一条既有断言**。本项目有条红线是「绝不为让测试变绿而放宽断言」，虽然这次是需求变了而不是为了让测试变绿，但**能不碰就不碰**：加一个字段，`seqPad` 及其单测一字不动，回归面为零。

### 7.2 引擎 `src/shared/rule-engine.ts`

| # | 改动 |
| --- | --- |
| 1 | `RuleContext` 加 `seedKey: string`（随机字符的种子来源，传文件 id） |
| 2 | `seqText()` 改成**按 `seqKind` 分派到 4 条支路**，其中数字那条**函数体原样搬走、输出逐字节不变** |
| 3 | 新增纯函数 `toLetters(n)`：`1→A`、`27→AA` |
| 4 | 新增纯函数 `letterText(rule, index)`：起始钳 ≥1 |
| 5 | 新增纯函数 `randomText(rule, ctx)`：`FNV-1a` + `xorshift32`，只输出 `a–z0–9` |
| 6 | 新增纯函数 `addDays(ymd, n)`：**纯整数公历日序算法**，不碰 `Date` |
| 7 | `dateText()` 的 `switch` 加 `MM月DD日` / `YYMMDD` 两支 |
| 8 | `applyRuleMode()` 的 `seqBefore` 布尔 → **三分支**；`at` 支用 `Array.from(...)` 按码点切片 |

**关于 `addDays` 为什么不能用 `new Date()`**：`new Date('2026-09-18')` 按 UTC 解析，而时区偏移与夏令时会让「加 1 天」在某些日子里偏出整整一天。批量改名的日期一旦错一天，是一整批文件都错，且**用户很难当场发现**。纯整数算法（年月日 → 日序 → 加 n → 还原）跨月、跨年、闰年都是确定的，也才写得出单测。

### 7.3 其他文件

| # | 位置 | 改什么 |
| --- | --- | --- |
| 1 | `src/main/ipc/rename.ipc.ts` 的 `sanitizeRule` | **★ 最阴的一处，见下方警告**：白名单补 6 个新字段 + 放行 `seqPosition === 'at'` + 放行新的两个 `DateFormat` |
| 2 | `src/shared/preview.ts` | `computeNewStem(parts, rule, { index, total, date })` → 补 `seedKey: item.id` |
| 3 | `src/shared/cli-args.ts` | `--rule '{...}'` 的解析同样要认这 6 个字段与 `'at'` |
| 4 | `src/renderer/src/stores/rule.ts` | `clamp()` 补 `seqAt`（≥1）、`seqRandomLen`（1–16）、`seqTimeStart`（合法日期串）；新增 `bumpRandomSeed()`、`switchSeqKind(k)`（切到时间且起点为空时填本机今天，**并且这处要显式回收临时 UI 状态**） |
| 5 | `src/shared/labels.ts` | `SEQ_KIND_OPTIONS`（4 项，顺序即界面顺序）+ `seqKindLabel()`；`seqPositionLabel` 补 `'at'` → `第 n 个字符后` |
| 6 | `src/shared/rule-summary.ts` | 序号段体现类型与位置：`序号(数字 / 起始 4 / 增量 4 / 位数 0 / 排在最前)`、`序号(字母 / 起始 1 / 增量 1 / 排在最前)`、`序号(随机 6 位 / 排在最前)`、`序号(时间 2026-09-18 起每天 / 排在最前)`；并把「步长」改叫「**增量**」、「补零」改叫「**位数**」，与界面用词对齐 |
| 7 | `src/renderer/src/components/RulePanel.vue` | 类型下拉 + 参数行条件渲染 + 位置第三档的内联框 + 示例行 + 换一批 |
| 8 | `tests/` | 新增本批的纯函数单测与 TC-42~49（§9） |

> ⚠️ **第 1 条是本批最危险的地方，必须单独说。**
> `sanitizeRule` 是**逐字段白名单** —— 它不认识的字段会被**直接丢掉**。新加的 6 个字段只要漏一个，就会出现：
> **预览是对的（渲染层直接用规则）、执行是错的（主进程拿到的是砍过的规则），而界面上一点异常都没有。**
> 本项目在 P2-A 已经被同一类问题坑过（「值永远不变、界面却无异常」是这里最难查的故障形态）。所以：
> - 每个新字段都要在 `sanitizeRule` 里有对应的 `str` / `num` / `bool` 收口，且**钳制范围与 `clamp()` 完全一致**；
> - 必须有一条断言「**同一份规则，预览结果与执行结果逐字节相等**」的单测（TC-48），光靠点界面发现不了。

> ⚠️ **`ruleSummary` 会写进 `history.json`。**
> 上了这一批之后，**新记录**的摘要是新写法（`序号(数字 / 起始 4 / 增量 4 / 位数 0 / 排在最前)`），**老记录**保持旧写法（`序号(起始 1 / 步长 1 / 补零 3 / 排在最后)`）。
> 这是**可以接受**的：摘要是纯显示字符串，不参与任何解析，两种写法各自自洽。
> **但绝对不要为了「新老一致」去写一个迁移改老记录** —— 那等于去改已经落盘的历史数据，而历史数据是撤销功能的依据。（与 P2-B「摘要截断只在渲染层」是同一条纪律。）

### 7.4 界面规格（细节见规格稿第五节）

| 项 | 值 |
| --- | --- |
| 控件高度 | 一律 `--md-ctrl-h = 40px`（可点击目标 ≥ 24×24 CSS px，WCAG 2.5.8 AA 达标） |
| 数字 / 日期框 | 起始 / 增量 / 位数 / 长度 / 增量（天）`104px`；「第 n 个字符后」`64px` 居中；起点日期 `142px` |
| 数字字体 | `font-num`（Inter）—— 位数对齐才看得出补了几位 |
| 「换一批」 | 高 **28px**、圆角 `radius-badge 6px`、底 `bg-card`、描边 `line-strong`。**既有按钮类只有 22px，够不上 AA 的 24px，所以这个新按钮单独给高度** |
| 示例凹槽 | 复用 `.md-adv__demo` 的规格，零新样式 |
| 新增色值 | **0 个** |
| 新增令牌 | **0 个** → 也就不需要在 `tokens.css` 的深色块里补任何值 |
| 禁用态 | 「启用序号」未勾选时，位置 / 类型 / 参数 / 换一批整组置灰；**示例行整块不显示**（没启用就没什么可示例的） |
| 键盘 | Tab：位置 → 类型 → 起始 → 增量 → 位数（或 长度 → 换一批）；选中第三档时焦点自动落到那个数字框 |

---

## 8. 编号清单（已按「前缀全量」grep 核对）

> ⚠️ 本仓库踩过坑：设计 P2-A 时用 `TC-3[0-9]` 去搜，**这个模式匹配不到 `TC-27~29`**，导致编号撞车。
> **规矩：查重一律按前缀全量搜（搜 `TC-` / `EL-` / `IX-` / `DEC-`），不按猜的区间搜。**
> 本次核对结果（全仓库 `*.md` 扫描）：`EL` max **120** · `IX` max **106** · `TC` max **41** · `DEC` max **14** · `EX` max **17** · `ST` max **5** · `SCR` max **8** · `ADR` max **6**。

| 类别 | 新增 |
| --- | --- |
| 元素 EL | `EL-121` 编号类型下拉<br>`EL-122` 类型参数区（随类型切换）<br>`EL-123` 位置第三档的「第 n 个字符后」内联输入<br>`EL-124` 示例行<br>`EL-125` 「换一批」按钮 |
| 交互 IX | `IX-107` 切换编号类型<br>`IX-108` 点「换一批」<br>`IX-109` 位置选第三档时显示内联框并聚焦 |
| 验收 TC | `TC-42` ~ `TC-49`（见 §9） |
| 决策 DEC | `DEC-15` 第 1 批范围 + 三个默认值取舍（字母大写 / 随机只用小写 / 长度独立成字段）<br>`DEC-16` **三模式互斥 → 五模式互斥**（正式重开 `DEC-02`，落地在第 5 批） |
| 界面 SCR | **不新增** —— 全都在规则区内部 |

> **`DEC-16` 为什么要现在记**：`DEC-02` 是**已关闭**的决策，而这一轮把它推翻了。项目规矩是「改上游必须同步下游」，决策也是如此 —— 一条被推翻的决策如果不留痕，下一个会话会拿着 `DEC-02` 去质疑第 5 批的设计。

---

## 9. 验收用例

| 编号 | 用例 | 步骤 | 预期 |
| --- | --- | --- | --- |
| **TC-42** | 类型切换 | 依次把类型切到 数字 / 字母 / 随机字符 / 时间 | 参数行**跟着换**：数字=起始·增量·位数；字母=起始·增量（无位数）；随机=长度+换一批；时间=起点·样式·增量（天）。**示例行每次都立刻变**，不是空着 |
| **TC-43** | 字母是 Excel 列标 | 类型=字母，起始 1、26、27、702、703，各看第一个文件的示例 | `A` / `Z` / `AA` / `ZZ` / `AAA`。起始填 0 → 自动变 1，不报错 |
| **TC-44** | 位置第三档 | 位置=「插在第 n 个字符后」，n=3；示例名 `【素材】` | `【素材1】`（插在「材」和「】」之间）。**扩展名不参与**：`【素材】.docx` 的 `.docx` 不会被插进去 |
| **TC-45** | 位置越界 + 码点 | n=3 但名字只有 2 个字；另一个名字含 emoji（如 `🎉庆`），n=1 | 前者落到末尾（`素材1`），**不报错**；后者 `🎉` 完整不劈开，生成合法文件名 |
| **TC-46** | 随机字符「预览 ≡ 执行」 | 类型=随机字符，长度 6；不碰任何参数，反复触发预览 5 次 | 5 次每个文件的随机串**完全一样**。改一次参数再改回来，仍然一样 |
| **TC-47** | 「换一批」真的换 | 记下当前 3 个示例结果 → 点「换一批」 | 结果变了；**再点一次不会回到上一批**（种子单调 +1）；撤不掉也没关系，它不是破坏性操作 |
| **TC-48** | **执行不丢字段（本批最重要）** | 每种类型各跑一次真实改名，把**预览里显示的新名**与**磁盘上真实的名字**逐条比对 | **逐字节相等**。特别要覆盖：`seqPosition='at'`、`字母`、`随机字符`、`时间` 四种组合。**这一条是唯一能抓住 `sanitizeRule` 漏字段的用例** |
| **TC-49** | 时间跨月 / 跨年 / 闰年 | 起点 2026-12-30 增量 3；起点 2028-02-27 增量 1；起点 2026-09-30 增量 1 | `2027-01-02` / `2028-02-28`（闰年 29 天月）/ `2026-10-01`。**一律不出「2026-09-31」这种不存在的日期** |
| 附 | 跨天守卫 | 把 `ExecuteRequest.date` 改成昨天再执行 | 被挡下并提示重新预览（既有机制，本次只是补文档与提示文案） |

---

## 10. 测试要求

按项目既定规矩（**不引入第三方测试框架**，单测用 `node --test`，界面用真实 Electron 冒烟）：

| 层 | 要求 |
| --- | --- |
| 单测 | **既有断言一条都不许改**：`seqText` 的数字分支输出必须逐字节不变（钉住 `seqPad: 99 → '000001'` 那条上限断言） |
| 单测 | `toLetters`：1 / 26 / 27 / 702 / 703 / 0 / -3 / 非整数 各一条 |
| 单测 | `randomText`：**同一 `seedKey` + 同一 seed → 同一结果**（跑 100 次全等）；**不同 `seedKey` → 结果不同**；seed +1 → 结果变；输出**只含 `[a-z0-9]`**（跑 1000 次都不出现别的字符） |
| 单测 | `addDays`：跨月 / 跨年 / 闰年 / n=0 / n 为大数；**断言绝不出非法日期**（月 13、日 32、2 月 30） |
| 单测 | `dateText`：新增两档格式正确，且**既有三档输出不变** |
| 单测 | `applyRuleMode`：`at` 支 —— 正常、k=0、k=长度、k 越界、含 emoji、`keepOriginal=false`（退化为排在最前）各一条 |
| 单测 | **预览 ≡ 执行**：同一份规则分别走 `buildPreview` 与执行器的计划构建，断言两边算出的新名**逐条逐字节相等**（对应 TC-48，覆盖四种类型 × 三档位置） |
| 单测 | `sanitizeRule`：6 个新字段逐个「喂进去 → 取出来相等」；喂非法值（负数 / 超长 / 非字符串）→ 落到合法默认值 |
| 界面冒烟（真实实例 + 真实点击） | 四种类型各切一遍，断言参数行与示例行的实际文本；点一次「换一批」断言示例变了；勾掉「启用序号」断言整组置灰且示例行消失 |
| 交付报告 | 必须写「哪些没验到」。**已知必列**：① 深色下原生日期框的日历图标观感（浏览器画的，只能真机肉眼确认）② 打包后的 exe ③ 「减少动画」开启时参数行切换的实际观感 |

**红线照旧：绝不为让测试变绿而放宽 / 跳过 / 删断言。挂了就给原始报错。**

---

## 11. 待同步的文档

| 文档 | 需要的同步 | 状态 |
| --- | --- | --- |
| `需求清单.md` | 编号类型 4 种 + 位置 3 档 + 示例行；`DEC-15` / `DEC-16` | ⬜ 待改 |
| `PRD.md` | §3 规则化模式细化；§5.1 元素清单加 `EL-121~125`；§10 验收表加 `TC-42~49`；§11.1 **`DEC-02` 标注「已由 `DEC-16` 重开」** | ⬜ 待改 |
| `交互说明文档.md` | §2 SCR-01 规则区加 `EL-121~125` / `IX-107~109`；§1 对照索引加行 | ⬜ 待改 |
| `设计规范.md` | 「编号类型下拉」「类型参数区」「示例行」「换一批」四个组件规格；§5.3 说明摘要新写法 | ⬜ 待改 |
| `技术方案文档.md` | §4.1.2 组合公式从四情形（A~D）扩到**六情形**（加 `at` × `keepOriginal`）；§11 遗留项补「第 1 批设计已定稿」；登记 4 个新纯函数 | ⬜ 待改 |
| `接口文档.md` | §5 `RuleConfig` 加 6 字段；§1.3.2 序列化约束补「`seqTimeStart` 用 `YYYY-MM-DD` 字符串，不传 `Date`」；**§6 显式写下「跨天必须重新预览」这条既有行为** | ⬜ 待改 |
| `P1轻量设计确认.md` | **修正那句「折叠区同时是 P2 多规则链 / 模板库的预留挂载点」**（P2-B 已指出对模板库是错的，本批顺带确认：对多规则链仍然对） | ⬜ 待改 |
| `数据库设计.md` | 无需改动（**规则不落盘**）—— 在文档里补一句这个事实，免得后人去找迁移 | ⬜ 待改 |

---

## 12. 本批明确不做的事

| 不做 | 归到哪 |
| --- | --- |
| 「仅使用编号作为文件名」独立开关 | **第 5 批**。⚠️ 顺带一个发现：**现在就能做到等价效果** —— 把「保留原文件名」取消勾选、前缀后缀留空、日期不启用，结果就是「名字只剩序号」。这样第 5 批可以只讨论「要不要给它一个显眼的入口」，而不是重新实现能力 |
| 三模式 → 五模式（自定义 / 插入 / 替换 / 删除 / …） | 第 5 批（`DEC-16`） |
| 扩展名更改（位置 / 替换 / 删除 / 新扩展名） | 第 5 批 |
| 「插入」模式与「自定义」模式 | 第 5 批 |
| 时间类型精确到时分秒 | **本批只到「日」**。要精确到时分，需要多一个时间输入框，且按天步进就失去意义 —— 如果你的实际场景要它，说一声，加一格 |
| 改「启用日期」的漂移机制 | 只补提示文案与文档（§6） |
| 多规则链式组合 | 继续推迟 |
| 「起始 / 增量 / 位数」的界面之外任何行为变化 | **一个字都不改** |

---

## 13. 待你（产品负责人）确认的事

1. **字母用大写 `A` 还是小写 `a`？** 我选**大写**（Excel 列标的肌肉记忆，且和原文件名里的小写词视觉上区分得开）。想小写的话，靠进阶设置里的「全部小写」也能达成，但那是全局的 —— 如果你要「只有字母是小写、原名保持原样」，就得给类型加个大小写开关，说一声我加。
2. **随机字符只用小写 + 数字（36 个）**，为了避开 Windows 大小写不敏感的撞名。你要不要**多给一个「包含大写」的勾**？（我倾向不给 —— 那等于把一个已知的撞名来源做成可选项。）
3. **时间类型的增量最小 1 天，不给 0。** 想「所有文件同一天」时，请用上面的「启用日期」。
4. **示例行固定拿 `【素材】` 试**，不跟随列表里的真实文件。如果你更想看到「你自己的第一个文件会变成什么」，那我改成读列表第一条（列表为空时仍退回 `【素材】`）。
5. **「位数 / 长度」做成两个独立字段**（不合并复用）。代价是界面上字母/随机/时间下都看不到「位数」；收益是 P0 的老行为与老单测**一字不动**。
6. **词用「序号」而不是「编号」**（§1.3）。想换的话我连 `ruleSummary` 一起换。
