# 第 8 批 轻量设计确认（文件提取）

> 产品名：**耄耋改名（MaoDie Renamer）**
> 文档版本：**v1.0**
> 日期：2026-09-23
> 上游文档：`需求清单.md` · `PRD.md` · `交互说明文档.md` · `设计规范.md` · `技术方案文档.md` · `接口文档.md` · `数据库设计.md` · `P3-2-8批范围澄清与交付总规划.md` · `P3-5五模式与扩展名更改轻量设计确认.md` · `P3-6提取文件夹名轻量设计确认.md` · `P3-7文件夹合并轻量设计确认.md`
> 配套交付：Ardot 画板（`fileId 724930301753449`，x = 23020）
> 文档状态：**已落地（2026-09-24）** —— 与 P3-7 同量级（都会真的复制 / 移动文件，且不可逆），已单独评审后落地

## ★ 已拍板（2026-09-23，产品负责人四答 + 沿用 P3-7）

> 产品负责人在「第 7 批已落地、开始下一批」的回合里，对总规划 §5 留下的四个分歧点逐条回了（全部取推荐项）。下面把结论汇总，决策留痕放在 §13。

| # | 问题 | 结论 |
| --- | --- | --- |
| **1** | 提取操作：复制还是移动 | **两个都做**；**复制默认（•），剪切默认关**（与 P3-7 一致） |
| **2** | 提取到哪：目标目录怎么组织 | **单一目录** —— 所有提取的文件进你指定的同一个文件夹（最简） |
| **3** | 三种筛选能否叠加 | **可叠加（AND）** —— 多个条件同时满足才提取（如「类型是图片 且 名称含'旅游'」） |
| **4** | 「按名称」怎么匹配 | **包含 + 可开正则开关** —— 默认「文件名含关键词」，高手可开正则（复用 F10 那套） |
| **5** | 做成什么形态 | **独立对话框，不碰主流程**（沿用 P3-7 拍板第 7 条） |
| **6** | 撤销 | **不做，先干跑再说**（沿用 P3-7 拍板第 3 条） |
| **7** | 提取源是什么 | **当前已加载文件列表**（复用主列表，零新增源输入控件）—— 本批与 P3-7「独立拖入区」不同 |
| **8** | 「按 Excel」怎么来 | **复用第 4 批（导入 Excel）的解析器**读取文件名列（总规划 §5 已定） |
| **9** | 完成后打开目标目录 | **默认勾上，可取消**（沿用 P3-7 拍板第 9 条） |
| **10** | 「覆盖」档 | **不开** —— 复用最高铁律「绝不覆盖」（与 P3-7 同，DEC-28） |

---

## 0. 结论摘要

> ★ **这一批与 P3-7 同量级，但规模更小。** P3-7 第一次真的复制 / 移动 / 删除文件；本批做的是 P3-7 的**子集 + 一个筛选器**：它**复用 P3-7 的运输层**（算落点 + 绝不覆盖 + 两阶段剪切 + 干跑报告），本批**唯一真正新增的逻辑是「筛选」**——按类型 / 按名称 / 按 Excel 从当前列表里挑出要提取的文件。所以它**同样会破「轻量化」里的两条**（新增 IPC 通道 + 新增独立界面 SCR），同样必须单独说明为什么值得破。

| 项 | 是否需要新增 | 说明 |
| --- | --- | --- |
| **IPC 通道** | **★ 新增** | 渲染层对话框要驱动主进程去搬文件，加 `extract:plan`（干跑）/ `extract:run`（执行）两个通道（或复用 `merge:plan/run` 加 `mode` 区分，§7 二选一） |
| 持久化字段 | **零** | 撤销延后；干跑报告不落盘；历史记录类型本批不做（与 P3-7 一致） |
| 颜色令牌 | **零** | 复用现有对话框令牌（同一套应用） |
| 界面 SCR | **★ 新增** | 提取对话框三块：`SCR-13` 外壳 / `SCR-14` 表单（含筛选）/ `SCR-15` 报告 |
| 错误码 `EX` | **★ 新增 2 个** | `EX-21` 按 Excel 提取时列表中匹配不到（或未匹配项）/ `EX-22` 按名称的正则语法非法；其余复用 P3-7 的 `EX-19`/`EX-20` 与既有读 / 写 / 冲突码 |
| 运行时第三方依赖 | **零** | 复用 node 自带 `fs.cp` / `fs.rename`；Excel 解析复用 P3-4 既有依赖 |
| **`RuleConfig` 新字段** | **零** | 本批与「改名规则」完全无关，是独立子系统 |
| **文件风险** | **★★★** | 真的复制 / 移动文件；靠「绝不覆盖 + 强制干跑报告」两道闸兜住（与 P3-7 同） |
| 真正新增的 | 一个**提取对话框**（独立子系统）+ 一个**纯函数 `extract-filter.ts`**（按条件从列表挑文件，**预览 ≡ 执行**）+ 复用 P3-7 的 `merge-plan.ts` / `merge-service.ts` 运输层 |

### P3 整体进度（8 批 · 只增不删）

| 批次 | 内容 | 状态 |
| --- | --- | --- |
| 1 | 编号系统升级 | ✅ 已落地 |
| 2 | 提取文件名 → 导出清单 | ✅ 已落地 |
| 3 | 按文件属性命名 | ✅ 已落地 |
| 4 | 导入 Excel | ✅ 已落地 |
| 5 | 三模式 → 五模式 + 扩展名更改 + 「只用编号」入口 | ✅ 已落地 |
| 6 | 提取文件夹名 | ✅ 已落地 |
| 7 | 文件夹合并 | ✅ 已落地（2026-09-23） |
| **8（本文档）** | **文件提取** | **✅ 已落地（2026-09-24）** |

---

## 1. 先讲清四件事

### 1.1 它不是改名的第 N 个模式，是 P3-7「搬文件」子系统的孪生

前六批都是「给一批文件算出新名字，原地改名」。P3-7 第一次把文件**复制 / 移动到另一个文件夹**（按四种方式归类）。本批**不改名、不归类**——它从当前已加载的文件列表里，**按条件挑出一部分**，复制 / 移动到**同一个目标目录**。

所以它**完全不碰**现有的五模式 / 规则引擎 / 改名执行器。它复用 P3-7 的运输层（`merge-plan.ts` 算落点 + `merge-service.ts` 执行器 + `fs-walk.ts` 护栏），只新增一个筛选器（`extract-filter.ts`）。

> ★ 形态上「独立对话框，不碰主流程」（拍板第 5 条）的准确含义：**不改动主界面和改名流程**，提取是一个单独打开的弹窗，关掉后对主流程零影响。

### 1.2 ★ 提取源 = 当前已加载文件列表（与 P3-7 不同）

P3-7 的源是**对话框自己带的拖入区**（且支持递归摊平文件夹）。本批**不另起拖入区**——它的源就是主界面里**当前已加载的那批文件**（用户通过「添加文件 / 导入 Excel」已经装进来的）。

```
主界面已加载 128 个文件  →  点「文件提取」  →  弹窗里设筛选条件
                                                      ↓
                              符合条件的子集（如 23 个）→ 复制/移动到目标目录
```

→ 好处：零新增源输入控件，且和「导入 Excel / 添加文件」自然衔接（用户先装好列表，再挑一部分提出去）。代价（边界见 §4 第 9 条）：**剪切后主列表项仍指向已被移走的文件**，本批不回写清理（§12）。

### 1.3 与最高铁律「绝不覆盖」的关系 —— 本批**不开「覆盖」档**

安全底线第 ① 条从第 0 批起就是「**绝不覆盖**」。提取天然会遇到「目标已存在同名文件」——本批的处理是：**默认跳过 + 标红 + 在干跑报告里逐条列出**，与 P3-7 / 改名一致。

→ 本批**不提供「覆盖」选项**（那是给最高铁律开口子，必须单独评审，见 DEC-28）。

### 1.4 撤销 —— 本批**不做**（拍板第 6 条）

现有 `F9` 撤销**只回滚改名**。本批搬动文件后，`F9` 回滚不了。

→ 沿用 P3-7 拍板：**不做撤销，先干跑再说**。干跑报告是本批的替代安全网。

---

## 2. 界面形态

### 2.1 入口：主工具栏一个按钮

```
┌──────────────────────────────────────────────────────────────┐
│  [ 添加文件 ]  [ 导入Excel ]   …        [ 文件提取 ]  ← 新增  │
└──────────────────────────────────────────────────────────────┘
```

点「文件提取」→ 打开独立对话框（不碰下面任何东西）。

### 2.2 对话框（SCR-13 外壳 + SCR-14 表单）

```
┌─ 文件提取 ────────────────────────────────────────────────┐
│                                                            │
│  源   当前已加载 128 个文件（来自主列表，不可在此增删）      │
│                                                            │
│  筛选条件（多个同时满足 AND）                               │
│    按类型   ☑ 图片  ☑ 文档  ☐ 视频  ☐ 音频  ☐ 压缩包  ☐ 其他 │
│    按名称   (•) 包含 [ 旅游        ]   ☑ 使用正则（复用F10） │
│    按Excel  [ 选择文件… 未选 ]   （列出要提取的文件名）      │
│                                                            │
│  操作    (•) 复制（保留源）  ( ) 剪切（移动，删源）          │
│                                                            │
│  目标目录  (•) 选择已有 [ D:\提取结果      ▾ ]               │
│            ( ) 新建于   [ D:\              ▾ ] 名称 [ 提取 ] │
│                                                            │
│  ☑ 完成后打开目标目录                                       │
│                                                            │
│                          [ 干跑并预览 ]                    │
└────────────────────────────────────────────────────────────┘
```

> 三种筛选**可叠加**：勾了「图片」+ 名称含「旅游」+ 选了 Excel，则三者 AND —— 既要是图片、又要名含旅游、又要在 Excel 名单里。

### 2.3 干跑报告（SCR-15，模态内嵌在对话框里）

点「干跑并预览」→ **不算真动**，先算出「哪些文件会被提取、各自落到哪」+「哪些会冲突」+「哪些 Excel 名单匹配不到」+「有没有错」，列成报告：

```
┌─ 干跑报告 ──────────────────────────────────────────────────┐
│  当前列表 128 → 筛出 23 个 → 目标 D:\提取结果                 │
│  将复制 21 · 将跳过 2（冲突）· 未匹配 0 · 错误 0              │
│                                                            │
│  示例：                                                    │
│   1.jpg         ✓  图片 + 含"旅游"                          │
│   旅游2.png     ✓                                          │
│   旅游3.jpg     → 冲突：目标已存在，跳过                    │
│   …                                                         │
│                                                            │
│  [ 取消 ]                              [ 确认执行 ]        │
└──────────────────────────────────────────────────────────────┘
```

点「确认执行」才真的搬。**报告里列的「将复制 21 / 跳过 2」必须和真执行的结果逐字节一致**（§3 / §7.3 第 3 行）。

---

## 3. 完整定义

### 3.1 提取源（拍板第 7 条）

源 = 主界面**当前已加载的文件列表**（与改名共用同一批文件）。对话框只读展示数量，**不在对话框内增删源**。

### 3.2 三种筛选条件（各算各的命中集合，最后取交集）

| 条件 | 命中规则 | 例子 |
| --- | --- | --- |
| **按类型** | 文件扩展名归入用户勾选的类别才命中；类别 = 图片 / 文档 / 视频 / 音频 / 压缩包 / 其他（与 P3-7②「按扩展名建文件夹」用同一张扩展名→类别映射表，避免两套口径） | 勾了「图片 + 文档」→ 只提取 `.jpg/.png/...` 与 `.docx/.pdf/...`；无扩展名文件只在勾了「其他」时命中 |
| **按名称** | 默认「文件名（不含路径、不含扩展名）包含关键词」即命中；开「使用正则」后按正则匹配（复用 F10 的正则开关与校验） | 含「旅游」→ `旅游2.png` 命中；正则 `^IMG_\d+` → 只命中形如 `IMG_0001` 的 |
| **按 Excel** | 复用 P3-4 的 Excel 解析器读「文件名列」；列表中**文件名在该列里出现**即命中（可选项含路径） | Excel 列着 `1.jpg` `3.jpg` → 列表里这两个命中 |

> 三种条件**可叠加（AND，拍板第 3 条）**：全部勾选的条件都满足才提取。只勾一种 = 只按那一种筛。

### 3.3 落点（单一目录，拍板第 2 条）

目标根目录 `T` = 用户选的单一目录。每个命中的源文件 `S` 落点一律 `T\<原名>`（**不建子文件夹**，与 P3-7① 同口径）。

### 3.4 复制 vs 剪切（拍板第 1 条）

| 操作 | 源文件 | 实现 |
| --- | --- | --- |
| **复制**（默认，•） | **保留** | `fs.cp` 源 → `T\<原名>` |
| **剪切**（默认关） | **删除** | 先 `fs.cp` 到落点，**全部复制成功后再删源**（两阶段，见 §4 边界 6，复用 P3-7） |

### 3.5 目标目录（拍板沿用 P3-7 第 8 条，二选一）

- **选已有**：从系统文件夹选择器挑一个已存在的目录当 `T`。
- **新建**：选一个父目录 + 填一个名称 → 在父目录下新建 `名称` 当 `T`（已存在则拒绝并要求改名）。

### 3.6 完成后打开目标目录（拍板第 9 条）

执行成功后用系统方式打开 `T`（Windows：`explorer` 打开该文件夹）。**默认勾上，可取消**。

---

## 4. 算法与边界

| # | 边界 | 预期行为 |
| --- | --- | --- |
| 1 | 目标已存在同名文件 | **跳过 + 标红**（绝不覆盖，§1.3）；干跑报告逐条列出 |
| 2 | 三种筛选**结果为空**（没筛出任何文件） | 干跑报告直接提示「没有符合条件的文件」，不打开执行 |
| 3 | **按类型**的类别判定 | 复用 P3-7②的扩展名→类别映射；无扩展名只在勾「其他」时命中；隐藏点文件（如 `.gitignore`）按其实际扩展名或「其他」判定 |
| 4 | **按名称**正则语法非法 | 表单即时红框提示（EX-22），**拒绝干跑**，不让非法正则混进 plan |
| 5 | **按 Excel** 名单里某文件名在列表匹配不到 | 干跑报告逐条标「未匹配」（EX-21），不中断整批；Excel 本身解析失败 → 报错、不进入干跑 |
| 6 | ★ 剪切（移动）中途失败 | **两阶段**：先确保**全部**复制成功，再删源。任一处删源失败 → 记 `EX-20` 半移动态、报告标红，**绝不**回滚已复制的文件（复用 P3-7） |
| 7 | 多条件叠加 | 任一条件不满足即不提取（AND）；只勾一种则只按那一种 |
| 8 | 日期 / 编码 | 不涉及日期分组；文件名比对统一用**原文件名精确匹配**，Excel 列也按文件名比（避免大小写/全半角歧义 → 报告里标「未匹配」让用户核对） |
| 9 | ★ 剪切后主列表项指向已移动文件 | 本批**不回写 / 不清理主列表**（剪切后列表项仍指向原路径，已不存在）；用户重新加载即可。与 P3-7 不回写一致。属已知边界，不在本批解决 |
| 10 | 目标 `T` 是某个源文件**自身所在目录** | 该文件复制即「复制到自己」→ 按冲突处理（跳过 + 标红），不报错中断 |
| 11 | 空源（列表为空） | 干跑报告直接提示「列表为空，请先添加文件」，不打开执行 |
| 12 | 某个源文件读不出（权限 / 被占用） | 该文件在干跑报告里标记错误，其余照常；不中断整批 |
| 13 | 「完成后打开目录」时 `T` 不可 reveal（如网络盘断连） | 静默吞掉异常，**不影响主结果** |

---

## 5. 我主动加的判断（都说明理由）

**① ★ 干跑报告做成「硬要求」，不是可选项。**
与 P3-7 同理由：搬动文件不可逆，先列清「会复制几个、跳过几个、未匹配几个、错几个」再让用户点确认，是最后一道人眼闸门。也让 §7.3 第 3 行的「预览 ≡ 执行」有落点。

**② ★ 不开「覆盖」档（DEC-28）。**
与 P3-7 同：给「覆盖」勾直接违反第 0 批起的最高铁律。本批只做「跳过 + 标红」，覆盖档留给单独评审（或永不做）。

**③ ★ 本批只新增「筛选」，运输层整体复用 P3-7。**
P3-8 与 P3-7 是孪生批次——都会复制 / 移动文件到目录。区别只在「挑哪些文件」（P3-7 是按四种方式归类全部，P3-8 是按条件挑子集进单一目录）。所以本批**不重写** `merge-plan.ts` / `merge-service.ts` / `fs-walk.ts` 的运输逻辑，只新增一个纯函数 `extract-filter.ts`（算命中集合），再把命中集合喂给 P3-7 的「单一目录」运输路径。这样「绝不覆盖 / 两阶段剪切 / 干跑 ≡ 执行」三道安全网一次写、两批共用，不会漂移。（§7 给出两种落地选法。）

**④ ★ 提取源直接吃「当前列表」，零新增源输入控件。**
P3-7 为独立拖入区写了递归 walker；本批源就是主列表，省掉整套源输入 UI，也和「添加文件 / 导入 Excel」自然接上。代价是剪切后列表项不回写（§4 边界 9 / §12）。

**⑤ ★ 按类型用「类别勾选」而非「手填扩展名」。**
用户勾「图片 / 文档 / …」比手填 `.jpg,.png` 更不易错，且与 P3-7②「按扩展名建文件夹」共用一张扩展名→类别映射表，保证「合并时按扩展名分目录」和「提取时按类型筛」口径一致，不会各说各话。

**⑥ 纯函数 `extract-filter.ts` 放在 `shared/`，与 `merge-plan.ts` 同级。**
筛选逻辑做成纯函数，主进程和（未来的）预览都调它 → 干跑报告和真执行用同一份命中集合，不可能算出两个结果。同时不 import `node:fs`，可被 `node --test` 直接单测（沿用 P3-1 起的「纯逻辑放 shared」约定）。

---

## 6. 与既有坑的关系

| 既有约定 / 坑 | 本批的关系 |
| --- | --- |
| **DEC-01 主进程绝不递归** | **继承**：本批源=主列表，不触发文件夹递归；若日后接入文件夹拖入，复用 `fs-walk.ts` 的不跨 symlink/junction 护栏 |
| **「绝不覆盖」铁律（DEC ①）** | **强化**：冲突默认跳过 + 标红；不开覆盖档（§1.3 / DEC-28） |
| **EX-07 扩展名保护（改名只动主体）** | **复用其拆法**：按类型判定、按名称判定都从 `name-split.ts` 取扩展名 / 主体，不自造拆分 |
| **F9 撤销只管改名** | **明确无关**：本批不做撤销（§1.4），`undoEntries` 不动 |
| **preview ≡ execute（改名 / P3-7）** | **同族原则延伸到提取**：干跑报告与执行共用 `extract-filter.ts` + `merge-plan.ts`（§5③ / §7.3 第 3 行） |
| **preload 强转 → 接口漏声明是编译期静默** | **本批会新加 IPC**，必须在 4 处同步（types / ipc / preload / 渲染调用点），否则 typecheck 仍 0 错、运行才炸（§7.3 第 2 行） |
| **P3-4 导入 Excel 解析器** | **直接复用**：「按 Excel」筛选读文件名列，不另写 Excel 解析（避免第二套 xlsx 读表逻辑，也避开 P3-4 踩过的共享字符串表 / 压缩坑） |
| **P3-7 运输层（merge-plan / merge-service / fs-walk）** | **直接复用**：落点 / 冲突 / 两阶段剪切 / 干跑报告骨架一次写两批共用 |

---

## 7. 改动面

> 落地有两种选法，二选一，本批不强制：
> **选法 A（最省，推荐）**：不新增 IPC 通道，给 `merge:plan` / `merge:run` 加一个 `mode: 'extract'` 字段；`extract:plan` 先跑 `extract-filter.ts` 算命中集合，再走 P3-7 的单一目录 plan；`extract:run` 复用 `merge-service.ts` 执行器。
> **选法 B（最清晰）**：新增 `extract:plan` / `extract:run` 两个通道，内部仍调用 `merge-plan.ts` / `merge-service.ts` 的纯函数与执行器。
> 两种选法都**不新增运输逻辑**、都**破轻量化（IPC）**，区别只在通道名。下面按「选法 B（新增通道）」列文件，选法 A 只需把 IPC 文件换成「改 merge.ipc.ts 加 mode」。

### 7.1 新增文件

| # | 文件 | 职责 |
| --- | --- | --- |
| 1 | `shared/extract-filter.ts`（**纯函数**） | 按类型 / 名称 / Excel 从当前列表算命中集合（AND 叠加）；`node --test` 直接单测 |
| 2 | `main/services/extract-service.ts`（**新增**，选法 B） | 调 `extract-filter` 算命中 → 调 `merge-plan.ts`（单一目录）算 plan → 调 `merge-service.ts` 执行器；或选法 A 下并入 `merge-service.ts` |
| 3 | `main/ipc/extract.ipc.ts`（**新增**，选法 B） | 注册 `extract:plan` / `extract:run` 两个通道；选法 A 下改 `merge.ipc.ts` |
| 4 | `renderer/components/ExtractDialog.vue`（**新增**，SCR-13/14/15） | 独立对话框：源（只读列表计数）/ 筛选（类型+名称+Excel，可叠加）/ 操作 / 目标 / 开关 / 干跑报告 |
| 5 | `renderer/components/Toolbar` 加一个「文件提取」按钮（EL-149） | 入口 |

### 7.2 改动既有文件

| # | 文件 | 改什么 |
| --- | --- | --- |
| 1 | `shared/types.ts` | 加 `ExtractRequest` / `ExtractPlan` / `ExtractResult` 类型（或给 `MergeRequest` 加 `mode`）；`MaoDieAPI` 加 `extractPlan` / `extractRun`（选法 B）/ 或 `mergePlan` 加 mode（选法 A） |
| 2 | `shared/errors.ts` | 加 `EX-21`（Excel 未匹配项 / 解析失败）/ `EX-22`（正则非法） |
| 3 | `preload/api.ts` | 加 `extractPlan` / `extractRun`（**强转易漏**，§7.3 第 2 行）/ 或 `mergePlan` 加 mode |
| 4 | `main/ipc/*` 的注册表 | 登记 `extract.ipc`（选法 B）/ 或改 `merge.ipc` |
| 5 | 渲染层调用点 | `Toolbar` 按钮 → 打开 `ExtractDialog` |
| 6 | **复用不改动** | `shared/merge-plan.ts` / `main/services/merge-service.ts` / `main/services/fs-walk.ts` / P3-4 的 Excel 解析器 —— 本批只调用，不改写其运输逻辑 |

### 7.3 ★ 必须一起改到的地方（漏一处就出静默 bug）

| # | 位置 | 不改会怎样 |
| --- | --- | --- |
| 1 | ★★ **`extract-filter.ts` 与干跑 / 执行共用同一份命中集合** | 若报告用一套筛选、执行另算 → 报告说提取 21 个、执行却 23 个（preview ≡ execute 同族） |
| 2 | ★★ **`MaoDieAPI` 漏声明 `extractPlan` / `extractRun`（或 `mergePlan` 漏加 mode）** | `preload` 强转 → typecheck 0 错、运行时才炸（与 P3-5 / P3-7 同一类形态） |
| 3 | ★★ **干跑报告与执行共用 `merge-plan.ts`** | 若报告一套逻辑、执行另算 → 报告说跳过、执行却覆盖（绝不覆盖铁律被破） |
| 4 | **剪切两阶段顺序** | 边复制边删 → 中途失败「半移动态」，文件既在源又在目标、或两处都丢（复用 P3-7） |
| 5 | **按类型用统一的扩展名→类别映射** | 手填扩展名 / 两套映射 → 合并按扩展名分目录、提取按类型筛，口径对不上 |
| 6 | **按名称正则非法** | 没在计划阶段拦截 → 执行时才炸；必须在 `extract-filter` 计划阶段校验正则语法（EX-22） |
| 7 | **按 Excel 复用 P3-4 解析器** | 自己另写 xlsx 读表 → 重新踩共享字符串表 / 压缩坑（P3-4 已踩过），且两批读表行为不一致 |
| 8 | **Excel 未匹配项处理** | 名单里的文件名列表里没有 → 必须逐条标「未匹配」（EX-21），不能静默丢弃让用户以为都提了 |
| 9 | **冲突检测时机** | 只在执行时检测 → 干跑报告不准；必须在 plan 阶段就检测并列出 |
| 10 | **「完成后打开目录」异常** | 目标不可 reveal（网络盘断连）→ 静默失败；必须 try/catch 且不影响主结果 |
| 11 | **扩展名提取** | 复用 `name-split.ts` 的拆法（不改 ext）；手写拆分会漏掉隐藏文件的点 |
| 12 | **`package.json` 的 `test:core`** | 新单测写了却不跑（P2-C 踩过） |

### 7.4 界面规格

| 项 | 值 |
| --- | --- |
| 对话框 | 复用现有模态对话框令牌（**零新增色值 / 令牌**） |
| 新增令牌 / 颜色 | **0 / 0** |
| 新增 SCR | `SCR-13` 外壳 / `SCR-14` 表单（含筛选）/ `SCR-15` 干跑报告 |
| 新增 EL | `EL-149` 入口按钮（其余 `EL-150~155` 为对话框内元素占位，见 §8） |

---

## 8. 编号清单（已按「前缀全量」重扫核对，排除 `node_modules` / `.git` / `out` / `artifacts` 与总规划）

> 口径：用脚本扫全仓库 `*.md/*.html/*.ts/*.vue/*.js/*.json/*.css`，排除 `node_modules` / `.git` / `out` / `artifacts` 与「范围澄清 / 总规划」类文档（其预留号会污染上限）。
> **已真占用**（截至第 7 批落地，实查代码 + 文档）：`EL` 148 · `IX` 125 · `TC` 87 · `DEC` 26 · `EX` 20 · `ST` 5 · `SCR` 12 · `ADR` 6 · `F` 16 · `R` 7。`MOD-1~6` 已在 `PRD.md` 作功能模块码使用（非批次分配），本批不新分配。
> ⚠️ 扫出的 `EL-900/901` 是**假上限**——`原型图.html` 里的模态外壳 DOM id，非设计编号（技能早警告过）。

| 类别 | 新增 |
| --- | --- |
| 元素 EL | `EL-149` 主工具栏「文件提取」按钮；`EL-150~155` 对话框内元素（外壳 / 表单 / 筛选区 / 报告占位，落地时按实际控件细分） |
| 交互 IX | `IX-126` 打开提取对话框 · `IX-127` 设置筛选条件（类型/名称/Excel，可叠加）· `IX-128` 选复制/剪切 + 目标目录 · `IX-129` 确认干跑报告 → 执行 |
| 验收 TC | `TC-88` ~ `TC-95`（见 §9） |
| 决策 DEC | `DEC-27` 本批范围（三种筛选可叠加 AND / 复制+移动默认复制 / 单一目标目录 / 独立对话框 / 源=当前列表 / 按名称包含+正则开关 / 按Excel复用P3-4 / 不做撤销先干跑）· `DEC-28` 不开「覆盖」档（给铁律开口子，留待单独评审）· `DEC-29` 运输层整体复用 P3-7（只新增筛选器，二选一落地选法） |
| 错误码 EX | `EX-21` 按 Excel 提取未匹配项 / 解析失败 · `EX-22` 按名称正则非法；其余复用 P3-7 的 `EX-19`/`EX-20` 与既有读 / 写 / 冲突码 |
| 界面 SCR | `SCR-13` 对话框外壳 · `SCR-14` 表单（含筛选）· `SCR-15` 干跑报告 |
| IPC 通道 | **新增** `extract:plan` / `extract:run`（破轻量化，已说明；或选法 A 给 `merge:plan/run` 加 `mode`） |
| 持久化字段 | **零新增**（撤销 / 历史记录类型延后，与 P3-7 一致） |

---

## 9. 验收用例

| 编号 | 用例 | 步骤 | 预期 |
| --- | --- | --- | --- |
| **TC-88** | 按类型筛选 + 复制 + 单一目录 | 列表含 `1.jpg` `2.png` `3.docx`，勾「图片」，目标空文件夹，复制 | 目标出现 `1.jpg` `2.png`；`3.docx` 不提取；源仍在；报告「将复制 2 / 跳过 0」 |
| **TC-89** | 按名称包含 + 复制 | 列表含 `旅游1.jpg` `会议2.png` `旅游3.png`，名称含「旅游」 | 提取 `旅游1.jpg` `旅游3.png`，`会议2.png` 不提取 |
| **TC-90** | 按名称 + 正则开关 | 开正则，模式 `^IMG_\d+`，列表含 `IMG_001.jpg` `pic002.png` | 只提取 `IMG_001.jpg` |
| **TC-91** | 按 Excel 筛选（复用 P3-4 解析器） | Excel 列 `1.jpg` `3.docx`，列表含 `1.jpg` `2.png` `3.docx` | 提取 `1.jpg` `3.docx`；`2.png` 不提取 |
| **TC-92** | ★ 多条件叠加 AND | 勾「图片」+ 名称含「旅游」，列表含 `旅游1.jpg`（图/含旅游）`风景2.png`（图/不含）`旅游3.docx`（不含图） | 只提取 `旅游1.jpg` |
| **TC-93** | 剪切模式（默认关） | 单一目录，操作切为剪切，执行 | 目标出现文件、**源被删除**；报告「将移动 N」 |
| **TC-94** | 冲突被兜住（绝不覆盖） | 目标已有同名文件，复制 | 该文件**跳过 + 标红**，报告列为冲突；源与目标都不动 |
| **TC-95** | ★★ **干跑 ≡ 执行** | 跑一次真实提取，把干跑报告的「将复制 / 跳过 / 未匹配」与磁盘真实结果逐条比对 | **逐字节相等**（覆盖「筛选/plan 阶段漏算」这类漏法） |
| 附 | Excel 未匹配项 | Excel 列一个列表里没有的文件名 | 报告逐条标「未匹配」（EX-21），不中断；实际提取数为命中数 |
| 附 | 正则非法即时提示 | 名称开正则，填非法模式如 `[` | 表单红框、拒绝干跑（EX-22） |
| 附 | 筛选结果为空 | 勾一个没有文件命中的条件 | 干跑报告提示「没有符合条件的文件」，不打开执行 |
| 附 | 完成后打开目标目录 | 执行后 | 默认打开目标文件夹；取消勾则不打开 |
| 附 | 半移动态 | 剪切执行中让某个源删不掉 | 记 `EX-20`、报告标红；已复制项**不回滚** |

---

## 10. 测试要求

按项目既定规矩（**不引入第三方测试框架**，单测用 `node --test`，界面用真实 Electron 冒烟）：

| 层 | 要求 |
| --- | --- |
| 单测 · `extract-filter` | 按类型（各类别命中 / 无扩展名只在「其他」）；按名称包含；按名称正则（合法命中 + 非法抛 EX-22）；按 Excel（命中 / 未匹配 EX-21 / 解析失败）；多条件 AND 交集；空结果 |
| 单测 · `merge-plan`（复用） | 单一目录落点 `T\<原名>`；冲突检测（目标已存在同名 → 标记 skip）；目标=源自身目录 → 冲突跳过 |
| 单测 · 错误处理 | 源读不出 → 标记 error；剪切两阶段路径分支（同盘 rename / 跨盘 cp+删） |
| 单测 · 老行为不变 | 改名全套用例**一条都不改**、必须仍全绿；P3-7 合并用例**一条都不改**、必须仍全绿（本批不碰二者引擎） |
| 界面冒烟（真实实例 + 真实点击） | ① 工具栏出现「文件提取」按钮 ② 打开对话框、显示列表计数 ③ 设筛选（类型/名称/Excel，可叠加）④ **点干跑出现报告** ⑤ 确认执行后文件真的在目标处、源按复制/剪切预期 ⑥ 冲突项被标红 ⑦ 完成后目标目录被打开（或没打开，看开关） |
| 交付报告 | 必须写「哪些没验到」。**已知必列**：① **真实磁盘上跨盘剪切**的表现 ② **网络盘 / 不可 reveal 路径**的「完成后打开」异常 ③ 剪切后主列表项指向已移动文件的实际观感 ④ 打包后的 exe |

**红线照旧：绝不为让测试变绿而放宽 / 跳过 / 删断言。挂了就给原始报错。**

---

## 11. 待同步的文档

| 文档 | 需要的同步 | 状态 |
| --- | --- | --- |
| `需求清单.md` | 第 8 批那行细化（三种筛选可叠加 / 复制+剪切 / 单一目录 / 独立对话框 / 源=当前列表）；状态改「设计完成（待评审）」 | ✅ 已同步（2026-09-24） |
| `PRD.md` | 加提取子系统的章节 + 元素 `EL-149` + 验收 `TC-88~95` + 决策 `DEC-27~29` + 错误码 `EX-21~22` | ✅ 已同步（2026-09-24） |
| `交互说明文档.md` | 加「文件提取」对话框（SCR-13/14/15）+ `IX-126~129` + 工具栏按钮 | ✅ 已同步（2026-09-24） |
| `设计规范.md` | 对话框规格（复用现有模态令牌，零新增）；补「筛选可叠加」「完成后打开」等 | ✅ 已同步（2026-09-24） |
| `技术方案文档.md` | 登记 `shared/extract-filter.ts` / `extract-service.ts` / `extract.ipc.ts`（或 `merge.*` 加 mode）；§4 加提取子系统；说明复用 P3-7 运输层 | ✅ 已同步（2026-09-24） |
| `接口文档.md` | 加 `extract:plan` / `extract:run`（或 `merge:plan/run` 的 `mode`）入参 / 返回（字段权威是 `types.ts`） | ✅ 已同步（2026-09-24） |
| `数据库设计.md` | 注明「本批零新增持久化；提取的历史记录类型（为撤销预留）本批不做」，避免后人以为漏了 | ✅ 已同步（2026-09-24） |
| `P3-2-8批范围澄清与交付总规划.md` | §5 第 8 批的「编号用量」按本批实际改写；补「已拍板」结论 | ✅ 已同步（2026-09-24） |

---

## 12. 本批明确不做的事

| 不做 | 归到哪 |
| --- | --- |
| **「覆盖」档** | DEC-28：违反最高铁律，留待单独评审（或永不做） |
| **移动 / 复制的撤销** | §1.4：拍板不做，先干跑；历史记录类型留给以后统一评估 |
| **另起拖入区 / 文件夹递归摊平** | 源=当前列表（拍板第 7 条）；若日后要「从文件夹提取并递归」，复用 P3-7 的 `fs-walk.ts`，不在本批 |
| **目标目录按类型/日期分子目录** | 拍板第 2 条：单一目录；子目录分组是 P3-7 的能力，本批不重复 |
| **剪切后回写 / 清理主列表** | §4 边界 9：已知边界，用户重新加载即可；与 P3-7 不回写一致 |
| **把提取源接进主列表做双向同步** | 本批提取是单向（列表→目标目录）；双向同步是独立小项 |
| **改 `RuleConfig` 的任何字段** | 本批与改名规则无关 |
| **动四个跨层形状 / 改名引擎 / `rename-executor` / P3-7 运输逻辑** | 完全独立子系统，只复用不改动 |

---

## 13. 决策留痕（产品负责人四答 + 沿用）

> 十项结论见文首「★ 已拍板」表。下面保留原问题与他的原话（来自本轮 AskUserQuestion），作为**决策留痕**。

**本轮四答（总规划 §5 分歧点）**
- 一、文件提取时原文件怎么处理（复制 / 移动 / 两个都给）？→ **复制+移动（默认复制）**。
- 二、提取出来的文件放进目标目录怎么组织？→ **单一目录（所有提取的文件进同一个文件夹）**。
- 三、三种筛选条件（按类型 / 按名称 / 按 Excel）能同时用吗？→ **可叠加（AND，如「图片 且 含'旅游'」）**。
- 四、「按名称」怎么判断要不要提取？→ **包含 + 可开正则开关（复用 F10）**。

**沿用 P3-7 的拍板（本批直接继承，不再另问）**
- 形态：独立对话框，不碰主流程。
- 撤销：不做，先干跑再说。
- 目标目录给法：选已有 / 新建二选一。
- 完成后打开目标目录：默认勾上，可取消。
- 覆盖档：不开（沿用铁律）。
- 按 Excel：复用第 4 批解析器（总规划 §5 已定）。

**本批新增的独立判断（非问询，设计侧定）**
- 提取源 = 当前已加载文件列表（零新增源输入控件），区别于 P3-7 的独立拖入区。
- 运输层整体复用 P3-7（`merge-plan.ts` / `merge-service.ts` / `fs-walk.ts`），本批只新增 `extract-filter.ts` 筛选器 + 二选一落地选法（DEC-29）。
