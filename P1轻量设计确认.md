# P1 轻量设计确认（F-10 正则表达式 / F-11 大小写转换）

> 产品名：**耄耋改名（MaoDie Renamer）**
> 文档版本：**v1.0**
> 日期：2026-09-15
> 上游文档：`PRD.md` v1.1 · `交互说明文档.md` v1.1 · `设计规范.md` v1.0 · `技术方案文档.md` v1.0.1
> 配套交付：设计稿画布新增画板 **「P1 增量设计 · 规则区进阶」**
> 文档状态：**待评审**（评审通过后再进开发）

---

## 0. 结论摘要

P1 只做两件事，**都不需要改规则引擎的结构**：

| 功能 | 做法 | 是否动引擎结构 |
| --- | --- | --- |
| **F-10 正则表达式** | 作为「删除 / 替换」模式的一个**匹配开关** | 否 |
| **F-11 大小写转换** | 作为新名算完后的一个**收尾步骤** | 否 |

两者都藏在一个默认折起的「**进阶设置**」折叠条里。**小白的心智与视觉与 P0 完全一致** —— 不新增页签、不重排现有布局、不新增任何颜色。

> ⚠️ **与既有文档的冲突，以本文档为准**：`技术方案文档.md` §11 第 6 条写的是「P1 需重构 `computeNewStem` 为规则数组顺序执行，DEC-02 需重新评审」。**本设计不需要重构，DEC-02 不变。** 详见 §13 待同步清单。

---

## 1. 范围

### 1.1 做什么

- **F-10**：在「删除字符 / 替换字符」里支持**正则表达式**匹配，替换串支持 `$1` `$2` 捕获组。
- **F-11**：**大小写转换** —— 保持原样 / 全部小写 / 全部大写 / 首字母大写。

### 1.2 明确不做

| 不做 | 原因 |
| --- | --- |
| 多规则链式组合 | 属于 P2，且与「轻量化」冲突（要重做成可增删排序的规则列表） |
| 规则模板库（F-12） | P2 |
| 正则作用于「规则化」模式 | 该模式不涉及匹配 |
| 深色模式下的适配（F-14） | P2；本轮只出浅色 |

---

## 2. 结构变化：只加一行

现有规则区（R-05）自上而下：`[三页签] → [参数区] → [区分大小写] → [自动处理重名冲突]`。

新增的**唯一**结构：在**参数区**与**自动处理重名冲突**之间插入一行 **「⚙ 进阶设置」折叠条**，**默认折起**。

```
┌─ R-05 规则设置区 ────────────────────────────────┐
│ (删除字符) (替换字符) (规则化)                    │  ← 三页签，不变
│ 待删字符串                                        │
│ [ 广告                          ]                 │  ← 参数区，不变
│ ⚙ 进阶设置                           已启用 2 项 ▾ │  ← ★ 新增（折起态）
│ ─────────────────────────────────────────────    │
│ ◯ 自动处理重名冲突                                │  ← 不变
│ 关闭时冲突项会被跳过。…                           │
└──────────────────────────────────────────────────┘
```

### 2.1 两种态

| 态 | 表现 |
| --- | --- |
| **折起（默认）** | 一行小字：齿轮图标（14px `#8A8178`）+「进阶设置」+ 右侧箭头（12px）。与 P0 视觉完全一致，只是多了一行 |
| **有启用项（仍折起）** | 图标与文字变 `#E08B33`，右侧出现胶囊徽标「已启用 N 项」（底 `#FFF3E2` / 字 `#E08B33` / 圆角 6px / 内边距 3×8px）。**折起来也知道自己开了东西** |
| **展开** | 下方出现浅橘块：底 `#FFF6E9`、圆角 10px、内边距 12px、行间距 10px —— 与现有「参数组」样式**完全相同** |

### 2.2 为什么放折叠区（而不是第四个页签）

1. **满足 PRD §3 的硬要求**：F-10「需在 UI 上做进阶用户的隔离设计」。第四个页签会让小白看到「正则」两个字，隔离失败。
2. **页签仍是三选一**，DEC-02 不动，P0 的交互文档不用推翻。
3. **给 P2 留了唯一且正当的挂载点**：将来的「多规则链」挂进这个折叠区，不用再改小白的界面。
   > ⚠️ **2026-09-18 更正**：这句原文还写了「**模板库**」，**对模板库是错的** —— 折叠区是给**进阶用户**做隔离用的，而模板库是给**不会配规则的人**的一键入口，藏进来正好把目的反过来。P2-B 实际把模板条放在**规则区顶部、页签之前常显**（见 `P2-B轻量设计确认.md` v1.0 §2.1 / DEC-14）。**「多规则链」留折叠区这个判断仍然成立，未变。**

---

## 3. F-10 正则匹配

### 3.1 控件

| 元素 | 说明 |
| --- | --- |
| 开关（EL-057） | 「用正则匹配」，默认**关**。仅出现在**删除 / 替换**模式 |
| 说明（**EL-104**） | **v1.1 改**：原「开启后，『删除字符 / 替换字符』里的内容按正则表达式解释」属**用术语解释术语** —— 没接触过的人看完仍不知道要填什么。现改为**两行大白话对照**：不用正则 = 你填什么就找一模一样的字（换个日期就找不着了）；用正则 = 你说「长什么样」，数字换了也认得出 |
| 照抄表（**EL-104**，点 **EL-103** 展开） | **v1.1 新增**：「我想做的事 → 查找填 → 替换填 → 举例结果」，**按当前模式过滤**（删除模式 3 行 / 替换模式 4 行；含 `$1` 的行只在替换模式出现）。每行写的举例结果由 `rule-engine` 真算校验（单测钉住）|
| 当场演示（**EL-105**） | **v1.1 新增**：固定示例名 `发票 2026-08-01.pdf`（不取列表里的真实文件 —— 教学示例必须保证看得出变化），把当前填写的内容套上去**实时算出**新名。匹配不上时用灰字如实显示「没变」并提示原因 |
| ~~语法提示 / 捕获组提示~~ | **v1.1 废弃**：原两行小字（`语法：. 任意字符 · \d 数字 · …` 与 `替换串里可用 $1 $2 引用捕获组`）已删除 —— 那是给**已经会正则的人**查的符号表，对新手是负担。`( )` 与 `$1` 的人话解释（「先圈出来存着」/「把第 1 段搬过来」）改放到照抄表的收尾一行 |

**开启后**，参数区里的「待删字符串 / 查找 / 替换为」三个输入框的内容按正则解释，输入框字号切到 `Inter`（技术信息字体）。

### 3.2 行为

| 场景 | 行为 |
| --- | --- |
| 正则关闭 | 与 P0 **逐字节一致**（纯字符串包含匹配） |
| 正则开启 + pattern 为空 | 规则不生效（与 P0 的空规则一致） |
| 正则开启 + `替换为` 留空 | 等价于「按正则删除」 |
| 正则开启 + 捕获组 | `$1` `$2` … 引用捕获组；`$0` 为整体匹配 |
| 正则开启 + 无捕获组却写了 `$1` | 展开为空串（不报错） |

### 3.3 正则非法

| 项 | 规格 |
| --- | --- |
| 触发 | pattern 无法编译（如输入 `(`、`[a-`、`*abc`） |
| 输入框 | 描边 2px `#E5544B`（复用「冲突 / 失败」语义色，**不新增色**） |
| 提示文案 | 输入框下方一行红字，如「正则表达式不完整：缺少右括号 `)`」。文案由运行时错误映射，不做穷举硬编码 |
| 状态栏 | 追加「· 正则表达式有误」 |
| 开始改名 | **置灰**（复用 P0 已有的置灰机制 EX-11 / EX-12，不新增交互） |
| 预览列表 | 保留上一次的合法结果，不清空、不整屏标红（避免闪动） |

---

## 4. F-11 大小写转换

### 4.1 控件

一个下拉（EL-059），**三种模式都出现**（包括规则化）：

| 选项 | 值 | 行为 |
| --- | --- | --- |
| 保持原样（默认） | `none` | 不做任何转换，与 P0 一致 |
| 全部小写 | `lower` | 主体所有字符转小写 |
| 全部大写 | `upper` | 主体所有字符转大写 |
| 首字母大写 | `capitalize` | **只把主体的第一个字符转大写，其余字符保持原样** |

> **「首字母大写」为什么不做全词归一**：`meetingNotes` 若强制「首字母大写 + 其余小写」会变成 `Meetingnotes`，把驼峰吃掉了。只动首字符是**非破坏性**的，结果在意料之中。中文首字符无大小写概念，等于不生效。

### 4.2 作用范围（关键）

- **只作用于「新名主体」，扩展名永远不动。**
- `IMG_0001.JPG` 主体是 `IMG_0001` → 全部小写后为 `img_0001`，扩展名仍是 `.JPG`。
- 「我的.备份.tar.gz」主体是「我的.备份.tar」（`.gz` 是扩展名）→ `.tar` 会跟着变 —— 与 P0 的拆分规则一致，不是 bug。

---

## 5. 执行顺序（唯一权威）

```
新名主体 = 大小写转换( 模式结果 )

  模式结果 = 删除模式：删掉 pattern
           = 替换模式：pattern → 替换串
           = 规则化模式：前缀 + 序号 + 日期 + 原主体 + 后缀
```

即：**先按模式算出新主体，最后整体做大小写**。

这样「删掉广告后统一小写」一条规则就能通吃；也避免了「大小写算完又被前缀拼接打乱」这种顺序歧义。

---

## 6. 不破坏 P0 的红线（5 条，全部要有测试卡住）

1. **页签仍是三选一**，DEC-02 不动。
2. **正则关闭时，输出与 P0 逐字节一致** —— 这是回归基线，必须有测试。
3. **大小写选「保持原样」时，输出与 P0 逐字节一致。**
4. **大小写只作用于主体，扩展名永远不动**（EX-07 的延伸，不能破）。
5. **正则开关在「规则化」模式不出现** —— 与现有「区分大小写」沿用同一条规则（该模式不涉及匹配）。

---

## 7. 边界与异常

| 编号 | 场景 | 处理 |
| --- | --- | --- |
| **EX-15** | 正则表达式非法 | 见 §3.3：红描边 + 红字原因 + 状态栏提示 + 开始改名置灰 |
| **EX-16** | 正则过于复杂（灾难性回溯，如 `(a+)+$`） | 预览 Worker **每处理若干项检查一次耗时，累计超 3 秒即中止**，提示「正则过于复杂，请简化」；同时限制 pattern 长度 **≤ 200 字符** |
| 复用 EX-05 / EX-06 | 大小写转换后两项撞名 | **不新增机制**，直接走 P0 既有的重名冲突流程（F-16 / SCR-04），磁盘文件无损失 |
| 复用既有 | 特殊字符：`ß` 转大写变 `SS`（字符数改变） | 同上，并入冲突流程 |
| — | 正则 + 大小写同时开启 | 先正则算完，再整体大小写（见 §5） |

> **为什么 ReDoS 要单独兜底**：JS 正则无法中断，「限制长度」挡不住所有回溯爆炸。**在 Worker 里按项检查耗时并主动中止**是唯一可落地且不卡界面的做法。

---

## 8. 历史页要跟着改

`ruleSummary`（`src/shared/rule-summary.ts`）必须能描述这两项，否则历史卡片看不出当时用了什么规则：

| 规则 | 摘要文案 |
| --- | --- |
| 删除 + 正则 | `删除「\d+」（正则）` |
| 替换 + 正则 | `替换「(\d{4})-(\d{2})」→「$2/$1」（正则）` |
| 任意模式 + 大小写 | 末尾追加 ` + 全部小写` / ` + 全部大写` / ` + 首字母大写` |

> 注意：`ruleSummary` 是**持久化字段**（写进 `history.json`）。改文案只影响新记录，老记录保持原样即可，不需要迁移。

---

## 9. 编号清单（需同步进 PRD / 交互说明文档）

| 类别 | 新增 |
| --- | --- |
| 元素 EL | `EL-056` 进阶折叠条 · `EL-057` 正则匹配开关 · `EL-058` 正则错误提示（v1.1 收窄）· `EL-059` 大小写转换下拉 · **v1.1 新增** `EL-103` 「? 看不懂？」按钮 / `EL-104` 小白化说明 / `EL-105` 当场演示 |
| 交互 IX | `IX-051` 展开 / 收起进阶设置 · `IX-052` 切换正则匹配 · `IX-053` 正则非法的处理 · `IX-054` 切换大小写转换 |
| 异常 EX | `EX-15` 正则表达式非法 · `EX-16` 正则过于复杂 |
| 验收 TC | `TC-21` ~ `TC-26`（见 §11）<br>⚠️ **编号修正**：本稿原写 `TC-19 ~ TC-24`，但 `TC-19` / `TC-20` 已被 PRD v1.1.2 的「添加文件夹多选 / 记住上次位置」占用，为保持三文档编号唯一，P1 顺延为 `TC-21 ~ TC-26` |
| 决策 DEC | `DEC-10` P1 范围与「进阶隔离」方案定稿 |

---

## 10. 字段与改动面

### 10.1 字段（`src/shared/types.ts` 的 `RuleConfig`）

```ts
interface RuleConfig {
  // …现有字段不变…

  /** F-10：正则匹配。默认 false（关闭时行为与 P0 逐字节一致） */
  regexEnabled: boolean

  /** F-11：大小写转换。默认 'none' */
  caseTransform: 'none' | 'lower' | 'upper' | 'capitalize'
}
```

**折叠条的展开 / 收起是纯 UI 状态，不进 `RuleConfig`、不进 IPC 契约**（引擎不需要知道它）。建议放在渲染层的 rule store 里作为独立 UI 状态，或在组件内用局部 ref。

### 10.2 改动面（4 处，缺一不可）

| # | 位置 | 改什么 |
| --- | --- | --- |
| 1 | `src/shared/types.ts` | 加两个字段 + `DEFAULT_RULE` 默认值 |
| 2 | `src/shared/rule-engine.ts` | ① `spliceAll` 支持正则分支；② `computeNewStem` 外面套一层 `caseTransform`；③ 新增 `compileRegex(pattern)` 供校验复用 |
| 3 | `src/shared/rule-summary.ts` | 摘要文案加「（正则）」与「 + 全部小写」 |
| 4 | `src/renderer/src/components/RulePanel.vue` | 折叠条 + 展开块；正则错误态；大小写下拉 |

> `src/renderer/src/stores/rule.ts` 不需要新增方法 —— 现有的 `patch` / `patchInner` 已经能覆盖这两个字段。
>
> **注意 ADR-001 的约束**：引擎必须在 `src/shared/`，预览 Worker 与主进程执行器**共用同一份源码**。正则与大小写都必须写在 `rule-engine.ts` 里，**不能**只写在渲染层。

---

## 11. 验收用例

> ⚠️ **编号修正（2026-09-15）**：下表原编为 `TC-19 ~ TC-24`，但 `TC-19` / `TC-20` 已分别被 PRD v1.1.2 的「添加文件夹多选」「记住上次位置」占用。为避免三文档编号冲突，**P1 顺延为 `TC-21 ~ TC-26`**（PRD §10 已按此登记）。

| 编号 | 用例 | 步骤 | 预期 |
| --- | --- | --- | --- |
| **TC-21** | 正则删除 | 删除模式 + 正则开，pattern `\d+`，文件 `abc123.txt` | 预览新名 `abc.txt`，变化片段高亮 |
| **TC-22** | 正则捕获组替换 | 替换模式 + 正则开，查 `(\d{4})-(\d{2})` → 替换 `$2/$1`，文件 `2026-09.txt` | `09/2026.txt` |
| **TC-23** | 正则非法 | 输入 `(` | 输入框 2px `#E5544B` 红描边 + 红字原因 + 状态栏「正则表达式有误」+「开始改名」置灰；**不崩溃** |
| **TC-24** | 大小写不改扩展名 | 大小写 = 全部小写，文件 `IMG_0001.JPG` | 预览新名 `img_0001.JPG`（**扩展名保持大写**） |
| **TC-25** | 大小写导致撞名 | 两个文件转小写后同名 | 走 SCR-04 冲突弹窗，默认「先不改」，**磁盘文件无损失** |
| **TC-26** | **P0 回归** | 正则关 + 大小写「保持原样」，跑既有 P0 用例集 | 输出与 P0 **逐字节一致**；1000 项预览 < 200ms、执行 < 10s 不退化 |

---

## 12. 测试要求

按项目既定规矩（不引入第三方测试框架）：

| 层 | 要求 |
| --- | --- |
| 共享层单测（`node --test`） | 正则：合法 / 非法 / 捕获组 / 空 pattern / `$1` 越界 / 特殊字符；大小写：四种取值 × 扩展名保护 × `ß` 边界 × 中文首字符；**回归：正则关 + none 时输出与 P0 逐字节一致** |
| 界面冒烟（真实实例 + 真实输入事件） | 点开折叠条 → 开正则 → 输入非法正则看红描边与按钮置灰 → 切换大小写看预览变化；断言取真实计算值，不 mock |
| 交付报告 | 必须写「哪些没验到」（如打包后的 exe、真实大目录性能） |

**红线：绝不为让测试变绿而放宽 / 跳过 / 删断言。** 挂了就给原始报错。

---

## 13. 待同步的文档（改完 PRD 一致性才算过）

| 文档 | 需要的同步 | 状态 |
| --- | --- | --- |
| `技术方案文档.md` §11 第 6 条 | **改成**：P1 只做 F-10 / F-11，均为正交选项，**不需要**重构 `computeNewStem`，**DEC-02 不变**；把「多规则链」明确归到 P2 | ✅ **已改**（v1.0.2；并新增遗留项 10 记录 EX-16 ReDoS 中止未落地） |
| `需求清单.md` P1 段 | F10 补「隔离方案 = 默认折起的『进阶设置』折叠条」；F11 补「大小写只作用于主体，扩展名不动」 | ✅ **已改**（v0.3.2；并在 P2 段补「多规则链式组合」） |
| `PRD.md` §3 / §11.3 | 补 F-10 的隔离方案结论、F-11 的四种取值；§11.3 把「多规则链」从 P1 明确移到 P2 | ✅ **已改**（v1.1.3；新增 DEC-10、§6.1 字段、TC-21~26） |
| `交互说明文档.md` §2.3 / §2.4 | 补 EL-056 ~ EL-059、IX-051 ~ IX-054、EX-15 / EX-16、§10 异常表 | ✅ **已改**（v1.2；并补 EL-060 状态矩阵「正则非法 → 置灰」行） |
| `设计规范.md` | 组件库补「折叠条」与「正则错误态输入框」；§6 补大小写规格 | ✅ **已改**（v1.2；落在 §4.2 输入类 + 新增 §4.5 折叠条 + §9.1 补 `icon-进阶设置.svg` + 画板 13。注：大小写是「选择类」，规格复用 §4.2 下拉框，故未写进 §6「耄耋形象」） |
| `交互说明文档.md` / `设计规范.md`（**v1.3 追补**） | **正则说明小白化**：新增 EL-103 ~ EL-105 与 IX-093 ~ IX-094；设计规范新增 §4.6；EL-058 收窄为「只保留非法时的红字原因」。编号说明：`EL-060/061` 与 `IX-060/061` 已被动作区（开始改名 / 撤销）占用，故新元素顺延至 **EL-103 ~ EL-105 / IX-093 ~ IX-094** | ✅ **已改**（交互说明 v1.3 / 设计规范 v1.3，2026-09-15，见 §15）|
| **验收编号冲突修正** | `TC-19` / `TC-20` 已被 PRD v1.1.2 占用 → P1 顺延为 `TC-21 ~ TC-26` | ✅ **已同步**（本稿 §9 / §11 与 PRD §10 已一致） |

---

## 14. 需要你（产品负责人）确认的事

1. **「进阶设置」这个名字可以吗？** 备选：「高级设置」「进阶选项」「⚙ 更多规则」。
2. **大小写要不要对小白常显？** 现在它藏在折叠区里。如果你觉得「统一小写」是小白也会想要的能力，可以把它挪到参数区下方常显（一行的成本）。
3. **「首字母大写」的定义**：本设计选「只动首字符、其余保持原样」。如果你想要的是「每个单词首字母大写」（`my file name` → `My File Name`），实现与文案都要改 —— 请明确。
4. **正则的 pattern 长度上限 200 字符**是否可接受（纯粹是防回溯爆炸的兜底）。

> **评审结论（2026-09-15）**：产品负责人确认「按计划开工」，上述 4 项**均按本文档的建议默认值落地**：
> ① 名称取「**进阶设置**」；② 大小写**按本稿方案藏在折叠区**（未挪到常显）；③ 「首字母大写」= **只动首字符、其余保持原样**；④ pattern 长度上限 **200 字符**。
> 若后续要改「大小写常显」或「每个单词首字母大写」，需回到本文档改规格并同步 PRD / 交互说明。

> **落地状态（2026-09-15）**：代码已落地（`src/shared/rule-engine.ts` + `RuleConfig` 两个新字段 + `RulePanel.vue` 折叠区），单测 `tests/p1-rules.test.ts` **18/18**、真实 Electron 冒烟 **21/21**。**EX-16 的 Worker 计时中止未实现**（只做了长度 ≤ 200 兜底），已登记在 `技术方案文档.md` §11 遗留项 10。

---


---

## 15. 变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v1.0 | 2026-09-15 | 首次成稿并评审通过（§14 的 4 项待确认事项全部按建议默认值落地） |
| **v1.1** | 2026-09-15 | **正则说明小白化**（落地后收到反馈：「开启正则后看不懂」）：① 废弃「按正则表达式解释」这一循环定义与两行符号表；② 新增 **EL-103**（「? 看不懂？」按钮）/ **EL-104**（大白话对照 + 按模式过滤的照抄表）/ **EL-105**（固定示例名的当场演示）；③ **EL-058 收窄**为「只保留非法时的红字原因」；④ 同步《交互说明文档.md》**v1.3**（EL-103 ~ EL-105 / IX-093 ~ IX-094）与《设计规范.md》**v1.3**（§4.6）。**P1 范围、5 条红线、DEC-02、DEC-10 全部未变；页签仍是三选一** |
**P1 轻量设计确认 v1.1 · 2026-09-15**
