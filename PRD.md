# 产品需求文档（PRD）

> 产品名：**耄耋改名（MaoDie Renamer）**
> 文档版本：**v1.1.8**
> 日期：2026-09-18
> 作者：独立开发者（AI 辅助）
> 上游文档：《立项书》v0.3、《需求清单》**v0.3.5**、《PRD 初稿》v0.2
> 配套文档：《原型图.html》、《交互说明文档》v1.7、**`P1轻量设计确认.md` v1.0（P1 唯一权威）**、**`P2-A轻量设计确认.md` v1.3**、**`P2-C轻量设计确认.md` v1.4**、**`P3-1编号系统升级轻量设计确认.md` v1.1（P3 第 1 批唯一权威）**
> 本文档状态：**可交付开发**（所有 P0 需求均有明确行为定义与验收用例）

---

## 0. 阅读指南

### 0.1 本文档与其它文档的关系

| 文档 | 作用 | 状态 |
| --- | --- | --- |
| 立项书 | 为什么做、值不值得做 | v0.3 ✅ |
| 需求清单 | 要做什么（功能粗颗粒） | v0.3 ✅ |
| PRD 初稿 | 产品整体设想 | v0.2（**已被本文档取代**） |
| **PRD（本文档）** | **怎么做才叫做完**（功能细颗粒） | **v1.1.1 ✅** |
| 原型图.html | 长什么样、怎么点 | v1.1 ✅ |
| 交互说明文档 | 点了会怎样（逐条规则） | v1.1 ✅ |

### 0.2 统一编号体系（三份文档共用）

> 开发时「PRD 的功能编号 ↔ 原型的界面编号 ↔ 交互文档的元素编号」可逐一对应，不再靠文字描述对齐。

| 前缀 | 含义 | 示例 |
| --- | --- | --- |
| `F-xx` | 功能需求编号（同需求清单） | F-03 删除指定字符 |
| `MOD-x` | 功能模块 | MOD-2 规则引擎 |
| `SCR-xx` | 界面（Screen） | SCR-01 主窗口 |
| `R-xx` | 界面区域（Region） | R-04 文件列表区 |
| `EL-xxx` | 界面元素（Element） | EL-060 开始改名按钮 |
| `IX-xxx` | 交互规则（Interaction） | IX-021 拖拽文件入列 |
| `ST-xx` | 耄耋形象状态（State） | ST-03 改名中 |
| `EX-xxx` | 异常与边界（Exception） | EX-05 批次内重名 |
| `TC-xxx` | 验收用例（Test Case） | TC-07 冲突不覆盖 |
| `DEC-xx` | 定稿决策（Decision） | DEC-04 拖拽提为 P0 |
| `MS-x` | 里程碑 | MS-1 引擎先行 |

### 0.3 优先级定义

| 级别 | 含义 |
| --- | --- |
| **P0** | 第一版（MVP）必须完成，缺一不可发布 |
| **P1** | 重要，第二版 |
| **P2** | 增强，后续迭代 |

---

## 1. 产品概述

### 1.1 一句话定位

一只**圆头炸毛的耄耋（橘猫）**，帮你把文件和文件夹的名字批量改好。

### 1.2 产品形态

| 项 | 内容 |
| --- | --- |
| 平台 | Windows 10 / 11（x64）桌面软件 |
| 安装形态 | 双击即用的安装包（exe），无需配置 |
| 网络 | **完全本地离线**，无任何网络请求 |
| 收费 | 完全免费、无广告、无账号 |
| 技术栈 | Electron + Vue3（首版），预留 Tauri 迁移 |
| 产品灵魂 | 耄耋形象状态机（见 §4.6） |

### 1.3 核心价值

| 用户痛点 | 我们的解法 | 对应功能 |
| --- | --- | --- |
| 改名软件要钱 / 要 VIP | 完全免费、无广告 | — |
| 界面陈旧、难用 | 可爱形象 + 极简交互，小白也能用 | F-06 |
| 批量改名怕改错、怕覆盖文件 | 实时预览 + 一键撤销 + 冲突永不覆盖 | F-08 / F-09 / **F-16** |
| 下载的文件名一堆广告、乱码 | 一键删除 / 替换指定字符 | F-03 / F-04 |
| 手工一个个改太累 | 拖入即改，1000 文件 10 秒内完成 | F-01 / F-07 |

### 1.4 Slogan（备选）

- 「耄耋帮你把名字改好。」
- 「文件名字太乱？交给耄耋。」
- 「炸毛，但不炸你的文件名。」

### 1.5 成功指标

| 维度 | 指标 | 目标 |
| --- | --- | --- |
| 可用性 | 一次改名成功率（非冲突场景） | 100% |
| 性能 | 1000 个文件改名耗时 | < 10 秒 |
| 安全 | 因本软件导致的文件覆盖 / 丢失事件 | **0 起**（硬指标） |
| 传播 | GitHub Star / 下载量 | 中期目标，非 MVP 验收项 |

---

## 2. 用户与场景

### 2.1 目标用户

| 类型 | 描述 | 典型场景 | 占比预期 |
| --- | --- | --- | --- |
| 学生党 | 整理课件、作业、网盘资源 | 批量删除下载文件名里的广告前缀 / 后缀 | 高 |
| 办公族 | 整理合同、报表、发票照片 | 统一加日期前缀 + 序号 | 高 |
| 资源收集者 | 整理图片、音乐、视频素材库 | 批量替换乱码、去广告字样 | 中 |
| 追梗年轻用户 | 被可爱形象吸引「图一乐」 | 单纯觉得猫可爱，顺便改个名 | 中（传播主力） |

### 2.2 用户故事（验收视角）

**US-1｜小明（大学生）**
> 作为学生，我从网盘下载了 30 份课件，文件名都带「【某某公众号】」前缀。我希望能**一次把它们全部拖进窗口**，设置「删除 `【某某公众号】`」，**看到预览确认没错**后一键改完。

验收要点：拖拽入列（F-07）→ 删除字符（F-03）→ 实时预览（F-08）→ 执行（F-01）。

**US-2｜小美（行政）**
> 作为行政，我要把 200 张发票照片统一改成「2026-09-发票-001.jpg」。我希望能**用「前缀 + 日期 + 序号」一条规则搞定**，并且**扩展名不要被改坏**。

验收要点：规则化（F-05）→ 扩展名保护（EX-07）→ 序号补零。

**US-3｜阿呆（手滑用户）**
> 我改错了，把不该删的字符删了。我希望**能一键改回去**，而且是**上次、上上次都能撤**。

验收要点：撤销（F-09）→ 历史 20 条。

**US-4｜小雷（谨慎用户）**
> 我要改的名字里，有两个改完会撞在一起（都变成 `报告.docx`）。我**绝对不希望它默默覆盖掉我的文件**。

验收要点：冲突检测（F-16）→ 默认跳过 + 标红。

### 2.3 核心使用场景频次

| 场景 | 频率 | 涉及功能 |
| --- | --- | --- |
| 去除文件名广告 / 乱码 | 高 | F-03 |
| 批量替换错误字词 | 高 | F-04 |
| 统一加序号 / 日期 / 前缀 | 高 | F-05 |
| 整理文件夹名称 | 中 | F-02 |
| 改错了要回退 | 中 | F-09 |

---

## 3. 功能范围与优先级

### 3.1 功能总表

| 编号 | 功能 | 优先级 | 所属模块 | 原型对应 |
| --- | --- | --- | --- | --- |
| F-01 | 批量重命名文件 | P0 | MOD-4 | SCR-01 |
| F-02 | 批量重命名文件夹 | P0 | MOD-4 | SCR-01 |
| F-03 | 删除指定字符 | P0 | MOD-2 | SCR-01 / R-05 |
| F-04 | 替换指定字符 | P0 | MOD-2 | SCR-01 / R-05 |
| F-05 | 按规则重命名（序号 / 日期 / 前后缀） | P0 | MOD-2 | SCR-01 / R-05 |
| F-06 | 可爱互动界面（耄耋状态机） | P0 | MOD-6 | SCR-01 / R-02 |
| F-07 | 拖拽文件进窗口 | **P0**（v0.3 上调） | MOD-1 | SCR-01 / R-03 |
| F-08 | 实时预览改名结果 | P0 | MOD-3 | SCR-01 / R-04 |
| F-09 | 一键撤销（含历史 20 条） | P0 | MOD-5 | SCR-02 |
| F-16 | 重名冲突处理 | **P0**（v0.3 新增） | MOD-4 | SCR-04 |
| F-10 | 正则表达式高级规则 | P1 | MOD-2 | SCR-01 / R-05（「⚙ 进阶设置」折叠区） |
| F-11 | 大小写转换 | P1 | MOD-2 | SCR-01 / R-05（「⚙ 进阶设置」折叠区） |
| F-12 | 改名规则模板库 | P2 | MOD-2 | **常显在 SCR-01 规则区（R-05）顶部、页签之前**（P2-B 已落地，DEC-14）：一行「常用规则」+ 6 个 chip（加日期前缀 / 补零编号 / 日期+编号 / 去掉括号 / 空格换下划线 / 全部小写），点一下**整份替换** `RuleConfig`。<br>**刻意不放进**「⚙ 进阶设置」折叠区 —— 折叠区是给进阶用户做隔离的（DEC-10），模板库是给**不会配规则的人**的一键入口，藏进去正好把目的反过来。**不做「当前模板」高亮**；不做用户自定义。详见 `P2-B轻量设计确认.md` v1.0 |
| F-13 | 历史记录（完整版） | P2 | MOD-5 | SCR-02（P0 已含简版）<br>**P2-C 已设计**（DEC-12）：完整明细（卡片内就地展开，只渲染前 100 条）+ 清空历史入口（footer 幽灵危险按钮，**含数据安全阀**） |
| F-14 | 深色 / 浅色主题 | P2 | — | **SCR-07 设置弹窗**（P2-A 已设计，DEC-11）：**暖褐色深底**（非冷灰黑），跟随系统 / 始终浅色 / 始终深色，**默认「始终浅色」** |
| F-15 | 命令行模式 | P2 | — | 入口在 **SCR-07 设置弹窗**里一行「命令行」（P2-C 已设计，DEC-12）：可复制示例命令 + 「复制程序路径」。**`--yes` 才真改**，不传只打印将要做的改动。<br>**P2-C 增量**（DEC-13）：命令行入口加了小白化说明 + **SCR-08 三步图文教程**（由设置里的「查看教程」按钮进入）。不做一键生成 `.bat` |
| （无编号） | **多规则链式组合**（2026-09-15 由 P1 移入 P2） | P2 | MOD-2 | — |

> **说明**：F-11 原「扩展名保护」部分已在 v0.3 提前到 P0（作为默认行为，见 EX-07），P1 仅保留「大小写转换」。

> **P1 范围与隔离方案定稿（2026-09-15，见 `P1轻量设计确认.md` v1.0 / DEC-10）**：P1 **只做 F-10 + F-11**，两者都是**正交选项**，不改规则引擎结构、不新增页签、不重排布局、不新增颜色。
> - **F-10 的「进阶用户隔离」怎么落**：**不做**第四个页签（那样小白会直接看到「正则」二字，隔离失败）。改为在规则区参数区与「自动处理重名冲突」之间插一行**默认折起的「⚙ 进阶设置」折叠条**；正则开关**只在「删除字符 / 替换字符」模式出现**（与既有「区分大小写」同一条规则）。小白不点开就完全看不到。
> - **F-11 的四种取值**：保持原样（默认）/ 全部小写 / 全部大写 / 首字母大写。**三种模式都出现**（含规则化）。「首字母大写」= 只把主体的第一个字符转大写、其余保持原样（非破坏性，不吃掉驼峰 `meetingNotes`）。
> - **作用范围与顺序**：大小写**只作用于「新名主体」，扩展名永远不动**；且**先按模式算出主体，最后整体做大小写**。
> - **多规则链式组合**（把多条规则按顺序叠加）**明确归 P2** —— 它才需要把 `computeNewStem` 重构为可增删排序的规则数组，届时 **DEC-02 需重新评审**；P1 落地后 **DEC-02 不变**。

### 3.2 MVP 范围边界（明确不做）

| 不做 | 原因 |
| --- | --- |
| 文件格式转换 | 那是 FlyingMouse 的事，本产品只做改名 |
| Mac / 手机版 | 首版聚焦 Windows 桌面 |
| 云端同步 / 账号系统 | 违背「本地离线」定位，且增加复杂度 |
| 收费 / VIP / 广告 | 违背「免费可爱」定位 |
| 撤销「系统级回收站还原」 | 本产品的「撤销」是改回名字，不涉及删除文件；**不提供任何删除文件功能** |

---

## 4. 功能详规

### 4.1 MOD-1 文件输入模块（F-01 输入侧 / F-07）

#### 4.1.1 输入方式（三种，全部 P0）

| 方式 | 元素 | 行为 | 交互编号 |
| --- | --- | --- | --- |
| 按钮添加文件 | EL-020 | 弹出系统多选文件框（SCR-06），可多选、可跨文件夹 | IX-020 |
| 按钮添加文件夹 | EL-021 | 弹出系统文件夹选择框（**可一次多选多个**），**选中文件夹即加入其本身，不递归其内部文件** | IX-021 |
| 拖拽入窗口 | EL-010（全窗口可落） | 拖入文件 / 文件夹到窗口任意位置 | IX-022 |

> **DEC-01（定稿决策）**：添加文件夹默认**只加文件夹本身**，不递归展开内部文件。
> 理由：递归展开会让「1000 个文件」瞬间变成上万条，预览列表失控，且用户真正想批量改的通常是「一批同级文件」。若用户需要文件夹内的文件，可直接进文件夹全选拖入。
> 后续可在 P1 增加「包含子文件夹内容」开关。
>
> **2026-09-14 补充（v1.1.2）**：对话框已支持**一次多选多个文件夹**（`multiSelections`）。
> **多选 ≠ 递归** —— 选中的每个文件夹仍只加入「它本身」，不展开内部。DEC-01 的语义未变，
> 变的是「一次能挑几个」。对话框标题同步改为「选择要改名的文件夹（可多选）」，让能力可被发现。

#### 4.1.2 入列规则

| 规则 | 说明 |
| --- | --- |
| 去重 | 同一路径（不区分大小写）只入列一次，重复项静默忽略并在状态栏提示「已忽略 N 个重复项」 |
| 排序 | 默认按「名称」升序；再次入列时新项追加到末尾并按原顺序保留 |
| 混合 | 文件与文件夹可混合入列，列表中以图标区分 |
| 上限 | 单批最多 **10,000** 项；超出时提示「单批最多 1 万个，超出部分未加入」 |
| 拖入非文件内容 | 拖入网页链接、纯文本等非文件系统对象 → 落区显示拒绝态，并提示「只能拖入文件或文件夹」（EX-08） |

#### 4.1.3 移除与清空

| 元素 | 行为 |
| --- | --- |
| EL-036 行内删除按钮 | 鼠标悬停在列表行时出现，点击移除该行 |
| EL-022 清空列表 | 一键清空全部；**若列表非空则需二次确认**（避免误清） |

---

### 4.2 MOD-2 规则引擎（F-03 / F-04 / F-05）

#### 4.2.1 ★ 规则组合方式（DEC-02 定稿决策）

> **初稿描述「用户可叠加多种规则，规则按顺序执行」，但界面只有「删除 / 替换 / 规则化」三选一的单选控件——存在矛盾。**

**定稿：MVP 采用「规则模式单选 + 规则化页签内可多要素叠加」。**

| 模式 | 页签 | 可配置要素 | 是否可叠加 |
| --- | --- | --- | --- |
| 删除模式 | 删除字符 | 待删字符串 | 单一 |
| 替换模式 | 替换字符 | 查找串 → 替换串 | 单一 |
| 规则化模式 | 规则化 | 前缀、后缀、序号、日期、**保留原文件名开关** | **五个要素可同时生效** |

**理由**：三种模式**互斥**可让小白一眼看懂「我现在在做什么」，也避免「删了又替换还加序号」这种复合结果难以在预览里解释。真正需要复合的典型场景（加前缀 + 日期 + 序号）恰好全部落在「规则化」页签内，已被覆盖。多规则链式组合留到 P1（配合 F-12 模板库）。

#### 4.2.2 规则参数定义

**R-01 删除字符（F-03）**

| 字段 | 类型 | 默认值 | 校验 | 示例 |
| --- | --- | --- | --- | --- |
| `deleteText` 待删字符串 | 文本 | 空 | 非空才生效；支持多字符 | `【某某公众号】` |
| `caseSensitive` 区分大小写 | 布尔 | **false** | — | 关闭时 `ABC` 与 `abc` 均被删 |
| 删除范围 | 固定 | 全部出现位置 | 不可配置 | `a广a告a` → `aaa` |

**R-02 替换字符（F-04）**

| 字段 | 类型 | 默认值 | 校验 | 示例 |
| --- | --- | --- | --- | --- |
| `findText` 查找串 | 文本 | 空 | 非空才生效 | `最终版` |
| `replaceText` 替换为 | 文本 | 空 | **允许为空**（等价于删除） | `定稿` |
| `caseSensitive` 区分大小写 | 布尔 | false | — | — |
| 替换范围 | 固定 | 全部出现位置 | 不可配置 | — |

**R-03 / R-04 前缀与后缀**

| 字段 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `prefix` 前缀 | 文本 | 空 | 加在**文件名主体最前**，支持变量（见 4.2.3） |
| `suffix` 后缀 | 文本 | 空 | 加在**扩展名之前**，支持变量 |

**R-05 序号**（**P3 第 1 批已扩展** —— 2026-09-18 / DEC-15）

| 字段 | 类型 | 默认值 | 校验 | 说明 |
| --- | --- | --- | --- | --- |
| `seqEnabled` 启用序号 | 布尔 | false | — | 关闭时其余序号参数无效 |
| `seqKind` **编号类型**（本批新增） | 枚举 | `number` | `number` / `letter` / `random` / `time` | 数字 / 字母（Excel 列标）/ 随机字符 / 时间。**它是序号组里的一个下拉，不是第四种模式**（DEC-16） |
| `seqStart` 起始值 | 整数 | 1 | ≥ 0 | 第一个文件取该值（数字 / 字母两类用） |
| `seqStep` **增量** | 整数 | 1 | ≥ 1 | 可负数？**否**，仅支持递增。数字 / 字母 = 每次加几；时间 = 每次往后几天（**最小 1 天**） |
| `seqPad` **位数** | 整数 | 3 | 0 ~ 6 | 0 = 不补零；3 → `001`。**只对「数字」类型出现**（字母不补零；随机字符的长度是另一个字段；时间的补零由样式决定） |
| `seqPosition` 位置 | 枚举 | `suffix` | `suffix` / `prefix` / `at` | 排在最后 / 排在最前 / **插在第 n 个字符后**（`at` 为本批新增）。相对「原文件名主体」的位置 |
| `seqAt` 插在第几个字符后（本批新增） | 整数 | 1 | 1 ~ 200 | 仅 `at` 档有意义。越界（超过名字长度）**自动落到末尾**，不报错；按**码点**切，不劈开 emoji；名字为空时退化成「排在最前」 |
| `seqRandomLen` 随机长度（本批新增） | 整数 | 6 | 1 ~ 16 | 仅「随机字符」用 |
| `seqRandomSeed` 随机种子（本批新增） | 整数 | 0 | ≥ 0 | 随机串是**确定性**的：`(文件 id, 种子)` → 同一串恒定（否则「预览 ≡ 执行」不成立）。「换一批」= 种子 **+1**（单调递增，点一次必然不同） |
| `seqTimeStart` 时间起点（本批新增） | 文本 | 空 | `YYYY-MM-DD` | 仅「时间」用。切到该类型时由界面填入**本机今天**（本地时区），填进去后即冻结成规则里的字面值；留空则回落到执行日 |
| `seqTimeFormat` 时间样式（本批新增） | 枚举 | `YYYY年MM月DD日` | 见 R-06 的 5 档 | 仅「时间」用，与「启用日期」**共用同一张表** |
| `keepOriginal` 保留原文件名 | 布尔 | **true** | — | 关闭后原主体被丢弃，只保留前缀 / 序号 / 日期 / 后缀（用于「发票-001」类归档命名） |

> 序号顺序 = **列表当前显示顺序**（默认名称升序）。用户手动删除行后重新按剩余行顺序编号。
> **示例行（EL-124）**：规则区序号组里**常显**，拿固定示例名 `【素材】` 试前三个位置 —— **不跟随列表里的真实文件**（跟随的话，用户就没有参照物了），文案里明说「不是你列表里的文件」。它走的是与预览 / 执行**同一个 `applyRuleMode`**，所以示例**不可能**与真正改下去的结果不一致。
> **旧表里 `keepOriginal` 重复了两遍**，本次一并删掉一行（内容不丢）。

**R-06 日期**

| 字段 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `dateEnabled` 启用日期 | 布尔 | false | — |
| `dateFormat` 格式 | 枚举 | `YYYY-MM-DD` | **5 档**：`YYYY-MM-DD` / `YYYYMMDD` / `YYYY年MM月DD日` / **`MM月DD日`** / **`YYMMDD`**（后两档为 P3 第 1 批新增，与「时间」类型共用同一张表 —— 两处各写一份必然漂移） |
| 日期来源 | 固定 | **执行时的系统当前日期** | 非文件修改时间（DEC-03） |

> **DEC-03（定稿决策）**：日期默认取「改名执行当天」，而非文件创建 / 修改日期。
> 理由：目标用户（办公归档、发票整理）的真实诉求是「这批文件属于哪个归档周期」，与文件自身时间戳无关；且取文件时间戳会让预览结果难以预测。后续 P1 可增加「取自文件修改日期」选项。

#### 4.2.3 变量占位符

前缀、后缀、替换串中可使用变量：

| 变量 | 含义 | 示例（起始 1，补零 3） |
| --- | --- | --- |
| `{n}` | 序号（受 `seqStart` / `seqStep` / `seqPad` 控制） | `{n}` → `001` |
| `{d}` | 日期（受 `dateFormat` 控制） | `{d}` → `2026-09-11` |

**示例**：前缀填 `{d}-发票-`，勾选「启用序号」（起始 1 / 补零 3），并**取消勾选「保留原文件名」** → `IMG_0023.JPG` → `2026-09-11-发票-001.JPG`。

> **变量去重规则**：若前缀或后缀中已使用了 `{n}` 或 `{d}`，则系统**不再自动追加**对应的序号 / 日期片段，避免重复出现（例如前缀填 `{d}-{n}-` 时结果只含一次日期与序号）。

> **`keepOriginal`（保留原文件名）**：默认开启，开启时新名保留原主体，关闭时丢弃原主体，用于「把相机编号照片统一改成 2026-09-11-发票-001.jpg」这类**完全重命名**场景（对应 US-2）。
> **各要素的排列次序以 §4.2.5 的表格为唯一依据**（序号在前还是在后，由 `seqPosition` 决定）——本节只定义各要素的含义，不重复定义次序。

#### 4.2.4 作用域与扩展名保护（EX-07）

| 对象 | 规则作用范围 |
| --- | --- |
| 文件 | **仅作用于「文件名主体」**，扩展名（最后一个 `.` 之后的部分）默认**完全保护、不参与任何规则** |
| 文件夹 | 无扩展名概念，规则作用于完整名称 |

**判定细则**：

| 情形 | 主体 | 扩展名 |
| --- | --- | --- |
| `报告.docx` | `报告` | `.docx` |
| `IMG_0001.JPG` | `IMG_0001` | `.JPG` |
| `我的.备份.tar.gz` | `我的.备份.tar` | `.gz` |
| `.gitignore` | `.gitignore`（整名，**不视为扩展名**） | 无 |
| `README`（无点） | `README` | 无 |
| `文件夹名.2026`（文件夹） | 整名，**不拆分** | 无 |

> 扩展名保护首版**不提供关闭开关**（EX-07 已是 P0 默认行为）。P1 的 F-11 再考虑「允许修改扩展名」的高级选项。

#### 4.2.5 规则执行顺序

页签互斥，因此单个文件的实际处理链固定为：

```
[规则化模式]  ① 前缀 → ② 序号 → ③ 日期 → ④ 后缀
[删除模式]    删除指定字符串
[替换模式]    查找串 → 替换串
```

规则化模式内的位置关系：

| `seqPosition` | `keepOriginal = true` | `keepOriginal = false` |
| --- | --- | --- |
| 后缀位（默认） | `前缀 + 日期 + 原主体 + 后缀 + 序号` | `前缀 + 日期 + 后缀 + 序号` |
| 前缀位 | `前缀 + 序号 + 日期 + 原主体 + 后缀` | `前缀 + 序号 + 日期 + 后缀` |

> **★ 本表是「要素排列次序」的唯一权威依据（v1.1.1 明确）**。§4.2.3 只定义各要素的含义与默认值，不定义次序。
> 背景：v1.1 时 §4.2.3 的一句说明性文字（「新名 = 前缀 + 序号 + 日期 + 原主体 + 后缀」）与本节表格的默认行（后缀位）不一致，已在本版修正该句并把次序统一收敛到本表。技术方案已按本表实现，四种组合各有单测固化（对应 TC-06）。

（前缀 / 后缀中含 `{n}` 或 `{d}` 时，对应的自动片段省略，见 4.2.3）

---

### 4.3 MOD-3 预览模块（F-08）

| 项 | 规格 |
| --- | --- |
| 刷新时机 | 规则参数**任意变更后 200ms 防抖**刷新（输入框连续输入不卡） |
| 刷新范围 | 全量重算；1000 行以上启用虚拟滚动，仅渲染可视区域 |
| 展示形式 | 每行 `原名` → `新名` |
| **变化高亮** | 新名中**发生变化的字符片段**用橘色加粗 + 浅橘底色标出；原名中对应片段用灰色删除线标出 |
| 无变化行 | 右侧显示灰色 `—`，并在状态栏统计中排除 |
| 非法行 | 新名整行标红，行尾显示原因徽标（如「含非法字符」） |
| 冲突行 | 新名整行标红 + 行尾 `⚠ 重名` 徽标 |
| 性能指标 | 1000 项预览计算 ≤ 200ms |
| 空状态 | 列表为空时显示引导插画：「把文件拖进来，或者点左边的按钮」（耄耋状态 ST-01） |

**差异高亮算法（供开发参考）**：取原名与新名的最长公共前缀长度 `p`、最长公共后缀长度 `s`（前缀与后缀不重叠），中间部分即为「变化片段」。

---

### 4.4 MOD-4 执行与冲突处理模块（F-01 / F-02 / F-16）

#### 4.4.1 ★ 重名冲突处理（F-16，DEC-05 定稿决策）

> **DEC-05**：默认策略为**「跳过冲突项」**，**任何情况下绝不覆盖已存在文件**。

**冲突的两类来源**：

| 类型 | 定义 | 编号 |
| --- | --- | --- |
| 批次内冲突 | 同一批中两个及以上文件改名后结果相同（**不区分大小写**，因 Windows 文件系统不区分） | EX-05 |
| 磁盘冲突 | 改名后与**该目录下已存在的其它文件**同名 | EX-06 |

**两种处理策略（EL-055 开关）**：

| 策略 | 开关状态 | 行为 |
| --- | --- | --- |
| **跳过（默认）** | 关闭「自动加序号」 | 冲突项**不执行改名**，保留原名；结果弹窗中列入「失败 / 跳过」清单，原因「重名冲突」 |
| 自动加序号 | 开启「自动加序号」 | 冲突项自动追加 `_1`、`_2`…（在扩展名前），直到不冲突为止；**最多尝试 99 次**，仍冲突则跳过 |

> 无论哪种策略，**都不会产生覆盖已有文件的写入操作**。技术上禁止使用 `fs.rename` 的直接覆盖语义，必须先在目标路径做存在性检查。

**冲突提示弹窗（SCR-04）触发条件**：预览中存在冲突项，且用户点击「开始改名」时。

#### 4.4.2 执行前确认（SCR-05）

| 条件 | 是否弹二次确认 |
| --- | --- |
| 待改名项 < 10 且无冲突 | **不弹**，直接执行 |
| 待改名项 ≥ 10 | 弹确认：「即将修改 N 个文件 / 文件夹的名称，是否继续？」 |
| 存在冲突项 | 弹冲突提示（SCR-04），列出冲突明细 |

#### 4.4.3 执行流程（两阶段防循环冲突）

> 为什么需要两阶段：若有 `A.txt` → `B.txt`、同时 `B.txt` → `C.txt`，直接按顺序改会在第一步就撞上尚未处理的 `B.txt`。改为「先全部改成临时名，再改成目标名」即可避免。

```
阶段 0  校验：过滤非法项、冲突项（按策略处理）→ 得到待执行清单
阶段 1  批量改名为临时名：原名 → __md_tmp_<任务ID>_<序号>
        （临时名保证不与任何既有文件冲突）
阶段 2  批量改名为目标名：__md_tmp_<...> → 新名
        若阶段 2 中某项失败 → 该项回滚为原名，并记入失败清单
结果    写入改名记录（供撤销）→ 弹出结果弹窗（SCR-03）
```

| 元素 | 规格 |
| --- | --- |
| 进度反馈 | 状态栏显示进度条 + `已处理 N / 总数`；耄耋进入 ST-03（改名中） |
| 可取消 | 执行中「开始改名」按钮变为「取消」；取消后**已改的项自动回滚为原名** |
| 时间预算 | 1000 项 < 10 秒；超过 2000 项时按每 50 项一次批量提交，避免界面卡死 |
| 失败不中断 | 单个文件失败不终止整批，继续处理其余项 |

#### 4.4.4 执行结果弹窗（SCR-03）

| 区域 | 内容 |
| --- | --- |
| 结果摘要 | `成功 N 项 · 跳过 M 项 · 失败 K 项` |
| 成功举例 | 折叠展示前 3 条 `原名 → 新名` |
| 失败 / 跳过清单 | 逐条列出 `原名`、`尝试的新名`、**原因**（见下表） |
| 主按钮 | EL-093「撤销本次」→ 直接调用 F-09 撤销本任务 |
| 次按钮 | EL-092「知道了」→ 关闭弹窗 |

**失败原因枚举**：

| 原因文案 | 触发条件 | 异常编号 |
| --- | --- | --- |
| 重名冲突 | 目标名已存在（跳过策略下） | EX-05 / EX-06 |
| 文件被占用 | 目标文件正被其它程序打开（`EBUSY` / `EPERM`） | EX-03 |
| 无写入权限 | 目录只读或无权限 | EX-03 |
| 路径过长 | 完整路径超过系统限制 | EX-02 |
| 名称非法 | 含 Windows 保留字符，或改后为空 | EX-01 / EX-04 |
| 源文件不存在 | 改名期间文件被移动 / 删除 | EX-10 |

---

### 4.5 MOD-5 撤销与历史模块（F-09）

#### 4.5.1 ★ 撤销范围（DEC-06 定稿决策）

> **DEC-06**：本地保留**最近 20 次**改名任务；每条任务**可单独撤销**，也支持**「全部撤销」**。

| 项 | 规格 |
| --- | --- |
| 存储位置 | 本地用户数据目录（Electron `app.getPath('userData')`），JSON 持久化 |
| 保留条数 | 最近 **20** 次；超出后**自动淘汰最旧一条** |
| 单条撤销 | 将该任务中的 `新名 → 原名` 全部改回；**倒序执行**（后改的先还原），避免中间态冲突 |
| 全部撤销 | 从**最新任务倒序**逐条撤销；任一条出现不可撤销项时**跳过并继续**，最后汇总提示 |
| 二次确认 | 撤销前必须二次确认，文案示例：「将把 12 个文件的名字改回去，确定吗？」 |
| 撤销后状态 | 该任务标记为「已撤销」，**不再可再次撤销**；记录保留在历史中（灰显）供查看 |
| 部分成功 | 若某些文件已被外部改名 / 移动，跳过该条并在结果中明确列出 |
| 撤销自身的可逆性 | 撤销操作**不产生新的可撤销记录**（避免「撤销的撤销」逻辑混乱） |

#### 4.5.2 历史页（SCR-02）

| 元素 | 内容 |
| --- | --- |
| EL-081 历史列表 | 每条显示：执行时间、项数、规则摘要（如「删除 `【某某公众号】`」）、首条示例 `A → B`、状态（可撤销 / 已撤销） |
| EL-082 单条撤销按钮 | 仅「可撤销」状态显示 |
| EL-083 全部撤销 | 页头按钮，撤销全部可撤销任务 |
| 空状态 | 「还没有改过名呢」+ 耄耋待机形象 |
| EL-080 返回主窗口 | 回到 SCR-01 |

> 主窗口 R-06 的「撤销」按钮（EL-061）语义 = **撤销最近一次任务**，点击后直接进入 SCR-02（若只有一条则直接弹确认）。

---

### 4.6 MOD-6 耄耋形象状态机（F-06 · 产品灵魂）

#### 4.6.1 状态定义

| 编号 | 状态 | 触发条件 | 形象表现 | 状态文字 | 持续 |
| --- | --- | --- | --- | --- | --- |
| ST-01 | **待机** | 打开软件 / 无操作 | 蹲坐，偶尔眨眼，耳朵微动，尾巴轻摆（循环） | `待机中…` | 循环 |
| ST-02 | **接收文件** | 拖拽悬停 / 点击添加 | 抬头、**飞机耳**、眼睛盯向列表，头部轻晃 | `收到啦，N 个！` | 1.5s 后回 ST-01 |
| ST-03 | **改名中** | 点击开始改名 | **「一口一口咬名字」**：一列小卡片（文件 / 文件夹图标 + 文件名）从右往左经过耄耋嘴边；耄耋前倾咬合（0.25s「咔嚓」）→ 被咬的卡片名字由「旧名」变为「新名」并转为橘色 → 卡片飞走、下一张补位；尾巴快摆（循环，约 1.6s 一张） | `正在改名…（N/M）` | 持续至完成 |
| ST-04 | **成功** | 全部改完且无失败 | **得意**：下巴上扬、眯眼笑、尾巴竖起、周围冒小花 | `改好啦，得意！` | 3s 后回 ST-01 |
| ST-05 | **失败** | 存在失败 / 跳过项 | **炸毛 + 哈气**：全身毛发炸起、张嘴露牙、冒怒气符号 | `炸毛了！有 N 项没改成` | 3s 后回 ST-01 |

> **关于形象素材（重要说明）**：**本 PRD 与原型中的耄耋形象是"动作与结构的占位实现"，不是最终美术稿。**
> - 原型里的橘猫为**手写 SVG**，目的是让原型离线可跑、可点击，与最终形象无关；
> - 最终形象按《立项书》决策使用**网络耄耋表情包**（Accepted Risk），架构上保留一键替换能力；
> - 因此**评审时应重点看「状态与动作设计」是否正确，而非猫咪画得好不好看**——动作设计确定后，替换任何形象都能直接套用。
> - 形象素材以「头 / 耳 / 眼 / 嘴 / 毛发 / 四爪」分组独立命名，替换时按分组对位即可。

#### 4.6.2 状态迁移图

```
        打开软件
           ↓
      ┌─────────┐  拖入/添加   ┌──────────┐
      │ ST-01   │ ──────────→ │ ST-02    │
      │ 待机    │ ←────────── │ 接收文件 │
      └─────────┘   1.5s      └──────────┘
           │ 点击「开始改名」        │
           ↓                        │
      ┌─────────┐                   │
      │ ST-03   │ ←─────────────────┘
      │ 改名中  │
      └─────────┘
        │       │
   无失败│       │有失败/跳过
        ↓       ↓
   ┌────────┐ ┌────────┐
   │ ST-04  │ │ ST-05  │
   │ 成功   │ │ 失败   │
   └────────┘ └────────┘
        └────┬────┘
             │ 3s
             ↓
          ST-01
```

#### 4.6.3 形象设计原则

| 原则 | 说明 |
| --- | --- |
| **可爱为主、暴躁为辅** | 炸毛（ST-05）是**幽默反差**，不是攻击性表达；不出现真实愤怒 / 攻击性文案 |
| 无攻击性文案 | 失败文案用「炸毛了！」，不用「你搞砸了」等指责用户的话 |
| 情绪不迁怒 | 失败时的表现指向「任务没成功」，不指向用户 |
| **动作不得有歧义（v1.1 新增）** | 形象动画禁止使用「双肢快速上下抽动 + 躯干抖动 + 张嘴」这类组合——ST-03 初版曾用「双爪快速扒拉 + 身体抖动 + 冒汗」，视觉上易被误读，已改为「咬文件」动作。**新增动作设计需先过一遍「会不会被想歪」自查。** |
| **撕咬要卡通化** | ST-03 的「咬」只做干脆的「咔嚓一口」：不做尖牙特写、不做破碎 / 流血效果；被咬的卡片是「乖乖变成新名字飞走」，不是被咬碎——契合「不渲染攻击性」 |
| 可替换性 | 形象素材以独立资源目录存放，支持「一键替换」，便于日后更换原创形象 |

---

## 5. 界面规格

> 详细交互见《交互说明文档》，本节只定义结构与编号。视觉稿见《原型图.html》。

### 5.1 界面清单

| 编号 | 界面 | 实现形态 | 优先级 |
| --- | --- | --- | --- |
| SCR-01 | 主窗口 | 独立窗口 | P0 |
| SCR-02 | 撤销 / 历史页 | 主窗口内视图切换（非新窗口） | P0 |
| SCR-03 | 结果弹窗 | 模态弹窗 | P0 |
| SCR-04 | 冲突提示弹窗 | 模态弹窗 | P0 |
| SCR-05 | 二次确认弹窗 | 模态弹窗 | P0 |
| SCR-06 | 系统文件 / 文件夹选择框 | 系统原生对话框 | P0 |
| **SCR-07** | **设置弹窗** | 模态弹窗 | **P2-A**（DEC-11）：主题三选一 / 音效 / 减少动画 / **命令行用法**（P2-C） |
| **SCR-08** | **命令行图文教程弹窗** | 模态弹窗（叠加在 SCR-07 之上） | **P2-C 增量**（DEC-13）：三步图文（`Win+R` 开命令行 → 命令拆四段 → 先试跑再 `--yes`），由设置里的「查看教程」按钮进入 |

### 5.2 SCR-01 主窗口布局（横版宽屏 · 左右分栏）

```
┌──────────────────────────────────────────────────────────────────────┐
│ R-01 耄耋改名                                        ─    □    ✕     │
├──────────────────────┬───────────────────────────────────────────────┤
│ R-02 形象区          │ R-04 文件列表区                               │
│   ┌──────────────┐   │  ┌─────────────────────────────────────────┐  │
│   │  耄耋形象    │   │  │ 共 12 项  ☐ 全选                        │  │
│   │  （动画区）  │   │  │ ① abc广告.txt      →  abc.txt          │  │
│   └──────────────┘   │  │ ② 【公众号】x.mp4  →  x.mp4            │  │
│   状态：待机中…      │  │ ③ IMG_0023.JPG     →  2026-001.jpg     │  │
│                      │  │ …                                       │  │
│ R-03 操作区          │  └─────────────────────────────────────────┘  │
│  [  添加文件  ]      │ R-05 规则设置区                               │
│  [ 添加文件夹 ]      │  ┌─────────────────────────────────────────┐  │
│  [  清空列表  ]      │  │ 常用规则                                │  │
│                      │  │ [加日期前缀][补零编号][日期+编号]…      │  │
│                      │  │ (•)删除字符 ( )替换字符 ( )规则化       │  │
│                      │  │ [ 参数输入框 ]      ☐ 区分大小写        │  │
│  拖到窗口里也行哦～  │  │ ☐ 自动处理重名冲突                      │  │
│                      │  └─────────────────────────────────────────┘  │
│                      │ R-06 动作区    [ 开始改名 ]   [ 撤销 ]        │
├──────────────────────┴───────────────────────────────────────────────┤
│ R-07 ● 就绪 · 共 12 项 · 预览 3 项将发生变化                          │
└──────────────────────────────────────────────────────────────────────┘
```

| 区域 | 编号 | 位置 | 内容 | 尺寸建议 |
| --- | --- | --- | --- | --- |
| 标题栏 | R-01 | 顶部通栏 | 产品名 + 最小化 / 最大化 / 关闭 | 高 40px |
| 形象区 | R-02 | 左栏上部 | 耄耋动画 + 状态文字 | 左栏宽 300px，形象区高 ~300px |
| 操作区 | R-03 | 左栏下部 | 添加文件 / 添加文件夹 / 清空列表 | 撑满剩余高度 |
| 列表区 | R-04 | 右栏上部 | 原名 → 新名预览列表 | 占右栏 ~50% 高 |
| 规则区 | R-05 | 右栏中部 | 规则页签 + 参数 | 自适应 |
| 动作区 | R-06 | 右栏下部 | 开始改名 / 撤销 | 高 64px |
| 状态栏 | R-07 | 底部通栏 | 状态点 + 统计文本 + 进度条 | 高 32px |

| 窗口规格 | 值 |
| --- | --- |
| 默认尺寸 | 1080 × 680 |
| 最小尺寸 | 900 × 560 |
| 可缩放 | 是；左右分栏比例固定 300px + 自适应 |
| 记住尺寸 | 是（下次启动恢复上次尺寸与位置） |

### 5.3 视觉风格

| 项 | 规格 |
| --- | --- |
| 主色 | 橘色系 `#F5A623` / `#FF8A3D`（橘猫） |
| 辅助色 | 奶油白背景 `#FFFBF5`、卡片白 `#FFFFFF` |
| 语义色 | 成功 `#4CAF7D`、失败 / 冲突 `#E5544B`、警告 `#F0A020`、中性灰 `#8A8178` |
| 文字 | 主文字 `#3A2A20`、次要 `#8A8178` |
| 字体 | 圆润中文字体（思源黑体 / 站酷快乐体，作为可选字体资源） |
| 圆角 | 卡片 16px、按钮 12px、输入框 10px |
| 阴影 | 柔和投影 `0 4px 16px rgba(180,120,60,.12)` |
| 动效 | 按钮按压缩放 0.96、悬停上浮 1px；改名完成撒花 + 轻音效（可关闭） |

---

## 6. 数据与状态定义

### 6.1 数据结构（供开发直接使用）

```ts
// 单个待处理项
interface FileItem {
  id: string;              // 唯一 id
  fullPath: string;        // 绝对路径
  dirPath: string;         // 所在目录
  stem: string;            // 名称主体（文件去掉扩展名；文件夹取全名）
  ext: string;             // 扩展名（含点，如 ".docx"；无则空串）
  isDir: boolean;
  newStem: string;         // 计算出的新主体
  status: 'pending'       // 待处理
        | 'unchanged'     // 无变化
        | 'changed'       // 将变化
        | 'conflict'      // 重名冲突
        | 'invalid';      // 非法（空名 / 非法字符 / 超长）
  reason?: string;         // 非法 / 冲突的原因文案
  diffRange?: [number, number]; // 差异高亮区间（用于渲染）
}

// 规则配置
interface RuleConfig {
  mode: 'delete' | 'replace' | 'rule';
  caseSensitive: boolean;             // 默认 false
  autoResolveConflict: boolean;       // 默认 false ← DEC-05
  regexEnabled: boolean;              // P1 / F-10：默认 false（关闭时输出与 P0 逐字节一致）
  caseTransform: 'none' | 'lower' | 'upper' | 'capitalize';  // P1 / F-11：默认 'none'
  delete: { text: string };
  replace: { find: string; to: string };
  rule: {
    prefix: string;
    suffix: string;
    seqEnabled: boolean;  seqStart: number; seqStep: number; seqPad: number;
    seqPosition: 'prefix' | 'suffix';
    dateEnabled: boolean; dateFormat: 'YYYY-MM-DD' | 'YYYYMMDD' | 'YYYY年MM月DD日';
    keepOriginal: boolean;              // 默认 true；false = 丢弃原主体
  };
}

// P1 执行顺序（唯一权威）：新名主体 = 大小写转换( 模式结果 )
//   · regexEnabled 只对 delete / replace 两种模式有意义（规则化不涉及匹配）
//   · caseTransform 只作用于「新名主体」，扩展名永远不动
//   · 「⚙ 进阶设置」折叠条的展开/收起是纯 UI 状态，不进本结构、不进 IPC 契约

// 一次改名任务（撤销记录）
interface RenameTask {
  id: string;
  createdAt: number;                        // 时间戳
  ruleSummary: string;                      // 规则摘要文案，用于历史页展示
  entries: {
    dirPath: string;
    fromName: string;
    toName: string;
    result: 'success' | 'skipped' | 'failed';
    reason?: string;
  }[];
  status: 'active' | 'undone';              // 可撤销 / 已撤销
}

// 全局状态
interface AppState {
  view: 'main' | 'history';
  files: FileItem[];
  rule: RuleConfig;
  running: boolean;
  progress: { done: number; total: number };
  history: RenameTask[];      // 最新在前，最多 20 条
  catState: 'ST-01'|'ST-02'|'ST-03'|'ST-04'|'ST-05';
}
```

### 6.2 数据持久化

| 数据 | 位置 | 时机 | 上限 |
| --- | --- | --- | --- |
| 历史改名记录 | `userData/history.json` | 每次改名成功后写入 | 20 条任务（FIFO 淘汰）；**明细总量另受上限约束**，极端场景下任务数可能少于 20 —— 见 DEC-09 |
| 窗口尺寸 / 位置 | `userData/window.json` | 窗口关闭时 | — |
| 用户偏好（音效开关等） | `userData/prefs.json` | 设置变更时 | — |
| 文件列表 / 规则 | **不持久化** | — | 关闭软件即清空（避免下次启动误改） |

> **DEC-07**：**不保存上次的文件列表**。理由：若下次启动自动恢复上次列表，用户可能忘记列表里有内容而误点「开始改名」。每次启动都从空列表开始，更安全。

---

## 7. 异常与边界

| 编号 | 异常 | 触发 | 处理 | 提示文案 |
| --- | --- | --- | --- | --- |
| EX-01 | 新名含非法字符 | 结果含 `\ / : * ? " < > \|` | 该行标 `invalid`，不执行 | 名称里不能包含 `\ / : * ? " < > \|` |
| EX-02 | 路径过长 | 完整路径 > 259 字符 | 该行标 `invalid` | 名字太长了，系统放不下 |
| EX-03 | 无权限 / 被占用 | `EPERM` / `EBUSY` | 该项失败，其余继续 | 文件被占用或无权限 |
| EX-04 | 改后名称为空 | 主体被完全删除 | 该行标 `invalid` | 改完名字就空了，检查一下规则 |
| EX-05 | 批次内互相重名 | 两项新名相同 | 标 `conflict`，按 DEC-05 处理 | 有 N 项改完会撞名 |
| EX-06 | 与磁盘已有文件重名 | 目标名已存在 | 标 `conflict`，按 DEC-05 处理 | 已经有同名文件了 |
| EX-07 | 扩展名被规则波及 | — | **由扩展名保护机制提前避免** | 不会发生（保护为默认） |
| EX-08 | 拖入非文件对象 | 拖入文本 / 链接 | 落区拒绝态 | 只能拖入文件或文件夹哦 |
| EX-09 | 拖入重复项 | 同路径已在列表 | 静默忽略 + 状态栏提示 | 已忽略 N 个重复项 |
| EX-10 | 撤销时源文件已不存在 | 文件被外部移动 / 改名 | 跳过该条，继续其余 | 有 N 个文件找不到了，已跳过 |
| EX-11 | 空列表点开始改名 | 列表为 0 | 按钮置灰不可点 | — |
| EX-12 | 全部项均无变化 | 规则未生效 | 按钮置灰，状态栏提示 | 当前规则不会改变任何名字 |
| EX-13 | 列表项超过 1 万 | 单批超限 | 超出部分不加入 | 单批最多 1 万个，超出部分未加入 |
| EX-14 | 目标为系统保护目录 | 如 `C:\Windows` 下文件 | 允许但失败 | 无写入权限 |
| **EX-15** | **正则表达式非法**（P1 / DEC-10） | 勾了「用正则匹配」但 pattern 编译失败（如输入 `(`） | 输入框 2px `#E5544B` 红描边 + 下方红字原因（由运行时错误映射，不穷举硬编码）+ 状态栏「正则表达式有误」+「开始改名」置灰；预览列表保留上一次的合法结果，不整屏标红 | 正则表达式不完整：缺少右括号 `)`（示例） |
| **EX-16** | **正则过于复杂（灾难性回溯）**（P1 / DEC-10） | 如 `(a+)+$` 这类会指数级回溯的写法 | 预览 Worker **每处理若干项检查一次耗时，累计超 3 秒即中止**；同时限制 pattern 长度 ≤ 200 字符 | 正则过于复杂，请简化 |
| **EX-17** | **清空历史时仍有可撤销任务**（P2-C / DEC-12） | 历史里存在 `status: 'active'` 的任务，用户点「清空历史记录」 | 二次确认**额外加红色警告块**，主按钮文案改「仍然清空」。理由：清空 = 删掉撤销记录 → 那些改名**再也撤不回来**，不警告会导致「改错了名 → 想撤销 → 发现历史被自己清了」的永久丢数据 | 其中 N 条改名记录仍然可以撤销。清空后这 N 条将无法再还原 —— 只能手动把文件名改回去。 |

---

## 8. 非功能需求

| 类别 | 需求 | 指标 |
| --- | --- | --- |
| 平台 | Windows 10 / 11 x64 | 首版仅此 |
| 性能 | 1000 项改名 | < 10 秒 |
| 性能 | 预览计算 1000 项 | ≤ 200ms |
| 性能 | 界面滚动（万级列表） | 虚拟滚动，60fps |
| 安全 | **绝不覆盖已有文件** | 硬性约束 |
| 安全 | 预览 + 撤销 | P0 |
| 隐私 | 完全本地离线 | 零网络请求，零数据收集 |
| 体积 | 安装包（**首版目标，不作为发布阻断项**，DEC-08） | 力争 < 100 MB；实测值记录在 MS-5，超标**不因此返工架构** |
| 免费 | 无广告、无内购、无账号 | — |
| 无障碍 | 键盘可操作性 | Tab 顺序合理，Enter 触发主操作 |
| 兼容 | 中文 / 英文文件名、Emoji、特殊符号 | 正确处理 UTF-8 |

---

## 9. 里程碑

| 编号 | 阶段 | 内容 | 交付物 | 依赖 |
| --- | --- | --- | --- | --- |
| MS-1 | **引擎先行** | 规则引擎 + 冲突检测 + 扩展名保护 + 两阶段改名 | 命令行可验证的改名核心 | — |
| MS-2 | **界面成型** | SCR-01 主窗口 UI + 列表 + 规则区 + 实时预览 | 能用的界面（无形象） | MS-1 |
| MS-3 | **注入灵魂** | 耄耋 5 状态动画 + 音效 + 微动效 | 有灵魂的 UI | MS-2 |
| MS-4 | **安全闭环** | 拖拽（P0）+ 撤销 + 历史页 + 结果 / 冲突弹窗 | 安全完整版 | MS-2 |
| MS-5 | **打包发布** | 安装包 + 自测 + 开源发布 | 可安装的 exe | MS-3 / MS-4 |

> **调整说明（v1.0）**：原初稿 M3「预览 + 撤销 + 拖拽」中，拖拽已提为 P0，纳入 MS-4；预览因是 P0 且与界面强耦合，提前到 MS-2。

---

## 10. 验收用例

| 编号 | 用例 | 步骤 | 预期 | 对应 |
| --- | --- | --- | --- | --- |
| TC-01 | 拖拽入列 | 拖 100 个文件进窗口 | 全部入列，状态栏显示「共 100 项」，耄耋 ST-02 | F-07 |
| TC-02 | 按钮添加文件夹 | 点「添加文件夹」选 1 个文件夹 | 只加入该文件夹本身，不展开内部 | DEC-01 |
| TC-03 | 删除字符 | 规则「删除 `广告`」，含 `abc广告.txt` | 预览显示 `abc.txt`，变化字符高亮 | F-03 |
| TC-04 | 替换字符 | 「`最终版` → `定稿`」 | `会议纪要-最终版.docx` → `会议纪要-定稿.docx` | F-04 |
| TC-05 | 扩展名保护 | 「删除 `.` 」 | 扩展名前的点被删，扩展名保留 | EX-07 |
| TC-06 | 规则化组合 | 前缀 `{d}-发票-` + 序号起始 1 补零 3 + 取消「保留原文件名」 | `IMG_0001.JPG` → `2026-09-11-发票-001.JPG` | F-05 |
| TC-07 | **冲突不覆盖** | 构造两项改后同名 | 不弹覆盖、不改动，两项列入「跳过 / 冲突」，磁盘文件无损失 | **F-16 / DEC-05** |
| TC-08 | 自动加序号 | 打开「自动加序号」重复 TC-07 | 生成 `报告_1.docx`、`报告_2.docx` | F-16 |
| TC-09 | 撤销单条 | 改名后进历史页，撤销该条 | 文件名全部还原，该条变「已撤销」 | F-09 |
| TC-10 | 历史 20 条 | 连续改名 21 次 | 只保留最近 20 条，最旧的被淘汰 | DEC-06 |
| TC-11 | 全部撤销 | 3 条任务，点「全部撤销」 | 3 条全部倒序撤销成功，汇总提示 | F-09 |
| TC-12 | 占用文件失败 | 用记事本打开待改文件后执行 | 该项失败原因为「文件被占用」，其余成功 | EX-03 |
| TC-13 | 非法字符 | 规则使新名含 `?` | 该行标红 + 原因「含非法字符」，不执行 | EX-01 |
| TC-14 | 改后为空 | 删除整个文件名主体 | 该行标红 + 「改完名字就空了」 | EX-04 |
| TC-15 | 1000 项性能 | 拖入 1000 个文件执行 | 总耗时 < 10 秒，界面不卡死 | 性能 |
| TC-16 | 离线验证 | 断网执行全流程 | 全部功能正常，无任何网络请求 | 隐私 |
| TC-17 | 状态记忆 | 调整窗口尺寸后重启 | 恢复上次尺寸与位置 | 5.2 |
| TC-18 | 空列表保护 | 列表为空时观察「开始改名」 | 按钮置灰不可点 | EX-11 |
| TC-19 | 按钮添加文件夹（多选） | 点「添加文件夹」，在同一个框里选中 3 个文件夹 | 3 个文件夹**全部**入列；每一个都只加文件夹本身、不展开内部；状态栏/列表计数正确 | DEC-01 · IX-021 |
| TC-20 | 添加文件夹记住上次位置 | 先加一批文件夹，再点一次「添加文件夹」 | 对话框直接停在**上次那批所在的父目录**（重启软件后重置） | IX-021 |
| TC-21 | 正则删除 | 删除模式 + 勾「用正则匹配」，内容填 `\d+`，文件 `abc123.txt` | 预览新名 `abc.txt`，变化片段高亮 | F-10 · DEC-10 |
| TC-22 | 正则捕获组替换 | 替换模式 + 勾正则，查 `(\d{4})-(\d{2})` → 替换 `$2/$1`，文件 `2026-09.txt` | `09/2026.txt` | F-10 · DEC-10 |
| TC-23 | 正则非法 | 输入 `(` | 输入框 2px `#E5544B` 红描边 + 红字原因 + 状态栏「正则表达式有误」+「开始改名」置灰；**不崩溃**（改回合法后自动恢复） | EX-15 · IX-053 |
| TC-24 | 大小写不改扩展名 | 大小写 = 全部小写，文件 `IMG_0001.JPG` | 预览新名 `img_0001.JPG`（**扩展名保持大写**） | F-11 · EX-07 延伸 |
| TC-25 | 大小写导致撞名 | 两个文件转小写后同名 | 走 SCR-04 冲突弹窗，默认「先不改」，**磁盘文件无损失** | F-11 · F-16 · EX-05 |
| TC-26 | **P0 回归** | 正则关 + 大小写「保持原样」，跑既有 P0 用例集 | 输出与 P0 **逐字节一致**；1000 项预览 / 执行性能不退化 | 红线（DEC-10） |
| TC-27 | 正则说明小白化 | 勾「用正则匹配」并展开进阶设置 | 开关旁显示「普通写法 vs 正则写法」大白话对照；点「? 看不懂？」冒出照抄表（**删除模式 3 行 / 替换模式 4 行**）；查找填 `(\d{4})-(\d{2})-(\d{2})`、替换填 `$1年$2月$3日` 时，演示区显示 `发票 2026-08-01.pdf → 发票 2026年08月01日.pdf`；**写法对不上时用灰字如实显示「没变」**，不谎报变化 | F-10 说明改进 · EL-103 ~ EL-105 · IX-093 / IX-094 |

| **TC-28** | 设置入口 | 点标题栏滑杆按钮（三键左侧） | 弹出设置弹窗（SCR-07）；`Esc` / 点遮罩 / 点「完成」三者都能关 | P2-A · DEC-11 |
| **TC-29** | 主题三态 | 依次选「始终浅色」「始终深色」「跟随系统」 | 界面立即换色；选「跟随系统」后改 Windows 深浅色，软件实时跟着切 | F-14 · DEC-11 |
| **TC-30** | 无首帧闪烁 | 冷启动（已处于深色模式） | 从窗口出现到界面就绪，**没有任何一帧是浅色** | F-14 · DEC-11 |
| **TC-31** | 补 P0 缺口 | 在设置里开「减少动画」 | 猫咪状态切换只换表情、不做位移；关掉后恢复 | 无障碍 · DEC-11 |
| **TC-32** | **令牌覆盖率（可自动检查）** | `tokens.css` 之外 grep 色值 | **必须归零**。P2-A 最重要的一条，不靠人眼 | 设计规范 §8.2 · DEC-11 |
| **TC-33** | 查看历史明细 | 历史页点某条的「查看全部 N 项」 | 就地展开完整明细，序号与「原名 → 新名」逐条正确；点「收起」收回 | F-13 · DEC-12 |
| **TC-34** | 明细渲染上限 | 构造 5000 条明细的任务并展开 | 只渲染 100 行，末行显示「还有 4900 项未显示（共 5000 项）」；界面不卡 | F-13 · DEC-12 |
| **TC-35** | 清空历史 · 无警告分支 | 所有任务都已撤销 → 点「清空历史记录」 | 正文无红色警告；主按钮文案为「清空」 | F-13 · DEC-12 |
| **TC-36** | **清空历史 · 安全阀** | 存在「可撤销」任务 → 点「清空历史记录」 | **出现红色警告块**，写明「N 条仍可撤销、清空后无法还原」；主按钮文案为「仍然清空」 | **EX-17** · DEC-12 |
| **TC-37** | **清空后不误报淘汰** | 先点清空，再进历史页 | 状态栏显示「已清空全部 N 条历史记录，文件未被改动」；**不出现**「较旧的记录已被清理」 | DEC-09 · DEC-12 |
| **TC-38** | **套用内置模板** | 点「加日期前缀」 | 页签跳到「规则化」；前缀框出现 `{d} `；「启用日期」自动勾上、格式 `YYYY-MM-DD`；**预览里的新名真的变成「今天的日期 + 原名字」** | F-12 · DEC-14 |
| **TC-39** | **模板替用户开正则要讲明白** | 在正则**未开启**时点「去掉括号」 | 页签跳到「替换字符」；查找框被填成该正则；**「⚙ 进阶设置」折叠条变橘 + 出现「已启用 1 项」**；状态栏提示「已套用模板：去掉括号（已自动开启正则）」 | F-12 · DEC-14 |
| **TC-40** | **正则必须匹配全部** | 文件名 `(1)(2)报告.docx` → 点「去掉括号」 | 变成 `报告.docx`（**两组括号都去掉**）。只去掉第一组就说明引擎少了 `g` 标志 | F-12 · DEC-14 |
| **TC-41** | **模板常量不被污染** | 点「加日期前缀」→ 手动把前缀改乱 → 再点一次「加日期前缀」 | **第二次拿到的仍是原始模板**（前缀回到 `{d} `）。少了深拷贝的后果是「模板越用越怪」，而**界面永远不报错** | F-12 · DEC-14 |
| **TC-42** | **编号类型 4 种** | 依次把类型切到 数字 / 字母 / 随机字符 / 时间 | 参数行**跟着换**：数字=起始·增量·位数；字母=起始·增量（**没有位数**）；随机=长度 + 「换一批」；时间=起点·样式·增量（天）。**示例行每次都立刻变**，不是空着 | F-05 · DEC-15 |
| TC-43 | 字母 = Excel 列标 | 类型=字母，起始分别填 1 / 26 / 27 / 702 / 703 | `A` / `Z` / `AA` / `ZZ` / `AAA`。起始填 0 → 自动变 1，**不报错** | F-05 · DEC-15 |
| **TC-44** | **位置第三档** | 位置=「插在第 n 个字符后」，n=3；示例名 `【素材】` | `【素材1】`（插在「材」和「】」之间）。**扩展名不参与**：`【素材】.docx` 的 `.docx` 不会被插进去 | F-05 · DEC-15 |
| TC-45 | 位置越界 + 码点 | n=3 但名字只有 2 个字；另一个名字含 emoji（如 `🎉庆`），n=1 | 前者**落到末尾**，不报错；后者 `🎉` 完整不劈开，生成合法文件名 | F-05 · DEC-15 |
| TC-46 | 随机字符「预览 ≡ 执行」 | 类型=随机字符，长度 6；不碰参数反复触发预览 5 次；再改一次参数又改回来 | 5 次每个文件的随机串**完全一样**；改回来之后仍然一样 | F-05 · DEC-15 |
| TC-47 | **「换一批」真的换** | 记下当前示例 → 点「换一批」→ 再点一次 | 结果变了；再点**不会回到上一批**（种子单调 +1） | F-05 · DEC-15 |
| **TC-48** | **执行不丢字段（本批最重要）** | 每种类型各跑一次真实改名，把**预览里显示的新名**与**磁盘上真实的名字**逐条比对 | **逐字节相等**。必须覆盖 `seqPosition='at'`、字母、随机字符、时间四种组合。**这是唯一能抓住 `sanitizeRule` 漏字段的用例**（漏字段的表现是「预览对、执行错、界面无异常」） | F-05 · DEC-15 |
| TC-49 | 时间跨月 / 跨年 / 闰年 | 起点 2026-12-30 增量 3；起点 2028-02-27 增量 1；起点 2026-09-30 增量 1 | `2027-01-02` / `2028-02-28`（闰年二月）/ `2026-10-01`。**绝不产出「2026-09-31」这种不存在的日期**（引擎用纯整数公历日序，禁 `Date`） | F-05 · DEC-15 |
| **TC-50** | 空列表时入口不可用 | 清空列表 | 「导出清单」与「清空列表」**同时置灰**；直调通道则返回 `E_LIST_EMPTY`，**不产出 0 行文件** | F-13 · DEC-17 |
| **TC-51** | **四种**格式都出得来 | 同一个列表分别导出 `.xlsx` / `.txt` / `.csv` / `.docx` | 四份都能打开；**行数 = 列表项数 + 1 行表头** | F-13 · DEC-17 |
| **TC-52** | 对照正确 | 导出后逐行比对屏幕上的「原名 → 新名」 | **逐条逐字节相等**（含 emoji 文件名、含 `,` `"` 的文件名） | F-13 · DEC-17 |
| **TC-53** | ★ **中文不乱码** | 用 **Windows 上的 Excel**（不是 VS Code）打开 `.csv` 与 `.txt` | 中文**全部正常显示**。若不带 BOM，这一条必红 | F-13 · DEC-17 |
| **TC-54** | 预览未算完时不可导出 | 改一次规则，立刻去点「导出清单」 | 按钮**处于置灰**；等预览完成后可点，且导出的新名是**当前屏幕上的那一份** | F-13 · DEC-17 |
| **TC-55** | ★ **`.docx` 在真 Word / WPS 里可读** | 用真 Word 或 WPS 文字打开导出的 `.docx` | ① **能打开、不报「文件已损坏」** ② 是**真表格**、**有线**、列不串 ③ 表头**加粗** ④ 超过一页时**表头行在每页重复** | F-13 · DEC-17 |
| 附 | 跨天守卫 | 把 `ExecuteRequest.date` 改成昨天再执行 | 被挡下并提示重新预览（**既有机制**；本批只把它写进文档 + 在规则区常显一句提示） | F-05 · DEC-03 |

> **编号说明**：P1 验收用例原在设计稿 `P1轻量设计确认.md` 中编为 `TC-19 ~ TC-24`，但 **TC-19 / TC-20 已被 v1.1.2 的「添加文件夹多选 / 记住上次位置」占用**。为保持三文档编号唯一，P1 顺延为 **TC-21 ~ TC-27**（设计稿已同步标注）。
>
> **P2-A / P2-C 编号修正（2026-09-15）**：P2-A 原编 `TC-27 ~ TC-31`，与 P1 的 `TC-27`（正则说明小白化）**撞车** —— 现顺延为 **`TC-28 ~ TC-32`**；P2-C 随之由 `TC-32 ~ TC-36` 顺延为 **`TC-33 ~ TC-37`**。
> 根因：设计 P2-A 时 grep 用的模式是 `TC-3[0-9]`，**匹配不到 TC-27~29**。教训：新增编号前「grep 全库」要按**前缀全量列出**（搜 `TC-`），不要按猜的区间搜。
> **P2-B 编号（2026-09-18）**：接 P2-C 的 `TC-37` 顺延为 **`TC-38 ~ TC-41`**（本批 4 条，详见 `P2-B轻量设计确认.md` §8）。

---

## 11. 定稿决策与决策锁

### 11.1 本文档新增的定稿决策

| 编号 | 决策 | 内容 | 影响面 |
| --- | --- | --- | --- |
| DEC-01 | 添加文件夹不递归 | 只加文件夹本身，不展开内部文件（2026-09-14 起对话框支持多选，语义不变） | MOD-1 |
| DEC-02 | 规则模式互斥 | 删除 / 替换 / 规则化三选一；规则化页签内多要素可叠加<br>⚠️ **已被 `DEC-16` 重开**（2026-09-18）：模式将由三选一扩到**五选一**（第 5 批落地）。**本批不改行为** —— 界面上仍是三选一 | MOD-2 |
| DEC-03 | 日期取当天 | 取执行时系统日期，非文件时间戳 | MOD-2 |
| DEC-04 | **拖拽提为 P0** | 与按钮添加并列为首版输入方式 | 需求范围 |
| DEC-05 | **冲突默认跳过** | 任何情况下不覆盖已有文件；提供「自动加序号」可选开关 | 安全底线 |
| DEC-06 | **撤销 20 条** | 保留最近 20 次任务，支持单条与全部撤销 | MOD-5 |
| DEC-07 | 不保存文件列表 | 每次启动从空列表开始，避免误操作 | 数据持久化 |
| **DEC-08** | **体积指标放宽**（v1.1.1 新增） | 「安装包 < 100 MB」改为**首版目标而非发布阻断项**。Electron 应用实测通常 75–95 MB，属紧目标；实测超标时**记录数值即可**，不为压体积牺牲安全防护或返工架构（换 Tauri 属架构级返工，不做） | 非功能需求 §8 / MS-5 |
| **DEC-09** | **历史条数上限取舍**（v1.1.1 新增） | 常态保留最近 **20 条**任务；但**明细总量设 5 万条上限**（防止 20 条 × 1 万项 = 20 万条 / 约 36 MB 导致历史页每次写入都卡顿）。极端场景下允许任务数**少于 20 条**（从最旧整条淘汰，**最新任务永不淘汰**），并在历史页显式提示「较旧的 N 条记录已被清理」。理由：撤销是「最近一次操作的兜底」，不是归档系统 | §6.2 / MOD-5 |
| **DEC-10** | **P1 范围与「进阶隔离」方案定稿**（2026-09-15 新增） | P1 **只做 F-10 正则 + F-11 大小写**，两者都是**正交选项**（正则 = 删除/替换模式内的匹配开关；大小写 = 模式结果算完后的收尾），**不需要重构 `computeNewStem`，DEC-02「模式互斥」不变**。隔离方案：**不做第四个页签**，改用默认折起的「⚙ 进阶设置」折叠条（PRD §3「需在 UI 上做进阶用户的隔离设计」的落地方式），正则开关只在删除/替换模式出现。「多规则链式组合」**归 P2**。详见 `P1轻量设计确认.md` v1.0 | MOD-2 / SCR-01 R-05 |
| **DEC-11** | **P2-A 设置面范围与深色主题方案定稿**（2026-09-15 新增） | ① **F-14 深色主题采用暖褐色深底**（不是冷灰黑）；**品牌橘三色两主题完全相同**；橘底上的前景（主按钮文字 / 危险按钮文字 / 复选勾）**两主题统一用深棕 `#2A1A10`**（8.3:1，顺手修掉浅色原本 2.03:1 的问题）。② 设置弹窗（SCR-07）**只放 3 项**（主题 / 音效 / 减少动画），**不暴露二次确认阈值**。③ 主题 **默认「始终浅色」**。④ 顺带补上 P0 遗留缺口：音效与「减少动画」两个开关此前**没有任何入口**（用户只能手改 `prefs.json`）。⑤ 主题切换**零新增 IPC 通道**（主进程 `nativeTheme.themeSource` + 渲染层 `prefers-color-scheme`）。详见 `P2-A轻量设计确认.md` v1.1 | F-14 / SCR-07 / 非功能需求 §8 |
| **DEC-12** | **P2-C 范围与历史数据安全阀**（2026-09-15 新增） | ① F-13「完整版」**只补两块**：完整明细（卡片内就地展开 + footer 清空入口）—— **不做导出、不做搜索 / 分页**（后者按 `数据库设计.md` 附录 A 需迁 SQLite，属独立工程批次）。② 明细**只渲染前 100 条**（超越部分提示「还有 N 项未显示」）—— 刻意取舍，避免几千条明细卡顿。③ ⚠️ **清空历史必须有数据安全阀**：若仍有「可撤销」任务，二次确认额外加红色警告块且主按钮改「仍然清空」（EX-17）。④ 清空后**必须重置淘汰检测基准**，否则用户主动清空会被假报成「较旧的记录已被清理」。⑤ F-15 命令行的**安全底线**：默认不覆盖 / 冲突跳过；**`--dry-run` 是默认行为，必须显式 `--yes` 才真改**。详见 `P2-C轻量设计确认.md` v1.2 | F-13 / F-15 / MOD-5 / SCR-02 / SCR-07 |
| **DEC-13** | **命令行小白化：说明重写 + 三步图文教程**（2026-09-17 新增） | 命令行入口（`DEC-12` 的 C 块）落地后，用户实测反馈「**普通小白用户根本不知道**」—— 入口做出来了，但用户看不懂那串命令是干嘛的。① **说明文案重写**：原文「给进阶用户用脚本批量改名」默认用户已懂「脚本」「.bat 文件」两个术语，改为**先讲使用场景**（「要让别的程序（或定时任务）自动帮你改名时用得上」）+ 立刻给台阶（「没接触过命令行也没关系，有图文教程」）。② 新增**引导卡 + 「查看教程」按钮**（EL-117 / EL-118）→ 打开 **SCR-08 三步图文教程**（`Win+R` 开命令行 → 命令拆四段照抄 → 先不加 `--yes` 试跑、确认后再加）。③ 教程图为 **SVG 线框示意图**，**刻意不随主题翻转** —— 它画的是「现实里那台机器上的黑窗口」，深色模式下改成浅灰反而与用户屏幕不一致。④ **不做一键生成 `.bat`**（用户原话：「这个暂时没必要，不做」）—— 教程只教怎么用，**不代用户往硬盘上写脚本**。⑤ 教程**只从设置里的那个按钮进入**（用户要求「教程最好能加载在上面的那个链接上」），不另开菜单。详见 `P2-C轻量设计确认.md` v1.4 §3.4 | F-15 / SCR-07 / SCR-08 |
| **DEC-14** | **P2-B 常用规则模板库的范围与两条判断**（2026-09-18 新增） | ① **模板 = 一份预填好的完整 `RuleConfig`**，套用是**整份替换**（不是只补不覆盖）—— 「套用」的语义必须明确：点完就知道自己现在的完整状态，不会出现「一半来自模板、一半是上次填的」。② **常显在规则区顶部、页签之前**，**不放折叠区** —— 折叠区是给进阶用户做隔离的（DEC-10），模板库是给不会配规则的人的一键入口，藏进去正好把目的反过来（**据此更正 `P1轻量设计确认.md` 里「折叠区是模板库预留挂载点」那句话**）。③ **不做「当前用的是哪个模板」的高亮**：用户套用后一改参数，高亮就成了谎话 —— 规则只有一个真源，不额外维护一份必然会过期的「来源」状态。④ **历史页的规则摘要只在显示层截断**（引号内超 14 字取前 12 字 + `…`，完整原文进 `title`）：`buildRuleSummary` 的返回值写进 `history.json`，在那一层截断等于**改变持久化数据**（新记录截断、老记录完整）。⑤ 只做 **6 个内置模板**，不做用户自定义（需要新增持久化 + 管理界面，且与推迟中的「多规则链」耦合）；**不加二次确认**（规则可再编辑，改坏了改回来很便宜，加确认会把「一键」变成「两步」）。详见 `P2-B轻量设计确认.md` v1.0 | F-12 / SCR-01 R-05 |
| **DEC-15** | **P3 第 1 批（编号系统升级）的范围与三个默认值取舍**（2026-09-18 新增） | ① 把 F-05 的序号能力扩成「**编号类型 4 种 + 位置 3 档 + 示例行**」，但**编号类型是序号组里的一个下拉，不是第四种模式**（模式互斥不变，见 DEC-16）。② 三个默认值取舍：**字母用大写**（Excel 列标的肌肉记忆，且与原文件名里的小写词视觉上分得开）；**随机字符只用小写字母 + 数字共 36 个字符**（不做「包含大写」开关 —— 那等于把一个已知的「Windows 大小写不敏感撞名」来源做成可选项）；**「长度」独立成字段**（不复用 `seqPad`，否则要把它的上限从 6 抬到 16，等于改既有断言）。③ **时间类型只到「日」**（要精确到时分得再加一格，且按天步进就失去意义）。④ **规则从来不落盘**（`StoreName` 只有 history / window / prefs），所以加 6 个字段**零迁移**。⑤ **老行为逐字节不变**：默认值组合（数字 / 起始 1 / 增量 1 / 位数 3 / 排在最后）= P0/P1 行为。⑥ **措辞与小白对齐**：「步长」→**增量**、「补零」→**位数**。⑦ **「仅使用编号作为文件名」本批不做**（归第 5 批）—— 顺带发现：现在就能等价做到（取消「保留原文件名」+ 前后缀留空 + 不启用日期），第 5 批只需决定要不要给它一个显眼入口。详见 `P3-1编号系统升级轻量设计确认.md` v1.1 §13 | F-05 / MOD-2 / R-05 |
| **DEC-17** | **P3 第 2 批（导出文件名清单）的范围与三条落盘安全底线**（2026-09-21 新增） | ① 第 2 批 = **导出文件名清单**（原名 / 新名对照），**不碰列表里任何文件**（只读内存里的列表、只写一个新文件）；「从文件名里抠一段当新名」归第 5 批模式层。② **四档格式**：xlsx / txt / csv / docx；**零运行时第三方依赖** —— xlsx 与 docx **自己写**（共用一个 ZIP 打包器）。③ **三条落盘安全底线**：**一律走系统「另存为」由用户亲手定路径**（程序绝不自己猜路径）、**绝不默认写到列表里那些文件的目录**（避免污染素材目录）、**只新建、绝不静默覆盖**（覆盖与否交给系统对话框自己问）。④ 为此**破例新增 1 个 IPC 通道**（`md:fs:exportList`，16 → 17）—— 对话框与落盘都只能在主进程，无法复用既有通道。⑤ **零持久化字段**（不记上次的格式与路径）。详见 `P3-2提取文件名轻量设计确认.md` v1.0 | F-13 / MOD-1 / 非功能需求 §8 |
| **DEC-16** | **三模式互斥 → 五模式互斥**（2026-09-18 新增，**正式重开 `DEC-02`**） | `DEC-02` 定的是「删除 / 替换 / 规则化三选一」。P3 第 1 批确认：**多规则链式组合继续推迟，但「模式」本身要从 3 个扩到 5 个**（加「插入」与「自定义」），所以 `DEC-02` 的字面表述**将在第 5 批被推翻**。⚠️ **为什么现在就记**：`DEC-02` 是**已关闭**的决策，一条被推翻的决策如果不留痕，下一个会话会拿着 `DEC-02` 去质疑第 5 批的设计 —— 项目规矩是「改上游必须同步下游」，决策也是如此。**本批只留痕、不改代码**：界面上三选一一个字都没动，`DEC-02` 的语义在第 1 批仍然成立 | MOD-2 / §4.2.1 / §12.2 |

### 11.2 决策锁校验

| 上游决策（v0.2 已关闭） | 本文档是否改变 |
| --- | --- |
| 形象素材：直接使用网络表情包（Accepted Risk） | **未改变** |
| 产品名称：沿用「耄耋改名」 | **未改变** |
| 技术栈：Electron | **未改变** |
| 预览 + 撤销：P0 安全项 | **未改变**（并细化） |
| 界面布局：横版宽屏左右分栏 | **未改变**（并细化尺寸） |

> **决策锁状态：17 项新增决策（DEC-01 ~ DEC-17）均为「向下补细节」「修正文档矛盾」或「指标口径调整」，不触碰任何已接受的风险决策。风险状态仍为 Accepted Risk。**
>
> ⚠️ 两处要单独点出：
> 1. 上一版这行写的是「13 项（DEC-01 ~ DEC-13）」，**P2-B 加了 DEC-14 时漏改**，本次一并对齐到 **DEC-16**。
> 2. **`DEC-16` 重开了 `DEC-02`** —— 这是本项目**第一次主动推翻一条已关闭的决策**。它不触碰任何已接受的风险决策，且**第 1 批不改行为**（五模式落地在第 5 批）。之所以现在就记：留痕是为了让下一个会话**不会**拿着 `DEC-02` 去质疑第 5 批的设计。

### 11.3 遗留待观察项（不阻塞开发）

| 项 | 说明 | 建议处理时机 |
| --- | --- | --- |
| 版权风险 | 仍为 Accepted Risk（非消除） | 发布前复核，保留一键换形象能力 |
| 舆论风险 | 形象演绎保持「可爱为主」 | 持续关注 |
| 正则规则（F-10） | ✅ **已关闭**（2026-09-15 / DEC-10）：P1 落地，隔离方案 = 默认折起的「⚙ 进阶设置」折叠条，**不做第四个页签** | 已落地 |
| **「设置入口缺失」P0 缺口** | ✅ **已关闭**（2026-09-15 / DEC-11）：`Prefs` 的音效 / 减少动画 / 二次确认阈值 / 主题此前**界面上一处都改不了**（用户只能手改 `prefs.json`）—— 交互说明 §9 与设计规范 §8 里「设置中提供『减少动画』开关」这条无障碍要求**此前达不到**。P2-A 建设置面（SCR-07）时一并补上 | 已落地 |
| 多规则链式组合 | **2026-09-15 明确归 P2，且已推迟**（原写在 P1）。DEC-02 限制了 MVP 能力；它才需要把 `computeNewStem` 重构为规则数组顺序执行，届时 DEC-02 需重新评审。挂载点已预留：P1 的「⚙ 进阶设置」折叠区。**触发条件仍是「若用户反馈强烈需重新评估」，目前零反馈** | 待用户反馈 |
| **「清空历史记录」入口** | ✅ **已关闭**（2026-09-15 / DEC-12）：`数据库设计.md` §4.7 早就给了方案（历史页 footer + 幽灵按钮，理由是从隐私出发）；P2-C 已把它连同**数据安全阀**一起定稿 | 已落地 |

---

## 12. 附录

### 12.1 名词表

| 名词 | 含义 |
| --- | --- |
| 主体（stem） | 文件名去掉扩展名后的部分 |
| 扩展名保护 | 规则不作用于扩展名 |
| 批次内冲突 | 同一批中两项改后同名 |
| 磁盘冲突 | 改后与目录下已有文件同名 |
| 两阶段改名 | 先改临时名再改目标名，避免循环冲突 |
| 落区 | 可接收拖拽的区域 |

### 12.2 变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v0.2 | 2026-09-11 | PRD 初稿（横版分栏、预览撤销 P0、Electron 选型） |
| v1.0 | 2026-09-11 | 定稿：补全功能详规 / 数据结构 / 异常流 / 验收用例；新增 7 项定稿决策；拖拽提 P0；重名冲突与撤销边界定稿 |
| **v1.1** | 2026-09-11 | **ST-03 动作重做**：废弃「双爪快速抽动 + 身体抖动 + 冒汗」（视觉有歧义），改为「一口一口咬掉旧名字」动画，图标类型随批次实际内容切换；§4.6.3 新增「动作不得有歧义」「撕咬要卡通化」两条设计原则；补「形象素材为占位、非最终美术」的说明 |
| **v1.1.1** | 2026-09-12 | **技术方案评审后的三处落定**：① 修正 §4.2.3 与 §4.2.5 的要素次序矛盾——**明确以 §4.2.5 表格为唯一依据**（次序由 `seqPosition` 决定），§4.2.3 收敛为只定义含义与默认值；② 新增 **DEC-08** 体积指标放宽（< 100 MB 改为首版目标、非发布阻断项）；③ 新增 **DEC-09** 历史条数上限取舍（常态 20 条，极端场景允许少于 20 条 + 界面提示）。同步修改 §6.2 与 §8 的对应表述 |
| **v1.1.2** | 2026-09-14 | **「添加文件夹」支持一次多选**：EL-021 对话框加 `multiSelections`（原为单选），一次可加入多个文件夹；对话框标题改为「选择要改名的文件夹（可多选）」；新增「记住上次所在目录」（仅本次运行有效）；新增验收用例 **TC-19**（多选）与 **TC-20**（记住上次位置）。**DEC-01 未变**：多选 ≠ 递归，选中的每个文件夹仍只加它本身 |
| **v1.1.3** | 2026-09-15 | **P1 范围与「进阶隔离」方案定稿（依 `P1轻量设计确认.md` v1.0）**：① §3.1 明确 P1 只做 F-10 + F-11（**均正交选项，DEC-02 不变**），「多规则链式组合」**移到 P2**；② 新增 **DEC-10** 记录该范围与隔离方案；③ §6.1 `RuleConfig` 新增 `regexEnabled` / `caseTransform` 两个字段并写明执行顺序；④ §10 新增验收用例 **TC-21 ~ TC-26**（**注意**：设计稿原编 TC-19~24，因 TC-19/20 已被 v1.1.2 占用，故顺延）；⑤ §11.3 关闭「正则规则（F-10）」遗留项 |
| **v1.1.4** | 2026-09-15 | **P1 正则说明小白化**（落地后反馈：「开启正则后看不懂」——「按正则表达式解释」属用术语解释术语，没接触过的人看完仍不知道要填什么）：① §10 新增验收用例 **TC-27**；② 明确的边界：**这只改「怎么说给小白听」，不改 F-10 的功能定义** —— P1 范围、5 条红线、DEC-02、DEC-10 全部未变，页签仍三选一；③ 具体做法：新增 EL-103（「? 看不懂？」按钮）/ EL-104（大白话对照 + 按模式过滤的照抄表）/ EL-105（固定示例名的当场演示），EL-058 收窄为「只保留非法时的红字原因」。同步《交互说明文档.md》v1.3、《设计规范.md》v1.3、《P1轻量设计确认.md》v1.1 |

| **v1.1.5** | 2026-09-15 | **P2-A / P2-C 设计定稿同步 + 验收编号修正**：① §3.1 补 F-13 / F-14 / F-15 各自的设计落点；② §5.1 界面清单新增 **SCR-07 设置弹窗**；③ §7 补 **EX-15**（正则非法，P1 漏登）/ **EX-16**（正则灾难性回溯，P1 漏登）/ **EX-17**（清空历史时仍有可撤销任务）；④ 新增 **DEC-11**（P2-A 设置面范围与深色主题方案）与 **DEC-12**（P2-C 范围与历史数据安全阀）；⑤ §10 新增验收用例 **TC-28 ~ TC-37**；⑥ §11.3 关闭「设置入口缺失」与「清空历史记录入口」两条遗留项，并把「多规则链」标注为**已推迟、待用户反馈**。⚠️ **验收编号修正**：P2-A 原编 `TC-27~31` 与 **P1 的 `TC-27` 撞车**，现顺延为 **TC-28~32**；P2-C 随之由 `TC-32~36` 顺延为 **TC-33~37**。同步《交互说明文档》《设计规范》《需求清单》《接口文档》《数据库设计》《技术方案文档》与两份 P2 设计确认稿 |
| **v1.1.6** | 2026-09-17 | **P2-C 增量 · 命令行小白化（DEC-13）**：① §3.1 F-15 补小白化落点；② §5.1 界面清单新增 **SCR-08 命令行图文教程弹窗**；③ 新增 **DEC-13**（说明文案重写 + 三步图文教程 + 明确不做一键生成 `.bat`）。<br>> **背景**：命令行入口（DEC-12 的 C 块）已落地，但用户实测反馈「**普通小白用户根本不知道**」—— 入口做出来了，用户却看不懂那串命令是干嘛的。**F-15 的完成标准因此从「入口存在」提到「小白也能上手」**。<br>> 本增量**不新增 TC 编号**（属 EL-119 的呈现细节），验收落在单测 `tests/p2c-guide.test.ts`（10 条）+ 冒烟 4 条 |
| **v1.1.8** | 2026-09-18 | **P3 立项 + 第 1 批（编号系统升级）落地（F-05 / DEC-15 / DEC-16）**：① 新增 **P3 段**（8 批清单，规矩「只增不删」；**第 7、8 批会复制 / 删除文件本身，必须单独走不可逆操作评审**）；② §4.2.2 的 **R-05 序号参数表重写** —— 新增 `seqKind` / `seqAt` / `seqRandomLen` / `seqRandomSeed` / `seqTimeStart` / `seqTimeFormat` 六个字段，**并顺手删掉原表里重复两遍的 `keepOriginal` 行**；③ R-06 日期格式由 3 档扩到 **5 档**；④ §10 新增验收用例 **TC-42 ~ TC-49**；⑤ 新增 **DEC-15**（第 1 批范围 + 三个默认值取舍）与 **DEC-16**（三模式互斥 → 五模式互斥，**正式重开 `DEC-02`**），并在 `DEC-02` 行上加「已被 DEC-16 重开」标注；⑥ §11.2 决策锁计数由 13 对齐到 **16**。<br>> **本批零新增通道、零新增持久化、零新增令牌、零新增界面（不新增 SCR）** —— 改动全部落在 R-05 规则区内部。|
| **v1.1.7** | 2026-09-18 | **P2-B 落地 · 常用规则模板库（F-12 / DEC-14）**：① §3.1 F-12 补落地形态（常显在 R-05 顶部、整份替换、不放进折叠区）；② §5.2 SCR-01 版图在 R-05 里画出「常用规则」一行；③ §10 新增验收用例 **TC-38 ~ TC-41**；④ 新增 **DEC-14**。**零新增界面（不新增 SCR）、零新增通道、零新增持久化、零新增令牌。**<br>> **背景**：需求清单原文只写了「内置常用规则（去广告、加日期、序号），一键套用」，没定义「套用」到底是替换还是叠加 —— 落地前必须先把语义定死，否则用户点下去不知道哪些会被改。 |

### 12.3 待办：与其它文档的一致性

| 文档 | 需要的同步 | 状态 |
| --- | --- | --- |
| 需求清单.md | F-07 提为 P0、新增 F-16、F-09 细化；**P2 分批与 F-13/F-14/F-15 细化**；**P2-B 的 F12 落地形态** | ✅ 已同步（v0.3.4） |
| 立项书.md | 新增 9.1 补充决策 | ✅ 已同步（v0.3） |
| 交互说明文档.md | P1 的 EL-056~059 / IX-051~054 / EX-15~16；P1 说明小白化 EL-103 ~ EL-105 / IX-093 ~ IX-094；**P2-A 的 SCR-07 / EL-106~110 / IX-095~099；P2-C 的 EL-111~116 / IX-100~103 / EX-17；P2-C 增量（DEC-13）的 EL-117~119 / IX-104~105；P2-B 的 EL-120 / IX-106** | ✅ 已同步（**v1.7**） |
| 设计规范.md | §1.6 深色主题、§4.1 按钮深棕字 + 幽灵危险按钮、§5.3 历史明细与清空、§5.5 SCR-07、§8.1 深色取舍、§8.2 令牌覆盖率；**§5.5 命令行引导卡、§5.6 SCR-08 图文教程（DEC-13）**；**§4.7 常用规则条、§5.3 历史摘要的显示层截断（P2-B / DEC-14）** | ✅ 已同步（**v1.6**） |
| 技术方案文档.md | §11 遗留项 6 改判（不重构、DEC-02 不变）、遗留项 10（EX-16 ReDoS 未落地）；**P2-A 主题切换机制、P2-C 通道 15→16；P2-B 遗留项 15 + `src/shared/templates.ts` 登记** | ✅ 已同步（**v1.0.5**） |
| 接口文档.md | **请求响应通道 15 → 16（新增 `md:history:clear`）；`AppInfo` 加 `execPath`** | ✅ 已同步 |
| 数据库设计.md | **§4.7 的「清空历史记录」从「建议补充」改为「已定稿」；§11 遗留项 1 关闭** | ✅ 已同步 |
| P1轻量设计确认.md | 验收编号 TC-19~24 → **TC-21~26**（与 TC-19/20 冲突） | ✅ 已同步（v1.1；含 §15 变更记录） |
| P2-A轻量设计确认.md | **验收编号 TC-27~31 → TC-28~32**（与 P1 的 TC-27 冲突） | ✅ 已同步（v1.2） |
| P2-C轻量设计确认.md | **验收编号 TC-32~36 → TC-33~37**（跟随 P2-A 顺延）；**C+ 增量（DEC-13）命令行小白化** | ✅ 已同步（**v1.4**） |
| P2-B轻量设计确认.md | 新建（P2-B 唯一权威）；**落地后状态改「已落地」、§10 同步表打勾、§12 答复留档** | ✅ 已同步（**v1.0**） |
| 需求清单.md（二次） | F-15 补「P2-C 增量小白化：说明重写 + SCR-08 三步图文教程；不做一键生成 .bat」 | ✅ 已同步 |
| PRD初稿.md | 已被本文档取代，建议归档 | ⬜ 建议改名加「_已归档」 |
