# 接口文档（IPC Contract）

> 产品名：**耄耋改名（MaoDie Renamer）**
> 文档版本：**v1.0.7**
> 日期：2026-09-18
> 上游文档：`PRD.md` v1.1.8 · `交互说明文档.md` v1.8 · `技术方案文档.md` v1.0.6 · **`P2-A轻量设计确认.md` v1.3** · **`P3-1编号系统升级轻量设计确认.md` v1.0**
> 配套文档：`数据库设计.md` v1.0.3 · `排期表.xlsx` v1.0
> 本文档作用：**主进程与界面之间的完整合同**。每个通道的名字、入参、返回、错误码、能不能取消，这里定死了；界面照这里调，主进程照这里实现，中间不需要口头沟通。

---

## 0. 阅读指南

### 0.1 这不是一份 HTTP 接口文档

本项目是**完全离线的桌面软件**，没有服务器、没有账号、没有网络请求（这是产品定位，也是技术方案 §7.3 的架构保证）。

所以本文档里的「接口」指的是两样东西：

| # | 接口形态 | 谁对谁说 | 本文档章节 |
| --- | --- | --- | --- |
| 1 | **IPC 通道** | 渲染进程 ⇄ 主进程 | §3 请求响应通道、§4 事件通道 |
| 2 | **`window.maodie` API** | 界面代码调用主进程能力的唯一入口 | §2 preload 暴露面 |

如果你是照着这份文档写界面，你**只会用到 §2 的 `window.maodie`**；§3 是实现主进程时照着填 handler 用的。

### 0.2 禁止事项先看一遍

写代码前请先读 **§7 代码层禁止事项**。那份清单不是建议，是**会在代码评审和 lint 里拦你**的硬规则。本产品要动用户的真实文件，「绝不覆盖」这件事靠的就是这几条。

### 0.3 通道总览（一眼看全）

| 域 | 通道数 | 前缀 | 干什么 |
| --- | --- | --- | --- |
| 应用 | 3 | `md:app:` | 版本信息、偏好设置 |
| 窗口 | 4 + 1 事件 | `md:window:` | 自绘标题栏的三按钮、尺寸状态 |
| 文件系统 | **4** | `md:fs:` | 系统选择框、路径解析入列、**导出清单（P3-2）** |
| 改名 | 2 + 1 事件 | `md:rename:` | 执行、取消、进度 |
| 历史 | 4 | `md:history:` | 列表、单条撤销、全部撤销、**清空（P2-C）** |
| **合计** | **18 个请求响应通道 + 3 个事件通道** | — | — |

---

## 1. 契约总览

### 1.1 三层边界

```
┌──────────────────────────────────────────────────────────────┐
│ 渲染进程（Vue3）                                              │
│   ↓ 只调用 window.maodie.*（§2）                              │
├──────────────────────────────────────────────────────────────┤
│ 预加载（preload/index.ts）                                    │
│   ├─ 逐个登记白名单通道（没登记的通道在渲染层不存在）           │
│   ├─ 做入参形状校验（开发环境不合法直接报错）                   │
│   └─ 把事件监听包装成「返回取消函数」的形式                     │
├──────────────────────────────────────────────────────────────┤
│ 主进程 ipc/*.ipc.ts                                           │
│   ├─ 每个 handler 只做「参数校验 → 调 service → 包装返回」      │
│   └─ 业务逻辑全在 services/*（可单独单测）                     │
└──────────────────────────────────────────────────────────────┘
```

**一条硬规则**：`ipc/*.ipc.ts` 里**不允许写业务逻辑**。handler 超过 20 行就说明逻辑该挪进 `services/`。理由：`services/` 能被 Vitest 直接测，IPC handler 不能。

### 1.2 命名规范

```
md:<域>:<动作>
```

| 部分 | 取值 | 规则 |
| --- | --- | --- |
| `md:` | 固定 | 产品前缀，避免与 Electron 内置或其他库的通道撞名 |
| `<域>` | `app` / `window` / `fs` / `rename` / `history` | 一个域一个文件，见 §3 |
| `<动作>` | 小驼峰动词短语 | `resolvePaths`、`undoTask`、`toggleMaximize` |

**事件通道**用 `<域>:<过去式 / 名词>` 形式表示「已发生」：`md:rename:progress`、`md:window:maximizeChanged`。

> 全部通道名集中在 `src/shared/channels.ts` 里定义为 `const`，**主进程与 preload 都从这里 import**，杜绝「一边写 `md:fs:pickFiles`、另一边写 `md:fs:pickfiles`」这类拼写事故。

### 1.3 通用约定

#### 1.3.1 返回值：两种通道，两种约定

| 通道类型 | 哪些通道 | 约定 |
| --- | --- | --- |
| **简单通道** | `md:app:*`、`md:window:*` | **直接返回数据**。这些通道只操作内存，失败即「程序有 bug」，因此用 `Promise.reject` 表达，不做业务错误包装 |
| **业务通道** | `md:fs:*`、`md:rename:*`、`md:history:*` | **统一返回 `MdResult<T>`**，业务失败**不 reject**，而是 `{ ok: false, code, detail }` |

```ts
// src/shared/types.ts
export type MdResult<T> =
  | { ok: true; data: T }
  | { ok: false; code: MdErrorCode; detail?: string };
```

> **为什么业务失败要 resolve 而不是 reject？**
> 批量改名里「部分失败」是**正常业务流程**（某个文件被占用），不是异常。若用 `reject` 表达，调用方就必须到处 `try/catch`，一旦漏掉就是未捕获的 Promise rejection，最坏情况下界面卡在「执行中」状态出不来。
> 把失败当数据返回，`switch (res.code)` 就处理完了，**编译器还会强制你考虑失败分支**（因为 `ok: false` 分支必须有返回）。

#### 1.3.2 序列化约束（IPC 用结构化克隆）

| 约束 | 说明 |
| --- | --- |
| **不传函数、Promise、class 实例、Symbol** | 结构化克隆不支持，会静默丢字段 |
| **时间一律用 `number`（毫秒时间戳）或 `YYYY-MM-DD` 字符串** | 不传 `Date` 对象。理由：这些数据还要写进 JSON 文件（`数据库设计.md`），统一格式省掉一层转换 |
| **不传 `Map` / `Set`** | 用普通对象或数组。`Map` 能克隆，但 JSON 存不了，出入口格式不一致容易出错 |
| **`undefined` 会丢键，`null` 会保留** | 需要「明确无值」时用 `null`；可选字段用 `?` 并在主进程侧兜底 |
| 路径一律 **Windows 绝对路径反斜杠形式**（`C:\Users\PC\a.txt`） | 跨进程统一，渲染层不做路径拼接 |
| **`RuleConfig` 的字段只许是 `string` / `number` / `boolean` / 字面量联合** | P3-1 新增的 6 个字段（`seqKind` / `seqAt` / `seqRandomLen` / `seqRandomSeed` / `seqTimeStart` / `seqTimeFormat`）全部属于这四类。**`seqTimeStart` 是 `YYYY-MM-DD` 字符串、不是 `Date`** —— 时间也走字符串，结构化克隆与 JSON 都不会把它变形（这一条正是「时间类型能保证预览 ≡ 执行」的前提） |

#### 1.3.3 超时与取消

| 项 | 约定 |
| --- | --- |
| 超时 | **invoke 通道不设超时**。`md:rename:execute` 处理 10000 项可能跑十几秒，设超时会导致中途被误判失败 |
| 取消 | 只有 `md:rename:execute` 可取消，通过 `md:rename:cancel` 触发（不同通道并发调用，互不阻塞） |
| 重复调用 | `md:rename:execute` 在已有任务运行中再次调用 → 返回 `{ ok: false, code: 'E_TASK_RUNNING' }`（不做排队，不做抢占） |

#### 1.3.4 幂等性

| 通道 | 幂等性 |
| --- | --- |
| `md:history:undoTask` | **安全性幂等**：已撤销的任务再撤销 → `{ ok: false, code: 'E_UNDO_ALREADY' }`，**不会二次改名** |
| `md:history:undoAll` | 同上。无可撤销任务时返回 `{ ok: true, data: { taskCount: 0, ... } }` |
| `md:rename:execute` | **不幂等**（改名是副作用操作）。由 `taskId` + 「执行中拒绝」共同保证不会被重复触发 |
| `md:fs:resolvePaths` | 幂等（只读扫描） |
| `md:fs:exportList` | 异步落盘（先弹「另存为」，再写临时文件并原子改名）—— **P3-2 / DEC-17** |
| `md:fs:importTable` | **P3-4 / DEC-19**（17 → 18）：系统「打开」对话框选表 → 读字节 → 解析成 `ImportedTable`。**无入参** —— 选哪个文件完全由对话框决定，渲染层连一个字段都传不进来（少一处收口点 = 少一处漏网）。只读，**不写任何文件** |
| `md:window:*`、`md:app:*` | 幂等 |

---

## 2. preload 暴露面（界面唯一入口）

`src/preload/index.ts` 通过 `contextBridge.exposeInMainWorld('maodie', api)` 暴露下面这一个对象。**界面代码里 `window.maodie` 是唯一能触达主进程的入口**，其他一律不存在。

```ts
// src/shared/types.ts —— 界面可用的完整 API 形状
export interface MaoDieAPI {
  app: {
    getInfo(): Promise<AppInfo>;
    getPrefs(): Promise<Prefs>;
    setPrefs(patch: Partial<Prefs>): Promise<Prefs>;
    /** 返回取消函数 */
    onStorageWarning(cb: (p: StorageWarningPayload) => void): () => void;
  };

  window: {
    getState(): Promise<WindowState>;
    minimize(): void;
    /** 返回切换后的最大化状态 */
    toggleMaximize(): Promise<boolean>;
    close(): void;
    /** 返回取消函数 */
    onMaximizeChanged(cb: (p: { maximized: boolean }) => void): () => void;
  };

  fs: {
    pickFiles(): Promise<MdResult<{ canceled: boolean; paths: string[] }>>;
    pickDirectory(): Promise<MdResult<{ canceled: boolean; path: string | null }>>;
    resolvePaths(req: ResolvePathsRequest): Promise<MdResult<ResolvedBatch>>;
  };

  rename: {
    execute(req: ExecuteRequest): Promise<MdResult<ExecuteResult>>;
    cancel(req: { taskId: string }): Promise<MdResult<{ canceled: boolean; rolledBack: number }>>;
    /** 返回取消函数 */
    onProgress(cb: (p: ProgressPayload) => void): () => void;
  };

  history: {
    list(): Promise<MdResult<RenameTask[]>>;
    undoTask(req: { taskId: string }): Promise<MdResult<UndoResult>>;
    undoAll(): Promise<MdResult<UndoAllResult>>;
  };
}
```

### 2.1 命名空间语义

| 命名空间 | 语义 | 对应界面 |
| --- | --- | --- |
| `app` | 与「这次运行的环境」有关的只读信息 + 用户偏好 | 设置、关于 |
| `window` | **无边框窗口**的控制。注意：这里的 `window` 是**进程级窗口**，与浏览器的 `globalThis.window` 无关，不要写混 | R-01 标题栏三按钮 |
| `fs` | 只读的文件系统交互 + 系统原生对话框 | R-03 添加按钮、拖拽落区 |
| `rename` | 有副作用的改名操作 | R-06 动作区 |
| `history` | 撤销与历史 | SCR-02 |

### 2.2 preload 实现骨架（照此形状写）

```ts
// src/preload/index.ts
import { contextBridge, ipcRenderer } from 'electron';
import { CH } from '../shared/channels';

/** 包装「主进程 → 渲染」的事件监听，返回取消函数，避免界面侧忘记 off */
function subscribe<T>(channel: string, cb: (p: T) => void): () => void {
  const h = (_e: unknown, payload: T) => cb(payload);
  ipcRenderer.on(channel, h);
  return () => ipcRenderer.removeListener(channel, h);
}

const api = {
  app: {
    getInfo: () => ipcRenderer.invoke(CH.APP_GET_INFO),
    getPrefs: () => ipcRenderer.invoke(CH.APP_GET_PREFS),
    setPrefs: (patch: unknown) => ipcRenderer.invoke(CH.APP_SET_PREFS, patch),
    onStorageWarning: (cb: (p: any) => void) => subscribe(CH.EV_APP_STORAGE_WARNING, cb),
  },
  // …其余命名空间同形
};

contextBridge.exposeInMainWorld('maodie', Object.freeze(api));
```

> **`Object.freeze` 不是装饰**：它防止界面代码（或被注入的脚本）替换掉 `api.rename.execute`。成本为零。

### 2.3 preload 里不许做的事

| 禁止 | 原因 |
| --- | --- |
| 暴露 `ipcRenderer` 本身 | 等于把「任意通道调用」的能力交出去，白名单形同虚设 |
| 暴露 `require` / `process` / `Buffer` / `__dirname` | 直接给渲染层 Node 权限，`nodeIntegration: false` 就白设了 |
| 暴露 `shell.openExternal` | 可以打开外部程序 / 网址，与「零网络请求」定位冲突 |
| 在 preload 里写 if/else 业务判断 | 业务逻辑要放进 `shared` 或主进程 `services`，这样它才可测 |
| 用 `ipcRenderer.send` 代替 `invoke` | 除 `md:window:minimize` / `close` 这两个「发了就不管」的通道外，一律用 `invoke` |

---

## 3. 请求响应通道

> 每个通道按固定模板写：**通道名 · 方向 · 入参 · 返回 · 错误码 · 备注**。

### 3.1 `md:app:*` — 应用信息与偏好

#### `md:app:getInfo`

| 项 | 内容 |
| --- | --- |
| 方向 | renderer → main（invoke） |
| 入参 | 无 |
| 返回 | `AppInfo` |
| 错误码 | 无（失败即 reject，属编程错误） |
| 用途 | 关于页、调试信息、判断是否开发环境 |

```ts
export interface AppInfo {
  version: string;          // 应用版本，如 "1.0.0"
  electronVersion: string;
  chromeVersion: string;
  platform: 'win32';        // 首版只支持 win32
  arch: 'x64' | 'arm64';
  userDataPath: string;     // 供「打开数据目录」类功能使用
  execPath: string;         // P2-C：可执行文件路径，供设置里「复制程序路径」
  isDev: boolean;
}
```

#### `md:app:getPrefs`

| 项 | 内容 |
| --- | --- |
| 方向 | renderer → main（invoke） |
| 入参 | 无 |
| 返回 | `Prefs`（见 §5.4；文件不存在时返回默认值，**不报错**） |

#### `md:app:setPrefs`

| 项 | 内容 |
| --- | --- |
| 方向 | renderer → main（invoke） |
| 入参 | `Partial<Prefs>`（只传要改的字段） |
| 返回 | 合并后的完整 `Prefs` |
| 备注 | 主进程合并 → 原子写 `prefs.json` → 返回合并结果（**返回完整对象，让界面不必自己猜合并结果**） |

```ts
// 入参示例
{ reduceMotion: true }
// 返回示例
{ version: 1, soundEnabled: true, reduceMotion: true, confirmThreshold: 10, theme: 'light' }
```

#### 事件 `md:app:storageWarning`

| 项 | 内容 |
| --- | --- |
| 方向 | main → renderer（send） |
| 载荷 | `StorageWarningPayload` |
| 用途 | 数据文件损坏 / 写失败时通知界面（技术方案 §4.2.3） |

```ts
export interface StorageWarningPayload {
  file: 'history' | 'window' | 'prefs';
  kind: 'corrupted' | 'write_failed';
  backupPath?: string;   // corrupted 时，坏文件被备份到哪里
  message: string;       // 可直接显示给用户的中文文案
}
```

> 界面收到后显示为**状态栏提示**（不弹窗，不打断操作）。文案示例：「历史记录文件损坏，已重置」。
> **注意**：这个事件在启动早期就可能触发，`StatusBar` 组件可能还没挂载。因此 `App.vue` 必须在 `onMounted` 最前面就注册监听，并把拿到的警告暂存到 store，等 StatusBar 挂载后再取。

### 3.2 `md:window:*` — 无边框窗口控制

> 本项目用 `frame: false` + 自绘标题栏（设计规范 §5.1 R-01 高 44px）。所以三个系统按钮的行为要自己实现。

#### `md:window:getState`

| 项 | 内容 |
| --- | --- |
| 方向 | renderer → main（invoke） |
| 入参 | 无 |
| 返回 | `WindowState` |
| 用途 | 启动时同步「最大化按钮该显示哪个图标」 |

```ts
export interface WindowState {
  width: number;
  height: number;
  x: number;
  y: number;
  maximized: boolean;
}
```

#### `md:window:minimize`

| 项 | 内容 |
| --- | --- |
| 方向 | renderer → main（**send，单向**） |
| 入参 | 无 |
| 返回 | 无 |
| 备注 | 唯一允许用 `send` 的通道之一：发了就不需要知道结果 |

#### `md:window:toggleMaximize`

| 项 | 内容 |
| --- | --- |
| 方向 | renderer → main（invoke） |
| 入参 | 无 |
| 返回 | `boolean`（切换后的最大化状态） |
| 备注 | 返回新状态，界面立即更新图标；同时也会发 `md:window:maximizeChanged` 事件（用于「双击标题栏」等不经过按钮的路径） |

#### `md:window:close`

| 项 | 内容 |
| --- | --- |
| 方向 | renderer → main（**send，单向**） |
| 入参 | 无 |
| 返回 | 无 |
| 备注 | **不弹「确认退出」**（交互说明 §2.3 EL-004 已定）。关闭前主进程会先把 `window.json` 写完 |

#### 事件 `md:window:maximizeChanged`

| 项 | 内容 |
| --- | --- |
| 方向 | main → renderer（send） |
| 载荷 | `{ maximized: boolean }` |
| 触发 | 窗口最大化状态变化（含用户双击标题栏、Win+↑ 等系统快捷键路径） |

### 3.3 `md:fs:*` — 文件系统与系统对话框

#### `md:fs:pickFiles`

| 项 | 内容 |
| --- | --- |
| 方向 | renderer → main（invoke） |
| 入参 | 无 |
| 返回 | `{ canceled: boolean; paths: string[] }` |
| 错误码 | `E_UNKNOWN` |
| 对应 | EL-020 / IX-020；快捷键 `Ctrl+O`（交互说明 §9） |

```ts
// main 侧实现要点
const r = await dialog.showOpenDialog(win, {
  title: '选择要改名的文件',
  properties: ['openFile', 'multiSelections'],
  filters: [{ name: '所有文件', extensions: ['*'] }],
});
return { ok: true, data: { canceled: r.canceled, paths: r.filePaths } };
```

> **注意**：拿到路径后**不要**立即入列。界面必须把返回的 `paths` 攒起来，统一调一次 `md:fs:resolvePaths`。理由：`resolvePaths` 会做去重、去除非文件对象、取目录快照，这些都是**批量**操作，逐个调用会重复 `readdir` 同一目录。

#### `md:fs:pickDirectory`

| 项 | 内容 |
| --- | --- |
| 方向 | renderer → main（invoke） |
| 入参 | 无 |
| 返回 | `{ canceled: boolean; path: string \| null }` |
| 对应 | EL-021 / IX-021；**DEC-01：只加入文件夹本身，不递归内部文件** |

```ts
const r = await dialog.showOpenDialog(win, {
  title: '选择要改名的文件夹',
  properties: ['openDirectory'],   // ★ 绝不能加 'multiSelections' 之外的递归相关项
});
```

> **DEC-01 的实现保证**：主进程在此通道里**永远只返回选中的那一个路径**，不做任何子项枚举。递归能力从接口形状上就不存在——界面拿到的就是一个路径字符串。

#### `md:fs:resolvePaths` ★ 入列的核心通道

| 项 | 内容 |
| --- | --- |
| 方向 | renderer → main（invoke） |
| 入参 | `ResolvePathsRequest` |
| 返回 | `ResolvedBatch` |
| 错误码 | `E_OVERFLOW`（超上限时**仍返回 ok:true**，超额数在 stats 里，见下）、`E_UNKNOWN` |
| 对应 | IX-020 / IX-021 / IX-022 / EX-08 / EX-09 / EX-13 |

```ts
export interface ResolvePathsRequest {
  /** 本次新加入的原始路径（来自对话框或拖拽） */
  paths: string[];
  /** 已在列表中的路径，用于去重（不区分大小写） */
  existingPaths: string[];
}

export interface ResolvedBatch {
  /** 新入列的项，已填好 id / stem / ext / isDir 等 */
  items: FileItem[];
  /** 目录快照：dirPath → 该目录下的现存名称清单（供预览 Worker 判磁盘冲突） */
  snapshot: Record<string, string[]>;
  stats: {
    accepted: number;            // 实际入列数
    ignoredDuplicates: number;   // EX-09 重复项
    overflow: number;            // EX-13 超限被丢弃数
    rejected: number;            // EX-08 非文件对象
    vanished: number;            // 扫描时已不存在（含断链 symlink）
  };
}
```

**处理顺序（严格按此顺序，与 `技术方案文档 §4.2.2` 一致）**

```
1. 过滤非文件系统对象（拖入的网页链接 / 纯文本）      → stats.rejected
2. 路径归一化（统一分隔符、path.resolve、去末尾分隔符）
3. 去重：与 existingPaths + 本批内已见路径比对（不区分大小写） → stats.ignoredDuplicates
4. 逐项 lstat：
     ├─ 不存在 / 断链 symlink                        → stats.vanished
     ├─ 是 symlink → 再 stat 判断目标类型：
     │     ├─ 目标是目录 → isDir = true,  isSymlink = true
     │     └─ 目标是文件 → isDir = false, isSymlink = true
     ├─ 普通目录  → isDir = true
     └─ 普通文件  → isDir = false
5. 上限裁剪：accepted + existingPaths.length > 10000 的部分丢弃 → stats.overflow
6. 目录快照：收集本批涉及的所有 dirPath（去重）→ 并发 readdir（并发度 8，跳错不中断）
7. splitName 填 stem / ext
8. 生成 id（nanoid 或 crypto.randomUUID）
```

**`stats` 对应界面反馈（交互说明 §11.2）**

| stats | 状态栏文案（3 秒后恢复常态） |
| --- | --- |
| `ignoredDuplicates > 0` | 已忽略 N 个重复项 |
| `overflow > 0` | 单批最多 1 万个，超出部分未加入 |
| `rejected > 0` | 只能拖入文件或文件夹哦 |
| `vanished > 0` | 有 N 个文件找不到了，已跳过 |
| 多项同时命中 | 依次追加，最多显示 2 条（避免状态栏溢出） |

> **符号链接的处理政策**（技术方案 §4.2.2）必须原样保留在实现里：**只改链接自身的名字，绝不 `realpath` 解引用**。因为解引用会把「改链接名」变成「改目标名」，是危险行为。

> **关于 `snapshot` 的体积**：如果用户拖入的是一个包含 5000 个文件的大目录里的 1000 个文件，`snapshot` 会带回 5000 个名称字符串（约 150 KB）。这个量级的结构化克隆约 1–3ms，可以接受。**若将来发现卡顿**，可改为「只传 `Set` 化的小写数组 + 按需分页」，但首版不做（YAGNI）。

### 3.4 `md:rename:*` — 改名执行

#### `md:rename:execute` ★ 最重要的一条通道

| 项 | 内容 |
| --- | --- |
| 方向 | renderer → main（invoke） |
| 入参 | `ExecuteRequest` |
| 返回 | `ExecuteResult` |
| 错误码 | `E_TASK_RUNNING`、`E_UNKNOWN` |
| 对应 | IX-060 / IX-061 / IX-062 / IX-063；TC-07 / TC-08 / TC-12 / TC-15 |

```ts
export interface ExecuteRequest {
  /** 本次任务的唯一 id，由渲染层生成（UUID），用于临时名与进度关联 */
  taskId: string;
  /** 待处理清单：只传算新名所需的最小信息 */
  items: Array<{
    id: string;
    dirPath: string;
    fromName: string;   // 当前名称（含扩展名）
    isDir: boolean;
  }>;
  rule: RuleConfig;
  /** 执行当天的日期，格式 YYYY-MM-DD。主进程会校验它等于本机今天 */
  date: string;
  /** 冲突策略：false = 跳过（默认，DEC-05）；true = 自动加序号 */
  autoResolveConflict: boolean;
}
```

> ### ★ 为什么入参不含「已经算好的新名」
>
> 这是 ADR-004 的落地。**主进程必须用同一份 `shared` 引擎自己重算一遍。**
>
> 若渲染层把算好的新名传进来，主进程就是在「照着别人给的名字改文件」——一旦渲染层有 bug（或有人改了渲染层代码），主进程会毫无察觉地执行错误的名字。**让主进程验证它自己将执行的东西**，这一条换来的性质是：
>
> > **主进程永远不需要信任渲染层。界面有 bug 也改不错文件。**
>
> 代价是 1000 项约 5–15ms 的重复计算。**明确接受**。

**执行流程**（详见 `技术方案文档 §4.2.1`）

```
阶段 0  构建计划（此阶段完成全部判定，执行器本身不做决策）
          0.1 校验 date == 本机今天（不一致则用本机今天并记日志）
          0.2 按 dirPath 分组 → readdir → 构造 DirSnapshot
              ★ 必须剔除「本批次在该目录的源名称」
          0.3 computeNewStem → joinName → validateNewName
          0.4 detectConflicts（按 autoResolveConflict 决定 skip 还是加序号）
          0.5 输出计划：ready / skipped / invalid
阶段 1  原名 → __md_tmp_<taskId>_<6位序号>（每步先查存在性）
阶段 2  临时名 → 目标名（每步先查存在性 —— 最后一道闸门）
收尾    汇总 entries → 写历史记录 → 返回
```

```ts
export interface ExecuteResult {
  taskId: string;
  canceled: boolean;
  summary: {
    total: number;      // 入参 items 总数
    success: number;    // 改名成功
    skipped: number;    // 冲突被跳过
    invalid: number;    // 校验未通过（非法字符 / 空名 / 超长）
    failed: number;     // 执行中失败（被占用 / 无权限 / 源不存在）
  };
  /** 成功示例，最多 3 条（对应 SCR-03 的「成功举例」折叠区） */
  successExamples: Array<{ dirPath: string; fromName: string; toName: string }>;
  /** 失败明细（含被跳过项），最多 200 条，见下方说明 */
  problems: Array<{
    id: string;
    dirPath: string;
    fromName: string;
    attemptedName: string;     // 尝试的目标名
    result: 'skipped' | 'failed' | 'invalid';
    code: MdErrorCode;
    reason: string;            // 可直接显示的中文文案
  }>;
  /** problems 被截断时的总数，用于界面显示「共 N 项，显示前 200 项」 */
  problemsTotal: number;
  problemsTruncated: boolean;
  /** 是否成功写入历史记录（false 时界面需提示「本次改名无法撤销」） */
  recordSaved: boolean;
  elapsedMs: number;
}
```

> **`problems` 为什么限 200 条**：PRD §4.4.4 要求「失败 / 跳过清单逐条列出」。但如果一次改 10000 项、几千项失败，全量传输会让 IPC 变慢、界面渲染卡死，而用户也读不完。**200 条 + 总数**是务实的折衷：能看到典型问题，也知道总量。界面文案：「共 1203 项异常，显示前 200 项」。
>
> **`recordSaved: false` 是必须处理的边界**：如果历史写入失败（磁盘满 / 权限问题），用户此后**无法撤销本次改名**。界面必须在结果弹窗里**明确警告**，而不是让用户以为还能撤销。这条对应 `数据库设计.md` §4.8 的失败处理。

#### `md:rename:cancel`

| 项 | 内容 |
| --- | --- |
| 方向 | renderer → main（invoke，**与 `execute` 并发**） |
| 入参 | `{ taskId: string }` |
| 返回 | `{ canceled: boolean; rolledBack: number }` |
| 错误码 | `E_TASK_NOT_FOUND`、`E_UNKNOWN` |
| 对应 | IX-061 |

```ts
export interface CancelRequest { taskId: string; }
export interface CancelResult {
  canceled: boolean;   // 是否真的中断了（任务已完成时返回 false）
  rolledBack: number;  // 被回滚的项数（界面文案：「改了 N 项已全部改回去」）
}
```

**回滚语义（必须准确实现）**

| 条目当前状态 | 回滚动作 |
| --- | --- |
| 还没动 | 无需处理 |
| 已改成临时名 | `tmp → fromName` |
| 已改成目标名 | `toName → fromName` |
| 失败 | 无需处理（失败时已回滚或原名未动） |

- 回滚**按 `tmp` / `done` 状态的逆序**执行，避免中间态撞名。
- 回滚**不中断**：某一项回滚失败时记 `E_UNKNOWN` 并继续，最后的 `rolledBack` 只统计真正成功的项；界面需要能表达「有 3 项没能回滚」的情况（状态栏提示即可）。
- 回滚成功后形象回到 **ST-01（待机）**，**不判为失败**（交互说明 §7.2）。

#### 事件 `md:rename:progress`

| 项 | 内容 |
| --- | --- |
| 方向 | main → renderer（send） |
| 载荷 | `ProgressPayload` |
| 频率 | 每 50 项 **或** 距上次推送 > 100ms（两者取先到） |
| 对应 | IX-062 |

```ts
export interface ProgressPayload {
  taskId: string;
  phase: 'planning' | 'to_temp' | 'to_target' | 'rolling_back';
  done: number;
  total: number;
  /** 当前正在处理的名称，供 ST-03 卡片与状态栏展示 */
  currentName: string | null;
}
```

| phase | 界面表现 |
| --- | --- |
| `planning` | 状态栏「正在准备…」，进度条 0%，ST-03 开始播 |
| `to_temp` / `to_target` | 状态栏「已处理 N / M」，进度条按 `done/total` 推进 |
| `rolling_back` | 状态栏「正在撤销已完成的改名…」，进度条反向或显示为特殊的「取消中」配色 |

> **节流的必要性**：10000 项若每项推一次，就是 10000 次 IPC + 10000 次 Vue 更新，界面必然卡死。50 项 / 100ms 的节流把 10000 项压到约 200 次推送。
> **进度不可回退**：`to_temp` 与 `to_target` 是两个阶段，各自的 `done` 都从 0 开始。界面要按 `phase` 分别展示（例如整体进度 = 阶段序号 × 0.5 + 阶段内进度 × 0.5），**不要简单地把两次 `done` 相加**。

### 3.5 `md:history:*` — 撤销与历史

#### `md:history:list`

| 项 | 内容 |
| --- | --- |
| 方向 | renderer → main（invoke） |
| 入参 | 无 |
| 返回 | `RenameTask[]`（**最新在前，常态最多 20 条**，DEC-06。极端场景下可能少于 20 条——明细总量受上限治理，见 DEC-09 与 `数据库设计.md` §4.4.4） |
| 错误码 | `E_UNKNOWN` |
| 对应 | EL-081 |

> 文件不存在或损坏时返回 **空数组**（不报错），并已通过 `md:app:storageWarning` 通知过界面。

#### `md:history:undoTask`

| 项 | 内容 |
| --- | --- |
| 方向 | renderer → main（invoke） |
| 入参 | `{ taskId: string }` |
| 返回 | `UndoResult` |
| 错误码 | `E_UNDO_ALREADY`、`E_TASK_NOT_FOUND`、`E_UNKNOWN` |
| 对应 | IX-071；TC-09 / TC-10 |

```ts
export interface UndoResult {
  taskId: string;
  status: 'ok' | 'partial' | 'failed' | 'already_undone';
  restored: number;          // 成功还原的项数
  skipped: Array<{ dirPath: string; fromName: string; toName: string; code: MdErrorCode; reason: string }>;
  elapsedMs: number;
}
```

**实现约束（缺一不可）**

| 约束 | 说明 |
| --- | --- |
| 倒序执行 | 按 `entries` **逆序**逐项 `toName → fromName`，避免中间态撞名（例如批次含 `A→B`、`B→C`，必须先把 `B→C` 还回去） |
| 前置校验 | 任务 `status` 必须为 `'active'`，否则返回 `E_UNDO_ALREADY` |
| 源不存在即跳过 | `toName` 不存在 → 记 `E_UNDO_SOURCE_GONE` 到 `skipped`，**继续处理其余项**（EX-10） |
| 不产生新记录 | 撤销**不写入新历史**（避免「撤销的撤销」），只把该任务 `status` 改为 `'undone'` |
| 状态回退 | 成功后该任务标记 `'undone'`，界面灰显 + 「已撤销」徽标，**不再可撤销** |

#### `md:history:undoAll`

| 项 | 内容 |
| --- | --- |
| 方向 | renderer → main（invoke） |
| 入参 | 无 |
| 返回 | `UndoAllResult` |
| 错误码 | `E_UNKNOWN` |
| 对应 | IX-072；TC-11 |

```ts
export interface UndoAllResult {
  taskCount: number;         // 参与撤销的任务数（原本 status === 'active' 的）
  undoneTasks: number;       // 成功撤销的任务数
  restoredFiles: number;     // 成功还原的文件总数
  skippedTotal: number;      // 被跳过的项总数
  perTask: Array<{
    taskId: string;
    status: 'ok' | 'partial' | 'failed' | 'already_undone';
    restored: number;
  }>;
}
```

**实现约束**

| 约束 | 说明 |
| --- | --- |
| 顺序 | 把 `status === 'active'` 的任务**按时间倒序**（最新先撤）逐条调用 `undoTask` |
| 不中断 | 任一条失败（如 `partial`）**继续处理下一条**，最后汇总 |
| 二次确认 | 由界面负责。确认弹窗文案见 `设计规范 §5.4`：「将把 N 个任务、共 M 个文件的名字全部改回去，确定吗？」 |
| 空历史 | 无可撤销任务时返回 `{ ok: true, data: { taskCount: 0, restoredFiles: 0, ... } }`，界面提示「还没有可以撤销的改名记录」 |

#### `md:history:clear`（**P2-C 新增 / DEC-12**）

| 项 | 内容 |
| --- | --- |
| 方向 | renderer → main（invoke） |
| 入参 | 无 |
| 返回 | `HistoryClearResult` |
| 错误码 | `E_UNKNOWN`（本通道**无业务性失败**）。写盘失败不新增错误码 —— 按既有 §4.8 机制发 `md:app:storageWarning` |
| 对应 | IX-101；TC-35 / TC-36 / TC-37；DEC-12 |

```ts
export interface HistoryClearResult {
  /** 被清掉的记录条数（界面用它写状态栏文案「已清空全部 N 条」） */
  clearedTasks: number;
}
```

**实现约束**

| 约束 | 说明 |
| --- | --- |
| **只删记录，绝不触碰文件** | 不删除、不改名、不移动任何文件。实现上**只做一件事**：把 `history.json` 写成空结构。这是 `数据库设计.md` §4.7 定下的底线，也是文案「文件本身不会被删除」的依据 |
| 原子写 | 必须走 `writeJsonAtomic`（与历史写入同一套）。**写失败时保留原记录并报错**，不能出现「写坏了 → 历史全丢」 |
| 不产生记录 | 清空本身**不写入历史**（与「撤销不产生新记录」同理） |
| 幂等 | 历史已为空时调用，返回 `{ ok: true, data: { clearedTasks: 0 } }`，**不报错** |
| ⚠️ 界面必须重置淘汰检测基准 | `stores/history.ts` 的「淘汰不静默」检测条件是「最旧那条不见了 + 总数没变多」—— **用户主动清空恰好两个条件都成立**，不重置基准就会假报「为保证性能，较旧的记录已被清理」。详见 `P2-C轻量设计确认.md` §2.6 |

---

## 4. 事件通道（main → renderer）

| 通道名 | 载荷 | 触发时机 | 优先级 |
| --- | --- | --- | --- |
| `md:rename:progress` | `ProgressPayload` | 改名执行中（节流推送） | P0 |
| `md:window:maximizeChanged` | `{ maximized: boolean }` | 窗口最大化状态变化 | P0 |
| `md:app:storageWarning` | `StorageWarningPayload` | 数据文件损坏或写失败 | P0 |

### 4.1 监听约定

| 约定 | 说明 |
| --- | --- |
| 注册方式 | 统一通过 `window.maodie.<域>.onXxx(cb)`，**返回取消函数** |
| 卸载 | Vue 组件 `onUnmounted` 里必须调用取消函数，否则组件销毁后回调仍会执行（对 ST-03 的定时器队列尤其致命） |
| 全局事件 | `md:app:storageWarning` 在 `App.vue` 里**只注册一次**（不能每个组件都注册） |
| 载荷不可变 | 渲染层不得修改载荷对象后回传，载荷是单向快照 |

### 4.2 明确不设事件的场景

| 场景 | 为什么不用事件 |
| --- | --- |
| 改名完成通知 | 用 `md:rename:execute` 的返回值表达。事件 + 返回值双通道会导致「结果到了两次」的竞态 |
| 历史记录变更 | 撤销通过 `invoke` 返回值告知，界面自己更新 store。不需要事件 |
| 列表变化 | 列表完全在渲染层维护（DEC-07 不持久化），主进程不感知 |

---

## 5. 数据模型（TypeScript 定义全集）

> 全部定义集中在 `src/shared/types.ts`。**这是主进程、preload、渲染层、Worker 四方共用的唯一类型来源。**

### 5.1 `FileItem`

```ts
export interface FileItem {
  /** 唯一 id，入列时生成（crypto.randomUUID） */
  id: string;
  /** 绝对路径（反斜杠形式），如 C:\Users\PC\Desktop\abc.txt */
  fullPath: string;
  /** 所在目录的绝对路径 */
  dirPath: string;
  /** 当前完整名称（含扩展名） */
  name: string;
  /** 规则作用的主体（文件去掉扩展名；文件夹取全名） */
  stem: string;
  /** 扩展名（含点，如 ".docx"；无则空串） */
  ext: string;
  /** 是否为文件夹（symlink 指向目录时也为 true） */
  isDir: boolean;
  /** 是否为符号链接 / junction（仅用于信息展示与审计，策略统一为「只改链接自身名字」） */
  isSymlink: boolean;

  /* ↓↓↓ 以下字段由渲染层的预览 Worker 填充，主进程不关心 ↓↓↓ */

  /** 计算出的新主体 */
  newStem: string;
  /** 完整新名 = newStem + ext（冗余字段，方便直接渲染） */
  newName: string;
  status: 'pending' | 'unchanged' | 'changed' | 'conflict' | 'invalid';
  /** status 为 conflict 时进一步区分 */
  conflictKind?: ConflictKind;
  /** 可直接显示的中文原因（冲突 / 非法时填写） */
  reason?: string;
  /** 机器可读的原因码 */
  reasonCode?: MdErrorCode;
  /** 差异高亮区间；null 或 undefined 表示无变化 */
  diffRange?: DiffRange | null;
  /** 开启「自动加序号」后为该项算出的最终名字（如 报告_1.docx） */
  resolvedName?: string;
}
```

> **与 PRD §6.1 的差异（3 处扩充，均为实现必需）**
>
> | 新增字段 | 为什么必需 |
> | --- | --- |
> | `name` | PRD 只有 `stem` + `ext`，但列表要显示完整名、差异高亮要比较完整名、状态栏要显示当前名。没有它就要在四处反复 `stem + ext`，是 bug 温床 |
> | `isSymlink` | `lstat` 判定为 symlink 时，`isDirectory()` 为 false，会把「指向目录的 junction」误当成文件去拆扩展名（`我的文件夹.旧` 会被拆成主体 `我的文件夹` + 扩展名 `.旧`）。必须记录并据此设置 `isDir` |
> | `newName` | 纯冗余字段。代价是 8 字节/项，收益是渲染层与 Worker 都不必再拼一次 |
>
> 未纳入的字段（明确 YAGNI）：`size`（文件大小）、`mtimeMs`（修改时间）。PRD 的列表设计要求只有「原名 → 新名」两列（设计规范 §5.1 行高 36px 单行），加了也没地方显示。

### 5.2 `RuleConfig`

> 与 PRD §6.1 完全一致，不扩充。

```ts
export interface RuleConfig {
  mode: 'delete' | 'replace' | 'rule';
  caseSensitive: boolean;              // 默认 false
  /** 对应 EL-055 开关。默认 false = 冲突跳过（DEC-05） */
  autoResolveConflict: boolean;
  delete: { text: string };
  replace: { find: string; to: string };
  rule: {
    prefix: string;
    suffix: string;
    seqEnabled: boolean;
    seqStart: number;      // 默认 1，≥ 0
    seqStep: number;       // 默认 1，≥ 1
    seqPad: number;        // 默认 3，范围 0–6
    seqPosition: 'prefix' | 'suffix' | 'at';   // 默认 'suffix'；'at' = 插在主体第 n 个码点之后（P3-1 新增）
    dateEnabled: boolean;
    dateFormat: 'YYYY-MM-DD' | 'YYYYMMDD' | 'YYYY年MM月DD日' | 'MM月DD日' | 'YYMMDD';  // 默认 'YYYY-MM-DD'（P3-1 起 5 档）
    keepOriginal: boolean; // 默认 true

    /* ── P3-1（第 1 批）新增 6 个字段，每个都有默认值；默认值下的输出与 P0/P1 逐字节一致 ── */
    seqKind: 'number' | 'letter' | 'random' | 'time';  // 默认 'number'
    seqAt: number;          // 默认 1，范围 1–200；仅 seqPosition === 'at' 时生效
    seqRandomLen: number;   // 默认 6，范围 1–16；仅 seqKind === 'random' 时生效
    seqRandomSeed: number;  // 默认 0，≥ 0；「换一批」把它 +1，整批随机串随之改变
    seqTimeStart: string;   // 默认 ''，格式 YYYY-MM-DD；为空时回落到调用方传入的目标日期
    seqTimeFormat: 'YYYY-MM-DD' | 'YYYYMMDD' | 'YYYY年MM月DD日' | 'MM月DD日' | 'YYMMDD';
                            // 默认 'YYYY年MM月DD日'；与「启用日期」共用同一张样式表
  };
}

/** 默认值（新建任务 / 重置规则时使用） */
export const DEFAULT_RULE: RuleConfig = {
  mode: 'delete',
  caseSensitive: false,
  autoResolveConflict: false,
  delete: { text: '' },
  replace: { find: '', to: '' },
  rule: {
    prefix: '', suffix: '',
    seqEnabled: false, seqStart: 1, seqStep: 1, seqPad: 3, seqPosition: 'suffix',
    dateEnabled: false, dateFormat: 'YYYY-MM-DD',
    keepOriginal: true,
    /* P3-1 的 6 个默认值 —— 这一组值 = 「行为与 P0/P1 完全相同」 */
    seqKind: 'number', seqAt: 1, seqRandomLen: 6, seqRandomSeed: 0,
    seqTimeStart: '', seqTimeFormat: 'YYYY年MM月DD日',
  },
};
```

> ⚠️ **「时间」类型不读时钟，「启用日期」取执行当天 —— 跨天必须重新预览**
>
> `seqTimeStart` 为空时，时间类型的日期**回落到调用方传入的目标日期**（= 执行当天，DEC-03）。主进程 `rename-executor.ts` 在执行前会校验它等于本机今天，**不一致就改用本机日期**（只 `console.warn`，不报错）—— 所以「23:58 预览、00:01 执行」会差一天，且**结果弹窗不会有任何异常**。
> 界面因此常显一条跨天提示（`RulePanel.vue` 的 `data-date-hint`：*日期取的是你点「开始改名」那天；如果中途跨过了零点，需要重新预览一次*）。
> **契约层面的结论**：`date` 由调用方给出，主进程只做「等于今天就照用、不等就用今天」的收敛；**保证「预览 ≡ 执行」的责任落在调用方 —— 跨天必须重新预览**。

### 5.3 `RenameTask`（历史记录 / 撤销记录）

```ts
export interface RenameTask {
  id: string;
  /** 执行时间，毫秒时间戳 */
  createdAt: number;
  /** 执行当天日期，YYYY-MM-DD */
  date: string;
  /** 规则摘要文案，用于历史页展示（如「删除「【某某公众号】」」） */
  ruleSummary: string;
  /** 状态：active = 可撤销；undone = 已撤销 */
  status: 'active' | 'undone';
  /** 撤销完成时间（毫秒时间戳），未撤销时为 null */
  undoneAt: number | null;
  /** ★ 只记录「改名成功」的明细（撤销只针对改成功的项） */
  entries: RenameEntry[];
  /** 本次任务的统计计数（含未成功的项，供历史页显示） */
  counts: {
    total: number;
    success: number;
    skipped: number;
    invalid: number;
    failed: number;
  };
}

export interface RenameEntry {
  dirPath: string;
  /** 改名前的名称 */
  fromName: string;
  /** 改名后的名称 */
  toName: string;
}
```

> **`entries` 只记录成功项 —— 这是 `数据库设计.md` §4.4 的关键优化**：
> - 跳过 / 非法的项**根本没改名**，不需要撤销记录；
> - 失败项**已回滚或原名未动**，也不需要；
> - 这样一次 10000 项的改名，若 9000 项成功，`entries` 就是 9000 条而不是 10000 条，且历史页要的「项数 / 结果」由 `counts` 提供，信息不丢。
>
> 详细容量治理见 `数据库设计.md` §4.4。

### 5.4 `Prefs` / `AppInfo` / `WindowState`

```ts
export interface Prefs {
  version: number;              // schema 版本，当前 1
  soundEnabled: boolean;        // 音效开关（默认 true）
  reduceMotion: boolean;        // 「减少动画」无障碍开关（默认 false）
  confirmThreshold: number;     // 超过多少项弹二次确认（默认 10）。**不进设置界面**（内部常量）
  theme: 'system' | 'light' | 'dark';   // P2-A：三态。**默认 'light'（始终浅色）**
}

export const DEFAULT_PREFS: Prefs = {
  version: 1,
  soundEnabled: true,
  reduceMotion: false,
  confirmThreshold: 10,
  theme: 'light',
};

export interface AppInfo {
  version: string;
  electronVersion: string;
  chromeVersion: string;
  platform: 'win32';
  arch: 'x64' | 'arm64';
  userDataPath: string;
  /** P2-C：可执行文件路径（`process.execPath`），给设置里「复制程序路径」用。
   *  加字段而不是加通道 —— 它本来就该属于「应用信息」 */
  execPath: string;
  isDev: boolean;
}

export interface WindowState {
  width: number; height: number; x: number; y: number; maximized: boolean;
}
```

### 5.5 `ConflictKind` / `DiffRange`

```ts
export type ConflictKind = 'none' | 'batch' | 'disk' | 'self' | 'case-only';

export interface DiffRange {
  oldStart: number; oldEnd: number;   // 原名中变化区间（半开）
  newStart: number; newEnd: number;   // 新名中变化区间（半开）
}
```

> **`DiffRange` 的区间是半开的**（`[start, end)`），与 JS 的 `slice` 语义一致。渲染时用 `name.slice(0, start) + <高亮>name.slice(start, end)</高亮> + name.slice(end)`。**不要写成闭区间**——这是最容易差一位的地方。

---

## 6. 错误码总表

> 定义位置：`src/shared/errors.ts`。前端文案与 PRD §4.4.4 的失败原因枚举**逐字对齐**，不做二次创作。

```ts
export const MD_ERROR = {
  // —— 校验类（预览阶段即可发现）——
  E_INVALID_CHAR:    'E_INVALID_CHAR',     // 含非法字符 / 保留名 / 结尾点或空格
  E_PATH_TOO_LONG:   'E_PATH_TOO_LONG',    // 路径超长
  E_EMPTY_NAME:      'E_EMPTY_NAME',       // 改后名称为空

  // —— 冲突类 ——
  E_CONFLICT_BATCH:  'E_CONFLICT_BATCH',   // 批次内撞名
  E_CONFLICT_DISK:   'E_CONFLICT_DISK',    // 与磁盘已有文件撞名

  // —— 执行类 ——
  E_BUSY:            'E_BUSY',             // 文件被占用
  E_PERM:            'E_PERM',             // 无写入权限
  E_SOURCE_MISSING:  'E_SOURCE_MISSING',   // 源文件不存在

  // —— 撤销类 ——
  E_UNDO_ALREADY:      'E_UNDO_ALREADY',
  E_UNDO_SOURCE_GONE:  'E_UNDO_SOURCE_GONE',

  // —— 流程 / 参数类 ——
  E_LIST_EMPTY:      'E_LIST_EMPTY',
  E_NO_CHANGE:       'E_NO_CHANGE',
  E_OVERFLOW:        'E_OVERFLOW',
  E_TASK_RUNNING:    'E_TASK_RUNNING',
  E_TASK_NOT_FOUND:  'E_TASK_NOT_FOUND',

  // —— P3-4：导入表格（EX-18）——
  E_TABLE_UNREADABLE: 'E_TABLE_UNREADABLE',
  E_TABLE_TOO_BIG:    'E_TABLE_TOO_BIG',

  E_UNKNOWN:         'E_UNKNOWN',
} as const;

export type MdErrorCode = typeof MD_ERROR[keyof typeof MD_ERROR];
```

**完整映射表**

| 错误码 | PRD 编号 | 触发条件 | 界面文案（可直接显示） | 用户下一步 | 可重试 |
| --- | --- | --- | --- | --- | --- |
| `E_INVALID_CHAR` | EX-01 | 含 `\ / : * ? " < > \|`、控制字符、Windows 保留名（`CON` / `NUL` / `COM1` 等）、或名称以点 / 空格结尾 | 名称里不能包含 `\ / : * ? " < > \|` | 改规则 | 改规则后 |
| `E_PATH_TOO_LONG` | EX-02 | 目录 + 名称 > 255 字符 | 名字太长了，系统放不下 | 改规则（缩短名称） | 改规则后 |
| `E_EMPTY_NAME` | EX-04 | 主体被完全删除，或只剩扩展名 | 改完名字就空了，检查一下规则 | 改规则 | 改规则后 |
| `E_CONFLICT_BATCH` | EX-05 | 同批中两项改后同名（不区分大小写） | 有 N 项改完会撞名 | 开「自动加序号」或改规则 | 处理后 |
| `E_CONFLICT_DISK` | EX-06 | 目标名与目录下已有文件同名（不区分大小写） | 已经有同名文件了 | 同上 | 处理后 |
| `E_BUSY` | EX-03 | `EBUSY`，或 `EPERM` 且目录可写（说明是占用） | 文件被占用 | 关掉打开该文件的程序 | 是 |
| `E_PERM` | EX-03 | `EACCES`，或 `EPERM` 且目录不可写 | 无写入权限 | 换目录 / 提权 | 是 |
| `E_SOURCE_MISSING` | EX-10 | 改名执行期间源文件被移动或删除 | 文件找不到了 | 重新入列 | 是 |
| `E_UNDO_SOURCE_GONE` | EX-10 | 撤销时目标名对应的文件已不存在 | 有 N 个文件找不到了，已跳过 | 无需操作 | 否 |
| `E_UNDO_ALREADY` | — | 对 `status === 'undone'` 的任务再次撤销 | 这条已经撤销过啦 | 无需操作 | 否 |
| `E_LIST_EMPTY` | EX-11 | 空列表执行（按钮已置灰，正常点不到） | —（防御性，不显示） | — | — |
| `E_NO_CHANGE` | EX-12 | 全部项均无变化（按钮已置灰） | 当前规则不会改变任何名字 | 改规则 | — |
| `E_OVERFLOW` | EX-13 | 单批超过 10000 项 | 单批最多 1 万个，超出部分未加入 | 分批处理 | 是 |
| `E_TASK_RUNNING` | — | 已有改名任务在执行中还调 `execute` | 正在改名中，等一下再试 | 等待或取消 | 是 |
| `E_TASK_NOT_FOUND` | — | `taskId` 无对应任务（已完成或不存在） | 这个任务已经结束了 | 无需操作 | 否 |
| `E_TABLE_UNREADABLE` | EX-18 | 表格不是 xlsx / 加密 / 损坏 / CRC 对不上 / 不支持的类型（`.xls`、`.docx`） | 表格读不出来，可能是加密或损坏的 | 换一份表 | 处理后 |
| `E_TABLE_TOO_BIG` | EX-18 | 单个 XML 部件超过 24MB，或行数超过 1 万（后者只截断并如实告知） | 这张表太大了，读不动（只支持单表 1 万行以内） | 拆表 | 处理后 |

> ⚠️ **EX-18 覆盖「读不出来」与「太大」两种情况**（设计 §4 第 4/5 行把两者都归到 EX-18），但给用户看的话**不能是同一句** —— 一个是「你这文件有问题」，另一个是「文件没错、是这个功能读不动」。所以拆成两个错误码，文档里仍记在同一行编号下。
| `E_UNKNOWN` | — | 未归类的系统错误 | 出了点小意外，稍后再试试 | 重试 | 是 |

### 6.1 `E_BUSY` 与 `E_PERM` 的判定方法

Windows 上「文件被占用」和「无权限」可能报同一个 `EPERM`，必须做一次区分（技术方案 §4.1.6）：

```
catch (err):
  if (err.code === 'EBUSY')  → E_BUSY
  if (err.code === 'EACCES') → E_PERM
  if (err.code === 'EPERM'):
      canWrite = await fs.access(dirPath, W_OK).then(() => true, () => false)
      → canWrite ? E_BUSY : E_PERM
  else → E_UNKNOWN
```

> 这个探测**只在出错路径上跑**，正常流程零开销。

### 6.2 文案的唯一来源

**界面上的任何错误文案都必须从 `MD_ERROR_TEXT[code]` 取**，不允许在组件里硬编码字符串。理由：同一条错误会出现在「列表行的原因徽标」「结果弹窗的明细」「状态栏提示」三处，硬编码必然出现措辞不一致。

```ts
// src/shared/errors.ts
export const MD_ERROR_TEXT: Record<MdErrorCode, string> = {
  E_INVALID_CHAR:   '名称里不能包含 \\ / : * ? " < > |',
  E_PATH_TOO_LONG:  '名字太长了，系统放不下',
  E_EMPTY_NAME:     '改完名字就空了，检查一下规则',
  E_CONFLICT_BATCH: '有 N 项改完会撞名',
  E_CONFLICT_DISK:  '已经有同名文件了',
  E_BUSY:           '文件被占用',
  E_PERM:           '无写入权限',
  E_SOURCE_MISSING: '文件找不到了',
  E_UNDO_SOURCE_GONE: '有 N 个文件找不到了，已跳过',
  E_UNDO_ALREADY:   '这条已经撤销过啦',
  E_LIST_EMPTY:     '',
  E_NO_CHANGE:      '当前规则不会改变任何名字',
  E_OVERFLOW:       '单批最多 1 万个，超出部分未加入',
  E_TASK_RUNNING:   '正在改名中，等一下再试',
  E_TASK_NOT_FOUND: '这个任务已经结束了',
  E_UNKNOWN:        '出了点小意外，稍后再试试',
};
```

> `E_CONFLICT_BATCH` 与 `E_UNDO_SOURCE_GONE` 文案里的 `N` 是占位符，由调用方替换为实际数量。

---

## 7. 代码层禁止事项

> 这一节是**硬规则**，不是建议。前 6 条可以用 ESLint 自动拦，后 4 条靠代码评审。

| # | 禁止 | 为什么 | 检查方式 |
| --- | --- | --- | --- |
| P-01 | `src/renderer/**` 与 `src/shared/**` 中出现 `import ... from 'fs'` / `'node:fs'` / `'electron'` | 渲染层必须没有文件权限；`shared` 必须可脱离 Electron 单测 | ESLint `no-restricted-imports` |
| P-02 | `src/main/services/rename-executor.ts` 内出现裸 `fs.rename` / `fs.promises.rename` / `renameSync` | 唯一的改名原语必须是内含存在性检查的 `safeRename`。这是「绝不覆盖」的代码级护栏 | ESLint `no-restricted-syntax`（针对 `fs.rename` 的 CallExpression） |
| P-03 | 全项目出现 `fs.unlink` / `fs.rm` / `fs.rmdir` / `rmSync` / `unlinkSync` | 本产品**不删除任何用户文件**。唯一例外：`src/main/services/storage.ts` 内清理自己生成的 `.tmp` 文件（需在该文件顶部加 `// eslint-disable-next-line` 并附注释说明） | ESLint + 代码评审 |
| P-04 | 全项目出现 `fetch(` / `XMLHttpRequest` / `net.request` / `axios` | 兑现「完全本地离线、零网络请求」。生产 CSP 里 `connect-src 'none'` 是第二道防线 | ESLint + 构建产物全文搜索 |
| P-05 | `BrowserWindow` 配置里 `nodeIntegration: true` 或 `enableRemoteModule` 或使用 `<webview>` 标签 | 直接破坏进程隔离 | 代码评审 + 启动时自检断言 |
| P-06 | `preload` 里暴露 `ipcRenderer` / `require` / `process` / `Buffer` / `shell` | 白名单形同虚设 | 代码评审 + preload 单测（断言 `window.maodie` 的键只有 5 个命名空间） |
| P-07 | 不加 `path.resolve` 规范化就直接用来自渲染层的路径 | 路径穿越风险 | 代码评审。规范做法：所有入参路径先过 `normalizePath()` |
| P-08 | 允许 `fromName` 与 `toName` 的 `dirPath` 不同 | 本产品只做**同目录改名**，绝不做「移动文件」。从接口形状上就要杜绝 | 代码评审 + `rename-plan.ts` 单测断言抛错 |
| P-09 | 在 `ipc/*.ipc.ts` 里写超过 20 行的业务逻辑 | handler 不可单测，逻辑必须住在 `services/` | 代码评审 |
| P-10 | 在组件里硬编码错误文案 / 色值 / 圆角 / 阴影 | 文案与视觉都必须单一来源（`MD_ERROR_TEXT` / `tokens.css`） | ESLint 自定义规则（色值）+ 评审 |
| P-11 | `path.realpath` / `fs.realpath` 解引用符号链接 | 会把「改链接名」变成「改目标名」，是危险行为 | ESLint `no-restricted-syntax` |
| P-12 | 在 ST-03 动画里做「双肢快速上下抽动 + 躯干抖动 + 张嘴」的组合 | PRD §4.6.3 硬约束：动作不得有歧义 | 代码评审 + 人工观看（必须请非项目成员看一遍） |

---

## 8. 通道 ↔ 功能 ↔ 验收用例 对照

| 通道 | 对应元素 | 功能编号 | 交互编号 | 验收用例 |
| --- | --- | --- | --- | --- |
| `md:app:getInfo` | — | — | — | — |
| `md:app:getPrefs` / `setPrefs` | 设置项 | — | — | — |
| `md:app:storageWarning` | EL-071 状态栏 | — | — | — |
| `md:window:minimize` | EL-002 | — | — | — |
| `md:window:toggleMaximize` | EL-003 | — | — | — |
| `md:window:close` | EL-004 | — | — | — |
| `md:window:getState` | — | — | — | TC-17 状态记忆 |
| `md:fs:pickFiles` | EL-020 | F-01 | IX-020 | TC-01 |
| `md:fs:pickDirectory` | EL-021 | F-02 | IX-021 | **TC-02（不递归）** |
| `md:fs:resolvePaths` | EL-010 / EL-012 | F-07 | IX-022 ~ IX-025 | **TC-01 / TC-02** |
| `md:rename:execute` | EL-060 | F-01 / F-02 / F-16 | IX-060 ~ IX-063 | **TC-03 ~ TC-08 / TC-12 / TC-13 / TC-14 / TC-15** |
| `md:rename:cancel` | EL-060（取消态） | — | IX-061 | — |
| `md:rename:progress` | EL-070 ~ EL-072 | F-06 | IX-062 | — |
| `md:history:list` | EL-081 | F-09 | IX-070 | **TC-10（20 条上限）** |
| `md:history:undoTask` | EL-082 | F-09 | IX-071 | **TC-09** |
| `md:history:undoAll` | EL-083 | F-09 | IX-072 | **TC-11** |
| `md:history:clear` | EL-113 | F-13（清空入口） | IX-101 | **TC-35 / TC-36 / TC-37** · EX-17 · DEC-12 |
| `md:fs:importTable` | EL-132 / EL-133 | F-16（导入表格） | **IX-114 ~ IX-116** | **TC-61 ~ TC-67** · EX-18 · DEC-19 |

---

## 9. 实现清单（开发时逐条打勾）

> 建议按此顺序实现：先 `app` / `window`（无依赖），再 `fs`，然后 `rename`，最后 `history`。

### 9.1 共享层（先写，被四处依赖）

- [ ] `channels.ts` — 全部通道名常量
- [ ] `types.ts` — §5 的全部类型
- [ ] `errors.ts` — `MD_ERROR` + `MD_ERROR_TEXT`
- [ ] `constants.ts` — 上限值（10000 项 / 20 条历史 / 255 字符 / 99 次冲突重试 / `__md_tmp_` 前缀）
- [ ] `sanitize-rule.ts` — **规则白名单收敛**（P3-1 从 `rename.ipc.ts` 挪进共享层：逐字段兜底、默认值一律取 `DEFAULT_RULE`；**漏一个字段就是「预览对、执行错、界面无异常」**）

### 9.2 主进程通道

- [ ] `md:app:getInfo` · [ ] `md:app:getPrefs` · [ ] `md:app:setPrefs`
- [ ] `md:window:getState` · [ ] `md:window:minimize` · [ ] `md:window:toggleMaximize` · [ ] `md:window:close`
- [ ] `md:fs:pickFiles` · [ ] `md:fs:pickDirectory` · [ ] `md:fs:resolvePaths`
- [ ] `md:rename:execute` · [ ] `md:rename:cancel`
- [ ] `md:history:list` · [ ] `md:history:undoTask` · [ ] `md:history:undoAll` · [ ] `md:history:clear`（P2-C）

### 9.3 事件推送

- [ ] `md:rename:progress`（节流：50 项 / 100ms）
- [ ] `md:window:maximizeChanged`
- [ ] `md:app:storageWarning`

### 9.4 preload

- [ ] `window.maodie` 五个命名空间齐全
- [ ] `Object.freeze` 外壳
- [ ] 三个 `onXxx` 都返回取消函数
- [ ] 单测：断言 `window.maodie` 的键只有 `app/window/fs/rename/history`

### 9.5 契约测试（集成测试层）

- [ ] 每个通道的「正常入参 → 期望返回」用例
- [ ] 每个通道的「非法入参 → 期望错误码」用例
- [ ] `md:rename:execute` 的 `E_TASK_RUNNING`（并发调用）
- [ ] `md:history:undoTask` 的 `E_UNDO_ALREADY`（重复撤销）
- [ ] `md:fs:resolvePaths` 的 `stats` 五项各自单独命中

---

## 10. 命令行模式（CLI · P2-C / F-15）

> **本节是 P2-C 落地时新定的契约。** 在此之前，`P2-C轻量设计确认.md` 只定了「入口长什么样」与「安全底线」两条，CLI 本体**没有契约也没有验收标准**（§12 那一行原本写的是「—（CLI 本身的验收随接口文档定稿）」）。缺口就在这里补上。
>
> 交付形式：`maodie.exe --rename …`，入口在 `src/main/cli/`。**不新增任何 IPC 通道** —— CLI 与界面共用同一套执行器。

### 10.1 用法

```
maodie.exe --rename --dir "<目录>" [规则] [--yes]

规则（三选一，与界面完全一致）：
  --delete "<文本>"
  --replace "<查找>" "<替换为>"
  --prefix "<前缀>" --suffix "<后缀>" --seq --date --date-format "<格式>"

P1 能力：
  --regex                             正则匹配
  --lower | --upper | --capitalize    大小写转换

安全相关：
  --auto-seq       冲突时自动加序号（默认不加 = 跳过）
  --case-sensitive 区分大小写
  --limit <N>      预览表格最多打印多少行（默认 50）

--help              打印用法（**任何时候都能用**，不受「界面开着」影响）
```

### 10.2 参数与 RuleConfig 的映射

| 参数 | 落到 `RuleConfig` 的哪里 | 备注 |
| --- | --- | --- |
| `--delete "<文本>"` | `mode='delete'` · `delete.text` | 与 `--replace` / 前后缀**三选一互斥** |
| `--replace "<查找>" "<替换为>"` | `mode='replace'` · `replace.find` / `replace.to` | 取两个值，缺一个即报错 |
| `--prefix` / `--suffix` | `mode='rule'` · `rule.prefix` / `rule.suffix` | 与上两者互斥 |
| `--seq` / `--date` | `rule.seqEnabled` / `rule.dateEnabled` | 同上互斥；`--date` 的日期**取执行当天**（DEC-03） |
| `--date-format <格式>` | `rule.dateFormat` | **只收** `YYYY-MM-DD` / `YYYYMMDD` / `YYYY年MM月DD日`，其余报错 |
| `--regex` | `regexEnabled` | P1 能力；pattern 长度仍受 ≤ 200 限制 |
| `--lower` / `--upper` / `--capitalize` | `caseTransform` | 只作用新名主体，扩展名不动（P1 红线） |
| `--auto-seq` | `autoResolveConflict = true` | 默认 `false` = **冲突跳过，绝不覆盖**（DEC-05） |
| `--case-sensitive` | `caseSensitive` | 默认 `false` = 不区分大小写 |
| `--yes` | —（不落配置） | 见 §10.3 安全底线 |

> 参数名刻意与 `scripts/cli-verify.ts`（`npm run cli:verify`，MS-1 的**开发者**引擎验证脚本，`--rule <JSON>` / `--execute`）**区分开**：那是内部工具、不进打包产物；本节是**面向用户**的命令行模式。两者受众不同，不合并。

### 10.3 ★ 三条安全底线（与界面完全一致）

1. **默认只读。** 不传 `--yes` 一律 dry-run：只打印「原名 → 新名 → 状态 → 原因」的表格，**不碰任何文件、不写历史**。命令行没有预览界面，所以默认必须是只读的 —— 与界面里「先预览、再执行」是同一条原则。
2. **绝不覆盖已有文件。** 冲突默认跳过（`--auto-seq` 才改成自动加序号），扩展名保护照旧，两阶段改名（先临时名、再目标名）照旧 —— 因为**复用主进程的 `execute()`**，不是另写一份。
3. **改名写入历史记录**，可在界面的「撤销 / 历史记录」里撤销。默认日期取执行当天。

`--yes` 与 `--dry-run` **不能同时给**（报了错才不会出现「脚本里写了 --yes 却什么也没做」）。`--dry-run` 可显式写上，语义与不传相同。

### 10.4 退出码

| 码 | 含义 |
| --- | --- |
| `0` | 成功（含 dry-run） |
| `1` | 执行时有失败项（`summary.failed > 0`） |
| `2` | 参数错 / 读不到目录 / **程序正在运行**（见 §10.5） |

出错时**必须**在 stderr 给出中文原因并打印用法；绝不允许静默退出。

### 10.5 ★ 与界面的互斥（实现红线）

命令行**不与界面并发运行**。界面开着时它持有单实例锁，此时跑命令行会：

```
检测到「耄耋改名」正在运行。请先关闭窗口，再使用命令行模式。
（同时运行会互相覆盖历史记录，所以这里直接退出了）
```

退出码 `2`。**为什么不并发**：两个进程各有一份 `history.json` 的内存镜像，而写入队列是**进程内**的 —— 后写的会把先写的**整体覆盖**，用户的撤销记录会凭空少掉一批。宁可让用户先关窗口。

因此 `src/main/index.ts` 里的命令行分流**必须排在 `app.requestSingleInstanceLock()` 之前**（否则第二个实例会直接 quit，命令静默什么也不做、连提示都没有）。

### 10.6 已知限制（如实登记）

- **`AppInfo.execPath` 在未打包时指向 Electron 引擎**，不是 `maodie.exe`。设置里「复制命令」给出的那条命令**只有打包成安装版之后才可直接使用**。这是「复制程序路径」这个功能本身要解决的问题（进阶用户不知道程序装在哪），但它不能变出一个尚未打包的 exe。
- 不同终端（PowerShell / cmd / Git Bash）的引号与路径转义差异**未验证**；路径含空格时请加引号。
- CLI 只处理 `--dir` 下的**直接子项，不递归**（DEC-01，与界面一致）。

---

## 11. 变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| **v1.0** | 2026-09-12 | 首次成文。定义 15 个请求响应通道 + 3 个事件通道；确立「简单通道 reject / 业务通道 MdResult」的返回约定；确立 ADR-004（`execute` 传规则与清单、由主进程重算）；补足 PRD 未覆盖的边界（`E_BUSY` 与 `E_PERM` 的区分、`problems` 200 条截断与 `recordSaved` 失败警告、符号链接指向目录时的 `isDir` 判定、`ProgressPayload` 分阶段进度的展示方式）；给出 12 条代码层禁止事项（6 条可 lint 拦截） |
| **v1.0.1** | 2026-09-12 | 依 PRD v1.1.1 的 **DEC-09** 修正 `md:history:list` 的返回约定：条数由「最多 20 条」改为「**常态最多 20 条**，极端场景可能少于 20 条」（明细总量上限治理所致）。通道数量、入参、错误码均无变化 |
| **v1.0.2** | 2026-09-15 | **P2-A（设置面 + 深色模式）落地后同步**：① §5.4 `Prefs.theme` 由 `'light'` 扩为 **`'system' \| 'light' \| 'dark'`**，默认保持 `'light'`（始终浅色）；② **通道数量一个都没加**（仍是 15 个请求响应 + 3 个事件）—— 主题切换靠主进程 `nativeTheme.themeSource` + 渲染层 `prefers-color-scheme` 媒体查询自带通知，**不需要「主题变了」的广播**（这是 P2-A 的一条硬约束）；③ `md:app:setPrefs` 的入参校验**必须放行 `theme`**（否则渲染层改主题会被静默丢弃 —— 落地时真踩过，已修）|

| **v1.0.3** | 2026-09-15 | **P2-C（历史完整版 + 命令行入口）设计定稿，契约变更**：① **新增第 16 个请求响应通道 `md:history:clear`**（§3.5 已补定义与实现约束）—— 合计由「15 个请求响应 + 3 个事件」改为 **「16 个请求响应 + 3 个事件」**（事件通道仍是 3 个不变）；② `AppInfo` 新增 `execPath` 字段（可执行文件路径，供设置里「复制程序路径」）—— **加字段而不是加通道**；③ 新增返回类型 `HistoryClearResult`；④ **不新增错误码**：写盘失败走既有 `storageWarning` 机制，业务上无失败分支；⑤ §8 对照表与 §9.2 清单已登记。⚠️ 两条实现红线：**只删记录、绝不触碰任何文件**；以及**界面必须重置淘汰检测基准**（否则用户主动清空会被假报成「较旧的记录已被清理」）。详见 `P2-C轻量设计确认.md` v1.2 / DEC-12 |
| **v1.0.4** | 2026-09-15 | **P2-C 命令行模式落地：补齐 CLI 契约（本节新增）**。此前本文档对 CLI **零定义** —— 设计稿只定了「入口长什么样」与「安全底线」，验收栏写的是「—（随接口文档定稿）」。现补：完整参数表与到 `RuleConfig` 的映射、三条安全底线（默认只读 / 绝不覆盖 / 写入历史可撤销）、退出码 0/1/2、**与界面互斥**（界面开着时拒跑，避免两进程互相覆盖 history.json）、以及「未打包时 execPath 不可直接用」等已知限制。**不新增任何 IPC 通道**（仍是 16 请求响应 + 3 事件）—— CLI 与界面共用同一套执行器。 |
| **v1.0.5** | 2026-09-18 | **P3-1（编号系统升级）落地后的同步**：① **§5.2 `RuleConfig` 新增 6 个字段** —— `seqKind` / `seqAt` / `seqRandomLen` / `seqRandomSeed` / `seqTimeStart` / `seqTimeFormat`（`DEFAULT_RULE` 同步补默认值），`seqPosition` 由两档扩为三档（加 `'at'`），`dateFormat` 由 3 档扩为 5 档（加 `MM月DD日` / `YYMMDD`）；② **§1.3.2 序列化约束加一行** —— `RuleConfig` 字段只许基础类型，**`seqTimeStart` 走字符串、不走 `Date`**；③ **§5.3 之前补「跨天必须重新预览」** —— 时间/日期取执行当天，主进程不一致时改用本机日期（只 warn 不报错），故跨天必须由调用方重新预览；④ **§9.1 共享层登记 `sanitize-rule.ts`**；⑤ **§6 错误码一条都不新增**。**通道数量未变**（仍是 16 个请求响应 + 3 个事件），**无任何迁移**（规则不落盘）。顺带修正版本头（此前停在 v1.0.2，落后于本表） |

---

**接口文档 v1.0.6 · 2026-09-21**
