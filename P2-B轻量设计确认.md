# P2-B 轻量设计确认（常用规则模板库）

> 产品名：**耄耋改名（MaoDie Renamer）**
> 文档版本：**v1.0**
> 日期：2026-09-17
> 上游文档：`PRD.md` v1.1.6 · `交互说明文档.md` v1.6 · `设计规范.md` v1.5 · `数据库设计.md` · `技术方案文档.md` · `接口文档.md`
> 配套交付：设计稿画布新增画板 **「P2-B 增量设计 · 常用规则模板」**
> 文档状态：**已落地**（2026-09-18 落地并测试通过；产品负责人答复见 §12）

---

## 0. 结论摘要

P2-B 做一件事：**规则区顶部放 6 个内置模板，点一下就把规则填好。**

| 项 | 是否需要新增 |
| --- | --- |
| IPC 通道 | **零**（纯渲染层，不碰主进程） |
| 持久化字段 | **零**（模板是内置常量，不留痕） |
| 颜色令牌 | **零**（用现有 `orange-soft` / `orange-dark`，悬停用 `orange-primary` + `#2A1A10`） |
| 规则引擎能力 | **零**（模板只是「把值填进 RuleConfig」的快捷方式） |
| 真正新增的 | 一个 `src/shared/templates.ts`（纯数据）+ 规则区顶部一行 UI |

### P2 整体进度

| 批次 | 内容 | 状态 |
| --- | --- | --- |
| P2-A | 设置面 + 深色模式（F-14） | ✅ 已落地并测试 |
| P2-C | 历史完整版（F-13）+ 命令行入口（F-15） | ✅ 已落地并测试 |
| P2-C 增量 | 命令行小白化（DEC-13，SCR-08 图文教程） | ✅ 已落地 |
| **P2-B（本文档）** | **F-12 常用规则模板库** | ✅ **已落地（2026-09-18）** —— 单测 18/18 + 冒烟 7 条 |
| — | 多规则链式组合 | 推迟，等用户反馈 |

---

## 1. 核心判断：模板 = 一份预填好的 RuleConfig

**模板不是新的规则类型，也不新增任何引擎能力。** 套用模板 = 把一份完整的 `RuleConfig` 写进 store，用户接着还能继续改。

这是这一版能「轻」下来的根本原因：**不碰引擎、不碰 IPC、不碰持久化。**

模板的数据形状：

```ts
// src/shared/templates.ts（新增，纯数据、无副作用）
export interface RuleTemplate {
  id: string
  /** chip 上的短名 */
  name: string
  /** title 悬停说明（一句大白话） */
  hint: string
  /** 完整的规则配置 —— 整份替换 */
  rule: RuleConfig
}

export const RULE_TEMPLATES: readonly RuleTemplate[] = [ /* 6 个 */ ]
```

> **为什么放 `src/shared/`**：与 `regex-cheatsheet.ts` 同理 —— 纯数据、要能被单测直接引用、且将来 CLI 若要支持 `--template` 也在同一层。（本项目有一条硬规矩：**纯数据/纯逻辑的模块一律放 `src/shared/`**，否则渲染层引用会让单测静默失活。）

---

## 2. 模板条：位置与形态

### 2.1 位置

**规则区最上方、页签之前。**

```
┌─ R-05 规则设置区 ────────────────────────────────────┐
│ 常用规则                                              │  ← 新增小标题
│ [加日期前缀] [补零编号] [日期+编号]                     │  ← 新增：模板条
│ [去掉括号] [空格换下划线] [全部小写]                    │
│ ─────────────────────────────────────────────────── │
│ (删除字符) (替换字符) (规则化)                          │  ← 现有页签
│ …                                                    │
```

**为什么放最上面**：对小白来说，「加日期前缀 / 去掉括号」比「删除字符 / 替换字符 / 规则化」好懂得多 —— **先给能看懂的一键入口，再给需要理解的**。

> ⚠️ **这里纠正一处在 P1 设计里留下的旧判断。**
> 我在 `P1轻量设计确认.md` 里写过「折叠区同时是 P2 多规则链 / 模板库的预留挂载点」。现在真做 P2-B，**这个说法对模板库是错的**：折叠区（「⚙ 进阶设置」）是**给进阶用户做隔离**的手段，而模板库恰恰是**给不会配规则的人的一键入口** —— 把它藏进折叠区，正好违背了它的目的。
> **「多规则链」继续留在折叠区是对的**（那是真正的进阶能力），但模板库要在外面。相关文档的这句话要改。

### 2.2 规格

| 项 | 规格 |
| --- | --- |
| 小标题 | 「常用规则」，12px Medium `ink-3`，左对齐 |
| chips 行 | `wrap` 布局、间隔 6px |
| chip 尺寸 | 高 26px、内边距 4×11px |
| chip 圆角 | **9px（复用既有 `radius-chip`）** |
| chip 默认 | 底 `orange-soft`、字 12.5 Medium `orange-dark` |
| chip 悬停 | 底转 `orange-primary #F5A623`、字转 `#2A1A10`（对比 **8.3:1**）+ `shadow-btn-hover` |
| 悬停说明 | 每个 chip 带 `title` 属性 |

> **关于圆角**：我在方案讨论时说「圆形全圆角（胶囊）」，实际做的时候改用既有令牌 `radius-chip` 9px。做成真正的胶囊需要新增一个圆角令牌，而这一版的价值主张是「零新增令牌」—— 一个圆角不值得破这个规矩。视觉上 9px 圆角的 chip 与页面其他元素（badge 6px、input 10px）也更协调。

### 2.3 一行放得下

6 个 chip 实算约 **490px**，右栏宽 714px（减去内边距约 690px）—— **一行放得下**，不需要换行、更不需要横向滚动。

> 加了新模板之后要重新算一次；超过一行 `wrap` 会自动换行，不会溢出。

---

## 3. 六个模板的完整定义

| # | id | 短名 | title 悬停说明 | 模式 | 预填内容 |
| --- | --- | --- | --- | --- | --- |
| 1 | `datePrefix` | 加日期前缀 | 在名字最前面加上今天的日期，原名字保留 | 规则化 | 前缀 `{d} `、`dateEnabled: true`、`YYYY-MM-DD`、`keepOriginal: true` |
| 2 | `seqPad` | 补零编号 | 在名字最后加上 001、002 这样的编号 | 规则化 | `seqEnabled: true`、起始 1、步长 1、补零 3、排在最后、保留原名 |
| 3 | `dateSeq` | 日期+编号 | 前面加日期、最后加编号，适合整理一整套资料 | 规则化 | 前缀 `{d}-`、`dateEnabled` + `seqEnabled`（补零 3、排在最后）、保留原名 |
| 4 | `stripBrackets` | 去掉括号 | 删掉名字里带括号的部分，比如【某某公众号】、(1)、（副本） | 替换 + **正则** | `regexEnabled: true`；查找 `[（(【\[](?:[^）)】\]]*)[）)】\]]`；替换留空 |
| 5 | `spaceToUnderscore` | 空格换下划线 | 把文件名里的空格换成下划线，适合上传网站或写进代码 | 替换 | 查找「 」（一个空格）→ 替换 `_` |
| 6 | `lowercase` | 全部小写 | 把名字统一成小写，扩展名不受影响 | 规则化 + 大小写 | 保留原名、各要素全关、`caseTransform: 'lower'` |

### 3.1 效果举例

| 模板 | 举例 |
| --- | --- |
| 加日期前缀 | `报告.docx` → `2026-09-17 报告.docx` |
| 补零编号 | `截图.png` → `截图001.png` |
| 日期+编号 | `素材.jpg` → `2026-09-17-素材001.jpg` |
| 去掉括号 | `【某某公众号】x.mp4` → `x.mp4`；`报告(1).docx` → `报告.docx`；`素材（副本）.png` → `素材.png` |
| 空格换下划线 | `my file name.txt` → `my_file_name.txt` |
| 全部小写 | `IMG_0001.JPG` → `img_0001.JPG`（**扩展名不动**） |

### 3.2 ⚠️ 「去掉括号」的正则有一个必须注意的点

**引擎必须按「全部匹配」处理。** PRD 里 F3/F4 明确要求「支持删除**所有出现位置**」。如果实现用了 `str.replace(re, ...)` 而正则没带 `g` 标志，那 `(1)(2).txt` 只会去掉第一个括号。

→ **要求**：引擎侧加 `g` 标志（或用循环），并有单测钉住「一个文件名里有 2 组以上括号时全部被去掉」。

---

## 4. 套用行为

### 4.1 整份替换

**套用 = 整份替换 `RuleConfig`**（模式 + 参数 + 正则开关 + 大小写），最后让页签跟着跳。

**为什么不做「只补不覆盖」**：那样「套用」的语义就模糊了 —— 用户不知道点下去会改什么、不会改什么。整份替换，点完就知道自己现在是什么完整状态，不会出现「一半来自模板、一半是上次填的」。

### 4.2 反馈

套用后状态栏给一句反馈（3 秒回常态）：

- 普通：「已套用模板：加日期前缀」
- 若该模板开了正则：「已套用模板：去掉括号（已自动开启正则）」

### 4.3 不加二次确认（刻意的）

规则只是几个输入框，**不像历史记录那样不可逆** —— 改坏了再改回来很便宜（而且有实时预览，用户马上能看到结果）。加了确认，「一键套用」就变成「两步套用」，正好把这个功能的意义抵消掉。

> 对比：P2-C 的「清空历史记录」加了二次确认，因为那删的是**不可逆的数据**。判断标准是「改错了能不能便宜地改回来」，不是「是不是危险操作」。

### 4.4 进阶折叠条会亮起

套用「去掉括号」会自动开正则，于是「⚙ 进阶设置」折叠条会变成橙色 + 徽标「已启用 1 项」（P1 已有的机制）。**这是好的** —— 用户能一眼看出「这个模板帮我开了一个进阶开关」。

---

## 5. 两个我主动加的判断

### 5.1 判断 ①：不做「当前用的是哪个模板」的高亮

看起来是个贴心功能（套用后让那个 chip 保持高亮），但它**会变成谎话**：用户套用「加日期前缀」之后又手改了日期格式，那个 chip 还亮着，等于在骗他「你现在用的是这个模板」。

**规则只有一个真源**（规则区那几个输入框）。不额外维护一份「来源」状态 —— 那是必然会过期的第二份真相。

### 5.2 判断 ②：历史页的规则摘要要截断

**问题**：`ruleSummary` 是**持久化字段**，显示在历史卡片上。套用「去掉括号」之后，摘要会变成：

```
替换「[（(【\[](?:[^）)】\]]*)[）)】\]]」→（删除）（正则）
```

一长串符号糊在卡片上，用户根本读不懂当时用了什么规则。

**解法：显示层截断。** 摘要里每一段引号内的内容超过 **14 个字符**就取前 **12 个** + 「…」，完整原文放 `title` 悬停可见：

```
替换「[（(【\[](?:[^…」→（删除）（正则）
```

> ⚠️ **截断必须发生在渲染层，绝对不能改 `buildRuleSummary`。**
> `buildRuleSummary` 的返回值会被写进 `history.json`。在那一层截断等于**改变了持久化的数据** —— 新记录是截断的、老记录是完整的，同一份文件里两种格式，数据就不一致了。**原始数据一个字都不动，只改显示。**

**同样不给 `RuleConfig` 加「来源模板」字段** —— 理由与判断 ① 相同：那是个会过期的状态。

---

## 6. 编号清单（已按「前缀全量」grep 核对）

> ⚠️ 本仓库踩过这个坑：设计 P2-A 时用 `TC-3[0-9]` 去搜，**这个模式匹配不到 `TC-27~29`**，导致编号撞车。
> **规矩：查重一律按前缀全量搜（搜 `TC-` / `EL-` / `IX-`），不按猜的区间搜。**

**现有上限（2026-09-17）：`EL-119` / `IX-105` / `TC-37` / `DEC-13`** → P2-B 顺延：

| 类别 | 新增 |
| --- | --- |
| 元素 EL | `EL-120` 常用规则条（含小标题 + 6 个 chip）<br>**6 个 chip 不单独编号** —— 它们是同一个组件的实例，与「列表行不单独编号」同理（`data-template="<id>"` 定位） |
| 交互 IX | `IX-106` 套用模板 |
| 验收 TC | `TC-38` ~ `TC-41`（见 §8） |
| 决策 DEC | `DEC-14` P2-B 范围 + 「不做高亮」/「摘要截断」两个判断 |
| 界面 SCR | **不新增** —— 模板条是规则区内部的一行，不是独立界面 |

---

## 7. 改动面

| # | 位置 | 改什么 |
| --- | --- | --- |
| 1 | `src/shared/templates.ts` | **新增**：`RuleTemplate` 类型 + 6 个模板常量 |
| 2 | `src/renderer/src/components/RulePanel.vue` | 顶部加模板条（小标题 + chips），点 chip 调 store |
| 3 | `src/renderer/src/stores/rule.ts` | 加一个 `applyTemplate(t: RuleTemplate)` —— 内部就是 `rule.value = clone(t.rule)`（**必须深拷贝**，否则模板常量会被后续编辑污染） |
| 4 | `src/renderer/src/stores/task.ts`（或状态栏所在的 store） | 状态栏提示「已套用模板：X」 |
| 5 | 历史卡片渲染处 | 摘要显示层截断 + `title` 放原文 |

> ⚠️ **第 3 条那个「必须深拷贝」是硬要求**：`RULE_TEMPLATES` 是模块级常量，如果直接 `rule.value = t.rule` 把引用赋进去，用户随后在界面上的每一次编辑都会**改到模板常量本身** —— 第二个用户（或同一用户第二次）点这个模板时，拿到的就不是原始模板了。这种 bug 表现为「模板越用越怪」，但**界面永远不报错**，极难排查。

---

## 8. 验收用例

| 编号 | 用例 | 步骤 | 预期 |
| --- | --- | --- | --- |
| **TC-38** | 套用模板 | 点「加日期前缀」 | 页签跳到「规则化」；前缀框出现 `{d} `；「启用日期」被勾上；日期格式为 `YYYY-MM-DD`；**预览里文件名真的变成 `2026-09-17 xxx`** |
| **TC-39** | 模板开正则 | 点「去掉括号」 | 页签跳到「替换字符」；查找框是那串正则；**「⚙ 进阶设置」折叠条变橙并显示「已启用 1 项」**；`【公众号】x.mp4` → `x.mp4` |
| **TC-40** | **正则要匹配全部** | 文件名 `(1)(2)报告.docx` → 点「去掉括号」 | 变成 `报告.docx`（**两组括号都去掉**，不是只去第一组） |
| **TC-41** | **模板常量不被污染** | 点「加日期前缀」→ 手动把前缀改乱 → 再点一次「加日期前缀」 | **第二次点仍然得到原始模板**（前缀回来是 `{d} `），不是上一轮改乱的值 |
| 附 | 摘要截断 | 套用「去掉括号」→ 去历史页看摘要 | 摘要被截断为 `替换「[（(【\[](?:[^…」→（删除）（正则）`；悬停能看到完整原文 |

---

## 9. 测试要求

按项目既定规矩（不引入第三方测试框架）：

| 层 | 要求 |
| --- | --- |
| 单测 | 6 个模板逐个套用后，断言 `RuleConfig` **逐字段等于**期望值（不能只断言「变了」） |
| 单测 | 套用「去掉括号」后 `regexEnabled === true`，且该正则**能被 `compileRegex` 编译通过** —— 模板里写错正则会当场红 |
| 单测 | **每个模板都要跑一遍预览**，断言至少有一个示例文件名发生变化 —— 防止做出一个「点了没反应」的模板 |
| 单测 | **模板常量不可变**：套用 → 改规则 → 再套用，断言第二次拿到的仍是原始值（对应 TC-41） |
| 单测 | 「去掉括号」在 `(1)(2)报告.docx` 上把两组全去掉（对应 TC-40） |
| 单测 | 摘要截断：超长正则被截断，且 `buildRuleSummary` 的原始输出**未被修改**（截断只在渲染层） |
| 界面冒烟（真实实例 + 真实点击） | 依次点 6 个 chip → 断言页签跟着跳、参数框里出现预期值、状态栏文案正确 |
| 交付报告 | 必须写「哪些没验到」（如打包后的 exe、深色主题下的 chip 观感） |

**红线照旧：绝不为让测试变绿而放宽 / 跳过 / 删断言。挂了就给原始报错。**

---

## 10. 待同步的文档

| 文档 | 需要的同步 | 状态 |
| --- | --- | --- |
| `需求清单.md` | F12 补「6 个内置模板的自定义 + 整份替换语义 + 不做高亮」 | ✅ 已同步（2026-09-18） |
| `PRD.md` | §3 F-12 细化；新增 DEC-14；§10 验收表加 TC-38~41；§5.1 元素清单加 EL-120 | ✅ 已同步（2026-09-18） |
| `交互说明文档.md` | §2 SCR-01 规则区加 EL-120 / IX-106；§1 对照索引加一行 | ✅ 已同步（2026-09-18） |
| `设计规范.md` | 新增「常用规则条」组件规格；**§5.3 历史卡片补摘要截断规则** | ✅ 已同步（2026-09-18） |
| `P1轻量设计确认.md` | **修正那句「折叠区同时是 P2 多规则链 / 模板库的预留挂载点」** —— 模板库不进折叠区，理由见本文档 §2.1 | ✅ 已同步（2026-09-18） |
| `技术方案文档.md` | §11 遗留项补「P2-B 设计已定稿」；`src/shared/templates.ts` 登记 | ✅ 已同步（2026-09-18） |

---

## 11. 已知取舍（如实记录）

| # | 取舍 | 说明 |
| --- | --- | --- |
| 1 | 只做 6 个内置模板，不做用户自定义 | 需求清单的原文范围就是「内置常用规则…一键套用」。自定义需要新增持久化 + 命名/删除/排序的完整管理界面，且与推迟中的「多规则链」耦合 —— 属于独立批次 |
| 2 | 套用直接覆盖、不确认 | 规则是可再编辑的，不像历史记录不可逆。加确认会抵消「一键」的意义 |
| 3 | 不做「当前模板」高亮 | 见 §5.1。宁可不给，也不给一个会变成谎话的指示 |
| 4 | 摘要截断只改显示 | 见 §5.2。不动持久化数据，不需要迁移 |
| 5 | 模板都是单模式 | 「多规则链」推迟中，所以模板只能是「一个模式 + 修饰项」的组合，做不出「先删广告再统一小写」这种链式模板 |
| 6 | 未覆盖 | 打包后的 exe；深色主题下 chip 的实际观感（本轮画板只出浅色） |

---

## 12. 待你（产品负责人）确认的事

1. **6 个模板的取舍**：现在覆盖「加日期 / 补零编号 / 日期+编号 / 去括号 / 换空格 / 全小写」。要不要增删？（模板是纯数据，加一个就几行，改起来很便宜）
2. **「去掉括号」的覆盖范围**：现在吃中文括号 `（）`、英文括号 `()`、方括号 `[]`、`【】`。要不要也吃 `《》` 和引号？—— 范围越大越容易误伤（把用户名字里正经的括号也删了），我倾向**保守**。
3. **CLI 要不要支持 `--template <名字>`**：我倾向**不做** —— 命令行用户直接写参数更直接；模板是给「在界面里不想自己配规则」的人用的。

> **产品负责人答复（2026-09-18）**：① 6 个模板**保持不动**（原话「我自己也不知道，暂时就这样吧」）；② 「去掉括号」**暂不吃** `《》` 与引号（「先不吃」）；③ CLI **不做** `--template`（「不做」）。**三条均按本文档的建议执行，范围未变。**

---

**P2-B 轻量设计确认 v1.0 · 2026-09-17 · 2026-09-18 落地（设计内容未改）**
