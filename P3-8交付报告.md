# 第 8 批（文件提取）交付报告

> 产品：耄耋改名（MaoDie Renamer）
> 批次：P3 第 8 批 · 文件提取
> 日期：2026-09-24
> 对应设计文档：`P3-8文件提取轻量设计确认.md` v1.0（状态已改为「已落地」）
> 配套画板：`fileId 724930301753449`，x = 23020

---

## 一、这次做了什么（大白话 + 例子）

前七批都是「给文件算个新名字、原地改名」。**这一批第一次按条件把列表里的文件真的搬走**——按你设的条件**挑出**一批文件，**复制或移动**到一个指定的文件夹。它是**第 7 批「文件夹合并」的孪生批**：复用同一套搬文件运输层，只是把「按四种方式归类」换成了「按三种条件筛选」。

**三种筛选条件（可叠加，AND）：**
1. 按类型：图片 / 文档 / 视频 / 音频 / 压缩包 / 其他 六类勾选
2. 按名称：关键字包含（可开正则开关，复用已有 F10 正则）
3. 按 Excel：选一张表，取「原文件名」那一列的名字来挑（复用第 4 批解析器）

**复制还是移动：** 两个都做，**复制是默认**（源文件保留）；**移动默认关**（开了才会移动并删掉源）。

**举个真实例子：**
> 你的列表里有一堆照片和文档：`旅游1.jpg`、`合同.pdf`、`旅游2.png`、`报告.docx`、`旅游3.png`。
> 打开「文件提取」→ 勾「图片」+ 名称填「旅游」→ 目标选 `D:\挑出来的` → 点「干跑并预览」。
> 报告列出：「将复制 3 个（旅游1/旅游2/旅游3），跳过 0，错误 0」。你点「确认执行」→ `D:\挑出来的\` 里多了这 3 张图，**原列表里的文件还在**（因为是复制）；`合同.pdf`、`报告.docx` 没被挑中，不动。

**三道安全闸（这一批动真文件，必须兜住）：**
- **干跑报告是硬要求**：没点「确认执行」之前，文件一个都不动；报告里写的「将复制 / 跳过」和真执行的结果**逐字节一致**（预览 ≡ 执行，与第 7 批同一份计算内核）。
- **绝不覆盖**：目标已有同名文件 → 默认**跳过 + 标红**，报告里逐条列出，不会偷偷覆盖你的东西。
- **Excel 对不上的名字不阻塞**：表里有的名字列表里找不到，单独列进 `unmatched`，不影响其余命中项照常提取。

**其它拍板项（都已落地）：** 源 = 当前已加载列表（不另起拖入区、不递归摊平）；目标文件夹「选已有 / 新建」都支持；执行完**默认打开目标目录**（可取消勾选）；不做撤销（先干跑）；独立对话框关掉对主流程零影响。

---

## 二、验证结果（全部跑过，不是嘴上说）

| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| 类型检查 | `npm run typecheck`（tsc + vue-tsc） | ✅ 0 错误 |
| 代码规范 | `npm run lint`（eslint） | ✅ 0 错误（仅 2 条**历史遗留**警告在 `RulePanel.vue:496`，与本批无关） |
| 单元测试 | 直接 `node --test --import tsx` 跑全部 14 个测试文件（绕过 `run-unit.js` 的 EBUSY 自锁） | ✅ **293 / 293 通过**（含本批一套：`p3-8-extract-filter` 13 子测试：categoryOf 映射 / 三条件 AND / 正则非法 EX-22 / Excel 识别 EX-21 / unmatched） |
| 生产构建 | `npm run build`（electron-vite build） | ✅ 主进程 145.41 kB / 预加载 4.85 kB / 渲染层 530.36 kB，全部构建通过 |

> 红线照旧：**绝不为让测试变绿而放宽 / 跳过 / 删断言**。挂了就给原始报错。
> 注：`npm run test:core` 经 `tests/run-unit.js` 在本沙箱会触发 `spawnSync ... EBUSY` 自锁（环境坑，非代码问题），故本批验证用等价命令直接 `node --test --import tsx` 跑全量，结果以本机会话内验证链为准。

---

## 三、★ 哪些没验到（如实登记，不瞒）

以下四项受「沙箱 ≠ 真机」限制，**代码逻辑已写，但没有在对应真实环境里跑过**，请你真机实测时重点看：

1. **真实界面冒烟（真实 Electron 点击）**：设计 §10 要求的「工具栏出现按钮 → 打开对话框 → 设筛选 → 点干跑出报告 → 确认执行后文件真在目标处 → 冲突标红 → 完成后打开目录」，**沙箱里没有可交互的 GUI，没有真点过**。逻辑与第 7 批同源，但本批独立对话框的点击链路未在真机验证。
2. **真实跨盘移动**：同盘移动走系统 `rename`（快路径），跨盘移动 = 复制 + 删源。跨盘分支逻辑已写，但沙箱里难造跨盘环境，没在真机跨盘跑过。
3. **网络盘 / 不可 reveal 的路径**：「完成后打开目录」若目标不可打开（如网络盘断连），已做 try/catch 且**不影响主结果**，但没真连网络盘测过。
4. **打包后的 exe**：沙箱里只有源码和开发态，打包成安装版后双击能否正常跑，**必须你在本机真机实测**。

---

## 四、★ 需要你拍板的一个冲突（重要，请看完）

**移动模式，是全产品唯一会删用户文件的地方。** 和第 7 批一样，这和项目最高铁律「**绝不删除用户文件（P-03）**」是**直接矛盾**的。

我已经尽量收着做：移动**默认关**（用户不主动开就不生效）、采用**两阶段**（先把所有文件复制成功，再统一删源；任何一处删源失败就标红报告、且**绝不回滚已复制的文件**）、报告里明示。但「删源」这个动作本身，仍然违反 P-03。

**请你二选一：**
- **① 保留移动**（默认关，用户主动开才生效）—— 功能更全，但承认它破了一条铁律；
- **② 干脆去掉移动，只留复制** —— 完全守住 P-03，但少一个「移动」能力。

这条冲突已写进设计文档 **DEC-28**（范围 DEC-27、运输层复用 P3-7 DEC-29），等你的决定。无论选哪个，我都会照做并补测。
> 说明：第 7 批的 DEC-26 是同一个「剪切 vs P-03」冲突，两批共用同一处风险，需你一并拍板。

---

## 五、文档同步清单（8 份，已按设计 §11 同步）

| 文档 | 同步内容 |
| --- | --- |
| `需求清单.md` | 第 8 批行：状态「待做 · 必须单独评审」→「✅ 已落地」，补三种筛选可叠加 / 复制+移动 / 单一目录 / 独立对话框 / 源=当前列表 |
| `PRD.md` | 追加「第 8 批（文件提取）」章节：EL-149 / IX-126~129 / TC-88~95 / DEC-27~29 / EX-21~22 / SCR-13~15 / 两通道 |
| `交互说明文档.md` | 追加「第 8 批交互」章节 + 对照表 |
| `设计规范.md` | 追加「对话框规格」附录（零新增令牌、筛选区 data-extract-* 锚点、TC-32 已验证全绿） |
| `技术方案文档.md` | 追加「P3-8 登记」附录（新增/改动文件、契约 20→22、零持久化、零依赖、单测、复用 P3-7 运输层） |
| `接口文档.md` | 追加「第 8 批通道」章节（`extract:plan` / `extract:run` 入参返回、EX-21/22、红线） |
| `数据库设计.md` | 追加「对数据层影响」附录（零新增持久化，避免后人以为漏了） |
| `P3-2-8批范围澄清与交付总规划.md` | §5 第 8 批块由「待确认」改写为**已落地结论 + 编号用量** |

---

## 六、编号清单（本批实际占用）

| 类别 | 本批占用 |
| --- | --- |
| 元素 EL | EL-149（入口按钮）· EL-150~155（对话框内元素） |
| 交互 IX | IX-126（打开对话框）· IX-127（设筛选）· IX-128（选复制/移动+目标）· IX-129（确认干跑→执行） |
| 验收 TC | TC-88 ~ TC-95 |
| 决策 DEC | DEC-27（本批范围）· DEC-28（不开「覆盖」档 / 移动冲突待拍板）· DEC-29（运输层整体复用 P3-7） |
| 错误码 EX | EX-21（Excel 列没找到 / 对不上）· EX-22（按名称正则非法） |
| 界面 SCR | SCR-13（外壳）· SCR-14（表单含筛选）· SCR-15（干跑报告） |
| IPC 通道 | **新增** `extract:plan` / `extract:run`（请求响应通道 20 → 22，事件通道仍 3 个） |
| 持久化字段 | 零新增 |

> 编号按前缀全量重扫、排除 `P3-2` 总规划文件后连续取，未与既有号段撞车。
