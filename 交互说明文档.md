# 交互说明文档

> 产品名：**耄耋改名（MaoDie Renamer）**
> 文档版本：**v2.1**
> 日期：2026-09-18
> 上游文档：`PRD.md` v1.1.8 · 《原型图.html》v1.1 · **`P1轻量设计确认.md` v1.0（P1 交互唯一权威）** · **`P2-A轻量设计确认.md` v1.3（P2-A 交互唯一权威）** · **`P3-1编号系统升级轻量设计确认.md` v1.1（P3 第 1 批交互唯一权威）**
> 本文档作用：**把「点了会怎样」写成开发可直接照着做的规则**，与 PRD 的功能编号、原型图的元素编号一一对应。

> ★ **2026-09-23 更新（v2.1 · P3-6）：提取文件夹名**
>
> · 属性组从 3 个 chip 变成 **4 个**（`EL-140`）：新增「**文件夹名**」`{文件夹}`。
> · **`IX-121`**：点这个 chip → 把 `{文件夹}` 追加到前缀末尾 —— **复用 `IX-112` 的同一个交互**，零新交互。
> · ★ 前缀 / 后缀下面那行提示改为**由属性变量清单生成**（`shared/attr-vars.ts`），`{文件夹}` 自动出现在里面。
> · 属性组下面新增一句常显小字：只填 `{文件夹}` 的话同文件夹同类文件会重名，建议配 `{n}`。
>
> ★ **2026-09-23 更新（v2.0 · P3-5）：模式页签 3 → 5**
>
> | 页签 | 值 | 说明 |
> | --- | --- | --- |
> | 删除 | `delete` | 原「删除字符」 |
> | 替换 | `replace` | 原「替换字符」 |
> | **自定义** | `rule` | **原「规则化」改名不改值** —— 老记录里仍显示「规则化」 |
> | **插入** | `insert` | **新增**：在第 N 个字后插入任意文字（`EL-136` / `IX-117`） |
> | **导入** | `import` | 第 4 批的导入表格能力**提升为模式**（`EL-137` / `IX-118`） |
>
> · 标签改成**两个汉字**（一排里更站得开）；默认页签从「删除字符」改成**「自定义」**。
> · **扩展名小组**（`EL-138` / `IX-119`）：默认关，勾上才出现档位下拉，与「大小写」正交。
> · **「只用编号」= 第 7 个内置模板**（`EL-139` / `IX-120`，零新机制）。
> · 历史页顶部加一句：「早期版本的记录里，**自定义**叫**规则化**」——是同一个功能改了名字。
> · ⚠️ 下面 §2 / §3 里凡是写「三选一 / 三模式」的表述，**一律以本表为准**。

---

## 0. 阅读方法

### 0.1 编号体系

| 前缀 | 含义 | 本文档中的位置 |
| --- | --- | --- |
| `SCR-xx` | 界面 | §1 |
| `R-xx` | 界面区域 | §2.2 |
| `EL-xxx` | 界面元素 | §2.3 起各节 |
| `IX-xxx` | 交互规则 | 各界面下的「交互规则」表 |
| `ST-xx` | 耄耋形象状态 | §7 |
| `EX-xxx` | 异常 / 边界 | §10 |
| `F-xx` / `DEC-xx` / `TC-xxx` | 功能 / 决策 / 验收用例（见 PRD） | 交叉引用 |

> **元素编号说明**：**静态元素**（按钮、输入框、区域等）的编号与原型 HTML 的 `id` 完全一致，可直接定位；**动态生成的元素**（列表行、历史卡片、弹窗内部控件如 EL-082 / EL-090 ~ EL-102）为逻辑编号，实现时按 `data-*` 属性或组件名对应即可。

### 0.2 怎么用这份文档

1. 打开《原型图.html》，按界面看效果。
2. 在本文档找到对应 `EL-xxx`，读它的**默认态 + 交互规则**。
3. 每条交互都标了触发条件、行为、反馈、异常分支，编码时按表实现即可。

> 原型 HTML 中每个元素的 `id` 就是 `EL-xxx`，可直接在浏览器控制台用 `document.getElementById('EL-060')` 定位到它。

---

## 1. 界面清单与跳转关系

| 编号 | 界面 | 形态 | 进入方式 | 退出方式 |
| --- | --- | --- | --- | --- |
| SCR-01 | 主窗口 | 独立窗口 | 启动软件 | 关闭窗口 |
| SCR-02 | 撤销 / 历史页 | 主窗口内视图切换 | 点击 EL-061「撤销」 | 点击 EL-080「返回主界面」 |
| SCR-03 | 结果弹窗 | 模态 | 改名执行完成 | EL-092 关闭 / EL-093 撤销本次 |
| SCR-04 | 冲突提示弹窗 | 模态 | 存在冲突项时点击「开始改名」 | EL-101 先不改 / EL-102 自动加序号 |
| SCR-05 | 二次确认弹窗 | 模态 | 待改项 ≥ 10 时点击「开始改名」 | 取消 / 确认 |
| SCR-06 | 系统文件 / 文件夹选择框 | 系统原生 | 点击 EL-020 / EL-021 | 系统行为 |
| **SCR-07** | **设置弹窗** | 模态 | 点击 **EL-106**「设置」（标题栏，窗口三键左侧） | **IX-099**：点遮罩 / 按 `Esc` / 点「完成」三者**等价**（**P2-A / DEC-11**）|
| **SCR-08** | **命令行图文教程弹窗** | 模态（叠加在 SCR-07 之上） | 点击 SCR-07 里的 **EL-118**「查看教程」（**P2-C 增量**） | **IX-105**：点遮罩 / 按 `Esc` / 点「知道了」（第 3 步时）/ 点「关闭」—— 均只关教程，**不连带关掉设置弹窗** |

### 1.1 主流程（编号为交互编号）

```
启动 → SCR-01 主窗口（ST-01 待机）
   │
   ├─[IX-020/021] 点击添加 ────┐
   ├─[IX-022]     拖拽入窗口 ──┤
   │                          ↓
   │                   文件入列 → ST-02 接收文件（1.5s 后回 ST-01）
   │                          ↓
   ├─[IX-040~049] 设置规则（规则参数变更即触发 IX-050 预览刷新，200ms 防抖）
   │                          ↓
   │                  预览列表更新（变化字符高亮 / 冲突标红）
   │                          ↓
   └─[IX-060] 点击「开始改名」
              │
              ├─ 按钮置灰？── 是 → 无动作（EX-11 / EX-12）
              ├─ 有冲突项？── 是 → SCR-04 冲突弹窗
              │                    ├─ 先不改 → 回到主界面
              │                    └─ 自动加序号 → 继续
              ├─ 待改 ≥ 10？── 是 → SCR-05 二次确认
              │                    ├─ 再改改 → 结束
              │                    └─ 确认 → 继续
              ↓
        ST-03 改名中（进度条 + 可取消 IX-061）
              ↓
        执行完成 → ST-04 成功 / ST-05 失败
              ↓
        SCR-03 结果弹窗
              ├─ 「知道了」→ 关闭
              └─ 「撤销本次」→ 执行撤销（IX-090）
```

> **图外分支（P2-A / DEC-11）**：任何时刻点击标题栏的「设置」（EL-106 / IX-095）都会弹出 **SCR-07**。
> 它**不打断**当前任务 —— 不改列表、不改规则、不重算预览、不重置猫咪状态。

---

## 2. SCR-01 主窗口

### 2.1 窗口行为

| 项 | 规则 |
| --- | --- |
| 默认尺寸 | 1080 × 680 |
| 最小尺寸 | 900 × 560（再小时布局横向挤压，列表区优先保留，形象区可缩短） |
| 记忆 | 关闭时保存尺寸与位置，下次启动恢复（TC-17） |
| 缩放 | 左栏固定 300px，右栏自适应；窗口宽度 < 960px 时左栏收窄至 260px |

### 2.2 区域清单

| 编号 | 区域 | 位置 | 元素 |
| --- | --- | --- | --- |
| R-01 | 标题栏 | 顶部通栏 | EL-001 ~ EL-004 |
| R-02 | 形象区 | 左栏上部（弹性） | EL-010 ~ EL-017 |
| R-03 | 操作区 | 左栏下部 | EL-020, EL-021, EL-022，**P3-2 起加 EL-126（导出清单）**，**P3-4 起加 EL-132（导入表格）** |
| R-04 | 文件列表区 | 右栏上部（弹性，占比最大） | EL-030 ~ EL-037 |
| R-05 | 规则设置区 | 右栏中部 | EL-040 ~ EL-055 |
| R-06 | 动作区 | 右栏下部 | EL-060, EL-061 |
| R-07 | 状态栏 | 底部通栏 | EL-070 ~ EL-072 |

### 2.3 元素清单（SCR-01）

| 编号 | 元素 | 类型 | 默认态 | 说明 |
| --- | --- | --- | --- | --- |
| EL-001 | 产品名「耄耋改名」 | 文本 | 显示 | 不可点击 |
| EL-002 | 最小化 | 按钮 | 可用 | 系统行为 |
| EL-003 | 最大化 | 按钮 | 可用 | 系统行为 |
| EL-004 | 关闭 | 按钮 | 可用 | 悬停变红；直接关闭，**不做「确认退出」**（因为改名已完成、列表不持久化） |
| EL-010 | 耄耋动画区 | 图形 | ST-01 | 状态机载体，同时是拖拽落区 |
| EL-011 | 状态文字 | 文本 | 「待机中…」 | 随 ST 变化 |
| EL-012 | 拖拽蒙层 | 覆盖层 | 隐藏 | 拖拽悬停时显示 |
| EL-013 | 咬名字动画区（bite-lane） | 动画层 | 隐藏 | **仅 ST-03 改名中显示**；叠在耄耋身前，承载下面几张卡片 |
| EL-014 | 名字卡片（chip） | 动画元素 | 隐藏 | 单张循环飞行；1.6s 一轮 |
| EL-015 | 卡片图标 | 图形 | — | 文件 → 文件图标；文件夹 → 文件夹图标 |
| EL-016 | 卡片旧名 | 文本 | — | 被咬前显示，超过 14 字截断（保留扩展名） |
| EL-017 | 卡片新名 | 文本 | 隐藏 | 被咬后淡入，橘色 |
| EL-020 | 添加文件 | 按钮（次要） | 可用 | 打开系统文件多选框 |
| EL-021 | 添加文件夹 | 按钮（次要） | 可用 | 打开系统文件夹选择框（**可多选**，一次可加入多个文件夹） |
| EL-022 | 清空列表 | 按钮（幽灵） | 可用 | 列表为空时点击无动作 |
| EL-030 | 列表计数 | 文本 | 「共 0 项」 | 实时更新 |
| EL-031 | 全选 | 复选框 | 未勾选 | 勾选后所有行进入选中态 |
| EL-032 | 列表容器 | 滚动列表 | 空 | 空时显示 EL-037 |
| EL-033 | 原名文本 | 文本 | — | 过长省略，悬停显示全名 |
| EL-034 | 箭头「→」 | 装饰 | — | — |
| EL-035 | 新名文本 | 文本 | — | 变化片段高亮 |
| EL-036 | 行内删除 | 图标按钮 | 隐藏 | 行悬停时出现 |
| EL-037 | 空状态引导 | 插画 + 文案 | 显示 | 列表为空时显示 |
| EL-040 | 规则页签组 | 单选组 | **「自定义」选中**（P3-5 默认改了） | **五选一**，互斥（EL-135：新增「插入」「导入」两页） |
| EL-041 | 删除 - 待删字符串 | 输入框 | 空 | 支持多字符；空时规则不生效 |
| EL-042 | 替换 - 查找 | 输入框 | 空 | 空时规则不生效 |
| EL-043 | 替换 - 替换为 | 输入框 | 空 | **允许留空**（等价删除） |
| EL-044 | 区分大小写 | 复选框 | **未勾选** | 规则化模式下隐藏（该模式不涉及匹配） |
| EL-045 | 前缀 | 输入框 | 空 | 支持 `{d}` `{n}` 变量 |
| EL-046 | 后缀 | 输入框 | 空 | 加在扩展名前 |
| EL-047 | 启用序号 | 复选框 | 未勾选 | 勾选后 EL-048 ~ EL-054 与 **P3-1 的 EL-121 ~ EL-125** 生效；未勾选时整组置灰，且 **EL-124 示例行整块不显示** |
| EL-048 | 序号起始值 | 数字框 | 1 | 最小 0 |
| EL-049 | **增量** | 数字框 | 1 | 最小 1。数字 / 字母 = 每次加几；时间 = 每次往后几天。**P3-1 起改称「增量」**（原文「步长」，与小白用户的说法对齐）|
| EL-050 | **位数** | 数字框 | 3 | 范围 0–6。**只对「数字」类型显示**（字母不补零；随机字符的长度是另一个框；时间的补零由样式决定）。**P3-1 起改称「位数」**（原文「补零位数」）|
| EL-051 | 启用日期 | 复选框 | 未勾选 | 勾选后 EL-052 生效 |
| EL-052 | 日期格式 | 下拉 | `YYYY-MM-DD` | **五选一**（P3-1 新增 `MM月DD日` / `YYMMDD`），与 EL-122 里「时间」类型的「样式」**共用同一张表** —— 两处各写一份必然漂移 |
| EL-053 | 保留原文件名 | 复选框 | **勾选** | 取消后丢弃原主体 |
| EL-054 | **位置** | 下拉 | 「排在最后」 | **三档**：排在最后 / 排在最前 / **插在第 n 个字符后**（P3-1 新增第三档；选中后就地长出 **EL-123**）。**P3-1 起标签由「序号位置」简化为「位置」** |
| EL-055 | 自动处理重名冲突 | 复选框 | **未勾选** | DEC-05 安全默认 |
| **EL-056** | **进阶设置折叠条** | 折叠条 | **折起** | **P1（DEC-10）**。位于参数区与 EL-055 之间。折起态：齿轮 14px `#8A8178` + 「进阶设置」+ 右侧箭头；**有启用项时**图标与文字变 `#E08B33` 并显示胶囊徽标「已启用 N 项」（底 `#FFF3E2` / 圆角 6px / 内边距 3×8px）。展开块：底 `#FFF6E9`、圆角 10px、内边距 12px、行距 10px（与现有参数组一致，不新增色） |
| **EL-057** | **用正则匹配** | 复选框 | **未勾选** | **P1（DEC-10）**。在 EL-056 展开块内；**只在「删除字符 / 替换字符」模式出现**（与 EL-044 同规则，规则化模式隐藏）。勾选后 EL-041 / EL-042 / EL-043 的内容按正则解释，输入框字号切 `Inter` |
| **EL-058** | **正则错误提示** | 文本 | **仅非法时出现** | **P1（DEC-10）**。**非法时**在输入框下方追加一行**红字** `#E5544B` 原因（由运行时错误映射，如「正则表达式不完整：缺少右括号 `)`」）。<br>注（2026-09-15）：原型期的「语法提示」两行小字（`. 任意字符 · \d 数字 · …`）**已由 EL-104 的小白化说明取代** —— 那是给已经会正则的人查的，新手看完仍不知道要填什么。|
| **EL-059** | **大小写转换** | 下拉 | 「保持原样」 | **P1（DEC-10）**。在 EL-056 展开块内，**三种模式都出现**。四选一：保持原样 / 全部小写 / 全部大写 / 首字母大写 |
| **EL-103** | **「? 看不懂？」帮助按钮** | 按钮 | 未展开 | **P1 说明改进（2026-09-15）**。在 EL-057 的**同一行右侧**；**必须放在复选框 `<label>` 之外** —— 塞进 label 里点它会连带勾选复选框。点开 / 收起 EL-104 |
| **EL-104** | **正则说明（小白化）** | 说明区 | 常显对照 + 折叠照抄表 | **P1 说明改进（2026-09-15）**。两部分：① **常显两行大白话对照** ——「不用正则：你填什么就找一模一样的字…换个日期就找不着了」/「用正则：你说「长什么样」，数字换了也认得出…」；② 点 EL-103 后**就地展开照抄表**（不弹浮层，避免遮挡文件列表），每行 = 「我想做的事 → 查找填 → 替换填 → 举例结果」，且**按当前模式过滤**（删除模式 3 行、替换模式 4 行；含 `$1` 的行只在替换模式出现）。表内每一行的「举例结果」由 `rule-engine` 真算校验（单测钉住），文案不许与引擎行为脱节 |
| **EL-105** | **当场演示** | 演示区 | 正则开启且写法合法时出现 | **P1 说明改进（2026-09-15）**。用**固定示例名** `发票 2026-08-01.pdf`（**不取列表里的真实文件** —— 教学示例必须保证看得出变化；真实文件不一定匹配得上当前写法，那时一片未变化反而更让人懵），把当前填写的内容套上去**实时算一遍**，显示「原名 → 新名」。调的是 `applyDelete` / `applyReplace`（与预览、执行共用同一份源码，ADR-001），所以演示结果不可能与实际执行不一致。**匹配不上时**新名用灰字 `#B9AC9E`（不谎报「变了」）并提示「这条示例里没找到能匹配的内容」；还没填内容时显示「在上面「查找」框里填点什么，这里就会立刻跟着变」。写法非法时整块隐藏，由 IX-053 的红字原因负责说明 |
| **EL-106** | **设置入口按钮** | 按钮 | 可用 | **P2-A（DEC-11）**。位于标题栏右侧、窗口三键（EL-002 / EL-003 / EL-004）的**左侧**。38×30px、圆角 7px，与三键同尺寸。图标是**滑杆** 14px（`icon-设置.svg`，`currentColor`），**刻意不用齿轮** —— P1 的 EL-056 已经用了齿轮，同屏出现两个齿轮会让人以为它们是同一个东西。默认态底透明、图标 `ink-2` / 深色 `#CBB9A8`；悬停态底 `brand-tint`、图标转 `orange-dark` |
| **EL-107** | **设置弹窗**（= SCR-07） | 模态 | 隐藏 | **P2-A（DEC-11）**。复用现有模态外壳（480 宽 / 圆角 16px / 内边距 24px / 遮罩 `scrim` / 220ms 入场），**零新视觉**。底部为「左说明 + 右按钮」的 spread 布局（说明文案「设置立即生效，不需要重启。」+ 按钮「完成」），并带一条上分割线 |
| **EL-108** | **主题三选一** | 单选组 | **「始终浅色」** | **P2-A（DEC-11）**。跟随系统 / 始终浅色 / 始终深色。视觉**复用页签样式**（`.md-tabs` + `.md-tab`）；但语义用 `role="group"` + `aria-pressed`，**不是** `role="tablist"` —— 这里是一次三选一，底下没有可切换的内容面板，用 tab 语义会承诺一个不存在的面板关系。下方一行说明：「跟随系统」会跟着 Windows 的深浅色自动切换 |
| **EL-109** | **音效开关** | 开关 | 开 | **P2-A（DEC-11）**。此前 `soundEnabled` 已生效（`task.ts` 播放音效时读它），但**界面上没有任何入口**，用户只能手改 `prefs.json`。本批补上 |
| **EL-110** | **减少动画开关** | 开关 | 关 | **P2-A（DEC-11）**。补齐 §9「动效可关」与设计规范 §8 要求的那个开关的**入口** —— 开关本身早就写好、也能生效，此前只是打不开。下方一行说明：「开启后猫咪状态切换只换表情、不做位移，动效时长归零。」|
| **EL-114** | **命令行行** | 折叠行 | **P2-C（DEC-12）**。设置弹窗（SCR-07）的**第 4 项**：标签 13.5 Medium `ink-1` + 右侧 12px 箭头。折起 / 展开是**纯 UI 状态** |
| **EL-115** | **命令代码块** | 代码块 | **P2-C（DEC-12）**。展开后显示一条可复制的示例命令：`maodie.exe --rename --dir "D:\下载\素材" --delete "广告" --yes`。底 `bg-sunken`、圆角 10px、内边距 10×12px、Inter 11.5 Medium `ink-1`。**只给一条**（保持简洁）|
| **EL-116** | **复制按钮组** | 按钮组 | **P2-C（DEC-12）**。次按钮样式：**复制命令** / **复制程序路径**（底白、描边 `line`、圆角 10px、文字 12.5 Medium `ink-1`）。后者的用途是解决进阶用户「**不知道程序装在哪**」这个最大障碍 —— 程序路径取自 `AppInfo.execPath` |
| **EL-117** | **命令行引导卡** | 说明卡 | **P2-C 增量（DEC-13）**。在「命令行」展开块**顶部**、命令代码块**之上**。左侧一句话说明（12.5 `ink-2`）「要让别的程序（或定时任务）自动帮你改名时用得上。没接触过命令行也没关系，有图文教程。」+ 右侧次按钮 **EL-118**。**取代**了原先那句「给进阶用户用脚本批量改名」—— 后者默认用户已经懂了「脚本」「命令行」是什么 |
| **EL-118** | **「查看教程」按钮** | 次要按钮 | **P2-C 增量（DEC-13）**。次按钮样式（与 EL-116 同规格）；点击打开 **SCR-08** 图文教程。**这是小白进教程的唯一入口**，必须留在设置弹窗内（不另开菜单、不加标题栏按钮） |
| **EL-119** | **命令行图文教程弹窗**（= SCR-08） | 模态 | **P2-C 增量（DEC-13）**。宽 **560px**；结构 = 顶部说明段 + 步骤页签（`第 1 / 2 / 3 步`）+ 步骤卡（**左图右字**）+ 底栏「第 N / 3 步」与翻页按钮。图是 **SVG 线框示意图**，**不随主题翻转**（见设计规范 §5.6） |
| **EL-120** | **「常用规则」模板条** | 常显（规则区 R-05 **顶部、页签之前**） | **P2-B（DEC-14）**。结构 = 小标题「常用规则」（12px Medium `ink-3`）+ 一行 **6 个 chip**（`wrap` 布局、间隔 6px）。chip 高 **26px**、内边距 **4 × 11px**、圆角 **9px**（复用 `radius-chip`）；底色 `orange-soft`、字 12.5px Medium `orange-dark`；悬停底转 `orange-primary`、字转 `#2A1A10`（对比 8.3:1）。<br>**6 个 chip 不单独编号** —— 它们是同一个组件的实例（与「列表行不单独编号」同理），定位用 `data-template="<id>"`。<br>⚠️ **它必须在折叠区之外**：这是给不会配规则的人准备的一键入口，藏进「⚙ 进阶设置」就违背目的（P2-B §2.1；并据此更正 `P1轻量设计确认.md` 里「折叠区是模板库预留挂载点」那句） |
| **EL-121** | **编号类型下拉** | 下拉 | 「数字」 | **P3-1（DEC-15）**。在序号组内、**EL-054 之下**（位置排在类型之前 —— 沿用 P0 的排布，也正是列需求时的语序）。四选一：**数字 / 字母（Excel 列标）/ 随机字符 / 时间**。它是序号组里的**一个参数**，不是第四种模式 —— 等到第 5 批把模式拆成五个，这个下拉照样待在这儿。<br>⚠️ 组标题仍叫「启用序号」，**不跟着类型改名**（它是这个组的开关）|
| **EL-122** | **类型参数区** | 参数行（随类型切换） | — | **P3-1（DEC-15）**。**数字** = 起始 · 增量 · 位数；**字母** = 起始 · 增量（**没有位数**）；**随机字符** = 长度 + EL-125；**时间** = 起点 · 样式 · 增量（天）。<br>**该消失的就消失，不是置灰**：摆一个填了也没用的框，只会让人怀疑自己填错了 |
| **EL-123** | **「第 n 个字符后」内联数字框** | 数字框 | 1 | **P3-1（DEC-15）**。**只在位置选第三档时出现**：就地长在 EL-054 同一行右侧、**不另开一行**；选回前两档就消失（**不是置灰** —— 它只对第三档有意义）。宽 **64px**、数字**居中**（`.md-input--num` 给的 104px 太宽，左对齐会看着像「还没填」）。范围 1–200；选中第三档时**焦点自动落进来**（IX-109）。下方常显一句「数字超出名字长度时，会自动放到末尾。」—— 把越界规则说在前面，省掉一次「为什么没生效」|
| **EL-124** | **示例行** | 演示区（常显） | 未勾 EL-047 时整块不显示 | **P3-1（DEC-15）**。拿**固定示例名** `【素材】` 试前三个位置，三个位置**用不同种子** —— 随机字符类型下会显示三个不同的串，而不是把同一个串贴三遍（后者会让人误以为「所有文件都会被改成同一个名字」）。文案里**明说**「（拿「【素材】」这个名字试的，不是你列表里的文件）」。<br>凹槽规格与 P1 的 EL-105 **完全同构**（底 `bg-sunken`、圆角 `radius-input`、槽内 `code` 提亮回 `bg-card`，新名用 `orange-dark` 字 + `highlight-bg` 底），**零新样式、零新令牌**。<br>它走的是与预览 / 执行**同一个 `applyRuleMode`**（ADR-001），所以示例**不可能**与真正改下去的结果不一致 |
| **EL-125** | **「换一批」按钮** | 按钮 | 仅「随机字符」类型出现 | **P3-1（DEC-15）**。高 **28px**、内边距 0 × 12px、圆角 `radius-badge`、底 `bg-card`、描边 `line-strong`。<br>**高 28px 是单独给的**：既有按钮类只有 22px，够不上 WCAG 2.5.8 AA 要求的 24×24 CSS px，所以这个新按钮**不复用**它们的高度。点一次 = 种子 **+1**（IX-108）|
| EL-060 | 开始改名 | 主按钮 | **置灰** | 无有效改名项时禁用 |
| EL-061 | 撤销 | 次要按钮 | 可用 | 进入 SCR-02 |
| EL-070 | 状态点 | 圆点 | 绿色 | 执行中橙色脉动，有冲突 / 非法时红色 |
| EL-071 | 状态文本 | 文本 | 「就绪」 | 见 §11.2 |
| EL-072 | 进度条 | 进度条 | 隐藏 | 仅执行中显示 |

### 2.4 交互规则（SCR-01）

#### 输入相关

| 编号 | 触发 | 行为 | 反馈 | 异常分支 |
| --- | --- | --- | --- | --- |
| **IX-020** | 点击 EL-020 | 打开系统文件多选框（可跨目录多选） | 选中后逐项入列 | 取消选择 → 无任何变化 |
| **IX-021** | 点击 EL-021 | 打开系统文件夹选择框（标题标「可多选」，支持 Ctrl + 点击逐个挑、Shift + 点击连选） | 选中的**每一个**都只加入「该文件夹本身」，**不展开内部**（DEC-01）；一次选 N 个即一次入列 N 项，攒起来**只调一次** `resolvePaths`（去重、取目录快照都是批量操作） | 取消选择 → 无任何变化 |
| **IX-021a** | 再次点击 EL-021 | 对话框直接停在**上次那批文件夹所在的父目录** | 仅本次运行内记住（不落盘），重启软件后回到默认位置 | — |
| **IX-022** | 从资源管理器拖文件进入窗口 | ① 进入时 EL-012 蒙层显示、ST-02；② 落下时逐项入列；③ 蒙层隐藏 | 状态文字「收到啦，N 个！」 | 见 IX-023 / IX-024 |
| **IX-023** | 拖入网页链接 / 纯文本等非文件对象 | 落区保持拒绝态，**不接收** | 状态栏提示「只能拖入文件或文件夹哦」 | EX-08 |
| **IX-024** | 拖入已在列表中的路径 | 静默忽略该重复项 | 状态栏追加「已忽略 N 个重复项」 | EX-09 |
| **IX-025** | 点击 EL-022 清空列表 | 列表非空 → 弹二次确认；为空 → 无动作 | 确认后列表清空、ST-01 | 清空只移除列表项，**不删除文件** |

> **IX-022 实现要点**：`dragenter` 需计数（嵌套元素会重复触发），`dragleave` 计数归零才隐藏蒙层；`drop` 后必须重置计数。

#### 列表相关

| 编号 | 触发 | 行为 | 反馈 |
| --- | --- | --- | --- |
| **IX-030** | 鼠标悬停列表行 | 该行显示 EL-036 删除按钮；背景轻微变化 | 行高亮 |
| **IX-031** | 点击 EL-036 | 移除该行，其它行序号重排，预览与序号规则**重新计算** | 计数与状态栏更新 |
| **IX-032** | 滚动列表 | 常规滚动；> 500 行时启用虚拟滚动 | 滚动条加粗缓动 |
| **IX-033** | 勾选 EL-031 全选 | 所有行进入选中态 | 再次点击取消全选 |

> 序号规则（`{n}`）以**列表当前顺序**为准；删除行后其余项序号会前移，需在交互上明确提示（状态栏文案：「删除后序号已重新编排」）。

#### 规则相关

| 编号 | 触发 | 行为 |
| --- | --- | --- |
| **IX-040** | 点击 EL-040 中任一页签 | 切换规则模式（**五选一**互斥单选）；EL-044 只在「删除 / 替换」下出现；**切换不清空任何参数**（P3-5 §5.④） |
| **IX-041** | 在 EL-041 输入 | 实时更新删除字符串 |
| **IX-042** | 在 EL-042 / EL-043 输入 | 实时更新查找 / 替换串 |
| **IX-043** | 勾选 / 取消 EL-047 | 启用或停用序号；未勾选时 EL-048/049/050/054 置灰不可编辑 |
| **IX-044** | 修改 EL-048/049/050/054 | 立即生效；起始值 < 0 时钳制为 0，步长 < 1 时钳制为 1，补零位数钳制 0–6 |
| **IX-045** | 勾选 / 取消 EL-051 | 启用或停用日期；未勾选时 EL-052 置灰 |
| **IX-046** | 切换 EL-052 | 立即按新格式重算预览 |
| **IX-047** | 勾选 / 取消 EL-053 | 取消后原主体被丢弃（用于归档重命名） |
| **IX-048** | 勾选 / 取消 EL-044 | 切换匹配是否区分大小写；仅对删除 / 替换模式有效 |
| **IX-049** | 勾选 / 取消 EL-055 | 切换重名冲突策略（跳过 / 自动加序号）；切换后立即重算预览 |
| **IX-050** | 任意规则参数变更 | **200ms 防抖后重算全量预览**并重绘列表 |
| **IX-051** | 点击 EL-056 | 展开 / 收起进阶设置。**纯 UI 状态，不进 `RuleConfig`、不进 IPC**；展开动作本身不触发重算 |
| **IX-052** | 勾选 / 取消 EL-057 | 切换「内容是否按正则解释」；切换后立即重算预览。**关闭时输出与 P0 逐字节一致** |
| **IX-053** | EL-057 已勾选，且 EL-041 / EL-042 的正则**无法编译** | ① 该输入框描边 **2px `#E5544B`**；② 输入框下方显示红字原因（由运行时错误映射，如「正则表达式不完整：缺少右括号 `)`」）；③ 状态栏追加「· 正则表达式有误」、状态点转红；④ **EL-060 置灰**；⑤ **保留上一次的合法预览**，不清空、不整屏标红（避免闪动）。见 EX-15 |
| **IX-054** | 切换 EL-059 | 立即按新模式重算预览。**只作用于「新名主体」，扩展名永不动**；**执行顺序 = 先按模式算出主体、最后整体做大小写**（`新名主体 = 大小写转换(模式结果)`）。「首字母大写」= 只把主体的第一个字符转大写、其余保持原样 |
| **IX-093** | 点击 EL-103 | 展开 / 收起 EL-104 的照抄表。**纯 UI 状态，不进 `RuleConfig`、不进 IPC**；不触发重算。切换 EL-040 页签时保持当前开合状态，但表内容按新模式过滤 |
| **IX-094** | EL-041 / EL-042 内容变更（EL-057 已勾选且写法合法） | EL-105 立刻按新内容重算示例名的结果（纯文本计算，不走 200ms 防抖）。**写法非法时演示区整体隐藏**；匹配不上时显示未变化 + 提示，不报错 |

> **IX-050 说明**：防抖是为了让连续输入不卡顿；实现时用 200ms 定时器，新的输入取消上一个定时器。预览计算可放在 Worker 中，超过 2000 项时显示「计算中…」占位。
> （原型演示为加快响应使用 120ms，实际实现请用 200ms。）

#### 设置相关（SCR-07 · P2-A / DEC-11）

| 编号 | 触发 | 行为 | 反馈 / 边界 |
| --- | --- | --- | --- |
| **IX-095** | 点击 EL-106「设置」 | 打开 SCR-07（EL-107）。**纯 UI 弹窗状态**：不改列表、不改规则、不重算预览、不重置猫咪状态 | 220ms 入场；**没有「取消」按钮** —— 三项都是即改即生效，不存在「未保存」的概念 |
| **IX-096** | 在 EL-108 里选「跟随系统 / 始终浅色 / 始终深色」 | 立即写 `prefs.theme` → 主进程设 `nativeTheme.themeSource` → **整个界面（含当前已打开的弹窗）立刻换色**。**零新增 IPC 通道**：渲染层靠 `prefers-color-scheme` 媒体查询自动收到通知，不需要「主题变了」的广播 | 选「跟随系统」后，改 Windows 深浅色时软件**实时**跟着切（不重启）。切换主题**不影响** `data-reduce-motion` |
| **IX-097** | 切换 EL-109 | 立即写 `prefs.soundEnabled`；后续改名完成音效按新值播放 | 即时生效，不需要重启 |
| **IX-098** | 切换 EL-110 | 立即写 `prefs.reduceMotion` → 挂 `<html data-reduce-motion>` → 所有动效时长归零（状态切换只换表情、不做位移） | 与主题**互不干扰**：两者都挂在 `<html>` 的 `data-*` 上，各管各的 |
| **IX-099** | 关闭 SCR-07 | **三种方式等价**：点遮罩 / 按 `Esc` / 点「完成」 | 立即关闭，**不刷新、不跳转、不二次确认**（例如不弹「要保存设置吗」—— 没有需要保存的东西）|
| **IX-102** | 点击 **EL-114**「命令行」 | 就地展开 / 收起用法（EL-115 + EL-116）。**纯 UI 状态**，不写任何偏好 | 箭头翻转；展开块底 `bg-warm`、圆角 10px、内边距 12px |
| **IX-103** | 点击 **EL-116** 的两个按钮 | 「复制命令」把**带本机完整程序路径**的示例命令写入剪贴板；「复制程序路径」只写 `AppInfo.execPath` | 复制成功给一次轻提示（复用既有提示位，**不弹窗**）。**不加 IPC 通道** —— 走渲染层剪贴板 API |
| **IX-104** | 点击 **EL-118**「查看教程」 | 打开 **SCR-08**（EL-119）图文教程。**纯 UI 弹窗状态**：不写偏好、不改规则、不改列表 | 叠加在设置弹窗之上（设置弹窗**不关闭、不卸载**）；220ms 入场。**教程每次打开都从第 1 步开始**（见下方红线） |
| **IX-105** | SCR-08 内翻步 / 关闭 | 点页签（`第 1 / 2 / 3 步`）或点底栏按钮（前两步「下一步」、第 3 步「知道了」）翻步；关闭 = 点遮罩 / `Esc` / 点「知道了」/ 点右上「关闭」 | **只关教程，不连带关设置弹窗**；回到设置后「命令行」展开块保持原样（展开态不丢） |
| **IX-106** | 点击 **EL-120** 的任一个 chip（套用模板） | **整份替换**规则（模式 + 参数 + 正则开关 + 大小写），页签自动跳到该模板的模式；状态栏给一句 3 秒轻提示「已套用模板：X」（若该模板开了正则，追加「（已自动开启正则）」）。**纯规则状态变更**：不弹窗、不二次确认、不改列表、不写任何偏好 | 立即生效（预览跟随既有的 200ms 防抖自动重算）。**不做「当前模板」高亮**：用户随后一改参数，高亮就会变成谎话（DEC-14） |
| **IX-107** | 切换 **EL-121** 编号类型 | 立即生效（预览跟随既有的 200ms 防抖重算）：**EL-122 参数行按类型换**、**EL-124 示例行立刻跟着变**。切到「时间」且起点还是空的时候，由 store 填入**本机今天**（本地时区）—— 填进去后它就冻结成规则里的**字面值**，不会像「启用日期」那样跟着执行日漂移 | **纯规则状态变更**：不弹窗、不改列表、不写偏好。`seqKind` 是 `RuleConfig` 的字面量字段，**不落盘**（规则从来不落盘，所以加字段零迁移）|
| **EL-126** | **「导出清单」按钮** | 按钮 | 仅列表非空时可点 | **P3-2（DEC-17）**。位于 R-03 操作区、「清空列表」**之上**；复用 `.md-btn--ghost` + `md-actionpanel__wide`，**零新增样式**。为什么不放底部动作区：那里主按钮是「开始改名」，是全程序唯一会改文件的动作；把不改文件的导出塞进去会糊掉「主按钮 = 危险动作」这条约定 |
| **EL-127** | **导出选项气泡** | 弹层（复用 `AppModal`） | 点 EL-126 打开 | **P3-2（DEC-17）**。宽 **480px**；三行：格式（`.md-select`，4 项，默认 `Excel (.xlsx)`）/ 范围（`.md-select`，全部 / 仅选中项）/ 列（`.md-check`×4，**默认勾原名 / 新名 / 状态**）。**零新增色值、零新增令牌** |
| **IX-108** | 点击 **EL-125**「换一批」 | 把 `seqRandomSeed` **+1**（**单调递增**，不是重新随机），整批随机串换一批。同一个文件**在自己那一批里仍然恒定**，所以「预览 ≡ 执行」不受影响 | 立即生效。**为什么必须有它**：没有的话，用户第一次随机出来的串会跟他一辈子，撞上重名也没法自救。用单调 +1 也顺带避免了「点了好像没变」的错觉 |
| **IX-110** | 点击 **EL-126**「导出清单」 | 打开 **EL-127** 选项气泡（默认：Excel / 全部 / 勾三列）。**纯 UI 状态**：不写偏好、不改规则、不改列表。**每次打开都回到默认选项** —— 不记上次用的格式与路径（DEC-17） | ：打开前 `previewPending` 为 true 时，「导出…」按钮**置灰**（预览有 200ms 防抖 + Worker 往返，算的过程中导出会拿到**上一轮**的旧新名） |
| **IX-111** | 在气泡里点「导出…」 | 先在内存里生成好内容，**再**调系统「另存为」；用户选定路径后写临时文件并原子改名。完成后调 `showTransient('已导出 N 项到 文件名')，**不弹成功弹窗** | **取消不是失败**：静默返回、不报错、不留残留文件。写盘失败走 `E_PERM`（状态栏「无写入权限」），**不产出半个文件** |
| **EL-130** | **属性组**（P3-3 / DEC-18） | 规则区「文件名字段」组附近 | 常显；列 `{创建}` / `{修改}` / `{大小}` 三个变量，点一下就**插进前缀末尾** | 三个变量必须**在这行里写出来** —— 不写的话功能完全正常、测试也能全绿，但**没有任何用户知道有这三个变量** |
| **EL-131** | **大小单位下拉**（P3-3） | 属性组内 | 5 档：自动 / B / KB / MB / GB。改一下 → 旁边的**示例行立刻重算** | 示例用的是**假属性值**（不是列表里的文件），文案已注明 |
| **IX-112** | 点 EL-130 里的属性项 | 把 `{创建}` / `{修改}` / `{大小}` **追加到前缀末尾** | 为什么是「点一下就插」而不是教用户手打：手打容易漏括号或写成全角括号，而写错的变量**不报错、只展开成原样文字** |
| **IX-113** | 改 EL-131 的单位 | **示例行立刻按新单位重算**，不用点别的 | 属性是入列时的快照，改单位只影响显示格式 |
| **EL-132** | **「导入表格」按钮**（P3-4 / DEC-19） | 按钮 | **列表为空时也可点**（空列表导入会全部落进「对不上」，那是有信息量的反馈） | 位于 R-03 操作区、「导出清单」**之上**；与 EL-126 同款样式，**零新增样式**。顺序是「先选文件、解析成功**再**开弹窗」—— 反过来的话用户要先面对一个空白弹窗 |
| **EL-133** | **导入预览弹窗**（P3-4） | 模态 | 宽 **700px**（比确认弹窗宽 —— 里面要摆「行 / 原名 / 新名 / 文件」四列对照表）。四个部分：①「我这样读的」+ 三个下拉（原名列 / 新名列 / 匹配方式）②三行统计 ③对照表 ④主按钮带数字「导入这 N 项」 | ★ **默认就列出「对不上 / 有问题」的行**，可切到「全部」；**静默丢掉未匹配的行是这功能最危险的失败方式**。改任一选项**立刻重算对照表**。表头认不出时**不报错**，退化成「第 1 列 = 新文件名」+ 按行顺序，并**明确写在弹窗里** |
| **EL-134** | **名字来源提示条 +「清除导入」**（P3-4） | 规则区**顶部**（与 P2-B 模板条同处，常显、不进折叠） | 文案：「本批有 N 个名字来自《xxx》，规则不影响它们」+「清除导入」 | 为什么必须有：被导入的项**不再受规则影响**，不说的话用户改了规则发现「怎么有几个没变」—— 又是一个**「界面无异常但行为不对」**的迷局。「清除导入」**只清名字的来源**，列表里的文件一个都不删，所以**不需要二次确认** |
| **IX-114** | 点 EL-132「导入表格」 | 弹系统「打开」对话框（限 `.xlsx` / `.csv` / `.txt`）→ 解析 → 打开 EL-133 | **取消不是失败**：静默返回、不提示（与「另存为」同一条纪律）。读不出来（加密 / 损坏 / 太大 / 是老 `.xls` 或 `.docx`）→ 状态栏给 `EX-18` 的中文原因，**绝不崩溃** |
| **IX-115** | 在 EL-133 里点「导入这 N 项」 | 把「真的会改」的那些项装上 `override` → 关弹窗 → 状态栏轻提示「已导入 N 项」 | **表外的项一个字都不动**（它们继续按规则算）。不合法 / 扩展名不一致 / 对不上的行**都不会被导入**，且都已在对照表里列出来过 |
| **IX-116** | 点 EL-134 的「清除导入」 | 去掉全部 `override` → 那些项**立刻回到按规则算** | **不删列表里的文件** → 所以不需要二次确认（与「清空列表」是完全不同的两件事） |
| **IX-109** | 位置（EL-054）选到第三档 | 就地显示 **EL-123**，并把**焦点自动送进那个数字框**；选回前两档时 EL-123 **整块消失**（不是置灰）| **纯 UI 状态**（那个框显不显示）不进 `RuleConfig`、不进 IPC；进 `RuleConfig` 的只有 `seqAt` 这个数值本身 |

> **SCR-08 状态归位红线（P2-C 增量）**：设置弹窗与教程弹窗都是**在 `App.vue` 里常驻挂载**的（靠内部 `v-if` 控制显隐），组件实例**不会随关闭而销毁**。因此「当前看到第几步」这类**临时 UI 状态**必须在弹窗关闭时**显式归零** —— 否则用户第二次打开教程会直接停在上次看的第 3 步，看起来像「点不动 / 卡住了」。
> **通用规律（写进实现约束）**：**常驻挂载的组件里，任何「临时 UI 状态」都要在关闭时归位。** 现有实现用 `watch(() => task.modal, ...)` 在离开本弹窗时归位。
> 同一个坑在**设置弹窗的「命令行」展开态**（`cliOpen`）上也踩过一次：展开命令行后关掉设置再重开，展开块仍然是展开的 —— 同一条规律，同样已归位。

> **IX-096 实现要点（P2-A）**：深色令牌**不挂** `[data-theme]`，而挂在 `@media (prefers-color-scheme: dark)` 上；主进程在 `new BrowserWindow` **之前**就把 `themeSource` 与窗口底色（`backgroundColor`）设好，所以冷启动的**第一帧就是对的颜色**（验收 TC-30）。
> `<html data-theme>` 仍会挂上（值为解析后的 `light` / `dark`），但**只用于排查与冒烟断言 —— 它不是样式的真源**。

#### 执行相关

| 编号 | 触发 | 行为 |
| --- | --- | --- |
| **IX-060** | 点击 EL-060 | 按 §1.1 的分支判断执行：置灰 → 无动作；有冲突 → SCR-04；≥ 10 项 → SCR-05；否则直接执行 |
| **IX-061** | 执行中点击 EL-060（此时文案为「取消」） | 中止执行；**已改的项自动回滚为原名**；ST 回到 ST-01 |
| **IX-062** | 执行中 | EL-070 橙色脉动；EL-072 进度条推进；EL-071 显示「已处理 N / M」；ST-03 |
| **IX-063** | 执行完成 | 写入历史记录 → ST-04（无失败）/ ST-05（有失败）→ 弹出 SCR-03 |
| **IX-070** | 点击 EL-061 | 切换到 SCR-02；若历史为空则状态栏提示「还没有可以撤销的改名记录」 |

> **EL-060 状态矩阵**

| 条件 | 按钮状态 | 文案 |
| --- | --- | --- |
| 列表为空 | 置灰 | 开始改名 |
| 有项但全部无变化 | 置灰 | 开始改名 |
| **正则非法（P1 / EX-15）** | 置灰 | 开始改名 |
| 有可改项 | 可用 | 开始改名 |
| 执行中 | 可用 | 取消 |

---

## 3. SCR-02 撤销 / 历史页

### 3.1 元素清单

| 编号 | 元素 | 类型 | 说明 |
| --- | --- | --- | --- |
| EL-080 | 返回主界面 | 按钮 | 回到 SCR-01，保留当前文件列表与规则 |
| EL-081 | 历史列表 | 列表 | 最新任务在最上，**最多 20 条**（DEC-06） |
| EL-082 | 单条撤销 | 按钮 | 仅「可撤销」状态显示 |
| EL-083 | 全部撤销 | 按钮 | 撤销所有「可撤销」任务 |
| EL-084 | 历史卡片 | 卡片 | 显示：时间 / 项数 / 规则摘要 / 首条示例 / **明细展开条（EL-111）** / 状态标签 |
| **EL-111** | **明细展开条** | 折叠条 | **P2-C（DEC-12）**。在「首条示例」下方一行：左侧 12px 箭头 + 文字「查看全部 N 项」。收起态箭头朝下、色 `ink-3`；展开后箭头朝上并转 `orange-dark`，文字改「收起」。**纯 UI 状态**，不进数据模型、不进 IPC |
| **EL-112** | **明细列表** | 列表 | **P2-C（DEC-12）**。底 `bg-sunken`、圆角 10px、内边距 8×10px、**最大高 220px 超出滚动**。每行：序号（Inter 11.5 `ink-4`，宽 20 右对齐）+「原名 → 新名」12.5 `ink-2`，行高 26px；无变化的行用 `ink-4` 并追加「（无变化）」。**只渲染前 100 条**，其余显示一行「还有 N 项未显示（共 M 项）」。**每条卡片各自独立展开、不联动**；「已撤销」的卡片同样可展开 |
| **EL-113** | **清空历史按钮** | 按钮 | **P2-C（DEC-12）**。历史页 **footer 右侧**（左侧是既有的保留规则说明），**幽灵危险按钮**：底透明、圆角 10px、内边距 6×14px、文字 12.5 Medium `bad`；悬停底 `bad-bg`。这是全软件**唯一一个「删掉用户数据」的操作**，所以不用实心红（太易误点）、也不用普通灰幽灵（会与「清空列表」混在一起）|

### 3.2 交互规则

| 编号 | 触发 | 行为 | 反馈 |
| --- | --- | --- | --- |
| **IX-071** | 点击某条的 EL-082 | 弹确认 → 该任务 `entries` **倒序**执行 `新名 → 原名` | 该项标记「已撤销」并灰显；状态栏「已撤销 N 项，名字都改回去啦」 |
| **IX-072** | 点击 EL-083 | 弹确认（显示任务数与文件总数）→ 从最新任务开始**倒序**逐条撤销 | 汇总提示「已全部撤销（N 个任务）」 |
| **IX-073** | 点击 EL-080 | 返回主窗口 | — |
| **IX-100** | 点击某条卡片的 **EL-111** | 就地展开 / 收起 **EL-112** 明细列表。**纯 UI 状态**：不重算预览、不改列表、不重置猫咪状态 | 展开后箭头朝上并转 `orange-dark`，文字改「收起」；**每条卡片各自独立**，不联动 |
| **IX-101** | 点击 **EL-113**「清空历史记录」 | 弹二次确认（走 **SCR-05**，`kind: 'clearHistory'`）；确认后调用 `md:history:clear` | **分支**：① 没有「可撤销」任务 → 普通确认，主按钮「清空」；② **有**「可撤销」任务 → 额外加红色警告块 + 主按钮改「**仍然清空**」（**EX-17**，防止永久丢数据）。确认后：列表走既有空态 → 状态栏「已清空全部 N 条历史记录，文件未被改动」→ **不弹结果弹窗**、**不产生可撤销记录**、**必须重置淘汰检测基准**（否则会假报「较旧的记录已被清理」） |

### 3.3 撤销的关键规则

| 规则 | 说明 |
| --- | --- |
| 倒序执行 | 同一任务内按 entries 逆序还原，避免中间态名称冲突 |
| 幂等保护 | 已是「已撤销」的任务不再可撤销，按钮置灰 |
| 部分失败 | 源文件已不存在 / 被外部改名 → 跳过该条并继续，结果中明确列出（EX-10） |
| 不产生新记录 | 撤销本身**不写入历史**，避免「撤销的撤销」逻辑混乱 |
| 与主列表联动 | 若被撤销的文件仍在当前列表中，列表中的名称同步改回并重新计算预览 |
| 空状态 | 无历史时显示耄耋待机插画 + 「还没有改过名呢」 |

---

## 4. SCR-03 结果弹窗

| 编号 | 元素 | 说明 |
| --- | --- | --- |
| EL-090 | 结果摘要 | 三张卡片：成功 / 跳过失败 / 未处理（非法） |
| EL-091 | 明细清单 | 成功示例取前 3 条；失败项逐条列出「原名 → 尝试的新名 → 原因」 |
| EL-092 | 知道了 | 关闭弹窗 |
| EL-093 | 撤销本次 | 直接对本次任务执行撤销 |

| 编号 | 交互 | 行为 |
| --- | --- | --- |
| **IX-090** | 点击 EL-093 | 关闭弹窗 → 撤销本次任务 → ST-04 成功 → 状态栏提示 |
| — | 点击遮罩 / 按 ESC | 等同「知道了」 |
| — | 标题文案 | 无失败 → 「全部改好啦！」；有失败 → 「改好啦，但有几项没成功」 |

---

## 5. SCR-04 冲突提示弹窗

| 编号 | 元素 | 说明 |
| --- | --- | --- |
| EL-100 | 冲突清单 | 逐条显示「原名 → 会撞成的新名 → 原因」 |
| EL-101 | 先不改（推荐） | 关闭弹窗，回到主界面，**不执行任何改名** |
| EL-102 | 自动加序号后继续 | 打开 EL-055 开关 → 自动为冲突项追加 `_1`、`_2`… → 继续执行 |

| 编号 | 交互 | 行为 |
| --- | --- | --- |
| **IX-091** | 点击 EL-101 | 关闭弹窗，主界面保持原状；状态栏提示「已取消，冲突项未改动」 |
| **IX-092** | 点击 EL-102 | 勾选 EL-055 → 重算预览（冲突项变为带 `_1` 的新名）→ 走 §1.1 的执行分支 |

> **文案原则**：默认按钮是「先不改」，且标注「推荐」。**任何一键操作都不允许覆盖已有文件**（DEC-05）。

---

## 6. SCR-05 二次确认弹窗

| 情况 | `kind` | 标题 | 正文 | 主按钮 |
| --- | --- | --- | --- | --- |
| 待改项 ≥ 10 | `rename` | 确认改名 | 「即将修改 N 个文件 / 文件夹的名称，是否继续？」 | 确认改名 |
| 撤销操作 | `undoOne` | 撤销上一次改名 | 「将把 N 个文件的名字改回去（规则摘要），确定吗？」 | 确认撤销 |
| 全部撤销 | `undoAll` | 全部撤销 | 「将把 N 个任务、共 M 个文件的名字全部改回去，确定吗？」 | 确认撤销 |
| 清空列表 | `clear` | 清空列表 | 「将移除列表中的 N 项。文件本身不会被删除。」 | 确认清空 |
| **清空历史**（无「可撤销」任务）<br>**P2-C / DEC-12** | `clearHistory` | 清空历史记录 | 「将删除全部 N 条改名记录。**文件本身不会被删除，也不会被改名。**」 | **清空**（**危险按钮**，底 `bad` 字 `on-danger`） |
| **清空历史**（**有**「可撤销」任务）<br>**P2-C / DEC-12 · EX-17** | `clearHistory` | 清空历史记录 | ↑ 两句 **+ 红色警告块**（底 `bad-bg`、圆角 10px、内边距 10×12px、文字 12.5 `bad`）：「其中 N 条改名记录仍然可以撤销。清空后这 N 条将**无法再还原** —— 只能手动把文件名改回去。」 | **仍然清空**（**危险按钮**） |

> ⚠️ **「清空历史」为什么必须分两支**：清空 = 删掉撤销记录 → 那些改名**再也撤不回来**。
> 不做第二个分支的话，用户会经历「改错了名 → 想去撤销 → 发现历史被自己清了」→ **永久丢数据**。
> 文案里那两句也必须写全：「文件本身不会被删除，也不会被改名」—— 否则用户会以为「清空历史 = 删文件」，那反而会让人不敢用这个功能。
>
> **实现提示**：现有 `ConfirmModal.vue` 的主按钮**恒为橙色**、`kind` 只有四种。**需要新增 `kind: 'clearHistory'` 与一个 danger 主按钮变体**。

| 交互 | 行为 |
| --- | --- |
| 点击遮罩 / ESC / 取消按钮 | 关闭弹窗，不执行 |
| 点击确认按钮 | 关闭弹窗并执行 |

---

## 7. 耄耋状态机交互（ST）

| 编号 | 状态 | 触发交互 | 动画 | 文案 | 自动回落 |
| --- | --- | --- | --- | --- | --- |
| ST-01 | 待机 | 启动 / 无操作 | 眨眼（4.6s 周期）、尾巴轻摆、耳朵偶尔抖动 | 「待机中…」 | — |
| ST-02 | 接收文件 | IX-020 / 021 / 022 | 飞机耳、歪头 6°、尾巴快摆 | 「收到啦，N 个！」 | 1.5s → ST-01 |
| ST-03 | 改名中 | IX-060 | **一口一口咬掉旧名字**（详见 §7.3）：卡片从右→左经过嘴边，耄耋前倾咬合、嘴巴「咔嚓」闭合，被咬的卡片由旧名变新名并转橘色，随后飞走、下一张补位；尾巴快摆 | 「正在改名…（N/M）」 | 执行结束 |
| ST-04 | 成功 | IX-063 无失败 / IX-071 / IX-090 | 眯眼笑、抬头 4°、尾巴竖起、三处闪光 | 「改好啦，得意！」 | 3s → ST-01 |
| ST-05 | 失败 | IX-063 有失败 | 全身炸毛（毛发放大 1.16）、张嘴露牙、怒气符号、哈气线 | 「炸毛了！有 N 项没改成」 | 3s → ST-01 |

### 7.1 状态优先级

同一时刻只允许一个状态。冲突时的优先级：

```
ST-03（执行中）  >  ST-05（失败）  >  ST-04（成功）  >  ST-02（接收）  >  ST-01（待机）
```

执行中（ST-03）时**不接受** ST-02 的抢占——拖拽入列在执行中仍可进行，但**不切换形象**，只更新列表。

### 7.2 状态与失败数量的映射

| 执行结果 | 状态 |
| --- | --- |
| 全部成功 | ST-04 |
| 有任一跳过 / 失败 | ST-05（失败数量显示在文案中） |
| 用户取消执行 | ST-01（不判定为失败） |

### 7.3 ST-03「一口一口咬掉旧名字」动画细则（v1.1 重做）

> **背景**：初版 ST-03 使用「双爪快速上下抽动 + 身体抖动 + 张嘴 + 冒汗」，视觉上有明显歧义，已废弃。新方案把「正在干活」具象化——用户一眼能看出**正在改第几个、改成什么**。

**画面构成**

| 元素 | 规格 |
| --- | --- |
| 卡片（EL-014） | 白色圆角卡片（圆角 9px，内边距 5×10px，字号 10.5px），内含「图标 + 名字」 |
| 图标（EL-015） | **文件**：灰蓝文件图形；**文件夹**：橘色文件夹图形。**跟随该条目的真实类型** |
| 名字 | 显示**列表里的真实文件名**，被咬前是旧名、被咬后换成新名（橘色）。超过 14 字截断，**保留扩展名**（如 `会议纪要-最终版-最新版.docx` → `会议纪要-…版.docx`） |
| 速度线 | 卡片右侧两条短线，仅在「飞向嘴边」阶段出现（替代已去掉的汗滴元素） |
| 位置 | 位于耄耋身前、下巴下方约 62% 高度处（EL-013，`top:62%`），横穿画面 |

**单张卡片时序（一轮 1.6s）**

| 进度 | 画面 |
| --- | --- |
| 0% | 卡片在右侧场外，缩小 + 透明 |
| 14% | 淡入，速度线亮起 |
| 38% | 卡片到达耄耋嘴边（居中） |
| 42% | **咬合**：耄耋头部前倾下探、嘴巴瞬间闭合（`chomp`），卡片被压扁一下 |
| 56% | 卡片底色转浅橘、描边变橘 |
| 62% | **旧名淡出、新名淡入**（橘色） |
| 86% | 卡片向左侧飞出 |
| 100% | 飞出并淡出，下一张接上 |

**队列规则（关键）**

| 规则 | 说明 |
| --- | --- |
| 取谁 | 从列表中取「**将发生变化 / 重名冲突**」的项，按列表顺序，最多取 12 个循环 |
| 文件还是文件夹 | **不写死**——图标由该条目的真实类型决定，因此**文件批次就咬文件、文件夹批次就咬文件夹、混合批次按真实比例交替** |
| 空列表兜底 | 列表为空时使用 3 条演示数据（仅用于原型演示，正式产品中 ST-03 必然有文件） |
| 与预览的关系 | 卡片上的新名**取自预览结果**，与列表 R-04 显示的新名保持一致 |
| 与进度条的关系 | 卡片节奏（1.6s/张）**独立于**真实进度（EL-072），不要求同步；卡片只是「在干活」的视觉表达 |
| 状态切换即停 | 离开 ST-03 立刻停止并隐藏卡片，不留残影 |

**设计约束**

| 约束 | 说明 |
| --- | --- |
| 不渲染攻击性 | 「咬」只做卡通化的干脆一口：**不做尖牙特写、不做破碎 / 流血效果**；卡片是「乖乖变成新名字飞走」 |
| 动作不得有歧义 | 严禁使用「双肢快速上下抽动 + 躯干抖动」组合；新增动作需先做「会不会被想歪」自查 |
| 双爪不再抽动 | 保留静态爪部造型，仅头、嘴、尾参与动作 |

---

## 8. 状态迁移总表

| 当前 | 事件 | 下一状态 |
| --- | --- | --- |
| — | 启动 | ST-01 |
| ST-01 | 添加 / 拖入文件 | ST-02 |
| ST-02 | 1.5s 超时 | ST-01 |
| ST-02 | 点击开始改名 | ST-03 |
| ST-01 | 点击开始改名 | ST-03 |
| ST-03 | 全部成功 | ST-04 |
| ST-03 | 有失败 / 跳过 | ST-05 |
| ST-03 | 用户取消 | ST-01 |
| ST-04 | 3s 超时 | ST-01 |
| ST-05 | 3s 超时 | ST-01 |
| ST-04 / ST-05 | 再次添加文件 | ST-02 |
| ST-01 ~ ST-05 | 撤销成功 | ST-04 |

---

## 9. 键盘与无障碍

| 按键 | 行为 |
| --- | --- |
| `Tab` | 按「操作区 → 列表 → 规则区 → 动作区」的自然顺序移动焦点 |
| `Enter` | 焦点在 EL-060 时触发开始改名；焦点在输入框时提交并触发预览刷新 |
| `Esc` | 关闭当前弹窗；无弹窗且在 SCR-02 时返回主界面 |
| `Ctrl + O` | 等同点击 EL-020 添加文件 |
| `Ctrl + Z` | 等同点击 EL-061 撤销（进入历史页） |
| `Delete` | 焦点在列表行时移除该行 |

| 无障碍 | 要求 |
| --- | --- |
| 焦点可见 | 所有可聚焦元素有 2px 橘色描边 |
| 对比度 | 正文文字对比度 ≥ 4.5:1 |
| 不依赖颜色 | 冲突 / 非法状态除颜色外还有文字徽标（「重名」「非法」） |
| 动效可关 | 设置中提供「减少动画」开关，关闭后状态切换只换表情不做位移。**开关入口：SCR-07 设置弹窗（EL-110 / IX-098）**，P2-A / DEC-11 补上（此前开关写好也能生效，但**界面上没有设置入口**，这条要求实际上达不到）|
| 深色主题 | 深色下正文对比度：主文字 13.0:1、次要 7.6:1、辅助 4.7:1（均 ≥ 4.5:1）。**已知取舍**（深色下「关」状态开关轨道约 1.6:1，低于 UI 组件 3:1 建议）如实登记在 `设计规范.md` §8.1，不当成达标 |

---

## 10. 异常处理交互表

| 编号 | 场景 | 交互表现 | 状态 |
| --- | --- | --- | --- |
| EX-01 | 新名含 `\ / : * ? " < > \|` | 该行整行标红、新名加删除线、徽标「非法」、悬停显示「含非法字符」；不执行 | 可继续改规则，其余项照常 |
| EX-02 | 完整路径 > 259 字符 | 同上，原因「路径过长」 | 同上 |
| EX-03 | 文件被占用 / 无权限 | 执行时该项失败，列入结果弹窗「以下几种情况没改成」 | 不中断整批 |
| EX-04 | 新名主体为空 | 该行标红，原因「改完名字就空了」 | 同上 |
| EX-05 | 批次内两项改后同名 | 两行均标红、徽标「重名」；执行时进 SCR-04 | 默认跳过，绝不覆盖 |
| EX-06 | 与目录下已有文件同名 | 同上，原因「已经有同名文件了」 | 同上 |
| EX-08 | 拖入非文件对象 | 落区拒绝态 + 状态栏提示 | 无变化 |
| EX-09 | 拖入重复项 | 静默忽略 + 状态栏「已忽略 N 个重复项」 | 无变化 |
| EX-10 | 撤销时源文件找不到 | 跳过该条，汇总提示「有 N 个文件找不到了，已跳过」 | 其余继续 |
| EX-11 | 空列表点开始改名 | 按钮置灰，无法点击 | — |
| EX-12 | 全部项均无变化 | 按钮置灰，状态栏「当前规则不会改变任何名字」 | — |
| EX-13 | 单批超过 10000 项 | 状态栏「单批最多 1 万个，超出部分未加入」 | 其余正常入列 |
| EX-14 | 系统保护目录 | 允许尝试，失败后列入结果弹窗 | 不中断 |
| **EX-15** | **正则表达式非法**（P1 / 如输入 `(`、`[a-`、`*abc`） | 输入框描边 2px `#E5544B`；输入框下方**红字**原因（由运行时错误映射，如「正则表达式不完整：缺少右括号 `)`」）；状态栏追加「· 正则表达式有误」、状态点转红；**EL-060 置灰**；**保留上一次的合法预览**（不清空、不整屏标红，避免闪动） | **不崩溃**；改回合法后自动恢复 |
| **EX-16** | **正则过于复杂**（P1 / 灾难性回溯，如 `(a+)+$`） | ① **pattern 长度 ≤ 200 字符**（超出即视为非法，走 EX-15）；② 预览 Worker **每处理若干项检查一次耗时，累计超 3 秒即中止**，提示「正则过于复杂，请简化」 | **不卡界面**（JS 正则无法中断，只能在 Worker 里主动放弃） |
| **EX-17** | **清空历史时仍有可撤销任务**（P2-C / DEC-12） | 走 **IX-101 的第二个分支**：二次确认**额外加红色警告块**，主按钮文案改「**仍然清空**」。理由：清空 = 删掉撤销记录 → 那些改名**再也撤不回来** | 不做这条会导致「改错了名 → 想撤销 → 发现历史被自己清了」→ **永久丢数据**（只能手工把文件名一个个改回去） |

---

## 11. 反馈与动效规范

### 11.1 时长与曲线

| 项 | 规范 |
| --- | --- |
| 按钮按压 | `scale(0.96)`，120ms |
| 按钮悬停 | 上浮 1px + 阴影加深，180ms |
| 弹窗出现 | `scale(0.92 → 1)` + 淡入，220ms，缓动 `cubic-bezier(.34,1.4,.64,1)` |
| 状态切换 | 320ms，缓动 `cubic-bezier(.34,1.5,.64,1)`（Q 弹感） |
| 预览刷新 | 200ms 防抖后立即重绘，无过渡动画（避免列表闪动） |
| 进度推进 | 0.18s 线性 |

### 11.2 状态栏文案规则（EL-071）

| 场景 | 文案 |
| --- | --- |
| 初始 | 「就绪」 |
| 列表非空、有变化 | 「共 N 项 · 预览 M 项将发生变化」 |
| 列表非空、无变化 | 「共 N 项 · 当前规则不会改变任何名字」 |
| 有冲突 | 追加「· K 项重名冲突（默认跳过，不会覆盖）」 |
| 有非法 | 追加「· J 项名称非法」 |
| 执行中 | 「已处理 N / M」 |
| 执行完成 | 由结果弹窗承担，关闭后回到上面几种 |
| 撤销完成 | 「已撤销 N 项，名字都改回去啦」 |
| 重复项 | 「已忽略 N 个重复项」（临时，3s 后回到常态文案） |

### 11.3 音效

| 时机 | 音效 | 可关闭 |
| --- | --- | --- |
| 改名全部成功 | 轻快「叮」+ 撒花动效 | 是 |
| 有失败 | 短促「噗」 | 是 |
| 按钮点击 | 无（避免嘈杂） | — |

> **开关入口（P2-A / DEC-11）**：SCR-07 设置弹窗的 **EL-109「音效」**（IX-097）。此前 `soundEnabled` 已经生效，但界面上没有入口，用户只能手改 `prefs.json`；P2-A 把它补上。

---

## 12. 三份文档的对照索引

| 功能 | PRD 功能编号 | 原型元素 | 交互编号 | 验收用例 |
| --- | --- | --- | --- | --- |
| 批量改名文件 / 文件夹 | F-01 / F-02 | EL-060 | IX-060 | TC-01 |
| 删除指定字符 | F-03 | EL-041 | IX-041 | TC-03 |
| 替换指定字符 | F-04 | EL-042 / EL-043 | IX-042 | TC-04 |
| 按规则重命名 | F-05 | EL-045 ~ EL-054 | IX-043 ~ IX-047 | TC-06 |
| 可爱互动界面 | F-06 | EL-010 / EL-011 | §7 | TC-01 |
| 拖拽文件进窗口 | F-07 | EL-012 | IX-022 | TC-01 |
| 实时预览 | F-08 | EL-032 ~ EL-035 | IX-050 | TC-03 |
| 一键撤销 | F-09 | EL-061 / EL-080 ~ EL-083 | IX-070 ~ IX-073 | TC-09 ~ TC-11 |
| 重名冲突处理 | F-16 | EL-055 / EL-100 ~ EL-102 | IX-049 / IX-091 / IX-092 | TC-07 / TC-08 |
| 扩展名保护 | EX-07 | — | §10 | TC-05 |
| 正则表达式与大小写（P1） | F-10 / F-11 | EL-056 ~ EL-059 / EL-103 ~ EL-105 | IX-051 ~ IX-054 / IX-093 ~ IX-094 | TC-21 ~ TC-27 |
| 设置面（P2-A） | — | SCR-07 / EL-106 ~ EL-110 | IX-095 ~ IX-099 | TC-28 ~ TC-32 |
| 深色 / 浅色主题（P2-A） | F-14 | EL-108 | IX-096 | TC-29 / TC-30 |
| **历史完整明细（P2-C）** | F-13 | EL-111 / EL-112 | IX-100 | TC-33 / TC-34 |
| **清空历史记录（P2-C）** | F-13 | EL-113 | IX-101 | TC-35 ~ TC-37 · **EX-17** |
| **命令行入口（P2-C）** | F-15 | EL-114 ~ EL-116 | IX-102 / IX-103 | **CLI 本体已在 `接口文档.md` §10 定稿**（原先只是草案）。安全底线由 `tests/p2c.test.ts` 的 3 条单测（dry-run 不改文件 / `--yes` 真改且写历史 / 参数错退出码 2）+ 真实 Electron 进程跑 `--help`·`--dry-run`·`--yes` 覆盖 |
| **命令行图文教程（P2-C 增量）** | F-15 | **SCR-08** / EL-117 ~ EL-119 | IX-104 / IX-105 | 单测 `tests/p2c-guide.test.ts`（10 条：SVG 结构 / 色值纪律 / 文案零占位符）；界面由冒烟 4 条覆盖（引导卡存在 / 三步逐张翻得到 / 示意图黑底不随主题翻 / 教程能关能重开且回到第 1 步）。**DEC-13** |
| **编号系统升级（P3 第 1 批）** | F-05 | **EL-121 ~ EL-125** | **IX-107 ~ IX-109** | 单测 `tests/p3-1-seq.test.ts`（含 **TC-48**：同一份规则「预览 ≡ 执行」**逐字节相等**，4 种类型 × 3 档位置共 12 组 —— 这是唯一能抓住 `sanitizeRule` 漏字段的用例；另有 `sanitizeRule` 六字段「喂进 = 取出」/ `toLetters` 列标序列 / `randomText` 确定性 / `addDays` 跨闰年且绝不产出非法日期 / `dateText` 新两档 / `applyRuleMode` 第三档边界含 emoji 与不保留原名）；界面由冒烟 **8 条**覆盖（起始态 / 勾上解禁且示例行出现 / 第三档与 64px 框 / 换类型参数行跟着换 / 「换一批」/ 时间类型与 5 档样式 / 跨天提示常显 / 取消后整组归位）。**DEC-15 / DEC-16** |
| **常用规则模板库（P2-B）** | F-12 | **EL-120**（常显在 R-05 顶部、页签之前） | IX-106 | 单测 `tests/p2b-templates.test.ts`（18 条：6 个模板逐字段比对 / 「去掉括号」的正则可编译 / 每个模板都真跑一遍预览 / 模板常量不被污染 / `(1)(2)报告.docx` 两组括号全去 / 摘要截断且 `buildRuleSummary` 原始输出未被改）；界面由冒烟 **7 条**覆盖（模板条形态与悬停 / TC-38 套用加日期前缀 / TC-41 常量不被污染 / TC-39 自动开正则 / TC-40 全部匹配 / 其余 4 个模板逐个点 / 历史摘要截断） |

---

## 13. 变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v1.0 | 2026-09-11 | 首次成文：覆盖 SCR-01 ~ SCR-06、10 类交互规则、5 个形象状态、14 条异常处理；与 PRD v1.0、原型图 v1.0 编号对齐 |
| **v1.1** | 2026-09-11 | **ST-03 动作重做**：新增 EL-013 ~ EL-017（咬名字动画区）与 §7.3 动画细则；废弃「双爪抽动 + 身体抖动」；补充「动作不得有歧义」「撕咬要卡通化」两条设计约束 |
| **v1.1.1** | 2026-09-14 | **EL-021 支持多选**：IX-021 由「选 1 个文件夹」改为「可一次多选」，明确多选仍是「每个只加其本身、不展开内部」（DEC-01 不变）；新增 IX-021a「记住上次所在目录」（仅本次运行内有效）。与 PRD v1.1.2 的 TC-19 / TC-20 对应 |
| **v1.2** | 2026-09-15 | **P1（第二版）交互补齐（依 `P1轻量设计确认.md` v1.0 / DEC-10）**：① §2.3 新增 **EL-056 ~ EL-059**（进阶设置折叠条 / 用正则匹配 / 正则语法与错误提示 / 大小写转换）；② §2.4「规则相关」新增 **IX-051 ~ IX-054**；③ 补 IX-053 的非法正则表现与 EL-060 状态矩阵新行「正则非法 → 置灰」；④ §10 异常表新增 **EX-15**（正则非法）/ **EX-16**（正则过于复杂）。**页签仍是三选一（EL-040），DEC-02 不变 —— P1 不新增页签** |
| **v1.3** | 2026-09-15 | **P1 正则说明小白化**（反馈：开启正则后「按正则表达式解释」属用术语解释术语，没接触过的人看完仍不知道填什么）：① EL-058 收窄为**只保留非法时的红字原因**，原「语法提示」两行小字废弃；② §2.3 新增 **EL-103**（「? 看不懂？」按钮）/ **EL-104**（大白话对照 + 按模式过滤的照抄表）/ **EL-105**（固定示例名的当场演示）；③ §2.4 新增 **IX-093 / IX-094**；④ §12 对照索引补 F-10 / F-11 一行（TC-21 ~ TC-27，含 PRD v1.1.4 新增的 TC-27）。<br>> 编号说明：`EL-060 / EL-061` 与 `IX-060 / IX-061` 已被动作区（开始改名 / 撤销）占用，故本次新元素顺延至 **EL-103 ~ EL-105 / IX-093 ~ IX-094** |
| **v1.4** | 2026-09-15 | **P2-A（设置面 + 深色模式）交互补齐（依 `P2-A轻量设计确认.md` v1.2 / DEC-11）**：① §1 界面清单新增 **SCR-07 设置弹窗**，并补一条「图外分支」说明设置弹窗**不打断**当前任务；② §2.3 新增 **EL-106 ~ EL-110**（设置入口按钮 / 设置弹窗 / 主题三选一 / 音效开关 / 减少动画开关）；③ §2.4 新增「设置相关」小节 **IX-095 ~ IX-099**，含 IX-096 的实现要点（深色令牌挂 `prefers-color-scheme` 而非 `[data-theme]`，主进程建窗前定底色 → 首帧不闪）；④ §9 无障碍「动效可关」补齐**入口**（SCR-07 / EL-110），并新增「深色主题」对比度一行；⑤ §11.3 音效补开关入口；⑥ §12 对照索引新增 P2-A 两行。<br>> 编号说明：`EL-060/061` 与 `IX-060/061` 早被动作区占用，故新元素顺延至 **EL-106 ~ EL-110 / IX-095 ~ IX-099**（验收用例顺延至 **TC-28 ~ TC-32**，`TC-27` 已被 P1 的小白化说明占用）|
| **v1.5** | 2026-09-15 | **P2-C（历史完整版 + 命令行入口）交互补齐（依 `P2C轻量设计确认.md` v1.2 / DEC-12）**：① §3.1 历史页元素清单更新 EL-084（卡片内容加明细展开条）并新增 **EL-111**（明细展开条）/ **EL-112**（明细列表）/ **EL-113**（清空历史按钮）；② §3.2 新增 **IX-100**（展开 / 收起明细，纯 UI 状态）与 **IX-101**（清空历史，**含二次确认的两个分支**）；③ §2.3 新增 **EL-114 ~ EL-116**（命令行行 / 命令代码块 / 复制按钮组）；④ §2.4 新增 **IX-102 / IX-103**（展开命令行用法 / 复制）；⑤ §6 二次确认表**重写**：新增 `kind` 列，补「清空历史」两种分支（**主按钮为危险按钮**），并注明 `ConfirmModal` 需新增 `kind: 'clearHistory'` 与 danger 变体；⑥ §10 异常表新增 **EX-17**（清空历史时仍有可撤销任务）；⑦ §12 对照索引新增 P2-C 三行。<br>> **两条必须写进实现的红线**：**只删记录、绝不触碰任何文件**；**清空后必须重置淘汰检测基准**（否则用户主动清空会被假报成「较旧的记录已被清理」）。<br>> 编号说明：本次新元素顺延至 **EL-111 ~ EL-116 / IX-100 ~ IX-103 / EX-17**；验收用例 **TC-33 ~ TC-37**（随 P2-A 的 TC-28~32 一起，均因 `TC-27` 已被 P1 占用而顺延）|
| **v1.6** | 2026-09-17 | **P2-C 增量 · 命令行小白化（依 DEC-13）**：命令行入口做出来了，但小白用户**根本不知道那是干嘛的**。本次补「说明 + 图文教程」两层：① §1 界面清单新增 **SCR-08 命令行图文教程弹窗**（明确它**叠加在设置之上、关闭不连带关设置**）；② §2.3 新增 **EL-117**（命令行引导卡）/ **EL-118**（「查看教程」按钮）/ **EL-119**（教程弹窗本体），并**改写 EL-114 展开块的说明文案** —— 原「给进阶用户用脚本批量改名」默认用户已经懂「脚本」「命令行」，换成「要让别的程序（或定时任务）自动帮你改名时用得上」；③ §2.4 新增 **IX-104 / IX-105**（打开教程 / 翻步与关闭），并补一条 **SCR-08 状态归位红线**（常驻挂载组件的临时 UI 状态必须显式归零）；④ §12 对照索引新增一行。<br>> **用户已明确不做的**：一键生成 `.bat` 文件（第四轮澄清第 4 项「这个暂时没必要，不做」）—— 教程只教「怎么用」，**不代用户落盘任何脚本**。<br>> **编号说明**：本次新元素顺延至 **EL-117 ~ EL-119 / IX-104 ~ IX-105 / SCR-08**；决策 **DEC-13**。教程本体不含新验收 TC（它属 EL-119 的呈现细节，由单测 `tests/p2c-guide.test.ts` + 冒烟 4 条覆盖）|
| **v1.7** | 2026-09-18 | **P2-B（常用规则模板库）交互补齐（依 `P2-B轻量设计确认.md` v1.0 / DEC-14）**：① §2.3 新增 **EL-120**「常用规则」模板条（**常显在 R-05 顶部、页签之前**，6 个 chip，规格全部复用既有令牌）；② §2.4 新增 **IX-106**（点 chip = 整份替换规则 + 页签跟随 + 状态栏 3 秒轻提示；不弹窗、不确认、不做高亮）；③ §12 对照索引加一行。**不新增界面**（模板条是规则区内部的一行，不是独立 SCR） |
| **v1.8** | 2026-09-18 | **P3 第 1 批（编号系统升级）交互补齐（依 `P3-1编号系统升级轻量设计确认.md` v1.1 / DEC-15）**：① §2.3 序号组的四处既有行更新 —— **EL-047**（勾选后生效范围含 EL-121~125）、**EL-049** 由「步长」改称 **「增量」**、**EL-050** 由「补零位数」改称 **「位数」**（且注明只在「数字」类型显示）、**EL-052** 日期样式由三选一扩到 **五选一**、**EL-054** 由「序号位置」改为 **「位置」** 且由两档扩到 **三档**；② §2.3 新增 **EL-121 ~ EL-125**（编号类型下拉 / 类型参数区 / 第三档内联数字框 / 示例行 / 「换一批」按钮）；③ §2.4 新增 **IX-107 ~ IX-109**（切换类型 / 换一批 / 第三档显示并聚焦）；④ §12 对照索引加一行。<br>> **本批零新增界面（不新增 SCR）、零新增通道、零新增持久化、零新增令牌、零新样式** —— 全部落在 R-05 规则区内部，新控件全部复用既有令牌。深色模式**不需要补任何令牌值**（也就不会漏补）。<br>> **编号说明**：新元素顺延至 **EL-121 ~ EL-125 / IX-107 ~ IX-109**；验收 **TC-42 ~ TC-49**；决策 **DEC-15**（第 1 批范围与三个默认值取舍）与 **DEC-16**（三模式互斥 → 五模式互斥，**正式重开 DEC-02**，第 5 批落地）。**本批界面上仍是三选一，DEC-02 的语义在第 1 批仍然成立** |

## 第 7 批（文件夹合并）交互 · 2026-09-23

独立对话框（SCR-10/11/12），入口为主工具栏「文件夹合并」按钮（EL-141）。详见 `P3-7文件夹合并轻量设计确认.md`。

| 功能 | 功能编号 | 元素 | 交互 | 验收 |
| --- | --- | --- | --- | --- |
| 文件夹合并（4 类型 × 复制/剪切） | — | **SCR-10/11/12** · **EL-141~148** | **IX-122~125** | **TC-80~87** · **EX-19/20** |

要点：① 工具栏出现按钮 → 打开独立对话框，关掉对主流程零影响；② 选源（拖入文件/文件夹，文件夹递归摊平）；③ 四种合并方式单选；④ 复制默认、剪切默认关；⑤ 目标选已有/新建；⑥ 点「干跑并预览」出报告（将复制/跳过/错误逐条列出），确认后才真搬；⑦ 冲突项标红（绝不覆盖）；⑧ 完成后默认打开目标目录（可取消）。

## 第 8 批（文件提取）交互 · 2026-09-24

独立对话框（SCR-13/14/15），入口为主工具栏「文件提取」按钮（EL-149）。详见 `P3-8文件提取轻量设计确认.md`。

| 功能 | 功能编号 | 元素 | 交互 | 验收 |
| --- | --- | --- | --- | --- |
| 文件提取（类型/名称/Excel 筛选 × 复制/移动） | — | **SCR-13/14/15** · **EL-149~155** | **IX-126~129** | **TC-88~95** · **EX-21/22** |

要点：① 工具栏出现按钮（列表为空时禁用）→ 打开独立对话框，关掉对主流程零影响；② 源=当前列表（只读计数，不递归）；③ 三种筛选可叠加（AND）：按类型勾选 / 按名称含+正则开关 / 按 Excel 选「原文件名」列；④ 复制默认、移动默认关；⑤ 目标选已有/新建；⑥ 点「干跑并预览」出报告（将复制/跳过/错误逐条列出，Excel 对不上的进 unmatched），确认后才真搬；⑦ 冲突项标红（绝不覆盖）；⑧ 完成后默认打开目标目录（可取消）。