# 数据库设计（本地数据落地设计）

> 产品名：**耄耋改名（MaoDie Renamer）**
> 文档版本：**v1.0.4**
> 日期：2026-09-18
> 上游文档：`PRD.md` v1.1.8（§6 数据与状态定义）· `技术方案文档.md` v1.0.6
> 配套文档：`接口文档.md` v1.0.5 · `排期表.xlsx` v1.0
> 本文档作用：**数据落在用户本机的哪里、长什么样、什么时候写、坏了怎么办。**

---

## 0. 关于这份文档的名字

本产品**没有服务器、没有账号、没有任何网络请求**（这是产品定位，也是 `技术方案文档 §7.3` 的架构保证）。所以严格来说，本项目不存在「数据库」——不存在需要连接的服务端存储，也不存在多客户端并发。

**这份文档实际交付的是「本地数据落地设计」**，内容包括：

| 传统「数据库设计」的内容 | 本文档的对应内容 |
| --- | --- |
| 表结构、字段、类型、约束 | §4 / §5 / §6 —— JSON 文件的完整字段表 |
| 索引、查询优化 | ❌ 不需要（20 条记录的顺序读取，无查询需求） |
| 事务、并发控制 | §8 —— 原子写 + 写入队列串行化 |
| 数据迁移（版本升级） | §3 —— `schemaVersion` 与逐级迁移 |
| 备份与恢复 | §9 —— 损坏检测与自动备份 |
| 容量规划 | **§4.4 —— 本文档最重要的一节**（一次改名 10000 项 × 20 条历史 = 20 万条记录的真实风险） |
| 附录：迁到正式数据库的方案 | 附录 A —— SQLite 表结构与迁移步骤 |

> **本文档仍以 `数据库设计.md` 命名**，以便与你的交付清单一一对应；内容按上表落地。

---

## 1. 设计原则

| # | 原则 | 展开 |
| --- | --- | --- |
| 1 | **只存"可以重建"或"必须留存"的东西** | 历史记录必须留存（撤销要用）；文件列表**不存**（DEC-07）——它可以从用户重新拖入重建，且存下来会导致下次启动误点「开始改名」 |
| 2 | **宁可丢历史，绝不丢文件** | 任何存储异常都不得影响改名流程本身。写历史失败时，改名照常完成，只是**明确告知用户「本次无法撤销」** |
| 3 | **坏数据不静默丢** | 解析失败的文件一律先备份再重建，绝不默默覆盖 |
| 4 | **格式对用户透明** | 全部是可读 JSON。用户想看看软件存了什么，用记事本就能打开（这是 ADR-002 选 JSON 的附带收益） |
| 5 | **可逆** | 若将来需要 SQL，附录 A 给了完整迁移方案。这个决策不是单向门 |

---

## 2. 数据总览

### 2.1 三个文件，各管一件事

| 文件 | 存什么 | 生命期 | 上限 | 对应需求 |
| --- | --- | --- | --- | --- |
| `history.json` | 改名任务记录（供撤销） | 跨会话长期保留 | 20 条任务 / 50000 条明细 | **F-09 / DEC-06** |
| `window.json` | 窗口尺寸与位置 | 跨会话保留 | 约 150 字节 | PRD §5.2 / TC-17 |
| `prefs.json` | 用户偏好（音效、减少动画等） | 跨会话保留 | 约 200 字节 | 交互说明 §11.3 / 设计规范 §7 |

### 2.2 明确不存的东西（DEC-07）

| 不存 | 理由（来自 PRD §6.2 与 DEC-07） |
| --- | --- |
| **文件列表** | 若下次启动自动恢复上次列表，用户可能忘记列表里有内容而误点「开始改名」。**每次启动都从空列表开始，更安全** |
| **当前规则配置** | 同上。规则会随列表一起「继承」到下一次，导致「以为只改了规则，其实连文件都还在」的误解 |
| 预览计算结果 | 完全可重算（毫秒级），存了反而可能不一致 |
| 猫咪状态 | 是运行时的瞬时表现，无持久化意义 |
| 用户改过名的文件的**元数据** | 改名只改名字，不读写文件内容，不做内容哈希 |

> ⚠️ **这两条「不存」是安全设计，不是省事**。任何「帮用户记住上次状态」的想法都要先过一遍：**它会不会增加误改的风险？**
>
> **P3-1 补记（2026-09-18）**：编号类型 / 位置第三档 / 随机长度与种子 / 时间起点与样式这 6 个新字段（`seqKind` / `seqAt` / `seqRandomLen` / `seqRandomSeed` / `seqTimeStart` / `seqTimeFormat`）**同样不落盘**。正因为「当前规则配置」本来就不存，本批才**零迁移** —— `schemaVersion`、`CURRENT_VERSION`、`MIGRATIONS` 一个数都不用动（见 `P3-1编号系统升级轻量设计确认.md` §1.2）。**这也是「只加字段、不加存储」这类需求成本最低的形态：先确认它是不是真的需要存。**

---

## 3. 存储位置与 schemaVersion

### 3.1 目录位置

```
%APPDATA%\MaoDieRenamer\            ← Electron app.getPath('userData')
├─ history.json                     改名记录（最多 20 条任务）
├─ window.json                      窗口尺寸与位置
├─ prefs.json                       用户偏好
├─ history.corrupt-20260912T113000.json    损坏备份（§9）
├─ prefs.v1.bak.json                迁移前备份（§3.4）
└─ logs\                            可选日志（默认关闭写入）
```

实际路径示例：`C:\Users\PC\AppData\Roaming\MaoDieRenamer\`

> ### ⚠ 主动把 userData 目录名设成英文（本方案新增）
>
> electron-builder 的 `productName` 是「耄耋改名」，Electron 默认会**用它作为 userData 目录名**，于是路径变成：
> `C:\Users\PC\AppData\Roaming\耄耋改名\`
>
> 中文路径能在 Windows 上正常工作，但会带来两个真实麻烦：
> 1. 部分第三方备份 / 同步工具（以及一些命令行工具）对非 ASCII 路径处理不佳，容易出现乱码或同步失败；
> 2. 用户手动找数据目录时，从浏览器 / 终端复制粘贴中文路径更容易出错。
>
> **决定**：在 `src/main/index.ts` 顶部、`app.whenReady()` **之前**显式设置：
>
> ```ts
> import { app } from 'electron';
> import path from 'node:path';
>
> // 注意：必须在 app 读取 userData 之前执行，否则不生效
> app.setPath('userData', path.join(app.getPath('appData'), 'MaoDieRenamer'));
> ```
>
> 用 `setPath('userData', ...)` 而不是 `app.setName('MaoDieRenamer')`——后者会连带改掉通知名、快捷方式名等显示名称，那是我们想保留中文的地方。

### 3.2 统一的文件信封

三个文件都用同一个外层结构，便于统一处理：

```jsonc
{
  "schemaVersion": 1,      // 必须是整数，从 1 开始
  "appVersion": "1.0.0",   // 写入时的应用版本，仅用于排障，不参与逻辑
  "payload": { /* 各文件自己的内容 */ }
}
```

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `schemaVersion` | `number` | ✅ | 数据结构版本。**所有版本判断只看它** |
| `appVersion` | `string` | ✅ | 写这个文件时的应用版本。出问题时能快速判断「是不是升级后产生的」 |
| `payload` | `object` | ✅ | 具体数据，见 §4 ~ §6 |

> **为什么要外层信封**：如果没有它，将来要做迁移就必须「猜这个文件是什么版本的」——靠字段的有无来猜是不可靠的（比如 v2 删了一个字段、v3 又加回来）。一个显式的 `schemaVersion` 是新旧版本之间唯一的可靠对话方式。

### 3.3 读取流程（`readJson`）

```
1. 文件不存在            → 返回 fallback（默认值），不报错、不创建文件
2. 读取 + JSON.parse 失败 → 备份为 <name>.corrupt-<时间戳>.json → 返回 fallback
                            → 发 md:app:storageWarning { kind: 'corrupted' }
3. 解析成功但缺 schemaVersion → 当作 0，走迁移链
4. schemaVersion > CURRENT_VERSION
                        → ★ 不迁移、不写入。
                          备份为 <name>.future-<时间戳>.json
                          → 发 storageWarning { kind: 'corrupted',
                              message: '数据文件版本比软件更新，已备份并跳过' }
5. schemaVersion < CURRENT_VERSION → 走迁移（§3.4）
6. schemaVersion === CURRENT_VERSION → 直接返回 payload
```

> **第 4 条（版本比软件新）是最容易被忽略、后果最严重的一条**：用户装了新版、又回退到旧版（或两台机器数据被同步工具覆盖）。此时旧版**不认识新结构**，如果它「宽容地」按旧结构解析再写回，就会**把新版才有的字段全部抹掉**——用户的撤销记录被静默摧毁。
> **正确做法是拒绝写入 + 备份 + 告知用户**。宁可这次用不了历史，也不能破坏数据。

### 3.4 迁移机制

```ts
// src/main/services/migrations.ts
export const CURRENT_VERSION: Record<StoreName, number> = {
  history: 1, window: 1, prefs: 1,
};

type Migration = (payload: any) => any;

/** 每个 store 一张迁移表：key = 从哪个版本升到 key+1 */
export const MIGRATIONS: Record<StoreName, Record<number, Migration>> = {
  history: {
    // 示例（将来真的加了字段时按此写法）：
    // 1: (p) => ({ ...p, tasks: p.tasks.map(t => ({ ...t, undoneAt: t.undoneAt ?? null })) }),
  },
  window: {},
  prefs: {},
};
```

**迁移执行规则**

| 规则 | 说明 |
| --- | --- |
| 逐级执行 | 从 `schemaVersion` 开始，依次执行 `n → n+1`，直到 `CURRENT_VERSION`。绝不写「从 1 直接到 5」的跳级迁移 |
| 迁移前备份 | 第一次迁移前把原文件复制为 `history.v{n}.bak.json`。**迁移出问题能回退** |
| 迁移后立即落盘 | 迁移结果用 `writeJsonAtomic` 写回，避免每次启动都重跑迁移 |
| 迁移失败 | 备份 + 返回 fallback + `storageWarning`。**迁移失败不阻止软件启动** |
| 只增不删 | 迁移代码一旦发布，**永远保留**（哪怕它迁移的是三年前的版本）。删掉它会让升级路径断裂 |

> **首版 `CURRENT_VERSION` 全是 1，迁移表是空的**。这不是过度设计——**建立机制的成本是 30 行代码，而在没有机制的情况下加迁移，代价是用户的历史记录全丢**。§3.3 第 4 条那个场景更是必须靠版本号才拦得住。

---

## 4. `history.json` 详细设计

### 4.1 完整示例

```jsonc
{
  "schemaVersion": 1,
  "appVersion": "1.0.0",
  "payload": {
    "tasks": [
      {
        "id": "9f2c4a1e-7b3d-4c8a-9e15-2f6b8d0a1c34",
        "createdAt": 1789290000000,
        "date": "2026-09-11",
        "ruleSummary": "删除「【某某公众号】」",
        "status": "active",
        "undoneAt": null,
        "counts": { "total": 30, "success": 28, "skipped": 2, "invalid": 0, "failed": 0 },
        "entries": [
          { "dirPath": "C:\\Users\\PC\\Desktop\\课件", "fromName": "【某某公众号】第1课.pdf", "toName": "第1课.pdf" },
          { "dirPath": "C:\\Users\\PC\\Desktop\\课件", "fromName": "【某某公众号】第2课.pdf", "toName": "第2课.pdf" }
        ]
      },
      {
        "id": "1a2b3c4d-5e6f-7081-92a3-b4c5d6e7f809",
        "createdAt": 1789200000000,
        "date": "2026-09-10",
        "ruleSummary": "{d}-发票- + 序号(起始 1 / 补零 3)，不保留原名",
        "status": "undone",
        "undoneAt": 1789210000000,
        "counts": { "total": 200, "success": 200, "skipped": 0, "invalid": 0, "failed": 0 },
        "entries": [ /* … 200 条 … */ ]
      }
    ]
  }
}
```

### 4.2 `tasks[]` 字段表

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `id` | `string` | ✅ | 任务唯一 id。由渲染层生成 UUID，执行时传给主进程（也是临时名 `__md_tmp_<taskId>_<序号>` 的组成部分） |
| `createdAt` | `number` | ✅ | 执行时间，**毫秒时间戳**。历史页显示时间用；**排序也用它**（不依赖数组顺序） |
| `date` | `string` | ✅ | 执行当天日期，`YYYY-MM-DD`。对应 DEC-03（日期取执行当天） |
| `ruleSummary` | `string` | ✅ | 规则摘要文案，历史页直接显示。生成逻辑见 `技术方案文档 §4.1`（`rule-summary.ts`） |
| `status` | `'active' \| 'undone'` | ✅ | `active` = 可撤销；`undone` = 已撤销（灰显、不再可撤） |
| `undoneAt` | `number \| null` | ✅ | 撤销完成时间。未撤销时为 `null`（**用 `null` 不用 `undefined`**——`undefined` 在 JSON 序列化时会丢键） |
| `counts.total` | `number` | ✅ | 本次涉及的总项数（入参 items 数） |
| `counts.success` | `number` | ✅ | 成功改名数。**等于 `entries.length`**（见 §4.3） |
| `counts.skipped` | `number` | ✅ | 因冲突被跳过数 |
| `counts.invalid` | `number` | ✅ | 校验未通过（非法字符 / 空名 / 超长）数 |
| `counts.failed` | `number` | ✅ | 执行中失败（被占用 / 无权限 / 源不存在）数 |
| `entries` | `RenameEntry[]` | ✅ | **只记录成功改名的明细**，见 §4.3 |

> **`counts` 里没有 `entries.length` 之外的信息冗余**：`counts` 保留全部五项是为了让历史页能显示「30 项中 28 项成功、2 项跳过」这种信息——这个信息**无法从 `entries` 反推**（因为 entries 只有成功的）。所以它不是冗余，是必需。

### 4.3 `entries[]` 字段表

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `dirPath` | `string` | ✅ | 文件所在目录（绝对路径，反斜杠形式）。**这是"从哪里改回去"的依据** |
| `fromName` | `string` | ✅ | 改名前的名称，**不含路径** |
| `toName` | `string` | ✅ | 改名后的名称，**不含路径** |

**为什么只存名称而不存 `fullPath`**：`fullPath` = `dirPath` + `\` + `name`，存两份纯属冗余，且一旦有不一致（改名目录重建过）会产生矛盾数据。撤销时用 `path.join(entry.dirPath, entry.toName)` 现算即可。

**撤销时的操作**：`toName → fromName`（在同一个 `dirPath` 内）。

> ### ★ 为什么不记录被跳过 / 失败 / 非法的项
>
> 这是本方案对 PRD §6.1 `RenameTask.entries` 的一处**重要收紧**：
>
> | 项的最终结果 | 需要撤销记录吗 | 理由 |
> | --- | --- | --- |
> | `success` 成功改名 | ✅ **需要** | 这是唯一真正改变了磁盘状态的情况 |
> | `skipped` 冲突跳过 | ❌ 不需要 | 文件根本没动，撤销它没有任何意义（撤销会尝试改名一个从未改过的文件） |
> | `invalid` 校验不过 | ❌ 不需要 | 同上，从未执行 |
> | `failed` 执行失败 | ❌ 不需要 | 失败时已回滚为原名（或原名从未变过），磁盘状态没变 |
>
> **收益（直接决定这个产品能不能撑住极端场景）**：
> - 一次 10000 项的改名，若 9000 项成功，`entries` 是 **9000 条而不是 10000 条**；若只有 8000 项符合规则，则是 8000 条。
> - 更重要的是：**撤销逻辑因此变得绝对安全**——撤销的每一项，都必然是「确实被改过名的」项，不存在「试图还原一个从未被改的文件」这种边界。
> - 历史页要展示的「项数与结果」由 `counts` 完整提供，**信息零损失**。

### 4.4 ★ 容量治理（本文档最关键的一节）

#### 4.4.1 先把问题算清楚

按 PRD 的上限推演最坏情况：

| 场景 | 单批项数 | 历史条数 | `entries` 总条数 | 文件大小（按每条约 180 字节估算） |
| --- | --- | --- | --- | --- |
| 日常使用（整理照片、课件） | 50 ~ 200 | 20 | 1,000 ~ 4,000 | **0.2 ~ 0.7 MB** |
| 重度使用（素材库批量） | 2,000 | 20 | 40,000 | **约 7 MB** |
| **极端（PRD 允许的上限）** | **10,000** | **20** | **200,000** | **约 36 MB** ⚠️ |

> 每条 180 字节的估算依据：一个典型 `entry` 的 JSON 文本约 `{"dirPath":"C:\\Users\\PC\\Desktop\\发票资料","fromName":"IMG_0023.JPG","toName":"2026-09-11-发票-0001.JPG"}` —— 约 130 个字符，其中含中文字符（UTF-8 每字 3 字节），折合约 180 字节。

**36 MB 的单文件会带来三个具体问题**：

| 问题 | 后果 |
| --- | --- |
| 写入耗时 | 每次改名完成都要**全量序列化 + 写 36MB**，约 300~800ms，且占用主进程 |
| 读取耗时 | 每次打开历史页解析 36MB，界面白屏可感知 |
| 内存放大 | `JSON.parse` 后对象表示远大于文本（约 3~5 倍），峰值可达 150MB+ |

**注意：这个 36MB 是「每写完一次就被重写一次」的**——不是只写一次。第 20 次改名时，每次都要处理这 36MB。

#### 4.4.2 四道治理措施

| # | 措施 | 效果 |
| --- | --- | --- |
| 1 | **`entries` 只记成功项**（§4.3） | 直接砍掉跳过 / 失败 / 非法项，典型削减 5%~20% |
| 2 | **任务条数上限 20**（DEC-06） | 硬要求，来自产品决策 |
| 3 | **总明细条数上限 50,000** | 把极端场景从 36MB 压到 **约 9 MB** |
| 4 | **最新任务永不淘汰** | 保证「刚做完的这次一定能撤销」 |

**上限 50000 的推导**：

```
50,000 条 × 180 字节 ≈ 9 MB
全量写入 9 MB 的耗时约 50~150ms —— 发生在改名完成之后（用户正在看结果弹窗），无感知
JSON.parse 峰值内存约 30~45 MB —— 可接受
```

#### 4.4.3 淘汰算法

```
写完新任务后，计算所有任务 entries.length 之和 sum：
  while (sum > 50000 && tasks.length > 1):
      // 从最旧的一条开始整条淘汰
      oldest = tasks 里 createdAt 最小的一条
      if (oldest.id === 最新任务的 id) break   // ★ 最新任务永不淘汰
      淘汰 oldest
      sum -= oldest.entries.length

另外：tasks.length 的硬上限始终是 20（DEC-06），超出时同样淘汰最旧的
```

#### 4.4.4 ★ 取舍说明（已定稿为 DEC-09）

**「保留最近 20 次」与「总量不超过 5 万条」在极端场景下会冲突。**

举例：用户连续做了 20 次「每次 10000 个文件」的改名，每次都成功。
- 按 DEC-06：应该保留 20 条任务。
- 按容量上限：只能留最新的 **5** 条（5 × 10000 = 50000）。

**本方案的处理**：

| 情况 | 行为 | 界面表现 |
| --- | --- | --- |
| 总条数 ≤ 50000 | 保留 20 条 | 正常显示「最多 20 条」 |
| 总条数 > 50000 | 任务数**允许少于 20 条**（从最旧开始淘汰整数条任务） | 历史页底部**显式提示**：「为保证性能，较旧的 3 条记录已被清理」 |

**为什么这样取舍**：撤销是「最近一次操作的兜底」，不是「归档系统」。

- 用户真正要撤的多半是**刚刚那一次**（对应 US-3 阿呆的场景：手滑了，改回去）。
- 用一个写一次要 800ms、打开要白屏的历史页，去换「三条三月前的记录」，是本末倒置。
- 而且**淘汰是整条淘汰，不是切掉半条**——半个任务的撤销记录会导致「撤到一半撤不动」这种更难解释的状态。

**替代方案（已评估，未采用）**

| 方案 | 为什么不采用 |
| --- | --- |
| 把 `entries` 增量写到 NDJSON（每任务一个文件，append-only） | 能彻底避免全量重写。但会把「1 个文件」变「20 个文件 + 1 个索引」，复杂度上升；而现实中触发这个量级的用户极少。**保留为升级路径**（见 §4.9 可观测点） |
| 超过上限时只保留「最近 5000 条明细」 | 会把一个任务截断成「半条」，撤销时只能还原一部分，用户完全无法理解 |
| 压缩存储（gzip） | 引入压缩 / 解压步骤与新的损坏可能性；换来的是「能多存但更慢」，方向反了 |
| 用 SQLite | 见 ADR-002。首版不引入原生依赖 |

> **✅ 已定稿（2026-09-12，DEC-09）**：产品侧确认接受本节取舍——**常态保留 20 条，极端场景允许少于 20 条，并在历史页显式提示**。
> 三条边界不变：① **从最旧整条淘汰**（不切半条）；② **最新任务永不淘汰**；③ 淘汰发生时界面必须显式告知，不静默。
> 若上线后发现「历史少于 20 条」频繁发生，按上表第一行的 **NDJSON 方案**改造即可——`history-store.ts` 对外接口不变，渲染层无感，属**可逆改造**。

#### 4.4.5 写入时机

| 时机 | 动作 |
| --- | --- |
| 改名执行完成（`ExecuteResult` 返回前） | 追加新任务 → 跑淘汰 → `writeJsonAtomic` |
| 撤销单条成功 | 把该任务 `status` 改 `'undone'`、`undoneAt` 置时间戳 → 写回。**不追加新任务**（撤销不产生新记录，交互说明 §3.3） |
| 全部撤销 | 逐条改 `status` → 全部完成后**写一次**（不是每条写一次，减少 IO） |
| 应用启动 | **只读不写** |
| 手动清空（若实现，见 §11） | 写一个 `tasks: []` 的空结构 |

> **写在 `ExecuteResult` 返回之前的必要性**：如果先返回结果、再异步写历史，用户看到「改好啦」后**立刻点撤销**，此时历史可能还没落盘 → 撤销失败。**必须先写、再返回**。
> 这一点直接对应 `接口文档 §3.4` 的 `ExecuteResult.recordSaved` 字段：写失败时该字段为 `false`，界面必须明确警告「本次改名无法撤销」。

### 4.5 排序与展示约定

| 项 | 约定 |
| --- | --- |
| 存储顺序 | 数组**按 `createdAt` 倒序**（最新在前）。写入时 `unshift`，读时也按 `createdAt` 再排一次兜底 |
| 为什么不只依赖数组顺序 | 若用户手动编辑过 JSON，或将来有并发写入，数组顺序可能不可信。**排序永远以 `createdAt` 为准**——这是数据自带的顺序，不依赖任何外部约定 |
| 历史页显示 | 时间（本地时区格式化）/ 规则摘要 / `counts` 摘要 / 首条 `entries` 示例 / 状态徽标（可撤销 / 已撤销） |
| 已撤销任务 | 卡片灰显、`已撤销` 徽标、**不显示撤销按钮**，但保留在列表中（用户还能看到改过什么） |

### 4.6 撤销的状态流转

```
                改名成功
                   ↓
              ┌─────────┐
              │ active  │ ← 可撤销
              └────┬────┘
                   │ 撤销成功（单条 / 全部）
                   ↓
              ┌─────────┐
              │ undone  │ ← 不可再撤销（E_UNDO_ALREADY）
              └─────────┘
                   │ 淘汰（超 20 条 或 超 50000 条）
                   ↓
                （被移除）
```

| 约束 | 说明 |
| --- | --- |
| 单向 | `undone` 不会回到 `active`。撤销是终态 |
| 撤销不产生新记录 | 避免「撤销的撤销」；`active` → `undone` 是**就地更新**，不新增任务 |
| 部分失败 | 若撤销时某些文件已不存在（EX-10），**该任务仍标记为 `undone`**（因为已经无法完整撤销），并在返回值里如实报告 `skipped`。**重新标回 `active` 是错误的**——会让用户反复点撤销、反复看到同一批失败 |
| 与主列表联动 | 若被撤销的文件仍在当前列表中，界面需同步改回名称并重算预览（交互说明 §3.3）。这是**界面行为**，不涉及存储 |

### 4.7 数据保留与隐私

| 项 | 现状 |
| --- | --- |
| 历史记录包含什么 | **完整的文件路径与文件名** —— 这是敏感信息（可能暴露用户的目录结构、项目名、客户名） |
| 是否上传 | ❌ 绝不上传（零网络请求，CSP `connect-src 'none'`） |
| 是否加密 | ❌ 不加密。理由：本地单机文件，加密的密钥也只能存在本机，对「防止本机其他用户读取」几乎无效，却会引入「密钥丢失 → 数据全废」的新风险 |
| 卸载时 | `deleteAppDataOnUninstall: false` —— **保留**。用户重装后历史还在 |
| 用户能否自己删 | 能（删掉 `%APPDATA%\MaoDieRenamer\history.json`），但**软件内没有提供入口** |

> ✅ **已成稿并落地到设计（2026-09-15 / P2-C / DEC-12）**：
> 提供一个 **「清空历史记录」** 入口（**历史页 footer，幽灵危险按钮样式**）。
>
> **只删记录，不碰任何文件**（这是必须写清的，否则用户会以为「清空历史 = 删文件」）。
>
> **理由**：用户改过一批包含客户名 / 项目名的文件后，这些完整路径会一直躺在历史里。虽然不上传，但用户「我想删掉它」的诉求是合理且常见的，而当前只能靠用户手动去 AppData 里找文件——对一个非技术用户来说等于没有这个能力。
>
> **成本**：一个按钮 + 一次 `writeJsonAtomic`，约 0.5 人日（与当初估算一致）。
>
> **P2-C 落地时补充的两条**（原方案没写到，是设计阶段新发现的）：
> 1. ⚠️ **数据安全阀（必须实现）**：清空 = 删掉撤销记录 → 那些改名**再也撤不回来**。所以若历史里仍有 `status === 'active'` 的任务，二次确认**必须额外加红色警告块**（「其中 N 条改名记录仍然可以撤销。清空后这 N 条将无法再还原」），主按钮文案改「**仍然清空**」。不做这条会导致「改错了名 → 想撤销 → 发现历史被自己清了」的**永久丢数据**。对应 **EX-17**。
> 2. ⚠️ **清空后必须重置淘汰检测基准**：`stores/history.ts` 的「淘汰不静默」检测条件是「最旧那条不见了 + 总数没变多」—— **用户主动清空恰好两个条件都成立**，不重置就会假报「为保证性能，较旧的记录已被清理」。见 `P2-C轻量设计确认.md` §2.6。
>
> 契约变更：新增请求响应通道 **`md:history:clear`**（15 → 16），详见 `接口文档.md` §3.5。

### 4.8 写入失败处理（必须显式实现）

| 失败原因 | 检测 | 处理 |
| --- | --- | --- |
| 磁盘满（`ENOSPC`） | `writeJsonAtomic` reject | 改名结果**照常返回**；`recordSaved: false`；发 `storageWarning { kind: 'write_failed' }`；结果弹窗**明确警告**「本次改名记录未保存，无法撤销」 |
| 无权限（`EACCES`） | 同上 | 同上 |
| 杀毒软件锁文件（`EBUSY`） | 同上 | 同上；可提示用户「稍后重试」 |
| 进程在写入中被终止 | 下一次读取时 JSON 解析失败 | 走 §9 损坏恢复：备份 + 重建空结构 |

> **这一段的核心原则（对应 §1 原则 2）**：
> **写历史失败，绝不能让改名流程失败。** 文件已经改好了，此时报错说「改名失败」是错上加错——用户会以为没改成，重新再改一次，那才是真正的数据事故。
> **正确行为是：改名照常完成 + 明确告知「这次不能撤销」。**

### 4.9 可观测点（上线后要不要升级方案的判断依据）

| 观测项 | 阈值 | 超过时的动作 |
| --- | --- | --- |
| `writeJsonAtomic(history)` 耗时 | > 200ms | 考虑升级到 NDJSON 分文件（§4.4.4 的替代方案） |
| `history.json` 文件大小 | > 15 MB | 下调总条数上限（如 30000） |
| 打开历史页的解析耗时 | > 300ms | 同上 |
| 历史页因淘汰而少于是 20 条的出现频率 | 经常发生 | 重新评审 DEC-06，讨论是否引入「按需归档」 |

> 首版在开发环境打印这几项耗时（仅在 `isDev` 时输出到控制台），**不上报**（零数据收集）。

---

## 5. `window.json` 详细设计

```jsonc
{
  "schemaVersion": 1,
  "appVersion": "1.0.0",
  "payload": {
    "width": 1080,
    "height": 680,
    "x": 420,
    "y": 180,
    "maximized": false,
    "savedAt": 1789290000000
  }
}
```

| 字段 | 类型 | 必填 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `width` | `number` | ✅ | 1080 | 窗口宽。**恢复前必须钳制**：不得小于 900（PRD 最小尺寸），不得大于屏幕可用宽 |
| `height` | `number` | ✅ | 680 | 窗口高。钳制到 ≥ 560 |
| `x` | `number` | ✅ | 居中 | 窗口左上角 X |
| `y` | `number` | ✅ | 居中 | 窗口左上角 Y |
| `maximized` | `boolean` | ✅ | false | 是否最大化 |
| `savedAt` | `number` | ✅ | — | 保存时间戳，仅用于排障 |

### 5.1 ★ 恢复时必须做的三项校验

| 校验 | 为什么 | 处理 |
| --- | --- | --- |
| 尺寸下限 | 用户手改过 JSON，或旧版本的最小尺寸不同 | `width < 900 → 900`，`height < 560 → 560` |
| **位置是否还在某块显示器上** | 用户拔掉外接显示器后，`x = 3000` 会让窗口出现在**屏幕外**，用户看到的是「软件打不开」 | 用 `screen.getAllDisplays()` 取所有显示器的工作区，判断窗口矩形与至少一块显示器**有交集**；无交集 → 回到主显示器居中 |
| 尺寸上限 | 用户在 4K 屏上设置为 3000 宽，换到 1080p 笔记本后窗口超出屏幕 | 钳制到主显示器可用宽 / 高 |

> **「窗口跑到屏幕外」是非技术用户最难自行诊断的问题**——他会认为软件坏了、卸载重装（而重装后 `window.json` 若被保留，问题依旧）。这个校验约 15 行代码，**必做**。

### 5.2 写入时机（这里必须用同步写）

| 时机 | 方式 | 理由 |
| --- | --- | --- |
| `resize` / `move` 事件 | **异步**写，**500ms 防抖** | 拖拽窗口时每秒触发几十次，不能每次都写 |
| 最大化 / 取消最大化 | 异步写，跟随上面 500ms 防抖 | — |
| **窗口 `close` 事件** | ★ **同步写（`fs.writeFileSync`）** | 见下 |

> ### ⚠ 关闭时为什么必须用同步写
>
> `win.on('close')` 之后，进程随时可能退出。如果这里用异步的 `writeJsonAtomic`，**进程会在写入完成前就结束**，用户最后一次调整的窗口尺寸就丢了。
>
> 常见错误写法（会偶发丢数据）：
> ```ts
> win.on('close', () => { void writeJsonAtomic(file, data); });  // ✗ 可能来不及写完
> ```
>
> 正确写法：`window.json` 只有约 150 字节，**同步写耗时 < 1ms**，完全可以接受：
> ```ts
> win.on('close', () => {
>   flushWindowStateSync();   // 内部用 fs.writeFileSync + fs.renameSync
> });
> ```
>
> 换来的好处：**不需要 `event.preventDefault()` + 手动 `window.destroy()`**（那种写法会让关闭窗口有一瞬间的卡顿感，而且容易写出「preventDefault 之后忘了 destroy」导致窗口关不掉的 bug）。

---

## 6. `prefs.json` 详细设计

```jsonc
{
  "schemaVersion": 1,
  "appVersion": "1.0.0",
  "payload": {
    "soundEnabled": true,
    "reduceMotion": false,
    "confirmThreshold": 10,
    "theme": "light"
  }
}
```

| 字段 | 类型 | 默认 | 取值范围 | 说明 | 对应 |
| --- | --- | --- | --- | --- | --- |
| `soundEnabled` | `boolean` | `true` | — | 音效开关（成功「叮」/ 失败「噗」） | 交互说明 §11.3 |
| `reduceMotion` | `boolean` | `false` | — | 「减少动画」无障碍开关。开启后状态切换只换表情、不做位移，动效时长归零 | 设计规范 §7 / §8 |
| `confirmThreshold` | `number` | `10` | 1 ~ 1000 | 待改项达到多少弹二次确认 | PRD §4.4.2 |
| `theme` | `'system' \| 'light' \| 'dark'` | `'light'` | 三个合法值 | **P2-A 已落地（F-14 / DEC-11）**：`system` 跟随系统 / `light` 始终浅色 / `dark` 始终深色；**默认 `'light'`** —— 让软件在任何机器上首屏都长一样，「跟随系统」仍可手选 | PRD §3.1 / DEC-11 |

**设计要点**

| 要点 | 说明 |
| --- | --- |
| **字段预留 → P2-A 已兑现** | 当初「只留字段不实现」的理由（**加字段要写迁移，留字段零成本**）在 P2-A 兑现了：扩成三态**没有写任何数据迁移**。兜底逻辑不变 —— `theme` 若不是三个合法值之一，一律回落到 `'light'`（`src/shared/theme.ts` 的 `normalizeTheme`）。这不违反 YAGNI：YAGNI 反对的是「为不确定的需求写实现代码」，而不是「在设计里留出已知的扩展位」 |
| **读取时逐字段兜底** | 不信任文件内容。`confirmThreshold` 若为 `'abc'` 或 `-5`，回落到默认值 10。用户手动改坏文件不应该让软件崩溃 |
| **写入时机** | 设置项变更时立即原子写（不是防抖）。设置变更频率很低，实时写更安全 |
| 越界钳制 | `confirmThreshold` 写入前钳制到 `[1, 1000]` |

---

## 7. 原子写入实现（`writeJsonAtomic`）

三个文件共用同一个函数。这是「写到一半断电不会损坏原文件」的技术保证。

```ts
// src/main/services/storage.ts
import { promises as fs } from 'node:fs';
import { dirname, basename, join } from 'node:path';

export async function writeJsonAtomic(file: string, data: unknown): Promise<void> {
  const tmp = join(dirname(file), `.${basename(file)}.tmp-${process.pid}-${Date.now()}`);
  const text = JSON.stringify(data, null, 2);

  const fh = await fs.open(tmp, 'w');
  try {
    await fh.writeFile(text, 'utf8');
    await fh.sync();               // ★ 关键：确保数据真正落盘，而不是留在 OS 缓存
  } finally {
    await fh.close();
  }

  await fs.rename(tmp, file);      // ★ 同文件系统内的 rename 是原子操作
}
```

### 7.1 三个必须踩准的细节

| # | 细节 | 踩错的后果 |
| --- | --- | --- |
| 1 | **临时文件必须与原文件同目录** | 若放到系统临时目录（`%TEMP%`，通常在不同盘），`rename` 会退化为「复制 + 删除」，**不再是原子操作**，且中途失败会留下半个文件 |
| 2 | **`fh.sync()` 不能省** | 少了它，数据可能只写进了操作系统的写缓存。此时断电 / 强制关机 → 文件内容为空或截断。**这个 bug 在拔电源测试之外几乎测不出来**，但真实发生 |
| 3 | **临时文件名要带 pid + 时间戳** | 固定名 `.tmp` 在异常退出后会残留，下次写入时可能读到上次的残留内容。带 pid 与时间戳可避免复用 |

### 7.2 临时文件的清理

| 场景 | 处理 |
| --- | --- |
| 写入成功 | `rename` 后临时文件已不存在（被 rename 走了） |
| 写入中抛错 | `finally` 里删掉临时文件（这是**唯一允许在 `storage.ts` 使用 `fs.unlink` 的地方**，且路径由程序自身生成，不接受外部输入） |
| 进程被强杀，残留了 `.tmp-*` | 启动时扫描 `userData` 目录，删除**命名匹配 `.history.json.tmp-*` 模式且修改时间超过 1 天**的残留文件。**只删自己生成的临时文件，绝不删其他文件** |

> **启动清理为什么要加「超过 1 天」这个条件**：不加的话，如果用户在写入的瞬间启动第二个实例（虽然打了单实例锁，但时间窗口极小），会把正在使用的临时文件删掉。加时间条件后，只清理确定已经废弃的文件。**这是"谨慎删除"的一贯做法。**

---

## 8. 写入串行化

单进程内其实不会真并发，但下面几种情况会**几乎同时**触发写入，交叉写同一个文件会产生损坏：

| 场景 | 并发写入 |
| --- | --- |
| 改名完成写 `history.json` 的同时，窗口 `resize` 触发了 `window.json`（不同文件，安全） | 不同文件 → 安全 |
| 撤销全部任务时逐条更新，与「改名完成后追加新任务」 | 都是 `history.json` → **危险** |
| 设置变更写 `prefs.json`，同时 `storageWarning` 触发了另一次写 | 同文件 → 危险 |

**处理**：所有写操作进同一个 Promise 队列（**按文件分队列**，不同文件不互相阻塞）：

```ts
const queues = new Map<string, Promise<unknown>>();

export function enqueueWrite<T>(file: string, job: () => Promise<T>): Promise<T> {
  const prev = queues.get(file) ?? Promise.resolve();
  const next = prev.then(job, job);      // 前一个失败也要继续执行后一个
  queues.set(file, next.catch(() => {}));// 队列本身不累积 rejection
  return next;
}
```

> **`prev.then(job, job)` 的第二个参数不能省**：如果前一次写入失败了，用 `prev.then(job)` 会让后续的写入全部被跳过（因为队列进入 rejected 状态）。**「一次写入失败导致之后所有写入都不执行」是比原问题更严重的 bug。**

---

## 9. 损坏恢复

| 情况 | 检测方式 | 处理 |
| --- | --- | --- |
| 文件内容不是合法 JSON | `JSON.parse` 抛错 | 备份为 `<name>.corrupt-<ISO时间戳>.json` → 返回默认值 → `storageWarning` |
| JSON 合法但结构不符（如 `payload.tasks` 不是数组） | 结构校验 | 同上 |
| 文件被截断（写入中被强杀） | `JSON.parse` 抛错 | 同上 |
| 文件大小为 0 | 单独检测 | 同上（不算「空数据」，算损坏） |
| 文件被其他程序锁定无法读 | `EBUSY` / `EPERM` | **不备份**（不是损坏），返回默认值 + `storageWarning`，**下次读取时重试** |
| `schemaVersion` 比软件新 | 版本比较 | 备份为 `.future-<时间戳>.json` → **不写回**（见 §3.3 第 4 条） |

**备份文件命名**：`history.corrupt-20260912T113000.json`（ISO 时间戳去掉冒号与短横线，保证文件名合法）。

**备份文件数量控制**：最多保留最近 **3 个**损坏备份，超出时删除最旧的。理由：不希望用户的 AppData 里堆一堆坏文件，但保留几个足够排障。

**用户可见性**：损坏恢复通过 `md:app:storageWarning` 通知界面 → 状态栏提示（`接口文档 §3.1`）。文案示例：

| 情况 | 文案 |
| --- | --- |
| 历史损坏 | 历史记录文件损坏，已重置（备份已保存） |
| 版本过新 | 数据文件版本比软件更新，已备份并跳过 |
| 写失败 | 改名记录未能保存，本次改名无法撤销 |

> **绝不静默**。用户发现「历史记录突然空了」却不知道为什么，比「看到一条提示说历史损坏了」要糟糕得多。

---

## 10. 完整 JSON Schema 汇总（供实现时校验）

```ts
// src/shared/types.ts —— 存储层类型（与接口文档 §5 的运行时类型区分）

export interface Envelope<T> {
  schemaVersion: number;
  appVersion: string;
  payload: T;
}

export type HistoryFile = Envelope<{ tasks: RenameTask[] }>;
export type WindowFile  = Envelope<{
  width: number; height: number; x: number; y: number;
  maximized: boolean; savedAt: number;
}>;
export type PrefsFile   = Envelope<{
  soundEnabled: boolean; reduceMotion: boolean;
  confirmThreshold: number; theme: 'system' | 'light' | 'dark';
}>;

/* ── 容量常量（集中在 src/shared/constants.ts） ── */
export const STORAGE = {
  /** DEC-06：最多保留 20 条任务 */
  HISTORY_MAX_TASKS: 20,
  /** 总明细条数软上限，控制文件体积与写入耗时（§4.4） */
  HISTORY_MAX_TOTAL_ENTRIES: 50_000,
  /** 单任务明细上限 = 单批项数上限 */
  TASK_MAX_ENTRIES: 10_000,
  /** 损坏备份最多保留个数 */
  MAX_CORRUPT_BACKUPS: 3,
  /** 单条 entry 的体积估算（字节），仅用于日志与阈值判断 */
  ENTRY_SIZE_ESTIMATE: 180,
} as const;
```

---

## 11. 遗留待确认项

| # | 项 | 现状 | 建议 |
| --- | --- | --- | --- |
| 1 | **「清空历史记录」入口** | ✅ **已定稿并落地到设计**（2026-09-15 / P2-C / DEC-12）：方案见 §4.7（历史页 footer 幽灵危险按钮）。**只删记录，不碰文件**；另补两条：数据安全阀（有可撤销任务时必须额外警告，EX-17）与「清空后重置淘汰检测基准」 | 无需动作 |
| 1b | **「历史完整明细」**（P2-C 补充） | 卡片内就地展开全部 entries；**只渲染前 100 条**（超出提示「还有 N 项未显示」）—— 刻意取舍，避免几千条明细卡顿 | 无需动作 |
| 2 | **`HISTORY_MAX_TOTAL_ENTRIES = 50000` 的取值** | 基于「9MB / 51~150ms」的推演 | MS-2 阶段用真实数据实测一次，再定稿 |
| 3 | **DEC-06 与容量上限的冲突** | ✅ **已关闭（DEC-09）**：产品侧确认接受「常态 20 条、极端场景可少于 20 条 + 历史页显式提示」。**NDJSON 分文件方案保留为升级路径** | 无需动作；若上线后触发频繁再评审 |
| 4 | **历史记录是否加密** | 不加密（§4.7 已说明理由） | 若后续有「多人共用一台电脑」的场景需求，需重新评审 |
| 5 | **`logs/` 日志是否落地** | 目录规划了，但默认关闭写入 | 首版可不实现；出问题排查时再开启 |
| 6 | **迁移表首版为空** | `CURRENT_VERSION` 全为 1，无迁移 | 机制已建好（§3.4）。**加第二个版本时必须同时补迁移函数与单测** |
| 7 | **是否需要「导入/导出历史」** | 未做 | P2 再议。会引入文件格式兼容问题 |

---

## 附录 A：迁移到 SQLite 的方案（若将来需要）

> 触发条件：出现「按条件搜索历史」「历史条数远超 20」「需要跨任务统计」等需求时。**当前不需要**（ADR-002）。
> 本附录存在的意义：**证明这个决策是可逆的**，而不是给自己留退路的话术。

### A.1 表结构（DDL）

```sql
-- 元信息（存 schema 版本）
CREATE TABLE IF NOT EXISTS schema_meta (
  key   TEXT PRIMARY KEY,
  value TEXT NOT NULL
);

-- 改名任务
CREATE TABLE IF NOT EXISTS rename_task (
  id             TEXT    PRIMARY KEY,
  created_at     INTEGER NOT NULL,                 -- 毫秒时间戳
  date           TEXT    NOT NULL,                 -- YYYY-MM-DD
  rule_summary   TEXT    NOT NULL,
  status         TEXT    NOT NULL CHECK (status IN ('active','undone')),
  undone_at      INTEGER,                          -- NULL = 未撤销
  total_count    INTEGER NOT NULL DEFAULT 0,
  success_count  INTEGER NOT NULL DEFAULT 0,
  skipped_count  INTEGER NOT NULL DEFAULT 0,
  invalid_count  INTEGER NOT NULL DEFAULT 0,
  failed_count   INTEGER NOT NULL DEFAULT 0
);

CREATE INDEX IF NOT EXISTS idx_task_created ON rename_task (created_at DESC);
CREATE INDEX IF NOT EXISTS idx_task_status  ON rename_task (status);

-- 改名明细（只记成功项）
CREATE TABLE IF NOT EXISTS rename_entry (
  id        INTEGER PRIMARY KEY AUTOINCREMENT,
  task_id   TEXT    NOT NULL REFERENCES rename_task(id) ON DELETE CASCADE,
  seq       INTEGER NOT NULL,                      -- ★ 原始执行顺序，撤销时按 seq DESC 倒序取
  dir_path  TEXT    NOT NULL,
  from_name TEXT    NOT NULL,
  to_name   TEXT    NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_entry_task ON rename_entry (task_id, seq DESC);

-- 偏好设置（键值对，便于将来加字段而不改表）
CREATE TABLE IF NOT EXISTS prefs (
  key   TEXT PRIMARY KEY,
  value TEXT NOT NULL
);

-- 窗口状态（单行表，id 恒为 1）
CREATE TABLE IF NOT EXISTS window_state (
  id         INTEGER PRIMARY KEY CHECK (id = 1),
  width      INTEGER NOT NULL,
  height     INTEGER NOT NULL,
  x          INTEGER NOT NULL,
  y          INTEGER NOT NULL,
  maximized  INTEGER NOT NULL,                     -- 0 / 1
  saved_at   INTEGER NOT NULL
);

INSERT OR IGNORE INTO schema_meta (key, value) VALUES ('schema_version', '1');
```

### A.2 关键差异与实现注意

| 项 | JSON 方案 | SQLite 方案 |
| --- | --- | --- |
| 撤销顺序 | 依赖 `entries` 数组顺序 | **必须显式存 `seq`**，撤销时 `ORDER BY seq DESC`。数组顺序在 SQL 里不存在 |
| 容量控制 | 自己算总条数、自己淘汰 | `DELETE FROM rename_task WHERE id IN (…最旧的…)`，并靠 `ON DELETE CASCADE` 级联删明细。**必须开 `PRAGMA foreign_keys = ON`**，否则级联不生效 |
| 原子性 | 靠「临时文件 + rename」 | 靠事务（`BEGIN` / `COMMIT`）。**反而更简单可靠** |
| 写历史失败 | `recordSaved: false` | 事务回滚 → 同样返回 `recordSaved: false` |
| 损坏恢复 | 备份 + 重建 JSON | `PRAGMA integrity_check`，损坏时备份 `.db` 文件并重建 |
| 打包 | 纯 JS，零原生依赖 | **需要 `better-sqlite3` + `electron-rebuild`**，这是主要成本 |

### A.3 迁移步骤（从 JSON 到 SQLite）

```
1. 加依赖：better-sqlite3 + @electron/rebuild
   并在 electron-builder 配置里处理 .node 原生模块（asarUnpack）
2. 新增 src/main/services/sqlite-store.ts，实现与 storage.ts 完全相同的对外接口：
     init() / listTasks() / appendTask() / markUndone() / getPrefs() / setPrefs() / getWindowState() / setWindowState()
   ★ 接口不变是这次迁移能低成本完成的前提
3. 首次启动检测：若 userData 下存在 history.json 且 SQLite 库为空 → 一次性导入
     迁移完成后把 history.json 重命名为 history.migrated.json（不删除）
4. CURRENT_VERSION 从 1 升到 2，并写一条迁移记录到 schema_meta
5. 全量回归：TC-09 / TC-10 / TC-11（撤销相关）必须逐条重跑
6. 打包验证：NSIS 安装包实测体积与安装后能否正常启动（原生模块最容易在这一步出问题）
```

> **第 3 步的「不删除旧文件」是关键**：迁移出错时用户还能拿回原数据。**任何数据迁移都不应该在第一次运行时销毁源数据**——至少留一个版本周期。

---

## 附录 B：与 PRD §6.2 的差异说明

| PRD §6.2 的表述 | 本方案 | 为什么改 |
| --- | --- | --- |
| `userData/history.json` | 目录名改为 `%APPDATA%\MaoDieRenamer\`（英文） | 规避中文路径在备份工具与命令行下的兼容问题（§3.1） |
| `history.json` 上限「20 条（FIFO 淘汰）」 | 20 条任务 + **总明细 50000 条软上限** | PRD 只约束了任务条数，未考虑「20 条 × 10000 项」时的 36MB 问题（§4.4） |
| `RenameTask.entries` 含 `result` 字段与全部项 | **`entries` 只记成功项**；全部计数移到 `counts` | 跳过 / 失败 / 非法项不需要撤销记录，且能让撤销逻辑绝对安全（§4.3） |
| `window.json` / `prefs.json` 未定义结构 | 完整字段表（§5 / §6） | PRD 只给了文件名 |
| 未提及迁移与损坏恢复 | 新增 `schemaVersion`、迁移机制（§3.4）、损坏备份（§9） | 「装了新版又回退」会静默摧毁数据，必须有版本号才拦得住（§3.3） |
| 未提及写入原子性 | 原子写 + 写入队列（§7 / §8） | 「写到一半断电 → 历史损坏」是真实会发生的场景 |

---

## 变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| **v1.0** | 2026-09-12 | 首次成文。完成 3 个数据文件的完整字段设计；**识别并解决「20 条任务 × 10000 项 = 20 万条明细 / 36MB」的容量风险**（总明细上限 50000 + 只记成功项 + 最新任务永不淘汰 + 允许任务数少于 20）；新增 `schemaVersion` 与迁移机制、拒绝写入「版本过新」的文件；给出原子写三步与临时文件清理规则；提出「清空历史记录」入口建议；导出 userData 目录名改为英文；附 SQLite 迁移 DDL 与步骤 |
| **v1.0.1** | 2026-09-12 | 依 PRD v1.1.1 的 **DEC-09** 把 §4.4.4 的取舍**由「待确认」转为「已定稿」**：常态 20 条、极端场景可少于 20 条并显式提示；明确三条边界（整条淘汰 / 最新永不淘汰 / 不静默）与可逆改造路径。§11 遗留项 3 关闭。字段结构、上限常量、原子写实现均无变化 |
| **v1.0.2** | 2026-09-15 | **P2-A（设置面 + 深色模式）落地后同步**：§6 `prefs.json` 的 `theme` 由「只有 `'light'`」改为**三态**（`'system' \| 'light' \| 'dark'`，默认 `'light'`），并把「字段预留但不实现」改写为「字段预留 → P2-A 已兑现」。**未新增任何数据迁移**（§3.4 的迁移机制本轮无新条目）—— 扩枚举值不改变文件结构，旧文件照读 |
| **v1.0.3** | 2026-09-18 | **P3-1（编号系统升级）落地后的确认：本批对数据层零影响**。① §2.2 补记 —— `RuleConfig` 新增的 6 个字段（编号类型 / 位置第三档 / 随机长度与种子 / 时间起点与样式）**全部不落盘**，因为「当前规则配置」本来就不存（DEC-07）；② 因此 **`schemaVersion` 不动、`CURRENT_VERSION` 不动、`MIGRATIONS` 无新条目、`history.json` / `window.json` / `prefs.json` 三个文件的字段结构与上限值一个字都不改**；③ 老 `history.json` 的 `ruleSummary` 仍是旧措辞（模板库里「补零」已改名「位数」、位置第三档是新档位），**刻意不写迁移去改老记录** —— 老记录就该保持它当时的写法。详见 `P3-1编号系统升级轻量设计确认.md` v1.0 |

---

**数据库设计 v1.0.4 · 2026-09-21**
